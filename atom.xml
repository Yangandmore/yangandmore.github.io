<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>杨小杨的博客</title>
  
  
  <link href="http://example.com/atom.xml" rel="self"/>
  
  <link href="http://example.com/"/>
  <updated>2021-04-17T07:10:38.654Z</updated>
  <id>http://example.com/</id>
  
  <author>
    <name>Yang</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>centos终端ssr配置</title>
    <link href="http://example.com/2021/01/18/centos%E7%BB%88%E7%AB%AFssr%E9%85%8D%E7%BD%AE/"/>
    <id>http://example.com/2021/01/18/centos%E7%BB%88%E7%AB%AFssr%E9%85%8D%E7%BD%AE/</id>
    <published>2021-01-18T00:21:35.000Z</published>
    <updated>2021-04-17T07:10:38.654Z</updated>
    
    <content type="html"><![CDATA[<h2 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h2><figure class="highlight awk"><table><tr><td class="code"><pre><code class="hljs awk">git clone http:<span class="hljs-regexp">//gi</span>t.mrwang.pw<span class="hljs-regexp">/Reed/</span>Linux_ssr_script.git<br>cd Linux_ssr_script &amp;&amp; chmod +x ./ssr<br>./ssr install<br></code></pre></td></tr></table></figure><h2 id="配置节点"><a href="#配置节点" class="headerlink" title="配置节点"></a>配置节点</h2><figure class="highlight arduino"><table><tr><td class="code"><pre><code class="hljs arduino">./ssr config<br></code></pre></td></tr></table></figure><p>这里就开始需要配置ssr的节点<br><figure class="highlight json"><table><tr><td class="code"><pre><code class="hljs json">&#123;<br>    <span class="hljs-attr">&quot;server&quot;</span>: <span class="hljs-string">&quot;&quot;</span>,<br>    <span class="hljs-attr">&quot;server_ipv6&quot;</span>: <span class="hljs-string">&quot;::&quot;</span>,<br>    <span class="hljs-attr">&quot;server_port&quot;</span>: <span class="hljs-number">999</span>,<br>    <span class="hljs-attr">&quot;local_address&quot;</span>: <span class="hljs-string">&quot;127.0.0.1&quot;</span>,<br>    <span class="hljs-attr">&quot;local_port&quot;</span>: <span class="hljs-number">1080</span>,<br><br>    <span class="hljs-attr">&quot;password&quot;</span>: <span class="hljs-string">&quot;&quot;</span>,<br>    <span class="hljs-attr">&quot;method&quot;</span>: <span class="hljs-string">&quot;&quot;</span>,<br>    <span class="hljs-attr">&quot;protocol&quot;</span>: <span class="hljs-string">&quot;&quot;</span>,<br>    <span class="hljs-attr">&quot;protocol_param&quot;</span>: <span class="hljs-string">&quot;&quot;</span>,<br>    <span class="hljs-attr">&quot;obfs&quot;</span>: <span class="hljs-string">&quot;&quot;</span>,<br>    <span class="hljs-attr">&quot;obfs_param&quot;</span>: <span class="hljs-string">&quot;&quot;</span>,<br>    <span class="hljs-attr">&quot;speed_limit_per_con&quot;</span>: <span class="hljs-number">0</span>,<br>    <span class="hljs-attr">&quot;speed_limit_per_user&quot;</span>: <span class="hljs-number">0</span>,<br><br>    <span class="hljs-attr">&quot;additional_ports&quot;</span> : &#123;&#125;, <span class="hljs-comment">// only works under multi-user mode</span><br>    <span class="hljs-attr">&quot;additional_ports_only&quot;</span> : <span class="hljs-literal">false</span>, <span class="hljs-comment">// only works under multi-user mode</span><br>    <span class="hljs-attr">&quot;timeout&quot;</span>: <span class="hljs-number">120</span>,<br>    <span class="hljs-attr">&quot;udp_timeout&quot;</span>: <span class="hljs-number">60</span>,<br>    <span class="hljs-attr">&quot;dns_ipv6&quot;</span>: <span class="hljs-literal">false</span>,<br>    <span class="hljs-attr">&quot;connect_verbose_info&quot;</span>: <span class="hljs-number">0</span>,<br>    <span class="hljs-attr">&quot;redirect&quot;</span>: <span class="hljs-string">&quot;&quot;</span>,<br>    <span class="hljs-attr">&quot;fast_open&quot;</span>: <span class="hljs-literal">false</span><br>&#125;<br></code></pre></td></tr></table></figure><br>配置注释：</p><blockquote><p>server: ssr服务器地址<br>server_port: 端口号<br>local_address: 本地路径127.0.0.1或者localhost<br>local_port: 本地端口号<br>password: 密码<br>method: 加密方式，如果没有使用则填入none<br>后面的配置则根据ssr的配置填入即可</p></blockquote><h2 id="启动-关闭客户端"><a href="#启动-关闭客户端" class="headerlink" title="启动\关闭客户端"></a>启动\关闭客户端</h2><figure class="highlight crmsh"><table><tr><td class="code"><pre><code class="hljs crmsh"><span class="hljs-comment"># 启动</span><br>./ssr <span class="hljs-literal">start</span><br><span class="hljs-comment"># 关闭</span><br>./ssr <span class="hljs-literal">stop</span><br></code></pre></td></tr></table></figure><p>如果没有任何异常则表示启动成功</p><h2 id="配置临时变量"><a href="#配置临时变量" class="headerlink" title="配置临时变量"></a>配置临时变量</h2><p>根据自己设置的本地路径和本地端口号来配置下面的字段。<br><figure class="highlight routeros"><table><tr><td class="code"><pre><code class="hljs routeros"><span class="hljs-builtin-name">export</span> <span class="hljs-attribute">http_proxy</span>=<span class="hljs-string">&quot;socks5://127.0.0.1:1080&quot;</span><br><span class="hljs-builtin-name">export</span> <span class="hljs-attribute">https_proxy</span>=<span class="hljs-string">&quot;socks5://127.0.0.1:1080&quot;</span><br><span class="hljs-comment"># 或者</span><br><span class="hljs-builtin-name">export</span> <span class="hljs-attribute">ALL_PROXY</span>=socks5://127.0.0.1:1080<br></code></pre></td></tr></table></figure></p><h2 id="验证"><a href="#验证" class="headerlink" title="验证"></a>验证</h2><figure class="highlight llvm"><table><tr><td class="code"><pre><code class="hljs llvm">curl cip.<span class="hljs-keyword">cc</span><br></code></pre></td></tr></table></figure><p>回车后可以看到当前电脑的配置是否与ssr相同。<br>但是Shadowsocks属于socks5协议，默认不对http协议的请求进行代理比如在以上配置完成后执行如下命令是访问不了Google的。<br><figure class="highlight nginx"><table><tr><td class="code"><pre><code class="hljs nginx"><span class="hljs-attribute">curl</span> www.google.com<br></code></pre></td></tr></table></figure></p><h2 id="配置转发"><a href="#配置转发" class="headerlink" title="配置转发"></a>配置转发</h2><p>借助privoxy把socks5转为HTTP协议即可。<br><figure class="highlight vala"><table><tr><td class="code"><pre><code class="hljs vala"><span class="hljs-meta"># 全局环境变量/etc/profile</span><br><span class="hljs-meta"># 用户环境变量~/.bash_profile或者~/.bashrc</span><br><span class="hljs-meta"># ======================</span><br><span class="hljs-meta"># 选择以上一个环境变量配置文件进行编辑（比如选择~/.bashrc）</span><br><span class="hljs-meta"># 如果是zsh则选择~/.zshrc</span><br>$ vim ~/.bashrc<br></code></pre></td></tr></table></figure><br>添加如下环境变量，注意这里的端口号为8118：<br><figure class="highlight routeros"><table><tr><td class="code"><pre><code class="hljs routeros"><span class="hljs-attribute">proxy</span>=<span class="hljs-string">&quot;http://127.0.0.1:8118&quot;</span><br><span class="hljs-builtin-name">export</span> <span class="hljs-attribute">https_proxy</span>=<span class="hljs-variable">$proxy</span><br><span class="hljs-builtin-name">export</span> <span class="hljs-attribute">http_proxy</span>=<span class="hljs-variable">$proxy</span><br><span class="hljs-builtin-name">export</span> <span class="hljs-attribute">ftp_proxy</span>=<span class="hljs-variable">$proxy</span><br></code></pre></td></tr></table></figure><br>加载环境<br><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">source</span> ~/.bashrc<br></code></pre></td></tr></table></figure></p><h1 id="配置privoxy"><a href="#配置privoxy" class="headerlink" title="配置privoxy"></a>配置privoxy</h1><figure class="highlight awk"><table><tr><td class="code"><pre><code class="hljs awk">yum install privoxy -y<br>vim <span class="hljs-regexp">/etc/</span>privoxy/config<br></code></pre></td></tr></table></figure><blockquote><p>找到<em>listen-address</em>，修改为<em>listen-address 127.0.0.1:8118</em><br>找到<em>forward-socks5</em>并且被注释的一行，修改为<em>forward-socks5 / 127.0.0.1:1080 .</em></p></blockquote><p>完成后开启服务：<br><figure class="highlight vala"><table><tr><td class="code"><pre><code class="hljs vala"><span class="hljs-meta">#启动</span><br>systemctl start privoxy<br><span class="hljs-meta">#关闭</span><br>systemctl stop privoxy<br><span class="hljs-meta">#开机启动</span><br>systemctl enable privoxy<br><span class="hljs-meta">#查看状态</span><br>systemctl status privoxy<br></code></pre></td></tr></table></figure></p><p>测试访问外网:<br><figure class="highlight powershell"><table><tr><td class="code"><pre><code class="hljs powershell"><span class="hljs-comment"># 访问需要代理才能访问的google</span><br><span class="hljs-variable">$</span> <span class="hljs-built_in">curl</span> www.google.com<br><span class="hljs-comment"># 访问搜狐的这个接口能够返回你的IP地址</span><br><span class="hljs-variable">$</span> <span class="hljs-built_in">curl</span> <span class="hljs-string">&quot;http://pv.sohu.com/cityjson?ie=utf-8&quot;</span><br></code></pre></td></tr></table></figure></p><h1 id="pac模式代理"><a href="#pac模式代理" class="headerlink" title="pac模式代理"></a>pac模式代理</h1><p>该模式提供pac规则文件来指定http走代理，这样可以只需要对pac文件维护就可以管理http代理。</p><p>这里需要使用pip下载而不能使用pip3，由于代码很老，只支持python2.x。<br><figure class="highlight crmsh"><table><tr><td class="code"><pre><code class="hljs crmsh">pip install --<span class="hljs-keyword">user</span> <span class="hljs-title">gfwlist2privoxy</span><br></code></pre></td></tr></table></figure></p><p>获取pac文件<br><figure class="highlight awk"><table><tr><td class="code"><pre><code class="hljs awk">cd /tmp<br>wget https:<span class="hljs-regexp">//</span>raw.githubusercontent.com<span class="hljs-regexp">/gfwlist/g</span>fwlist<span class="hljs-regexp">/master/g</span>fwlist.txt<br>gfwlist2privoxy -i gfwlist.txt -f gfwlist.action -p <span class="hljs-number">127.0</span>.<span class="hljs-number">0.1</span>:<span class="hljs-number">1080</span> -t socks5<br>cp gfwlist.action <span class="hljs-regexp">/etc/</span>privoxy/<br></code></pre></td></tr></table></figure><br>其中gfwlist.action就是privoxy的pac文件，可以进入看一下文件的内容，如果需要的话，可以删除关于<strong>github.xxxx</strong>的(可能是我的代理问题)。</p><p>修改privoxy的配置文件<br><figure class="highlight vala"><table><tr><td class="code"><pre><code class="hljs vala">vim /etc/privoxy/config<br><br><span class="hljs-meta"># 注释掉这行，防止pac失效，而全部走代理了</span><br><span class="hljs-meta"># forward-socks5 / 127.0.0.1:1080 .</span><br><span class="hljs-meta"># 添加激活pac文件</span><br>actionsfile gfwlist.action<br></code></pre></td></tr></table></figure></p><p>最后重启即可<br><figure class="highlight ebnf"><table><tr><td class="code"><pre><code class="hljs ebnf"><span class="hljs-attribute">service privoxy restart</span><br></code></pre></td></tr></table></figure></p>]]></content>
    
    
    <summary type="html">在centos终端配置ssr</summary>
    
    
    
    <category term="ssr" scheme="http://example.com/categories/ssr/"/>
    
    
    <category term="ssr" scheme="http://example.com/tags/ssr/"/>
    
  </entry>
  
  <entry>
    <title>数据结构与算法分析阅读笔记其二</title>
    <link href="http://example.com/2020/12/14/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95%E5%88%86%E6%9E%90%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0%E5%85%B6%E4%BA%8C/"/>
    <id>http://example.com/2020/12/14/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95%E5%88%86%E6%9E%90%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0%E5%85%B6%E4%BA%8C/</id>
    <published>2020-12-14T14:34:39.000Z</published>
    <updated>2021-04-17T07:10:38.655Z</updated>
    
    <content type="html"><![CDATA[<h2 id="表、栈和队列"><a href="#表、栈和队列" class="headerlink" title="表、栈和队列"></a>表、栈和队列</h2><p><strong>抽象数据类型（abstract data type,ADT）</strong>是带有一组操作的一些对象的集合。</p><h1 id="表"><a href="#表" class="headerlink" title="表"></a>表</h1><p>表一般包括普通的数组表和链表。</p><h3 id="Collection接口"><a href="#Collection接口" class="headerlink" title="Collection接口"></a>Collection接口</h3><p>该功能包含了一些重要的部分。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">Collection</span>&lt;<span class="hljs-title">AnyType</span>&gt; <span class="hljs-keyword">extends</span> <span class="hljs-title">Iterable</span>&lt;<span class="hljs-title">AnyType</span>&gt; </span>&#123;<br><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">size</span><span class="hljs-params">()</span></span>;<br><span class="hljs-function"><span class="hljs-keyword">boolean</span> <span class="hljs-title">isEmpty</span><span class="hljs-params">()</span></span>;<br><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">clear</span><span class="hljs-params">()</span></span>;<br><span class="hljs-function"><span class="hljs-keyword">boolean</span> <span class="hljs-title">contains</span><span class="hljs-params">(AnyType x)</span></span>;<br><span class="hljs-function"><span class="hljs-keyword">boolean</span> <span class="hljs-title">add</span><span class="hljs-params">(AnyType x)</span></span>;<br><span class="hljs-function"><span class="hljs-keyword">boolean</span> <span class="hljs-title">remove</span><span class="hljs-params">(AnyType x)</span></span>;<br>java.util.<span class="hljs-function">Iterator&lt;AnyType&gt; <span class="hljs-title">iterator</span><span class="hljs-params">()</span></span>;<br>&#125;<br></code></pre></td></tr></table></figure><br>函数的功能基本上可以通过英文名看出。<br>Collection接口扩展了Iterable接口。实现Iterable接口的那些类可以拥有增强的for循环，该循环用于这些类之上来观察他们所有的项。</p><h3 id="Iterator接口"><a href="#Iterator接口" class="headerlink" title="Iterator接口"></a>Iterator接口</h3><p>实现Iterable接口的集合必须提供一个iterator的方法。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">Iterator</span>&lt;<span class="hljs-title">AnyType</span>&gt; </span>&#123;<br><span class="hljs-function"><span class="hljs-keyword">boolean</span> <span class="hljs-title">hasNext</span><span class="hljs-params">()</span></span>;<br><span class="hljs-function">AnyType <span class="hljs-title">next</span><span class="hljs-params">()</span></span>;<br><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">remove</span><span class="hljs-params">()</span></span>;<br>&#125;<br></code></pre></td></tr></table></figure><br><strong>思路：</strong><br>通过iterator方法，每个集合均可创建并返回给客户一个实现Iterator接口的对象，并将当前为止的概念在对象内部存储下来。</p><blockquote><p>每次调用next都会返回集合的下一项。<br>hasNext用来告诉是否存在下一项。<br>删除有next最新返回的项。</p></blockquote><p>当编译器见到一个正在用于Iterable的对象的增强的for循环的时候，他用对iterator方法的那些调用代替增强的for循环以得到一个Iterator对象，然后调用next和hasNext。<br><strong>注意：当使用Iterator（而不是通过一个增强的for循环间接使用）时，重要的是记住一个基本法则：如果对正在被迭代的集合进行结构上的改变（add、remove、clear）那么迭代器就不再合法（抛出异常）。如果迭代器用了自己的remove方法，那么这个迭代器依旧合法。</strong></p><h3 id="List接口、ArrayList、LinkedList类"><a href="#List接口、ArrayList、LinkedList类" class="headerlink" title="List接口、ArrayList、LinkedList类"></a>List接口、ArrayList、LinkedList类</h3><p>先看看表(List)，除了Collection接口外，还需要一些其他方法：<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">List</span>&lt;<span class="hljs-title">AnyType</span>&gt; <span class="hljs-keyword">extends</span> <span class="hljs-title">Collection</span>&lt;<span class="hljs-title">AnyType</span>&gt; </span>&#123;<br><span class="hljs-function">AnyType <span class="hljs-title">get</span><span class="hljs-params">(<span class="hljs-keyword">int</span> idx)</span></span>;<br><span class="hljs-function">AnyType <span class="hljs-title">set</span><span class="hljs-params">(<span class="hljs-keyword">int</span> idx, AnyType newVal)</span></span>;<br><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">add</span><span class="hljs-params">(<span class="hljs-keyword">int</span> idx, AnyType x)</span></span>;<br><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">remove</span><span class="hljs-params">(<span class="hljs-keyword">int</span> idx)</span></span>;<br><br><span class="hljs-function">ListIterator&lt;AnyType&gt; <span class="hljs-title">listIterator</span><span class="hljs-params">(<span class="hljs-keyword">int</span> pos)</span></span>;<br>&#125;<br></code></pre></td></tr></table></figure><br><strong>说明：</strong></p><blockquote><p>1.get和set使得用户可以通过索引idx获取或修改表中的值。<br>2.List接口指定ListIterator方法，他返回一个更复杂的迭代器。</p></blockquote><p><strong>ArrayList 和 LinkedList</strong></p><blockquote><p>ArrayList：提供了一种可增长数组的实现。优点在于对get、set的调用花费常数时间；缺点则是在新增和删除时花费代价较高，除非变动是在末端。<br>LinkedList：提供了双链表的实现。优点在于对新增和删除的开销很小（变动项的位置是已知的）；缺点则是不容易做索引，对get的调用是昂贵的，除非非常接近端点位置。</p></blockquote><p><strong>简单示例：</strong><br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">makeList1</span><span class="hljs-params">(List&lt;Integer&gt; lst, <span class="hljs-keyword">int</span> N)</span> </span>&#123;<br>lst.clear();<br><span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i=<span class="hljs-number">0</span>; i&lt;N; i++)<br>lst.add(i);<br>&#125;<br></code></pre></td></tr></table></figure><br>不管是ArrayList还是LinkedList，都是运行O(N)。因为每次调用都是在末端进行（忽略ArrayList的扩展问题a）,因此添加的花费时间为常数时间。</p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">makeList2</span><span class="hljs-params">(List&lt;Integer&gt; lst, <span class="hljs-keyword">int</span> N)</span> </span>&#123;<br>lst.clear();<br><span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i=<span class="hljs-number">0</span>; i&lt;N; i++)<br>lst.add(<span class="hljs-number">0</span>, i);<br>&#125;<br></code></pre></td></tr></table></figure><p>它对于LinkedList的运行时间是O(N)，但是对于ArrayList的运行时间则是O(N^2)，因为在ArrayList中，在前端进行添加数据是一个O(N)的操作。</p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">int</span> <span class="hljs-title">sum</span><span class="hljs-params">(List&lt;Integer&gt; lst)</span> </span>&#123;<br><span class="hljs-keyword">int</span> total = <span class="hljs-number">0</span>;<br><span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i=<span class="hljs-number">0</span>; i&lt;N; i++)<br>total += lst.get(i);<br>&#125;<br></code></pre></td></tr></table></figure><p>这里对于ArrayList的运行时间是O(N)，但是对于LinkedList运行时间则是O(N^2)，因为他在对get的调用时间为一个O(N)。<strong>但是要是使用一个增强的for循环，那么他对任何list的运行都是O(N)，因为迭代器将有效的从一项到下一项进行推进。</strong></p><p><strong>示例：删除所有的偶数项</strong><br>在一个数组中，如：6、5、1、4、2。删除所有的偶数，结果为5、1.<br>在使用ArryaList是一个失败的策略。因为ArrayList也和一个地方删除都是低效率的。而LinkedList则会暴露两个问题，get的效率不高，remove同样低效，因为到达位置i的代价是昂贵的。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">removeEvensVer1</span><span class="hljs-params">(List&lt;Integer&gt; lst)</span> </span>&#123;<br><span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>;<br><span class="hljs-keyword">while</span>(i &lt; lst.size())<br><span class="hljs-keyword">if</span> (lst.get(i)%<span class="hljs-number">2</span> == <span class="hljs-number">0</span>)<br>lst.remove(i);<br><span class="hljs-keyword">else</span><br>i++;<br>&#125;<br></code></pre></td></tr></table></figure><br>因此上面的代码对这两个List使用都是低效率的。</p><p>而下面则是通过迭代器来一步步遍历该表。这里并不是使用get获取，而是使用一个迭代器遍历，获取了高效率，但是在使用remove方式的对象是Collection的接口，因此该操作依旧是低效率的。<strong>最后需要注意的该程序会发生异常，因为在增强for循环中使用list.remove会导致迭代器非法。</strong><br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">removeEvensVer2</span><span class="hljs-params">(List&lt;Integer&gt; lst)</span> </span>&#123;<br><span class="hljs-keyword">for</span>(Integer x: lst)<br><span class="hljs-keyword">if</span> (x % <span class="hljs-number">2</span> == <span class="hljs-number">0</span>)<br>lst.remove(x);<br>&#125;<br></code></pre></td></tr></table></figure></p><p>最后则是使用iterator正确的方式：<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">removeEvensVer3</span><span class="hljs-params">(List&lt;Integer&gt; lst)</span> </span>&#123;<br>Iterator&lt;Integer&gt; itr = lst.iterator();<br><span class="hljs-keyword">while</span>(itr.hasNext())<br><span class="hljs-keyword">if</span> (itr.next()%<span class="hljs-number">2</span> == <span class="hljs-number">0</span>)<br>itr.remove();<br>&#125;<br></code></pre></td></tr></table></figure><br>看到对于LinkedList中，由于对迭代器的remove方法调用只是花费常数时间，因此对于LinkedList整个函数花费时间是线性的；而对于ArrayList，即使使用迭代器删除节点，但是remove后必须对数组项进行移动，因此仍然花费二次时间。</p><h3 id="关于ListIterator接口"><a href="#关于ListIterator接口" class="headerlink" title="关于ListIterator接口"></a>关于ListIterator接口</h3><p>之前说该接口扩展了List的Iterator的功能。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">ListIterator</span>&lt;<span class="hljs-title">AnyType</span>&gt; <span class="hljs-keyword">extends</span> <span class="hljs-title">Iterator</span>&lt;<span class="hljs-title">AnyType</span>&gt; </span>&#123;<br><span class="hljs-function"><span class="hljs-keyword">boolean</span> <span class="hljs-title">hasPrevious</span><span class="hljs-params">()</span></span>;<br><span class="hljs-function">AnyType <span class="hljs-title">previous</span><span class="hljs-params">()</span></span>;<br><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">add</span><span class="hljs-params">(AnyType x)</span></span>;<br><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">set</span><span class="hljs-params">(AnyType newVal)</span></span>;<br>&#125;<br></code></pre></td></tr></table></figure></p><blockquote><p>1.previous 和 hasPrevious 可以从表后向前进行遍历。<br>2.add方法，将一个新的项以当前位置放入表中。同remove一样，他也是通过迭代器看作相对位置。这一功能对ArrayList更有帮助。<br>3.set方法，改变被迭代器看到的值，这一功能对LinkedList更具有优势。</p></blockquote><h1 id="ArrayList类的实现"><a href="#ArrayList类的实现" class="headerlink" title="ArrayList类的实现"></a>ArrayList类的实现</h1><p>主要通过实际设计一个MyArrayList，更能看清ArrayList中代码设计的思考点。显概括主要的细节：</p><blockquote><p>1.MyArrayList将保持基础数组，数组的容量，以及存储在MyArrayList中的当前项数。<br>2.MyArrayList将提供一种机制以改变基础数组的容量。<br>3.MyArrayList将提供get和set的实现。<br>4.MyArrayList将提供基本的例程，如size、isEmpyt、clear、remove、add等。<br>5.MyArrayList将提供一个实现Iterator接口的类。这个类将存储迭代序列中的下一项的下标，提供next、hasNext、remove等方法。MyArrayList的迭代器方法直接返回实现Iterator接口的该类的新构造的实例。</p></blockquote><h3 id="基本类"><a href="#基本类" class="headerlink" title="基本类"></a>基本类</h3><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> classs MyArrayList&lt;AnyType&gt; implements Iterable&lt;AnyType&gt; &#123;<br><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> DEFAULT_CAPACITY = <span class="hljs-number">10</span>;<br><span class="hljs-keyword">private</span> <span class="hljs-keyword">int</span> theSize;<br><span class="hljs-keyword">private</span> AnyType[] theItems;<br><br><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">MyArrayList</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123; doClear(); &#125;<br><br><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">clear</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123; doClear(); &#125;<br><br><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title">doClear</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123; theSize = <span class="hljs-number">0</span>; ensureCapacity(DEFAULT_CAPACITY); &#125;<br><br><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">size</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123; <span class="hljs-keyword">return</span> theSize; &#125;<br><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">isEmpty</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123; <span class="hljs-keyword">return</span> size() == <span class="hljs-number">0</span>; &#125;<br><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">trimToSize</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123; ensureCapacity(size()); &#125;<br><br><span class="hljs-function"><span class="hljs-keyword">public</span> AnyType <span class="hljs-title">get</span><span class="hljs-params">(<span class="hljs-keyword">int</span> idx)</span> </span>&#123;<br><span class="hljs-keyword">if</span> (idx &lt; <span class="hljs-number">0</span> || idx &gt;= size())<br>throw...<br><span class="hljs-keyword">return</span> theItems[idx];<br>&#125;<br><br><span class="hljs-function"><span class="hljs-keyword">public</span> AnyType <span class="hljs-title">set</span><span class="hljs-params">(<span class="hljs-keyword">int</span> idx, AnyType newVal)</span> </span>&#123;<br><span class="hljs-keyword">if</span> (idx &lt; <span class="hljs-number">0</span> || idx &gt;= size())<br>throw...<br>AnyType old = theItems[idx];<br>theItems[idx] = newVal;<br><span class="hljs-keyword">return</span> old;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">ensureCapacity</span><span class="hljs-params">(<span class="hljs-keyword">int</span> newCapacity)</span> </span>&#123;<br><span class="hljs-keyword">if</span> (newCapacity &lt; theSize)<br><span class="hljs-keyword">return</span>;<br>AnyType[] old = theItems;<br>theItems = (AnyType[]) <span class="hljs-keyword">new</span> Object[newCapacity];<br><span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i=<span class="hljs-number">0</span>; i&lt;size(); i++)<br>theItems[i] = old[i];<br>&#125;<br><br><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">add</span><span class="hljs-params">(AnyType x)</span> </span>&#123;<br>add(size(), x);<br><span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">add</span><span class="hljs-params">(<span class="hljs-keyword">int</span> idx, AnyType x)</span> </span>&#123;<br><span class="hljs-keyword">if</span> (theItems.length == size())<br>ensureCapacity(size() * <span class="hljs-number">2</span> + <span class="hljs-number">1</span>);<br><span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = theSize; i&gt;idx; i--)<br>theItems[i] = theItems[i-<span class="hljs-number">1</span>];<br>theItems[idx] = x;<br>theSize++;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-keyword">public</span> AnyType <span class="hljs-title">remove</span><span class="hljs-params">(<span class="hljs-keyword">int</span> idx)</span> </span>&#123;<br>AnyType removedItem = theItems[idx];<br><span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = idx; i&lt;size() - <span class="hljs-number">1</span>; i++)<br>theItems[i] = theItems[i + <span class="hljs-number">1</span>];<br>theSize--;<br><span class="hljs-keyword">return</span> removedItem;<br>&#125;<br><br><span class="hljs-keyword">public</span> java.util.<span class="hljs-function">Iterator&lt;AnyType&gt; <span class="hljs-title">iterator</span><span class="hljs-params">()</span> </span><br><span class="hljs-function"></span>&#123;<span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> ArrayListIterator();&#125;<br><br><span class="hljs-keyword">private</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ArrayListIterator</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">java</span>.<span class="hljs-title">util</span>.<span class="hljs-title">Iterator</span>&lt;<span class="hljs-title">AnyType</span>&gt; </span>&#123;<br><span class="hljs-keyword">private</span> <span class="hljs-keyword">int</span> current = <span class="hljs-number">0</span>;<br><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">hasNext</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123; <span class="hljs-keyword">return</span> current &lt; size(); &#125;<br><br><span class="hljs-function"><span class="hljs-keyword">public</span> AnyType <span class="hljs-title">next</span><span class="hljs-params">()</span> </span>&#123;<br><span class="hljs-keyword">if</span> (!hasNext())<br><span class="hljs-keyword">throw</span> ...<br><span class="hljs-keyword">return</span> theItems[current++];<br>&#125;<br><br><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">remove</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123; MyArrayList.<span class="hljs-keyword">this</span>.remove(--current); &#125;<br>&#125;<br>&#125;<br></code></pre></td></tr></table></figure><h1 id="LinkedList类的实现"><a href="#LinkedList类的实现" class="headerlink" title="LinkedList类的实现"></a>LinkedList类的实现</h1><p>通过设计一个MyLinkedList的实现。作为双链表的方式来实现，还需要保留两端的引用。</p><blockquote><p>1.MyLinkedList类本身，包含到两端的链、表的大小及一些方法。<br>2.Node类，一个节点包含数据以及前一个节点和后一个节点。<br>3.LinkedListIterator类，该类抽象类位置的概念。提供了next、hasNext、remove等方法。<br>4.添加头节点和尾节点，这些是标记节点，可以通过排除许多特殊情形极大的简化了编码。</p></blockquote><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">MyLinkedList</span>&lt;<span class="hljs-title">AnyType</span>&gt; <span class="hljs-keyword">implements</span> <span class="hljs-title">Iterable</span>&lt;<span class="hljs-title">AnyType</span>&gt; </span>&#123;<br><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Node</span>&lt;<span class="hljs-title">AnyType</span>&gt; </span>&#123;<br><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">Node</span><span class="hljs-params">(AnyType d, Node&lt;AnyType&gt; p, Node&lt;AnyType&gt; n)</span></span><br><span class="hljs-function"></span>&#123; data=d; prev=p; next=n; &#125;<br><br><span class="hljs-keyword">public</span> AnyType data;<br><span class="hljs-keyword">public</span> Node&lt;AnyType&gt; prev;<br><span class="hljs-keyword">public</span> Node&lt;AnyType&gt; next;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">MyLinkedList</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123; doClear(); &#125;<br><br><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">clear</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123; doClear() &#125;<br><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">size</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123; <span class="hljs-keyword">return</span> theSize; &#125;<br><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">isEmpty</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123; <span class="hljs-keyword">return</span> size()==<span class="hljs-number">0</span>; &#125;<br><br><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">add</span><span class="hljs-params">(AnyType x)</span></span><br><span class="hljs-function"></span>&#123; add(size(), x); <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span> &#125;<br><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">add</span><span class="hljs-params">(<span class="hljs-keyword">int</span> idx, AnyType x)</span></span><br><span class="hljs-function"></span>&#123; addBefore(getNode(idx, <span class="hljs-number">0</span>, size()), x); &#125;<br><span class="hljs-function"><span class="hljs-keyword">public</span> AnyType <span class="hljs-title">get</span><span class="hljs-params">(<span class="hljs-keyword">int</span> idx)</span></span><br><span class="hljs-function"></span>&#123; <span class="hljs-keyword">return</span> getNode(idx).data; &#125;<br><span class="hljs-function"><span class="hljs-keyword">public</span> AnyType <span class="hljs-title">set</span><span class="hljs-params">(<span class="hljs-keyword">int</span> idx, AnyType newVal)</span> </span>&#123;<br>Node&lt;AnyType&gt; p = getNode(idx);<br>AnyType oldVal = p.data;<br>p.data = newVal;<br><span class="hljs-keyword">return</span> oldVal;<br>&#125;<br><span class="hljs-function"><span class="hljs-keyword">public</span> AnyType <span class="hljs-title">remove</span><span class="hljs-params">(<span class="hljs-keyword">int</span> idx)</span></span><br><span class="hljs-function"></span>&#123; <span class="hljs-keyword">return</span> remove(getNode(idx)); &#125;<br><br><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title">addBefore</span><span class="hljs-params">(Node&lt;AnyType&gt; p, AnyType x)</span></span><br><span class="hljs-function"></span><br><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title">addBefore</span><span class="hljs-params">(Node&lt;AnyType&gt; p, AnyType xp)</span></span><br><span class="hljs-function">&#125;</span><br></code></pre></td></tr></table></figure>]]></content>
    
    
    <summary type="html">数据结构与算法分析数据阅读笔记</summary>
    
    
    
    <category term="数据结构" scheme="http://example.com/categories/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/"/>
    
    <category term="算法分析" scheme="http://example.com/categories/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/%E7%AE%97%E6%B3%95%E5%88%86%E6%9E%90/"/>
    
    
    <category term="数据结构与算法分析" scheme="http://example.com/tags/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95%E5%88%86%E6%9E%90/"/>
    
  </entry>
  
  <entry>
    <title>数据结构与算法分析阅读笔记</title>
    <link href="http://example.com/2020/12/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95%E5%88%86%E6%9E%90%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/"/>
    <id>http://example.com/2020/12/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95%E5%88%86%E6%9E%90%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/</id>
    <published>2020-12-09T13:52:40.000Z</published>
    <updated>2021-04-17T07:10:38.655Z</updated>
    
    <content type="html"><![CDATA[<h2 id="数学知识复习"><a href="#数学知识复习" class="headerlink" title="数学知识复习"></a>数学知识复习</h2><p>后面会用到这些数学知识进行计算</p><h1 id="指数"><a href="#指数" class="headerlink" title="指数"></a>指数</h1><script type="math/tex; mode=display">X^A X^B = X^{A+B}</script><script type="math/tex; mode=display">\frac{X^A}{X^B} = X^{A-B}</script><script type="math/tex; mode=display">(X^A)^B = X^{AB}</script><script type="math/tex; mode=display">X^N+X^N = 2X^N \neq X^{2N}</script><script type="math/tex; mode=display">2^N+2^N = 2^{N+1}</script><h1 id="对数"><a href="#对数" class="headerlink" title="对数"></a>对数</h1><p>在计算机科学中，除非有特殊声明，否则所有的对数都以2为底的。<br>定义1.1:<script type="math/tex">X^A = B</script>当前仅当<script type="math/tex">\log_x B = A</script><br>定理1.1:<script type="math/tex">\log_A B = \frac{\log_C B}{\log_C A};\ A,B,C \gt 0, A \neq 1</script><br>定理1.2:<script type="math/tex">\log AB = \log A + \log B;\ A,B \gt 0</script></p><h1 id="级数"><a href="#级数" class="headerlink" title="级数"></a>级数</h1><p>示例1:<script type="math/tex">\sum_{i=0}^N 2^i = 2^{N+1}-1</script><br>示例2:<script type="math/tex">\sum_{i=0}^N A^i = \frac{A^{N+1}-1}{A-1}</script><br>在示例2中，如果<script type="math/tex">0 \lt N \lt 1</script>则:<script type="math/tex">\sum_{i=0}^N A^i \le \frac{1}{1-A}</script><br>当N趋向于<strong>无穷</strong>时，结果越趋向于上述值。这些公式是”几何级数”公式。<br>示例3:<script type="math/tex">\sum_{i=0}^N i = \frac{N(N+1)}{2} \approx \frac{N^2}{2}</script><br>示例4:<script type="math/tex">\sum_{i=0}^N i^2 = \frac{N(N+1)(2N+1)}{6} \approx \frac{N^3}{3}</script><br>示例5:<script type="math/tex">\sum_{i=0}^N i^k \approx \frac{N^{k+1}}{\vert k+1 \vert};k\neq-1</script><br>在示例5中如果k=-1时，示例5不成立。则需要下面的公式（数为调和数，其和为调和数和，误差称为欧拉常数)示例7:<script type="math/tex">H_N = \sum_{i=1}^N \frac{1}{i} \approx log_e N;\gamma \approx 0.57721566</script><br>示例7:<script type="math/tex">\sum_{i=1}^N f(N) = Nf(N)</script><br>示例8:<script type="math/tex">\sum_{i=n_0}^N f(i) = \sum_{i=1}^N f(i) - \sum_{i=1}^{n_0-1} f(i)</script></p><h1 id="模运算"><a href="#模运算" class="headerlink" title="模运算"></a>模运算</h1><p>概念:N整除A-B，那么就说A与B模N同余。<script type="math/tex">A \equiv B(mod \ N)</script> 无论是A还是B被N去除，所得的余数都是相同的。<br>公式1:<script type="math/tex">A+C \equiv B+C(mod\ N)</script><br>公式2:<script type="math/tex">AD \equiv BD(mod\ N)</script></p><h2 id="算法分析"><a href="#算法分析" class="headerlink" title="算法分析"></a>算法分析</h2><h1 id="数学基础"><a href="#数学基础" class="headerlink" title="数学基础"></a>数学基础</h1><p>这里书中描述了使用四个定义：</p><script type="math/tex; mode=display">定义1：如果存在正常数 c 和 n_0，使得当 N \geq n_0时T(N) \leq cf(N)，则记为T(N)=O(f(N))。</script><script type="math/tex; mode=display">定义2：如果存在正常数 c 和 n_0，使得当 N \geq n_0时T(N) \geq cg(N)，则记为T(N)=\Omega(f(N))。</script><script type="math/tex; mode=display">定义3：T(N)=\Theta(h(N))当且仅当T(N)=O(h(N))和T(N)=\Omega(h(N))。</script><script type="math/tex; mode=display">定义4：如果对每一正常数c都存在常数n_0使得当N \gt n_0时T(N)\lt cp(N)，则T(N)=o(p(N))。</script><script type="math/tex; mode=display">有时也可以说，如果T(N)=O(p(N))且T(N)\neq\Theta(p(N))，则T(N)=o(p(N))。</script><p>这些定义的目的就是要<strong>在函数间建立一种相对的级别</strong>。通过函数的值无法比较函数的大小，就需要通过<strong>相对增长率</strong>来比较函数。</p><h4 id="示例"><a href="#示例" class="headerlink" title="示例*"></a>示例*</h4><p>函数A=1000N，函数B=N*N。对于较小的数值函数A比函数B大，但是函数B的增长速度更快，因此后面函数B肯定比函数A大。而这个转折点的N=1000，**第一定义说明，存在某点N=1000。</p><script type="math/tex; mode=display">1000N=O(N^2)</script><p>这种记法称为<strong>大O标记法。</strong></p><h3 id="定义简单描述"><a href="#定义简单描述" class="headerlink" title="定义简单描述"></a>定义简单描述</h3><script type="math/tex; mode=display">定义1:T(N)的增长率小于或等于f(N)的增长率。</script><script type="math/tex; mode=display">定义2:T(N)的增长率大于或等于g(N)的增长率。</script><script type="math/tex; mode=display">定义3:T(N)的增长率等于h(N)的增长率。</script><script type="math/tex; mode=display">定义4:T(N)的增长率小于p(N)的增长率，不同于O，因为大O包含增长率相等的可能性。</script><h3 id="重点掌握的结论"><a href="#重点掌握的结论" class="headerlink" title="重点掌握的结论"></a>重点掌握的结论</h3><p>法则1:</p><script type="math/tex; mode=display">如果T_1(N) = O(f(N))且T_2(N) = O(g(N))</script><script type="math/tex; mode=display">那么(a)\  T_1(N)+T_2(N) = O(f(N)+g(N))(也可以写成max(O(f(N)),O(g(N))))</script><script type="math/tex; mode=display">(b)\ T_1(N) * T_2(N) = O(f(N) * g(N))</script><p>法则2:</p><script type="math/tex; mode=display">如果T(N)是k次多项式，T(N)=\Theta(N^k)</script><p>法则3:</p><script type="math/tex; mode=display">对任意常数k，log_k N = O(N)。对数增长非常缓慢</script><p>法则4:</p><script type="math/tex; mode=display">总能通过计算极限\lim_{N\to\infty}\frac{f(N)}{g(N)}来确定两个函数f(N)和g(N)的相对增长率(或着洛必达法则)</script><p>该极限可能有四种可能的值:</p><script type="math/tex; mode=display">极限是0，意味着f(N)=o(g(N))</script><script type="math/tex; mode=display">极限是c\neq0，意味着f(N)=\Theta(g(N))</script><script type="math/tex; mode=display">极限是\infty，意味着g(N)=o(f(N))</script><script type="math/tex; mode=display">极限摆动，两者无关(本书籍不会出现)</script><h3 id="各类函数的相应增长率："><a href="#各类函数的相应增长率：" class="headerlink" title="各类函数的相应增长率："></a>各类函数的相应增长率：</h3><div class="table-container"><table><thead><tr><th>函数</th><th>名称</th></tr></thead><tbody><tr><td>c</td><td>常数</td></tr><tr><td>log N</td><td>对数</td></tr><tr><td>log 2 N</td><td>对数平方</td></tr><tr><td>N</td><td>线性</td></tr><tr><td>N log N</td><td></td></tr><tr><td>N^2</td><td>二次</td></tr><tr><td>N^3</td><td>三次</td></tr><tr><td>2^N</td><td>指数</td></tr></tbody></table></div><h4 id="对比示例"><a href="#对比示例" class="headerlink" title="对比示例*"></a>对比示例*</h4><p>如：<br><strong>f(N)=N log N</strong><br><strong>g(N)=N^1.5</strong><br>比较哪个增长快，就是比较增长率的大小。<br>通过上面的学习知道两者的比较可以简化为<strong>log N</strong>和<strong>N^0.5</strong>两个函数<br>最后通过上面的表格可以看出，N的对数永远比N小<br>因此得出结论：g(N)大于f(N)</p><h3 id="坏习惯！！！"><a href="#坏习惯！！！" class="headerlink" title="坏习惯！！！"></a>坏习惯！！！</h3><ul><li>不要把常数带进来</li><li>不要把低阶项带进来</li><li>尽量简化</li><li>粗糙的精度可以减少细微的影响</li></ul><h3 id="运行时间计算方式"><a href="#运行时间计算方式" class="headerlink" title="运行时间计算方式"></a>运行时间计算方式</h3><p>这也是在计算大O时的主要方式，像前面介绍的，我们需要抛弃常量、抛弃低阶项，从而计算大O运行时间。<strong>由于大O是一个上界，因此我们需要仔细计算，绝不要低估程序的运行时间，因为分析的结果为在一定时间范围内能够终止程序提供了保障。</strong></p><h4 id="简单的例子"><a href="#简单的例子" class="headerlink" title="简单的例子*"></a>简单的例子*</h4><script type="math/tex; mode=display">这里计算一个\sum_{i=1}^N i^3</script><p>代码片段如下：<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">int</span> sum = <span class="hljs-number">0</span>;<br><span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = <span class="hljs-number">1</span>; i&lt;=n; i++)<br>sum += i * i * i;<br><span class="hljs-keyword">return</span> sum;<br></code></pre></td></tr></table></figure><br>程序非常简单。分析一下这里的计算时间：</p><blockquote><p>1.第一行和第四行各占用一个时间单元<br>2.第三行每运行一次就会占用4个单元格(两次乘法、一次加法、一次赋值)，执行N次占用4N个时间单元<br>3.第二行，初始化、比较、自增都隐含着开销，因此这里初始化为1个时间单元、比较测试为N+1个时间单元、自增为N个时间单元。共2N+2个时间单元<br>4.最后得到6N+6的时间单元<br>上述就是该函数片段的大O了。</p></blockquote><p>但是，在计算完毕后，我们发现工作量大，计算繁琐，并且这还是一个简单的函数。为了解决这个问题，前辈就提供了若干个一般法则。</p><h3 id="一般法则"><a href="#一般法则" class="headerlink" title="一般法则"></a>一般法则</h3><ul><li>法则1:for循环<br>  <strong>一个for循环运行时间至多是该循环内部语句的运行时间乘以迭代次数(包括测试)</strong></li><li>法则2:嵌套for循环<br>  <strong>从里向外进行计算</strong><br>下面代码计算为O(N^2):<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i=<span class="hljs-number">0</span>; i&lt;n; i++)<br><span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> j=<span class="hljs-number">0</span>; j&lt;n; j++)<br>k++<br></code></pre></td></tr></table></figure></li><li>法则3:顺序语句<br>  <strong>直接将各个语句时间求和即可</strong><br>下面计算为O(N)+O(N^2)=O(N^2):<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i=<span class="hljs-number">0</span>; i&lt;n; i++)<br>a[i]=<span class="hljs-number">0</span>;<br><span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i=<span class="hljs-number">0</span>; i&lt;n; i++)<br><span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> j=<span class="hljs-number">0</span>; j&lt;n; j++)<br>a[i] += a[j] + i + j;<br></code></pre></td></tr></table></figure></li><li>法则4:if/else语句<br>对于:<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> ( condition )<br>s1<br><span class="hljs-keyword">else</span><br>s2<br></code></pre></td></tr></table></figure>  <strong>一个if/else语句的运行时间永远不会大于他的比较时间加上s1和s2中运行时间的总和。</strong></li><li>递归<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">long</span> <span class="hljs-title">fib</span><span class="hljs-params">(<span class="hljs-keyword">int</span> n)</span> </span>&#123;<br><span class="hljs-keyword">if</span> (n &lt;= <span class="hljs-number">1</span>) &#123;<br><span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;<br>&#125; <span class="hljs-keyword">else</span> &#123;<br><span class="hljs-keyword">return</span> fib(n - <span class="hljs-number">1</span>) + fib(n - <span class="hljs-number">2</span>);<br>&#125;<br>&#125;<br></code></pre></td></tr></table></figure>当N=40左右的时候运行，程序的效率会低得吓人。<br>另T(N)等于程序fib的运行时间，在N=0、N=1，则我们可以算出运行时间。T(0)=T(1)=1。<br>若N&gt;2时可以看出总运行时间为：第三行的时间+第五行的时间。因此这里需要注意第五行的运行方式。<br>fib(n-1)为T(N-1)，因此类推得出总运行时间为：T(N-1)+T(N-2)+2，此时的2为第三行的的工作加上第五行的加法计算。<br>另外，fib(n)=fib(n-1)+fib(n-2)。<br>所以，T(N)&gt;=fib(n)。<br>证明(类似斐波那契数列方式)，当N&gt;4时，fib(N)&gt;=(3/2)^N。<br>由于这是一个指数函数，大致计算除函数的运行轨迹。</li></ul><h4 id="一般法则的实用示例"><a href="#一般法则的实用示例" class="headerlink" title="一般法则的实用示例*"></a>一般法则的实用示例*</h4><p><strong>问题：求解最大子序列。</strong></p><ul><li>算法一<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">int</span> <span class="hljs-title">maxSubSum1</span><span class="hljs-params">(<span class="hljs-keyword">int</span>[] a)</span> </span>&#123;<br><span class="hljs-keyword">int</span> maxSum = <span class="hljs-number">0</span>;<br><span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i=<span class="hljs-number">0</span>; i&lt;a.length; i++) &#123;<br><span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> j=i; j&lt;a.length; j++) &#123;<br><span class="hljs-keyword">int</span> thisSum = <span class="hljs-number">0</span>;<br><span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> k=i; k&lt;=j; k++)<br>thisSum += a[k];<br><br><span class="hljs-keyword">if</span> (thisSum &gt; maxSum)<br>maxSum = thisSum;<br>&#125;<br>&#125;<br><span class="hljs-keyword">return</span> maxSum;<br>&#125;<br></code></pre></td></tr></table></figure>算法类似穷举式地尝试所有的可能。<blockquote><p>1.第一个循环大小为N。<br>2.第二个循环大小为N-i，但是也可能是N。<br>3.第三个运行会直接影响到整个函数的运行时间，大小为j-i+1</p></blockquote></li></ul><p>分析后可以看一下运行时间(这里的1主要是<code>thisSum +=a[k]</code>，后者的if等语句运行时间并不在同一个数量级)：</p><script type="math/tex; mode=display">\sum_{i=0}^{N-1} \sum_{j=i}^{N-1} \sum_{k=i}^j 1</script><p>该和指出了程序中第7行被执行多少次。<br>计算：</p><script type="math/tex; mode=display">\sum_{k=i}^j 1 = j-i+1</script><script type="math/tex; mode=display">\sum_{j=i}^{N-1} (j-i+1) = \frac{(N-i+1)(N-i)}{2}</script><script type="math/tex; mode=display">\sum_{i=0}^{N-1} \frac{(N-i+1)(N-i)}{2} = \sum_{i=1}^N \frac{(N-i+1)(N-i+2)}{2}</script><script type="math/tex; mode=display">=\frac{1}{2}\sum_{i=1}^N i^2 - (N+\frac{3}{2})\sum_{i=1}^N i + \frac{1}{2}(N^2+3N+2)\sum_{i=1}^N 1</script><script type="math/tex; mode=display">=\frac{1}{2}\frac{N(N+1)(2N+1)}{6} - (N+\frac{3}{2})\frac{N(N+1)}{2}+\frac{N^2+3N+2}{2}N</script><script type="math/tex; mode=display">=\frac{N^3+3N^2+2N}{6}</script><p>得到该函数的答案是O(N^3)。</p><ul><li><p>算法二</p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> staic <span class="hljs-keyword">int</span> <span class="hljs-title">maxSubSum2</span><span class="hljs-params">(<span class="hljs-keyword">int</span> [] a)</span> </span>&#123;<br><span class="hljs-keyword">int</span> maxSum = <span class="hljs-number">0</span>;<br><span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i=<span class="hljs-number">0</span>; i&lt;a.length; i++) &#123;<br><span class="hljs-keyword">int</span> thisSum = <span class="hljs-number">0</span>;<br><span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> j=i; j&lt;a.length; j++) &#123;<br>thisSum += a[j];<br><span class="hljs-keyword">if</span> (thisSum &gt; maxSum)<br>maxSum = thisSum;<br>&#125;<br>&#125;<br><span class="hljs-keyword">return</span> maxSum;<br>&#125;<br></code></pre></td></tr></table></figure><p>从循环个数不难看出显然时O(N^2)。</p></li><li><p>算法三<br>对这个问题有一个递归和相对复杂的O(N logN)解法，在没有出现线性解法时，该解法一般是个极好的范例。<br>  <strong>该解法才用一种分治的策略。</strong><br>在本例中，最大子序列和可能出现的三处。整个部分的左半边、右半边活着跨越中部与左右半边只和。前两种情况可以<strong>递归求解</strong>。而中间部分则通过前半部分的最大和加上后半部分的最大和求的。</p></li></ul><div class="table-container"><table><thead><tr><th>前半部分</th><th>后半部分</th></tr></thead><tbody><tr><td>4、-3、5、-2</td><td>-1、2、6、-2</td></tr></tbody></table></div><p><strong>分析：</strong><br>前半部分最大子序列和为6(1-3)，后半部分最大子序列和为8(6-7)。<br>前半部分包含最后一个元素的最大和是4(1-4)，后半部分最大子序列和为7(5-7)。因此横跨这两部分且通过中间的最大和为11。<br><strong>结论：</strong><br>我们知道了最好的方式是包含两部分的元素。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">int</span> <span class="hljs-title">maxSumRec</span><span class="hljs-params">(<span class="hljs-keyword">int</span> [] a, <span class="hljs-keyword">int</span> left, <span class="hljs-keyword">int</span> right)</span> </span>&#123;<br><span class="hljs-keyword">if</span> (left == right)<br><span class="hljs-keyword">if</span> (a[left]&gt;<span class="hljs-number">0</span>)<br><span class="hljs-keyword">return</span> a[left];<br><span class="hljs-keyword">else</span><br><span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br><br><span class="hljs-keyword">int</span> center = (left + right) / <span class="hljs-number">2</span>;<br><span class="hljs-keyword">int</span> maxLeftSum = maxSumRec(a, left, center);<br><span class="hljs-keyword">int</span> maxRightSum = maxSumRec(a, center+<span class="hljs-number">1</span>, right);<br><br><span class="hljs-keyword">int</span> maxLeftBorderSum = <span class="hljs-number">0</span>, leftBorderSum = <span class="hljs-number">0</span>;<br><span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = center; i&gt;=left; i--) &#123;<br>leftBorderSum += a[i];<br><span class="hljs-keyword">if</span> (leftBorderSum &gt; maxLeftBorderSum)<br>maxLeftBorderSum = leftBorderSum;<br>&#125;<br><span class="hljs-keyword">int</span> maxRightBorderSum = <span class="hljs-number">0</span>, rightBorderSum = <span class="hljs-number">0</span>;<br><span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = center+<span class="hljs-number">1</span>;i&lt;right; i++) &#123;<br>rightBorderSum += a[i];<br><span class="hljs-keyword">if</span> (rightBorderSum &gt; maxRightBorderSum)<br>maxRightBorderSum = rightBorderSum;<br>&#125;<br><br><span class="hljs-keyword">return</span> max3(maxLeftSum, maxRightSum, maxLeftBorderSum + maxRightBorderSum);<br>&#125;<br><br><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">int</span> <span class="hljs-title">maxSubSum3</span><span class="hljs-params">(<span class="hljs-keyword">int</span> [] a)</span></span><br><span class="hljs-function">return <span class="hljs-title">maxSumRec</span><span class="hljs-params">(a, <span class="hljs-number">0</span>, a.length - <span class="hljs-number">1</span>)</span></span>;<br></code></pre></td></tr></table></figure><br><strong>说明：</strong></p><blockquote><p>1.2至6行处理基准情况。如果left==right，那么就只有一个元素，并且当该元素为非负时他就是最大子序列。<br>2.9、10两行进行两个递归调用。他们使用来处理小于原问题的问题进行。<br>3.12-17、18-23行计算到达中间分界处的两个最大和的和数。这两个值的和为扩展到左右两部分的最大和。<br>4.最后的max3则是计算3者的最大值。</p></blockquote><p><strong>计算：</strong></p><blockquote><p>1.另T(N)时求解大小的为N的最大子序列和问题所花费的时间。<br>2.如果N=1，则算法2至6行处理一个时间单位。T(1)=1。<br>3.12-23行，所有的两个循环会接触到素有的子元素，总共花费另O(N)。<br>4.9、10两行在调用时则是直接调用本身T(N/2)的时间单元。</p></blockquote><p>因此总的时间花费为：</p><script type="math/tex; mode=display">T(1) = 1</script><script type="math/tex; mode=display">T(N) = 2T(\frac{N}{2})+O(N)</script><script type="math/tex; mode=display">T(N)=2T(\frac{N}{2})+N</script><p>根据：</p><script type="math/tex; mode=display">T(1)=1;T(2)=2\times2;T(4)=4\times3;T(8)=8\times4;T(16)=16\times5$$得到$$N=2^k，则T(N)=N\times(k+1)=Nlog N+N=O(Nlog N)</script><ul><li>算法四<br>该算法更需要理解。<br>如果a[i]是负的，那么他不可能代表最优序列的起点，因为任何包含a[i]的作为起点的子序列都可以通过用a[i+1]做起点而得到改进。<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">int</span> <span class="hljs-title">maxSubSum4</span><span class="hljs-params">(<span class="hljs-keyword">int</span> [] a)</span> </span>&#123;<br><span class="hljs-keyword">int</span> maxSum = <span class="hljs-number">0</span>, thisSum = <span class="hljs-number">0</span>;<br><span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> j=<span class="hljs-number">0</span>; j&lt;a.length; j++) &#123;<br>thisSum += a[j];<br><span class="hljs-keyword">if</span> (thisSum &gt; maxSum)<br>maxSum = thisSum;<br><span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (thisSum = <span class="hljs-number">0</span>)<br>thisSum = <span class="hljs-number">0</span>;<br>&#125;<br><span class="hljs-keyword">return</span> maxSum;<br>&#125;<br></code></pre></td></tr></table></figure>  <strong>对于这些正确性不那么容易看出来的算法，正式的正确性证明几乎总是需要的。</strong></li></ul><h4 id="运行时间中的对数处理方式示例"><a href="#运行时间中的对数处理方式示例" class="headerlink" title="运行时间中的对数处理方式示例*"></a>运行时间中的对数处理方式示例*</h4><p>只有一些特殊种类的问题才能够程序O(log N)型。</p><ul><li><p>折半查找<br>给定一个整数X和整数A0，A1，…，A(N-1)，后者已经预先排序并在内存中，求下标i使得Ai=X，如果不在则返回i=-1。<br>  <strong>分析：</strong><br>明显的算法是从左到右扫描数据，其运行花费线性时间。然而数据已经排序完成，使得这不是最好的算法。该算法需要的是一个稳定的数据序列。</p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> &lt;AnyType extends Comparable&lt;? <span class="hljs-keyword">super</span> AnyType&gt;&gt;<br><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">binarySearch</span><span class="hljs-params">(AnyType[] a, AnyType x)</span> </span>&#123;<br><span class="hljs-keyword">int</span> low = <span class="hljs-number">0</span>, high = a.length - <span class="hljs-number">1</span>;<br><span class="hljs-keyword">while</span>(low &lt;= high) &#123;<br><span class="hljs-keyword">int</span> mid = (low + high) / <span class="hljs-number">2</span>;<br><span class="hljs-keyword">if</span> (a[mid].compareTo(x)&lt;<span class="hljs-number">0</span>)<br>low = mid + <span class="hljs-number">1</span>;<br><span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (a[mid].compareTo(x)&gt;<span class="hljs-number">0</span>)<br>high = mid - <span class="hljs-number">1</span>;<br><span class="hljs-keyword">else</span><br><span class="hljs-keyword">return</span> mid;<br>&#125;<br><span class="hljs-keyword">return</span> -<span class="hljs-number">1</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>  <strong>计算：</strong><br>每次循环中的花费时间为O(1)，因此循环次数非常终于。循环从high-low=N-1开始，并在high-low&lt;=-1结束。每次循环后high-low的值至少为该次循环前的值折半，于是循环的次数最多为:<script type="math/tex">\lceil log (N-1) \rceil + 2即:log (N)</script></p></li><li><p>欧几里德算法<br>示例使用的是计算最大公因数。两个整数的最大公约数(gcd)是同时整除两者的最大整数，例如：gcd(50,15)=5。</p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">long</span> <span class="hljs-title">gcd</span><span class="hljs-params">(<span class="hljs-keyword">long</span> m, <span class="hljs-keyword">long</span> n)</span> </span>&#123;<br><span class="hljs-keyword">while</span>(n != <span class="hljs-number">0</span>) &#123;<br><span class="hljs-keyword">long</span> rem = m % n;<br>m = n;<br>n = rem;<br>&#125;<br><span class="hljs-keyword">return</span> m;<br>&#125;<br></code></pre></td></tr></table></figure><p>上述代码假设M&gt;=N(如果N&gt;M，则循环的第一次迭代将他们互换)，连续计算余数知道余数是0为止，最后的非零余数就是最大公因数。<br>在两次迭代以后，余数最多是原始值的一半。迭代次数至多是:</p><script type="math/tex; mode=display">2log N=O(N)</script><p>欧几里德算法在平均情况下的性能需要大量篇幅的高度复杂的数学分析，其迭代的平均次数约为：</p><script type="math/tex; mode=display">\frac{(12\ ln2\ lnN)}{\pi^2} + 1.47</script></li><li><p>幂运算<br>用来处理一个整数的幂。<br>计算X^N的明显的算法是使用N-1次乘法自乘。有一种递归算法效果更好。N&lt;=1是该递归的基准情形。</p><script type="math/tex; mode=display">若N是偶数，有X^N=X^{\frac{N}{2}} \cdot X^{\frac{N}{2}}</script><script type="math/tex; mode=display">若N是奇数，有X^N=X^{\frac{N-1}{2}} \cdot X^{\frac{N-1}{2}} \cdot X</script><p>  <strong>例如：计算X^62，该算法只用到了9次乘法</strong></p><script type="math/tex; mode=display">X^3=(X^2)X,X^7=(X^3)^2X,X^{15}=(X^7)^2X,X^{31}=(X^{15})^2X,X^{62}=(X^{31})^2</script><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">long</span> <span class="hljs-title">pow</span><span class="hljs-params">(<span class="hljs-keyword">long</span> x, <span class="hljs-keyword">int</span> n)</span> </span>&#123;<br><span class="hljs-keyword">if</span> (n == <span class="hljs-number">0</span>)<br><span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;<br><span class="hljs-keyword">if</span> (n == <span class="hljs-number">1</span>)<br><span class="hljs-keyword">return</span> x;<br><span class="hljs-keyword">if</span> (isEven(n))<br><span class="hljs-keyword">return</span> pow(x*x, n/<span class="hljs-number">2</span>);<br><span class="hljs-keyword">else</span><br><span class="hljs-keyword">return</span> pow(x*x, n/<span class="hljs-number">2</span>)*;<br>&#125;<br></code></pre></td></tr></table></figure><p>程序将以O(log n)运行。</p></li></ul>]]></content>
    
    
    <summary type="html">数据结构与算法分析数据阅读笔记</summary>
    
    
    
    <category term="数据结构" scheme="http://example.com/categories/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/"/>
    
    <category term="算法分析" scheme="http://example.com/categories/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/%E7%AE%97%E6%B3%95%E5%88%86%E6%9E%90/"/>
    
    
    <category term="数据结构与算法分析" scheme="http://example.com/tags/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95%E5%88%86%E6%9E%90/"/>
    
  </entry>
  
  <entry>
    <title>Mac中Python3.x编译Caffe</title>
    <link href="http://example.com/2020/09/20/Mac%E4%B8%ADPython3-x%E7%BC%96%E8%AF%91Caffe/"/>
    <id>http://example.com/2020/09/20/Mac%E4%B8%ADPython3-x%E7%BC%96%E8%AF%91Caffe/</id>
    <published>2020-09-19T17:08:09.000Z</published>
    <updated>2021-04-17T07:10:38.651Z</updated>
    
    <content type="html"><![CDATA[<h2 id="一、环境"><a href="#一、环境" class="headerlink" title="一、环境"></a>一、环境</h2><blockquote><p>系统 Mac<br>conda 4.5.11<br>这里推荐<strong><em>anaconda</em></strong>来配置需您环境进行管理python。通过他来建立虚拟环境从而进行环境隔离，方便临时调试。<br>python 3.6<br>opencv 4.1</p></blockquote><h2 id="二、软件准备"><a href="#二、软件准备" class="headerlink" title="二、软件准备"></a>二、软件准备</h2><h2 id="anaconda"><a href="#anaconda" class="headerlink" title="anaconda"></a>anaconda</h2><p>可以去<a href="https://www.anaconda.com/products/individual">官网</a>或者<a href="https://mirror.tuna.tsinghua.edu.cn/help/anaconda/">镜像站下载</a>，这里主要了解环境配置即可。</p><ul><li><p>查看所有环境<br><code>conda envs --info</code></p></li><li><p>创建指定python版本及该环境名称<br><code>conda create --name test_name python=3.6</code></p></li><li><p>进入该环境，进入后会看到命令行前面有当前环境的名称，表示已经处于当前的python环境中<br><code>conda activate test_name</code></p></li></ul><h2 id="opencv4-1"><a href="#opencv4-1" class="headerlink" title="opencv4.1"></a>opencv4.1</h2><p>这里下载的是<strong><em>Sources</em></strong>文件，自己完成编译安装。<br><a href="https://opencv.org/releases/">官网下载</a></p><ul><li>1.解压缩包</li><li>2.进入opencv文件夹后创建build文件夹<figure class="highlight dos"><table><tr><td class="code"><pre><code class="hljs dos"><span class="hljs-built_in">cd</span> oepncv<br><span class="hljs-built_in">mkdir</span> build<br></code></pre></td></tr></table></figure></li><li>3.配置opencv<figure class="highlight routeros"><table><tr><td class="code"><pre><code class="hljs routeros">cd build<br>cmake -D <span class="hljs-attribute">CMAKE_BUILD_TYPE</span>=RELEASE \<br>      -D <span class="hljs-attribute">CMAKE_INSTALL_PREFIX</span>=/usr/local \<br>      -D <span class="hljs-attribute">BUILD_opencv_python2</span>=OFF \<br>      -D <span class="hljs-attribute">BUILD_opencv_python3</span>=ON <span class="hljs-built_in">..</span><br></code></pre></td></tr></table></figure></li><li>4.安装opencv<figure class="highlight gauss"><table><tr><td class="code"><pre><code class="hljs gauss"><span class="hljs-built_in">make</span> -j6<br><span class="hljs-built_in">make</span> install<br></code></pre></td></tr></table></figure></li><li>5.验证<br>输入如下命令查看版本号是否正确<figure class="highlight ebnf"><table><tr><td class="code"><pre><code class="hljs ebnf"><span class="hljs-attribute">opencv_version</span><br></code></pre></td></tr></table></figure></li></ul><h2 id="brew及相关软件"><a href="#brew及相关软件" class="headerlink" title="brew及相关软件"></a>brew及相关软件</h2><h4 id="下载brew"><a href="#下载brew" class="headerlink" title="下载brew"></a>下载brew</h4><figure class="highlight awk"><table><tr><td class="code"><pre><code class="hljs awk"><span class="hljs-regexp">/usr/</span>bin<span class="hljs-regexp">/ruby -e &quot;$(curl -fsSL https:/</span><span class="hljs-regexp">/raw.githubusercontent.com/</span>Homebrew<span class="hljs-regexp">/install/m</span>aster/install)<span class="hljs-string">&quot;</span><br></code></pre></td></tr></table></figure><h4 id="下载相关软件"><a href="#下载相关软件" class="headerlink" title="下载相关软件"></a>下载相关软件</h4><figure class="highlight mipsasm"><table><tr><td class="code"><pre><code class="hljs mipsasm"><span class="hljs-keyword">brew </span><span class="hljs-keyword">install </span>-vd snappy leveldb gflags glog szip lmdb<br><span class="hljs-keyword">brew </span><span class="hljs-keyword">install </span>openblas<br><span class="hljs-keyword">brew </span><span class="hljs-keyword">install </span>hdf5<br><span class="hljs-keyword">brew </span><span class="hljs-keyword">install </span><span class="hljs-keyword">boost </span><span class="hljs-keyword">boost-python3</span><br><span class="hljs-keyword">brew </span><span class="hljs-keyword">install </span>autoconf<br><span class="hljs-keyword">brew </span><span class="hljs-keyword">install </span>automake libtool<br></code></pre></td></tr></table></figure><h4 id="protobuf"><a href="#protobuf" class="headerlink" title="protobuf"></a>protobuf</h4><p>这里需要注意在安装protobuf的时候，需要查看一下版本，最好限定在3.5.1内，我在使用brew安装就是这个版本。<br><figure class="highlight mipsasm"><table><tr><td class="code"><pre><code class="hljs mipsasm"><span class="hljs-keyword">brew </span><span class="hljs-keyword">install </span>protobuf<br></code></pre></td></tr></table></figure><br>如果版本超出，则需要自己下载包并自己编译安装。<br><figure class="highlight awk"><table><tr><td class="code"><pre><code class="hljs awk">wget https:<span class="hljs-regexp">//gi</span>thub.com<span class="hljs-regexp">/protocolbuffers/</span>protobuf<span class="hljs-regexp">/archive/</span>v3.<span class="hljs-number">5.1</span>.zip<br>unzip v3.<span class="hljs-number">5.1</span>.zip<br>cd protobuf-<span class="hljs-number">3.5</span>.<span class="hljs-number">1</span><br>./autogen.sh<br>./configure<br>make<br>make install<br></code></pre></td></tr></table></figure></p><h2 id="Caffe下载"><a href="#Caffe下载" class="headerlink" title="Caffe下载"></a>Caffe下载</h2><figure class="highlight awk"><table><tr><td class="code"><pre><code class="hljs awk">git clone https:<span class="hljs-regexp">//gi</span>thub.com<span class="hljs-regexp">/BVLC/</span>caffe.git<br></code></pre></td></tr></table></figure><p>这样下载的软件就好了。</p><h2 id="三、配置文件"><a href="#三、配置文件" class="headerlink" title="三、配置文件"></a>三、配置文件</h2><p>此时开始配置Caffe项目。</p><h2 id="config文件拷贝"><a href="#config文件拷贝" class="headerlink" title="config文件拷贝"></a>config文件拷贝</h2><figure class="highlight reasonml"><table><tr><td class="code"><pre><code class="hljs reasonml">cd caffe/<br>cp <span class="hljs-module-access"><span class="hljs-module"><span class="hljs-identifier">Makefile</span>.</span></span>config.example <span class="hljs-module-access"><span class="hljs-module"><span class="hljs-identifier">Makefile</span>.</span></span>config<br></code></pre></td></tr></table></figure><h2 id="修改common-hpp文件"><a href="#修改common-hpp文件" class="headerlink" title="修改common.hpp文件"></a>修改common.hpp文件</h2><p>打开<code>./include/caffe/common.hpp</code>文件，找到70行，并在后面添加如下代码：<br><figure class="highlight vala"><table><tr><td class="code"><pre><code class="hljs vala"><span class="hljs-comment">// Supporting OpenCV4</span><br><span class="hljs-meta">#if (CV_MAJOR_VERSION == 4)</span><br><span class="hljs-meta">#define CV_LOAD_IMAGE_COLOR cv::IMREAD_COLOR</span><br><span class="hljs-meta">#define CV_LOAD_IMAGE_GRAYSCALE cv::IMREAD_GRAYSCALE</span><br><span class="hljs-meta">#endif</span><br></code></pre></td></tr></table></figure></p><h2 id="修改Makefile-config文件-重要"><a href="#修改Makefile-config文件-重要" class="headerlink" title="修改Makefile.config文件(重要)"></a>修改Makefile.config文件(重要)</h2><p>这里开始修改<strong><em>Makefile.config</em></strong>文件，这里非常重要，每一步都需要保证准确即可。</p><ul><li>1.取消 CPU_ONLY 前面的注释符号</li><li>2.取消 USE_OPENCV 前面的注释符号</li><li>3.取消 USE_HDF5 前面的注释符号，并将值改成 1 </li><li>4.取消 OPENCV_VERSION 前面的注释符号，并将值改成 4</li><li>5.将 BLAS 的值改成 open</li><li>6.配置python路径(有brew和anaconda两种方式，根据环境决定)</li><li>7.取消 WITH_PYTHON_LAYER 前面的注释符号</li><li>8.增加部分库的环境配置，有些库由于安装方式可能需要手动引入</li><li>9.blas环境配置</li><li>10.Mac需要添加CXX配置项</li></ul><p>下面是上述配置的最终代码(注意这里并不是最终文件而是需要修改的代码样式):<br><figure class="highlight awk"><table><tr><td class="code"><pre><code class="hljs awk"><span class="hljs-comment"># 1.取消 CPU\_ONLY 前面的注释符号</span><br>CPU_ONLY := <span class="hljs-number">1</span><br><br><span class="hljs-comment"># 2.取消 USE\_OPENCV 前面的注释符号</span><br>USE_OPENCV := <span class="hljs-number">0</span><br><br><span class="hljs-comment"># 3.取消 USE\_HDF5 前面的注释符号，并将值改成 1 </span><br>USE_HDF5 := <span class="hljs-number">1</span><br><br><span class="hljs-comment"># 4.取消 OPENCV\_VERSION 前面的注释符号，并将值改成 4</span><br>OPENCV_VERSION := <span class="hljs-number">4</span><br><br><span class="hljs-comment"># 5.将 BLAS 的值改成 open</span><br>BLAS := open<br><br><span class="hljs-comment"># 6.配置python路径(有brew和anaconda两种方式，根据环境决定)</span><br>ANACONDA_HOME := $(HOME)<span class="hljs-regexp">/anaconda3/</span>envs/end_plate_demo<br>PYTHON_INCLUDE := $(ANACONDA_HOME)/include \<br>$(ANACONDA_HOME)<span class="hljs-regexp">/include/</span>python3.<span class="hljs-number">6</span>m \<br>$(ANACONDA_HOME)<span class="hljs-regexp">/lib/</span>python3.<span class="hljs-number">6</span><span class="hljs-regexp">/site-packages/</span>numpy<span class="hljs-regexp">/core/i</span>nclude<br>PYTHON_LIB := $(ANACONDA_HOME)/lib<br><br><span class="hljs-comment"># 7.取消 WITH\_PYTHON\_LAYER 前面的注释符号</span><br>WITH_PYTHON_LAYER := <span class="hljs-number">1</span><br><br><span class="hljs-comment"># 8.增加部分库的环境配置，有些库由于安装方式可能需要手动引入</span><br><span class="hljs-comment"># 这里引入了openblas和boost-python3的lib库</span><br>INCLUDE_DIRS := $(PYTHON_INCLUDE) <span class="hljs-regexp">/usr/</span>local<span class="hljs-regexp">/include /u</span>sr<span class="hljs-regexp">/local/</span>Cellar<span class="hljs-regexp">/openblas/</span><span class="hljs-number">0.3</span>.<span class="hljs-number">10</span>_1/include<br>LIBRARY_DIRS := $(PYTHON_LIB) <span class="hljs-regexp">/usr/</span>local<span class="hljs-regexp">/lib /u</span>sr<span class="hljs-regexp">/lib /u</span>sr<span class="hljs-regexp">/local/</span>Cellar<span class="hljs-regexp">/openblas/</span><span class="hljs-number">0.3</span>.<span class="hljs-number">10</span>_1<span class="hljs-regexp">/lib /u</span>sr<span class="hljs-regexp">/local/</span>Cellar<span class="hljs-regexp">/boost-python3/</span><span class="hljs-number">1.73</span>.<span class="hljs-number">0</span>/lib<br><br><span class="hljs-comment"># 9.blas环境配置</span><br>BLAS_INCLUDE := $(shell brew --prefix openblas)/include<br>BLAS_LIB := $(shell brew --prefix openblas)/lib<br><br><span class="hljs-comment"># 10.Mac需要添加CXX配置项</span><br>CUSTOM_CXX := clang++ -std=c++<span class="hljs-number">11</span><br></code></pre></td></tr></table></figure></p><h2 id="四、预编译"><a href="#四、预编译" class="headerlink" title="四、预编译"></a>四、预编译</h2><figure class="highlight jboss-cli"><table><tr><td class="code"><pre><code class="hljs jboss-cli"><span class="hljs-keyword">cd</span> ~<span class="hljs-string">/caffe</span><br>mkdir build<br><span class="hljs-keyword">cd</span> build<br>cmake <span class="hljs-string">..</span><br></code></pre></td></tr></table></figure><h2 id="五、修改编译后的文件配置"><a href="#五、修改编译后的文件配置" class="headerlink" title="五、修改编译后的文件配置"></a>五、修改编译后的文件配置</h2><p>进入build文件夹</p><h2 id="修改CaffeConfig-cmake"><a href="#修改CaffeConfig-cmake" class="headerlink" title="修改CaffeConfig.cmake"></a>修改CaffeConfig.cmake</h2><p>查找并修改代码如下：<br><figure class="highlight stylus"><table><tr><td class="code"><pre><code class="hljs stylus"><span class="hljs-function"><span class="hljs-title">set</span><span class="hljs-params">(Caffe_CPU_ONLY ON)</span></span><br></code></pre></td></tr></table></figure></p><h2 id="修改CMakeCache-txt-重要"><a href="#修改CMakeCache-txt-重要" class="headerlink" title="修改CMakeCache.txt(重要)"></a>修改CMakeCache.txt(重要)</h2><p>注意，这里的代码修改非常重要，失败的原因是没有修改Boost相关路径导致。</p><h4 id="Python"><a href="#Python" class="headerlink" title="Python"></a>Python</h4><figure class="highlight awk"><table><tr><td class="code"><pre><code class="hljs awk">PYTHON_EXECUTABLE:FILEPATH=....<span class="hljs-regexp">/anaconda3/</span>envs<span class="hljs-regexp">/end_plate_demo/</span>bin/python3.<span class="hljs-number">6</span><br><br><span class="hljs-regexp">//</span>Path to a file.<br>PYTHON_INCLUDE_DIR:PATH=....<span class="hljs-regexp">/anaconda3/</span>envs<span class="hljs-regexp">/end_plate_demo/i</span>nclude/python3.<span class="hljs-number">6</span>m<br><br><span class="hljs-regexp">//</span>Path to a library.<br>PYTHON_LIBRARY:FILEPATH=....<span class="hljs-regexp">/anaconda3/</span>envs<span class="hljs-regexp">/end_plate_demo/</span>lib/libpython3.<span class="hljs-number">6</span>m.dylib<br></code></pre></td></tr></table></figure><h4 id="CXX"><a href="#CXX" class="headerlink" title="CXX"></a>CXX</h4><figure class="highlight avrasm"><table><tr><td class="code"><pre><code class="hljs avrasm"><span class="hljs-symbol">CMAKE_CXX_FLAGS:</span>STRING=-<span class="hljs-keyword">std</span>=c++<span class="hljs-number">11</span><br></code></pre></td></tr></table></figure><h4 id="Boost"><a href="#Boost" class="headerlink" title="Boost"></a>Boost</h4><figure class="highlight awk"><table><tr><td class="code"><pre><code class="hljs awk">Boost_PYTHON_LIBRARY_RELEASE:STRING=<span class="hljs-regexp">/usr/</span>local<span class="hljs-regexp">/Cellar/</span>boost-python3<span class="hljs-regexp">/1.73.0/</span>lib/libboost_python38-mt.dylib<br><br>Boost_SYSTEM_LIBRARY_RELEASE:STRING=<span class="hljs-regexp">/usr/</span>local<span class="hljs-regexp">/Cellar/</span>boost-python3<span class="hljs-regexp">/1.73.0/</span>lib/libboost_system-mt.dylib<br><br>Boost_THREAD_LIBRARY_RELEASE:STRING=<span class="hljs-regexp">/usr/</span>local<span class="hljs-regexp">/Cellar/</span>boost-python3<span class="hljs-regexp">/1.73.0/</span>lib/libboost_thread-mt.dylib<br></code></pre></td></tr></table></figure><h4 id="CUDA"><a href="#CUDA" class="headerlink" title="CUDA"></a>CUDA</h4><figure class="highlight pgsql"><table><tr><td class="code"><pre><code class="hljs pgsql">CPU_ONLY:<span class="hljs-type">BOOL</span>=<span class="hljs-keyword">ON</span><br></code></pre></td></tr></table></figure><h2 id="六、再次编译"><a href="#六、再次编译" class="headerlink" title="六、再次编译"></a>六、再次编译</h2><figure class="highlight apache"><table><tr><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">cmake</span> ..<br><span class="hljs-attribute">sudo</span> make <span class="hljs-literal">all</span> -j<span class="hljs-number">8</span><br><span class="hljs-attribute">sudo</span> make pycaffe -j<span class="hljs-number">8</span><br></code></pre></td></tr></table></figure><h2 id="七、环境引入"><a href="#七、环境引入" class="headerlink" title="七、环境引入"></a>七、环境引入</h2><figure class="highlight routeros"><table><tr><td class="code"><pre><code class="hljs routeros">vim ~/.bash_profile<br><span class="hljs-comment"># 添加如下</span><br><br><span class="hljs-attribute">CAFFE_HOME</span>=....caffe/python<br><span class="hljs-builtin-name">export</span> <span class="hljs-attribute">PATH</span>=<span class="hljs-variable">$PATH</span>:$&#123;CAFFE_HOME&#125;<br></code></pre></td></tr></table></figure>]]></content>
    
    
    <summary type="html">在Mac中Python3.x环境下编译Caffe</summary>
    
    
    
    <category term="机器学习" scheme="http://example.com/categories/%E6%9C%BA%E5%99%A8%E5%AD%A6%E4%B9%A0/"/>
    
    
    <category term="机器学习" scheme="http://example.com/tags/%E6%9C%BA%E5%99%A8%E5%AD%A6%E4%B9%A0/"/>
    
  </entry>
  
  <entry>
    <title>npm常用命令操作</title>
    <link href="http://example.com/2020/07/17/npm%E5%B8%B8%E7%94%A8%E5%91%BD%E4%BB%A4%E6%93%8D%E4%BD%9C/"/>
    <id>http://example.com/2020/07/17/npm%E5%B8%B8%E7%94%A8%E5%91%BD%E4%BB%A4%E6%93%8D%E4%BD%9C/</id>
    <published>2020-07-17T02:14:01.000Z</published>
    <updated>2021-04-17T07:10:38.655Z</updated>
    
    <content type="html"><![CDATA[<h2 id="npm配置"><a href="#npm配置" class="headerlink" title="npm配置"></a>npm配置</h2><figure class="highlight arduino"><table><tr><td class="code"><pre><code class="hljs arduino">npm config list<br><br></code></pre></td></tr></table></figure><h2 id="切换镜像"><a href="#切换镜像" class="headerlink" title="切换镜像"></a>切换镜像</h2><h3 id="查看当前镜像"><a href="#查看当前镜像" class="headerlink" title="查看当前镜像"></a>查看当前镜像</h3><figure class="highlight routeros"><table><tr><td class="code"><pre><code class="hljs routeros">npm<span class="hljs-built_in"> config </span><span class="hljs-builtin-name">get</span> registry<br></code></pre></td></tr></table></figure><h3 id="切换淘宝镜像"><a href="#切换淘宝镜像" class="headerlink" title="切换淘宝镜像"></a>切换淘宝镜像</h3><figure class="highlight tcl"><table><tr><td class="code"><pre><code class="hljs tcl">npm config <span class="hljs-keyword">set</span> <span class="hljs-keyword">registry</span> <span class="hljs-keyword">http</span>://<span class="hljs-keyword">registry</span>.npm.taobao.org/<br></code></pre></td></tr></table></figure><h3 id="切换官方镜像"><a href="#切换官方镜像" class="headerlink" title="切换官方镜像"></a>切换官方镜像</h3><figure class="highlight arduino"><table><tr><td class="code"><pre><code class="hljs arduino">npm config set registry https:<span class="hljs-comment">//registry.npmjs.org/</span><br></code></pre></td></tr></table></figure><h2 id="npm账户使用"><a href="#npm账户使用" class="headerlink" title="npm账户使用"></a>npm账户使用</h2><h3 id="用户登陆"><a href="#用户登陆" class="headerlink" title="用户登陆"></a>用户登陆</h3><figure class="highlight coffeescript"><table><tr><td class="code"><pre><code class="hljs coffeescript"><span class="hljs-built_in">npm</span> adduser<br></code></pre></td></tr></table></figure><p>需要使用npmjs网址的账户名和密码登陆。</p><h3 id="显示当前账户"><a href="#显示当前账户" class="headerlink" title="显示当前账户"></a>显示当前账户</h3><figure class="highlight coffeescript"><table><tr><td class="code"><pre><code class="hljs coffeescript"><span class="hljs-built_in">npm</span> whoami<br></code></pre></td></tr></table></figure><h3 id="删除上传项目"><a href="#删除上传项目" class="headerlink" title="删除上传项目"></a>删除上传项目</h3><figure class="highlight brainfuck"><table><tr><td class="code"><pre><code class="hljs brainfuck"><span class="hljs-comment">npm</span> <span class="hljs-comment">unpublish</span> <span class="hljs-comment">项目包名</span> --<span class="hljs-comment">force</span> --<span class="hljs-comment">otp</span> <span class="hljs-comment">2fa码</span><br></code></pre></td></tr></table></figure><p>如果不加入<strong>—otp</strong>的参数，在运行命令后也会需要这个2fa码。（如果使用了2fa登陆才会需要）</p>]]></content>
    
    
    <summary type="html">简易的srs直播后台搭建</summary>
    
    
    
    <category term="npm" scheme="http://example.com/categories/npm/"/>
    
    
    <category term="直播" scheme="http://example.com/tags/%E7%9B%B4%E6%92%AD/"/>
    
  </entry>
  
  <entry>
    <title>android端的音频处理工具MediaExtractor和MediaMuxer</title>
    <link href="http://example.com/2020/07/16/MediaExtractorANDMediaMuxer/"/>
    <id>http://example.com/2020/07/16/MediaExtractorANDMediaMuxer/</id>
    <published>2020-07-16T03:55:28.000Z</published>
    <updated>2021-04-17T07:10:38.651Z</updated>
    
    <content type="html"><![CDATA[<h2 id="一、简介"><a href="#一、简介" class="headerlink" title="一、简介"></a>一、简介</h2><p>MediaExtractor:视频文件的提取器，能够将视频和音频分离<br>MediaMuxer:音视频文件合成器，能够将视频文件合成新的音视频文件</p>]]></content>
    
    
    <summary type="html">android端的音频处理</summary>
    
    
    
    <category term="音视频" scheme="http://example.com/categories/%E9%9F%B3%E8%A7%86%E9%A2%91/"/>
    
    
    <category term="android" scheme="http://example.com/tags/android/"/>
    
    <category term="音频" scheme="http://example.com/tags/%E9%9F%B3%E9%A2%91/"/>
    
  </entry>
  
  <entry>
    <title>Android-PCM</title>
    <link href="http://example.com/2020/07/15/Android-PCM%E5%A4%84%E7%90%86/"/>
    <id>http://example.com/2020/07/15/Android-PCM%E5%A4%84%E7%90%86/</id>
    <published>2020-07-15T05:29:49.000Z</published>
    <updated>2021-04-17T07:10:38.649Z</updated>
    
    <content type="html"><![CDATA[<h2 id="一、简介"><a href="#一、简介" class="headerlink" title="一、简介"></a>一、简介</h2><p>主要完成对音频的实时录制，并输出PCM音频数据。PCM(脉冲编码调制)是数字通信的一种编码方式。通过对模拟信号的抽样、量化、编码完成数据转换。</p><h2 id="采样频率"><a href="#采样频率" class="headerlink" title="采样频率"></a>采样频率</h2><p>人对频率的识别范围是 20HZ - 20000HZ。</p><ul><li>8000hz 为电话采样。</li><li>22050 的采样频率是常用的。</li><li>44100已是CD音质, 超过48000的采样对人耳已经没有意义</li></ul><blockquote><p>对采样率为44.1kHz的AAC（Advanced Audio Coding）音频进行解码时，一帧的解码时间须控制在23.22毫秒内。通常是按1024个采样点一帧。</p></blockquote><p>不同于视频帧，音频帧略有不同。视频帧即一帧代表一副图像。单音频帧和编码格式相关，不同编码不同的标准。只需要根据音频的采样频率和采样精度，就可以确定他的帧数，如44100HZ 16位的PCM音频。每秒音频数据是44100 <em> 16kbs即44100 </em> 2 byte。</p><h2 id="采样位数"><a href="#采样位数" class="headerlink" title="采样位数"></a>采样位数</h2><p>采样精度取决于采样位数的大小：</p><ul><li>1 字节(也就是8bit) 只能记录 256 个数, 也就是只能将振幅划分成 256 个等级</li><li>2 字节(也就是16bit) 可以细到 65536 个数, 这已是 CD 标准了;</li><li>4 字节(也就是32bit) 能把振幅细分到 4294967296 个等级, 实在是没必要了</li></ul><p>上面是单声道(mono)双声道(stereo)则是双倍。</p><h2 id="比特率-bitrate"><a href="#比特率-bitrate" class="headerlink" title="比特率 bitrate"></a>比特率 bitrate</h2><p>码率是指经过编码后的音频数据每秒钟需要用多少个比特来表示.</p><h2 id="有损和无损"><a href="#有损和无损" class="headerlink" title="有损和无损"></a>有损和无损</h2><p>我们所使用的CD的采样标准就是44.1k，即无损。</p><h2 id="录制音频-AudioRecord"><a href="#录制音频-AudioRecord" class="headerlink" title="录制音频 AudioRecord"></a>录制音频 AudioRecord</h2><p>Android提供了AudioRecord类完成音频的录制工作，并且返回PCM数据格式。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">AudioRecord</span><span class="hljs-params">(<span class="hljs-keyword">int</span> audioSource, <span class="hljs-keyword">int</span> sampleRateInHz, <span class="hljs-keyword">int</span> channelConfig, <span class="hljs-keyword">int</span> audioFormat, <span class="hljs-keyword">int</span> bufferSizeInBytes)</span></span><br></code></pre></td></tr></table></figure></p><ul><li>audioSource 音源设备，常用麦克风MediaRecorder.AudioSource.MIC。</li><li>samplerateInHz 采样频率，44100Hz是目前所有设备都支持的频率。范围在4000～192000。</li><li>channelConfig 音频通道，单声道<code>AudioFormat.CHANNEL_IN_MONO</code>还是立体声<code>AudioFormat.CHANNEL_IN_STEREO</code>，还有其他声道如左、右声道。</li><li>audioFormat 该参数为量化深度，即为每次采样的位数，最好使用16bit，因为8bit的采样位数有效设备不兼容。</li><li>bufferSizeInBytes 可通过getMinBufferSize()方法确定，每次从硬件读取数据所需要的缓冲区的大小。</li></ul><p>实例：<br>权限：<br><figure class="highlight xml"><table><tr><td class="code"><pre><code class="hljs xml"><span class="hljs-tag">&lt;<span class="hljs-name">uses-permission</span> <span class="hljs-attr">android:name</span>=<span class="hljs-string">&quot;android.permission.RECORD_AUDIO&quot;</span> /&gt;</span><br><span class="hljs-comment">&lt;!-- 文件管理权限，可以忽略 --&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">uses-permission</span> <span class="hljs-attr">android:name</span>=<span class="hljs-string">&quot;android.permission.READ_EXTERNAL_STORAGE&quot;</span>/&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">uses-permission</span> <span class="hljs-attr">android:name</span>=<span class="hljs-string">&quot;android.permission.WRITE_EXTERNAL_STORAGE&quot;</span>/&gt;</span><br></code></pre></td></tr></table></figure></p><p>工具类：</p><blockquote><p>1.打开AudioRecord权限<br>2.根据采样频率、声道类型、采样精度调用AudioRecord.getMinBufferSize() 获取最小Buffer大小<br>3.创建AudioRecord对象<br>4.startRecording开启录音、stop关闭录音<br>5.线程中通过read获取音频数据<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">AudioRecordChannel</span> </span>&#123;<br><br>    <span class="hljs-keyword">private</span> AudioRecord audioRecord;<br>    <span class="hljs-keyword">private</span> ExecutorService executor;<br><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">int</span> minBufferSize;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">boolean</span> isStart = <span class="hljs-keyword">false</span>;<br><br>    <span class="hljs-keyword">private</span> FileOutputStream fos = <span class="hljs-keyword">null</span>;<br><br>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">AudioRecordChannel</span><span class="hljs-params">()</span> </span>&#123;<br>        minBufferSize = AudioRecord.getMinBufferSize(<span class="hljs-number">44100</span>, AudioFormat.CHANNEL_IN_STEREO, AudioFormat.ENCODING_PCM_16BIT);<br>        audioRecord = <span class="hljs-keyword">new</span> AudioRecord(MediaRecorder.AudioSource.MIC, <span class="hljs-number">44100</span>, AudioFormat.CHANNEL_IN_STEREO, AudioFormat.ENCODING_PCM_16BIT, minBufferSize);<br><br>        executor = Executors.newSingleThreadExecutor();<br>    &#125;<br><br>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">openFile</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> IOException </span>&#123;<br>        File file = <span class="hljs-keyword">new</span> File(Environment.getExternalStorageDirectory().getAbsolutePath(), <span class="hljs-string">&quot;test.pcm&quot;</span>);<br>        <span class="hljs-keyword">if</span> (!file.exists()) &#123;<br>            file.createNewFile();<br>        &#125;<br>        fos = <span class="hljs-keyword">new</span> FileOutputStream(file);<br>    &#125;<br><br>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">start</span><span class="hljs-params">()</span> </span>&#123;<br>        <span class="hljs-keyword">if</span> (!isStart) &#123;<br>            isStart = <span class="hljs-keyword">true</span>;<br>            executor.submit(<span class="hljs-keyword">new</span> AudioTask());<br>        &#125;<br>    &#125;<br><br>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">stop</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> IOException </span>&#123;<br>        audioRecord.stop();<br>        isStart = <span class="hljs-keyword">false</span>;<br>        executor.shutdown();<br>        <span class="hljs-keyword">if</span> (fos != <span class="hljs-keyword">null</span>) &#123;<br>            fos.flush();<br>            fos.close();<br>            fos = <span class="hljs-keyword">null</span>;<br>        &#125;<br>        audioRecord.release();<br>    &#125;<br><br>    <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">AudioTask</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">Runnable</span> </span>&#123;<br><br>        <span class="hljs-meta">@Override</span><br>        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">run</span><span class="hljs-params">()</span> </span>&#123;<br>            audioRecord.startRecording();<br>            <span class="hljs-keyword">byte</span>[] data = <span class="hljs-keyword">new</span> <span class="hljs-keyword">byte</span>[minBufferSize];<br>            <span class="hljs-keyword">while</span> (isStart) &#123;<br>                <span class="hljs-keyword">int</span> dataLen = audioRecord.read(data, <span class="hljs-number">0</span>, data.length);<br>                <span class="hljs-comment">// 写文件</span><br>                <span class="hljs-keyword">if</span> (fos  != <span class="hljs-keyword">null</span>) &#123;<br>                    <span class="hljs-keyword">try</span> &#123;<br>                        fos.write(data);<br>                    &#125; <span class="hljs-keyword">catch</span> (IOException e) &#123;<br>                        e.printStackTrace();<br>                    &#125;<br>                &#125;<br>            &#125;<br>        &#125;<br>    &#125;<br><br>&#125;<br></code></pre></td></tr></table></figure></p></blockquote><h2 id="播放音频-AudioTrack"><a href="#播放音频-AudioTrack" class="headerlink" title="播放音频 AudioTrack"></a>播放音频 AudioTrack</h2><p>播放方式分为<strong>MODE_STATIC</strong> 和<strong>MODE_STREAM</strong>。前者一次性获取完整数据后播放，后者边获取流数据边进行播放。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">AudioTrack</span><span class="hljs-params">(AudioAttributes attributes, AudioFormat format, <span class="hljs-keyword">int</span> bufferSizeInBytes, <span class="hljs-keyword">int</span> mode, <span class="hljs-keyword">int</span> sessionId)</span></span><br></code></pre></td></tr></table></figure></p><ul><li>attributes:播放流类型。声音的作用<code>setUsage()</code>、播放的是什么<code>setContentType()</code>。</li><li>format:播放流格式。流类型<code>setEncoding()</code>、声道<code>setChannelMask()</code>。</li><li>bufferSizeInBytes:数据大小。</li><li>mode:播放模式即<code>MODE_STATIC</code>和<code>MODE_STREAM</code>。</li><li>sissionId:会话ID</li></ul><p>实例</p><blockquote><p>1.确认数据的获取方式？本地的完整数据还是远程流数据。根据不同方式选择MODE_<br>2.初始化，创建对象<br>3.根据不同MODE，对其write数据(MODE_STATIC需要先write填充数据，后paly、而MODE_STREAM需要先play，后write填充数据)</p></blockquote><p>下面展示的是流数据。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">AudioTrackChannel</span> </span>&#123;<br><br>    <span class="hljs-keyword">private</span> AudioTrack audioTrack;<br>    <span class="hljs-keyword">private</span> ExecutorService executor;<br><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">int</span> minBufferSize;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">boolean</span> isStart = <span class="hljs-keyword">false</span>;<br><br>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">AudioTrackChannel</span><span class="hljs-params">()</span> </span>&#123;<br>        minBufferSize = AudioRecord.getMinBufferSize(<span class="hljs-number">44100</span>, AudioFormat.CHANNEL_IN_STEREO, AudioFormat.ENCODING_PCM_16BIT);<br>        audioTrack = <span class="hljs-keyword">new</span> AudioTrack(<br>                <span class="hljs-keyword">new</span> AudioAttributes.Builder()<br>                        .setUsage(AudioAttributes.USAGE_MEDIA)<br>                        .setContentType(AudioAttributes.CONTENT_TYPE_MUSIC)<br>                        .build(),<br>                <span class="hljs-keyword">new</span> AudioFormat.Builder()<br>                        .setEncoding(AudioFormat.ENCODING_PCM_16BIT)<br>                        .setChannelMask(AudioFormat.CHANNEL_IN_STEREO)<br>                        .build(),<br>                minBufferSize,<br>                AudioTrack.MODE_STREAM,<br>                AudioManager.AUDIO_SESSION_ID_GENERATE<br>        );<br><br>        executor = Executors.newSingleThreadExecutor();<br>    &#125;<br><br>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">play</span><span class="hljs-params">()</span> </span>&#123;<br>        <span class="hljs-keyword">if</span> (!isStart) &#123;<br>            isStart = <span class="hljs-keyword">true</span>;<br>            audioTrack.play();<br>            executor.submit(<span class="hljs-keyword">new</span> AudioTask());<br>        &#125;<br>    &#125;<br><br>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">stop</span><span class="hljs-params">()</span> </span>&#123;<br>        audioTrack.stop();<br>        isStart = <span class="hljs-keyword">false</span>;<br>        executor.shutdown();<br>        audioTrack.release();<br>    &#125;<br><br>    <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">AudioTask</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">Runnable</span> </span>&#123;<br><br>        FileInputStream fis;<br><br>        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">AudioTask</span><span class="hljs-params">()</span> </span>&#123;<br>            <span class="hljs-keyword">try</span> &#123;<br>                fis = <span class="hljs-keyword">new</span> FileInputStream(<span class="hljs-keyword">new</span> File(Environment.getExternalStorageDirectory().getAbsolutePath(), <span class="hljs-string">&quot;test.pcm&quot;</span>));<br>            &#125; <span class="hljs-keyword">catch</span> (FileNotFoundException e) &#123;<br>                e.printStackTrace();<br>            &#125;<br>        &#125;<br><br>        <span class="hljs-meta">@Override</span><br>        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">run</span><span class="hljs-params">()</span> </span>&#123;<br>            <span class="hljs-keyword">byte</span>[] data = <span class="hljs-keyword">new</span> <span class="hljs-keyword">byte</span>[minBufferSize];<br>            <span class="hljs-keyword">while</span> (<span class="hljs-keyword">true</span>) &#123;<br>                <span class="hljs-keyword">try</span> &#123;<br>                    <span class="hljs-keyword">if</span> (!(isStart &amp;&amp; fis.available() &gt; <span class="hljs-number">0</span>)) <span class="hljs-keyword">break</span>;<br>                &#125; <span class="hljs-keyword">catch</span> (IOException e) &#123;<br>                    e.printStackTrace();<br>                &#125;<br><br>                <span class="hljs-keyword">try</span> &#123;<br>                    <span class="hljs-keyword">int</span> readCount = fis.read(data);<br>                    <span class="hljs-keyword">if</span> (readCount == AudioTrack.ERROR_INVALID_OPERATION || readCount == AudioTrack.ERROR_BAD_VALUE) &#123;<br>                        <span class="hljs-keyword">continue</span>;<br>                    &#125;<br>                    audioTrack.write(data, <span class="hljs-number">0</span>, readCount);<br><br>                &#125; <span class="hljs-keyword">catch</span> (IOException e) &#123;<br>                    e.printStackTrace();<br>                &#125;<br><br>            &#125;<br><br>            <span class="hljs-keyword">try</span> &#123;<br>                fis.close();<br>            &#125; <span class="hljs-keyword">catch</span> (IOException e) &#123;<br>                e.printStackTrace();<br>            &#125;<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure></p><h2 id="wav文件"><a href="#wav文件" class="headerlink" title="wav文件"></a>wav文件</h2><p>wav格式是微软公司开发的一种音频格式。</p><h2 id="文件头"><a href="#文件头" class="headerlink" title="文件头"></a>文件头</h2><p>wav格式的音频会存在一个头信息内容。大小由44个字节组成。</p><blockquote><p>4字节数据，内容为“RIFF”，表示资源交换文件标识<br>4字节数据，内容为一个整数，表示从下个地址开始到文件尾的总字节数<br>4字节数据，内容为“WAVE”，表示WAV文件标识<br>4字节数据，内容为“fmt ”，表示波形格式标识（fmt ），最后一位空格。<br>4字节数据，内容为一个整数，表示PCMWAVEFORMAT的长度<br>2字节数据，内容为一个短整数，表示格式种类（值为1时，表示数据为线性PCM编码）<br>2字节数据，内容为一个短整数，表示通道数，单声道为1，双声道为2<br>4字节数据，内容为一个整数，表示采样率，比如44100<br>4字节数据，内容为一个整数，表示波形数据传输速率（每秒平均字节数），大小为 采样率 <em> 通道数 </em> 采样位数<br>2字节数据，内容为一个短整数，表示DATA数据块长度，大小为 通道数 * 采样位数<br>2字节数据，内容为一个短整数，表示采样位数，即PCM位宽，通常为8位或16位<br>4字节数据，内容为“data”，表示数据标记符<br>4字节数据，内容为一个整数，表示接下来声音数据的总大小</p></blockquote><p>实例<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">byte</span>[] header = <span class="hljs-keyword">new</span> <span class="hljs-keyword">byte</span>[<span class="hljs-number">44</span>];<br>  header[<span class="hljs-number">0</span>] = <span class="hljs-string">&#x27;R&#x27;</span>; <span class="hljs-comment">// RIFF</span><br>  header[<span class="hljs-number">1</span>] = <span class="hljs-string">&#x27;I&#x27;</span>;<br>  header[<span class="hljs-number">2</span>] = <span class="hljs-string">&#x27;F&#x27;</span>;<br>  header[<span class="hljs-number">3</span>] = <span class="hljs-string">&#x27;F&#x27;</span>;<br><span class="hljs-comment">// wavData + 44 - 8</span><br>  header[<span class="hljs-number">4</span>] = (<span class="hljs-keyword">byte</span>) (totalDataLen &amp; <span class="hljs-number">0xff</span>);<span class="hljs-comment">//数据大小</span><br>  header[<span class="hljs-number">5</span>] = (<span class="hljs-keyword">byte</span>) ((totalDataLen &gt;&gt; <span class="hljs-number">8</span>) &amp; <span class="hljs-number">0xff</span>);<br>  header[<span class="hljs-number">6</span>] = (<span class="hljs-keyword">byte</span>) ((totalDataLen &gt;&gt; <span class="hljs-number">16</span>) &amp; <span class="hljs-number">0xff</span>);<br>  header[<span class="hljs-number">7</span>] = (<span class="hljs-keyword">byte</span>) ((totalDataLen &gt;&gt; <span class="hljs-number">24</span>) &amp; <span class="hljs-number">0xff</span>);<br>  header[<span class="hljs-number">8</span>] = <span class="hljs-string">&#x27;W&#x27;</span>;<span class="hljs-comment">//WAVE</span><br>  header[<span class="hljs-number">9</span>] = <span class="hljs-string">&#x27;A&#x27;</span>;<br>  header[<span class="hljs-number">10</span>] = <span class="hljs-string">&#x27;V&#x27;</span>;<br>  header[<span class="hljs-number">11</span>] = <span class="hljs-string">&#x27;E&#x27;</span>;<br>  <span class="hljs-comment">//FMT Chunk</span><br>  header[<span class="hljs-number">12</span>] = <span class="hljs-string">&#x27;f&#x27;</span>; <span class="hljs-comment">// &#x27;fmt &#x27;</span><br>  header[<span class="hljs-number">13</span>] = <span class="hljs-string">&#x27;m&#x27;</span>;<br>  header[<span class="hljs-number">14</span>] = <span class="hljs-string">&#x27;t&#x27;</span>;<br>  header[<span class="hljs-number">15</span>] = <span class="hljs-string">&#x27; &#x27;</span>;<span class="hljs-comment">//过渡字节</span><br>  <span class="hljs-comment">//数据大小</span><br>  header[<span class="hljs-number">16</span>] = <span class="hljs-number">16</span>; <span class="hljs-comment">// 4 bytes: size of &#x27;fmt &#x27; chunk</span><br>  header[<span class="hljs-number">17</span>] = <span class="hljs-number">0</span>;<br>  header[<span class="hljs-number">18</span>] = <span class="hljs-number">0</span>;<br>  header[<span class="hljs-number">19</span>] = <span class="hljs-number">0</span>;<br>  <span class="hljs-comment">//编码方式 10H为PCM编码格式</span><br>  header[<span class="hljs-number">20</span>] = <span class="hljs-number">1</span>; <span class="hljs-comment">// format = 1</span><br>  header[<span class="hljs-number">21</span>] = <span class="hljs-number">0</span>;<br>  <span class="hljs-comment">//通道数 2</span><br>  header[<span class="hljs-number">22</span>] = (<span class="hljs-keyword">byte</span>) channels;<br>  header[<span class="hljs-number">23</span>] = <span class="hljs-number">0</span>;<br>  <span class="hljs-comment">//采样率，每个通道的播放速度 44100</span><br>  header[<span class="hljs-number">24</span>] = (<span class="hljs-keyword">byte</span>) (sampleRate &amp; <span class="hljs-number">0xff</span>);<br>  header[<span class="hljs-number">25</span>] = (<span class="hljs-keyword">byte</span>) ((sampleRate &gt;&gt; <span class="hljs-number">8</span>) &amp; <span class="hljs-number">0xff</span>);<br>  header[<span class="hljs-number">26</span>] = (<span class="hljs-keyword">byte</span>) ((sampleRate &gt;&gt; <span class="hljs-number">16</span>) &amp; <span class="hljs-number">0xff</span>);<br>  header[<span class="hljs-number">27</span>] = (<span class="hljs-keyword">byte</span>) ((sampleRate &gt;&gt; <span class="hljs-number">24</span>) &amp; <span class="hljs-number">0xff</span>);<br>  <span class="hljs-comment">//音频数据传送速率,采样率*通道数*采样精度/8 (44100 * 2 * 16 /8)</span><br>  header[<span class="hljs-number">28</span>] = (<span class="hljs-keyword">byte</span>) (byteRate &amp; <span class="hljs-number">0xff</span>);<br>  header[<span class="hljs-number">29</span>] = (<span class="hljs-keyword">byte</span>) ((byteRate &gt;&gt; <span class="hljs-number">8</span>) &amp; <span class="hljs-number">0xff</span>);<br>  header[<span class="hljs-number">30</span>] = (<span class="hljs-keyword">byte</span>) ((byteRate &gt;&gt; <span class="hljs-number">16</span>) &amp; <span class="hljs-number">0xff</span>);<br>  header[<span class="hljs-number">31</span>] = (<span class="hljs-keyword">byte</span>) ((byteRate &gt;&gt; <span class="hljs-number">24</span>) &amp; <span class="hljs-number">0xff</span>);<br>  <span class="hljs-comment">// 确定系统一次要处理多少个这样字节的数据，确定缓冲区，通道数*采样精度 / 8</span><br>  header[<span class="hljs-number">32</span>] = (<span class="hljs-keyword">byte</span>) (channels * <span class="hljs-number">16</span> / <span class="hljs-number">8</span>);<br>  header[<span class="hljs-number">33</span>] = <span class="hljs-number">0</span>;<br>  <span class="hljs-comment">//每个样本的数据精度 16 or 8</span><br>  header[<span class="hljs-number">34</span>] = <span class="hljs-number">16</span>;<br>  header[<span class="hljs-number">35</span>] = <span class="hljs-number">0</span>;<br>  <span class="hljs-comment">//Data chunk</span><br>  header[<span class="hljs-number">36</span>] = <span class="hljs-string">&#x27;d&#x27;</span>;<span class="hljs-comment">//data</span><br>  header[<span class="hljs-number">37</span>] = <span class="hljs-string">&#x27;a&#x27;</span>;<br>  header[<span class="hljs-number">38</span>] = <span class="hljs-string">&#x27;t&#x27;</span>;<br>  header[<span class="hljs-number">39</span>] = <span class="hljs-string">&#x27;a&#x27;</span>;<br><span class="hljs-comment">// PCM数据大小</span><br>  header[<span class="hljs-number">40</span>] = (<span class="hljs-keyword">byte</span>) (wavDataLen &amp; <span class="hljs-number">0xff</span>);<br>  header[<span class="hljs-number">41</span>] = (<span class="hljs-keyword">byte</span>) ((wavDataLen &gt;&gt; <span class="hljs-number">8</span>) &amp; <span class="hljs-number">0xff</span>);<br>  header[<span class="hljs-number">42</span>] = (<span class="hljs-keyword">byte</span>) ((wavDataLen &gt;&gt; <span class="hljs-number">16</span>) &amp; <span class="hljs-number">0xff</span>);<br>  header[<span class="hljs-number">43</span>] = (<span class="hljs-keyword">byte</span>) ((wavDataLen &gt;&gt; <span class="hljs-number">24</span>) &amp; <span class="hljs-number">0xff</span>);<br>  out.write(header, <span class="hljs-number">0</span>, <span class="hljs-number">44</span>);<br></code></pre></td></tr></table></figure></p><h2 id="wav体"><a href="#wav体" class="headerlink" title="wav体"></a>wav体</h2><p>直接使用PCM数据即可。</p><h2 id="分离PCM16的左右声道"><a href="#分离PCM16的左右声道" class="headerlink" title="分离PCM16的左右声道"></a>分离PCM16的左右声道</h2><p>在使用PCM16双声道数据中，左右声道的采样值是间隔存储的。每个采样值占用2byte字节，因此只需要间隔保存即可分离左右声道信息，但是分割后只是声道数量-1，采样精度依旧是16bit而不是8bit。</p><p>实例<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">pcm16_split</span><span class="hljs-params">(String filePath)</span> <span class="hljs-keyword">throws</span> IOException </span>&#123;<br>File srcPath = <span class="hljs-keyword">new</span> File(filePath);<br>File leftPath = <span class="hljs-keyword">new</span> File(Environment.getExternalStorageDirectory().getAbsolutePath(), <span class="hljs-string">&quot;test_left.pcm&quot;</span>);<br>File rightPath = <span class="hljs-keyword">new</span> File(Environment.getExternalStorageDirectory().getAbsolutePath(), <span class="hljs-string">&quot;test_right.pcm&quot;</span>);<br><br><span class="hljs-keyword">if</span> (!leftPath.exists()) leftPath.createNewFile();<br><span class="hljs-keyword">if</span> (!rightPath.exists()) rightPath.createNewFile();<br><br>FileInputStream fis = <span class="hljs-keyword">new</span> FileInputStream(srcPath);<br>FileOutputStream lfos = <span class="hljs-keyword">new</span> FileOutputStream(leftPath);<br>FileOutputStream rfos = <span class="hljs-keyword">new</span> FileOutputStream(rightPath);<br><br><span class="hljs-keyword">byte</span>[] data = <span class="hljs-keyword">new</span> <span class="hljs-keyword">byte</span>[<span class="hljs-number">4</span>];<br><span class="hljs-keyword">while</span> (fis.read(data) &gt; <span class="hljs-number">0</span>) &#123;<br>lfos.write(data, <span class="hljs-number">0</span>, <span class="hljs-number">2</span>);<br>rfos.write(data, <span class="hljs-number">2</span>, <span class="hljs-number">2</span>);<br>&#125;<br><br>fis.close();<br>lfos.flush();<br>rfos.flush();<br>lfos.close();<br>rfos.close();<br>&#125;<br></code></pre></td></tr></table></figure><br><img src="/img/AndroidAudio/0.png" alt="github"><br>上图为左右声道分离后的音频波形图。</p>]]></content>
    
    
    <summary type="html">android端处理PCM数据</summary>
    
    
    
    <category term="音视频" scheme="http://example.com/categories/%E9%9F%B3%E8%A7%86%E9%A2%91/"/>
    
    
    <category term="android" scheme="http://example.com/tags/android/"/>
    
    <category term="音频" scheme="http://example.com/tags/%E9%9F%B3%E9%A2%91/"/>
    
  </entry>
  
  <entry>
    <title>yuv-tool工具类</title>
    <link href="http://example.com/2020/07/13/yuv-tool%E5%B7%A5%E5%85%B7%E7%B1%BB/"/>
    <id>http://example.com/2020/07/13/yuv-tool%E5%B7%A5%E5%85%B7%E7%B1%BB/</id>
    <published>2020-07-13T14:58:21.000Z</published>
    <updated>2021-04-17T08:13:22.415Z</updated>
    
    <content type="html"><![CDATA[<span id="more"></span><h2 id="一、相关地址"><a href="#一、相关地址" class="headerlink" title="一、相关地址"></a>一、相关地址</h2><p>相关博客:<br>[YUV图片原理]<a href="https://yangandmore.github.io/2019/03/14/%E5%9B%BE%E7%89%87YUV%E6%95%B0%E6%8D%AE%E6%A0%BC%E5%BC%8F/">https://yangandmore.github.io/2019/03/14/%E5%9B%BE%E7%89%87YUV%E6%95%B0%E6%8D%AE%E6%A0%BC%E5%BC%8F/</a><br>[RGB图片原理]<a href="https://yangandmore.github.io/2019/03/27/%E5%9B%BE%E7%89%87RGB%E6%95%B0%E6%8D%AE%E6%A0%BC%E5%BC%8F/">https://yangandmore.github.io/2019/03/27/%E5%9B%BE%E7%89%87RGB%E6%95%B0%E6%8D%AE%E6%A0%BC%E5%BC%8F/</a><br>[libyuv for android库编译和简单使用]<a href="https://yangandmore.github.io/2019/03/07/libYUV%E7%BC%96%E8%AF%91/">https://yangandmore.github.io/2019/03/07/libYUV%E7%BC%96%E8%AF%91/</a><br>[libturbojpeg for android库编译和简单使用]<a href="https://yangandmore.github.io/2019/03/08/libjpeg-turbo%E7%BC%96%E8%AF%91/">https://yangandmore.github.io/2019/03/08/libjpeg-turbo%E7%BC%96%E8%AF%91/</a><br>[yuv-tool for android库的项目]<a href="https://yangandmore.github.io/2020/07/13/yuv-tool%E5%B7%A5%E5%85%B7%E7%B1%BB/">https://yangandmore.github.io/2020/07/13/yuv-tool%E5%B7%A5%E5%85%B7%E7%B1%BB/</a><br>[turbo-jpeg-tool for android库的项目]<a href="https://yangandmore.github.io/2019/03/08/libjpeg-turbo%E7%BC%96%E8%AF%91/">https://yangandmore.github.io/2019/03/08/libjpeg-turbo%E7%BC%96%E8%AF%91/</a></p><p>[github地址]<a href="https://github.com/Yangandmore/yuv-tool">https://github.com/Yangandmore/yuv-tool</a><br>代码功能持续更新中…</p>]]></content>
    
    
    <summary type="html">yuv-tool工具，完成对libyuv进行封装</summary>
    
    
    
    <category term="音视频" scheme="http://example.com/categories/%E9%9F%B3%E8%A7%86%E9%A2%91/"/>
    
    
    <category term="android" scheme="http://example.com/tags/android/"/>
    
    <category term="libyuv" scheme="http://example.com/tags/libyuv/"/>
    
  </entry>
  
  <entry>
    <title>ffmpegforandroid编译</title>
    <link href="http://example.com/2020/06/18/ffmpegforandroid%E7%BC%96%E8%AF%91/"/>
    <id>http://example.com/2020/06/18/ffmpegforandroid%E7%BC%96%E8%AF%91/</id>
    <published>2020-06-18T05:38:35.000Z</published>
    <updated>2021-04-17T07:10:38.654Z</updated>
    
    <content type="html"><![CDATA[<h2 id="一、简介"><a href="#一、简介" class="headerlink" title="一、简介"></a>一、简介</h2><p>我的环境：<br>mac+ndkr13b+ffmpeg3.4.7</p><h2 id="二、编译FFmpeg"><a href="#二、编译FFmpeg" class="headerlink" title="二、编译FFmpeg"></a>二、编译FFmpeg</h2><h1 id="1-修改configure"><a href="#1-修改configure" class="headerlink" title="1.修改configure"></a>1.修改configure</h1><p>需要将 <strong>configure</strong> 文件里面的名称修改，否则编译后会出现数字。</p><p>修改前：<br><figure class="highlight makefile"><table><tr><td class="code"><pre><code class="hljs makefile">SLIBNAME_WITH_MAJOR=&#x27;<span class="hljs-variable">$(SLIBNAME)</span>.<span class="hljs-variable">$(LIBMAJOR)</span>&#x27;  <br>LIB_INSTALL_EXTRA_CMD=&#x27;$<span class="hljs-variable">$(RANLIB)</span><span class="hljs-string">&quot;<span class="hljs-variable">$(LIBDIR)</span>/<span class="hljs-variable">$(LIBNAME)</span>&quot;</span>&#x27;  <br>SLIB_INSTALL_NAME=&#x27;<span class="hljs-variable">$(SLIBNAME_WITH_VERSION)</span>&#x27;  <br>SLIB_INSTALL_LINKS=&#x27;<span class="hljs-variable">$(SLIBNAME_WITH_MAJOR)</span><span class="hljs-variable">$(SLIBNAME)</span>&#x27;<br></code></pre></td></tr></table></figure><br>修改后：<br><figure class="highlight makefile"><table><tr><td class="code"><pre><code class="hljs makefile">SLIBNAME_WITH_MAJOR=&#x27;<span class="hljs-variable">$(SLIBPREF)</span><span class="hljs-variable">$(FULLNAME)</span>-<span class="hljs-variable">$(LIBMAJOR)</span><span class="hljs-variable">$(SLIBSUF)</span>&#x27;  <br>LIB_INSTALL_EXTRA_CMD=&#x27;$<span class="hljs-variable">$(RANLIB)</span><span class="hljs-string">&quot;<span class="hljs-variable">$(LIBDIR)</span>/<span class="hljs-variable">$(LIBNAME)</span>&quot;</span>&#x27;  <br>SLIB_INSTALL_NAME=&#x27;<span class="hljs-variable">$(SLIBNAME_WITH_MAJOR)</span>&#x27;  <br>SLIB_INSTALL_LINKS=&#x27;<span class="hljs-variable">$(SLIBNAME)</span>&#x27; <br></code></pre></td></tr></table></figure></p><p>完成后保存即可。</p><h1 id="2-脚本"><a href="#2-脚本" class="headerlink" title="2.脚本"></a>2.脚本</h1><p>配置的基础脚本如下。<br>build.sh 编译架构名称：<br><figure class="highlight routeros"><table><tr><td class="code"><pre><code class="hljs routeros"><span class="hljs-comment">#!/bin/bash</span><br><br><span class="hljs-comment"># 获取指定架构名称</span><br><span class="hljs-keyword">if</span> [ <span class="hljs-string">&quot;<span class="hljs-variable">$#</span>&quot;</span> -lt 1 ]; then<br><span class="hljs-attribute">THE_ARCH</span>=armv7<br><span class="hljs-keyword">else</span><br><span class="hljs-attribute">THE_ARCH</span>=$(tr [A-Z] [a-z] &lt;&lt;&lt; <span class="hljs-string">&quot;<span class="hljs-variable">$1</span>&quot;</span>)<br>fi<br><br><span class="hljs-comment"># 查找指定架构参数信息</span><br>case <span class="hljs-string">&quot;<span class="hljs-variable">$THE_ARCH</span>&quot;</span> <span class="hljs-keyword">in</span><br>arm|armv5|armv6|armv7|armeabi)<br># 配置参数<br># toolchain下的文件夹名称前缀<br><span class="hljs-attribute">TOOLCHAIN_LEFT</span>=arm-linux-androideabi<br># android版本下的编译器<br><span class="hljs-attribute">ANDROID_PLATFORMS</span>=arm<br># CMAKE_SYSTEM_PROCESSOR下的配置名称<br><span class="hljs-attribute">PROCESSOR_PLATFORMS</span>=arm<br># prebuilt文件夹下的编译器名称<br><span class="hljs-attribute">TOOLCHAIN_PLEBUILT</span>=arm-linux-androideabi<br># include下全名<br><span class="hljs-attribute">INCLUDE_PLATFORMS</span>=arm-linux-androideabi<br># CFLAGS是否有配置项<br><span class="hljs-attribute">CFLAGS_MARCH</span>=<span class="hljs-string">&quot;-march=armv5te&quot;</span><br># 文件夹名称<br><span class="hljs-attribute">BUILD_NAME</span>=armeabi<br>;;<br>armv7a|armeabi-v7a)<br># 配置参数<br># toolchain下的文件夹名称前缀<br><span class="hljs-attribute">TOOLCHAIN_LEFT</span>=arm-linux-androideabi<br># android版本下的编译器<br><span class="hljs-attribute">ANDROID_PLATFORMS</span>=arm<br># CMAKE_SYSTEM_PROCESSOR下的配置名称<br><span class="hljs-attribute">PROCESSOR_PLATFORMS</span>=arm<br># prebuilt文件夹下的编译器名称<br><span class="hljs-attribute">TOOLCHAIN_PLEBUILT</span>=arm-linux-androideabi<br># include下全名<br><span class="hljs-attribute">INCLUDE_PLATFORMS</span>=arm-linux-androideabi<br># CFLAGS是否有配置项<br><span class="hljs-attribute">CFLAGS_MARCH</span>=<span class="hljs-string">&quot;-march=armv7-a -mfloat-abi=softfp -mfpu=neon&quot;</span><br><span class="hljs-attribute">BUILD_NAME</span>=armeabi-v7a<br>;;<br>armv8|armv8a|aarch64|arm64|arm64-v8a)<br># 配置参数<br># toolchain下的文件夹名称前缀<br><span class="hljs-attribute">TOOLCHAIN_LEFT</span>=aarch64-linux-android<br># android版本下的编译器<br><span class="hljs-attribute">ANDROID_PLATFORMS</span>=arm64<br># CMAKE_SYSTEM_PROCESSOR下的配置名称<br><span class="hljs-attribute">PROCESSOR_PLATFORMS</span>=aarch64<br># prebuilt文件夹下的编译器名称<br><span class="hljs-attribute">TOOLCHAIN_PLEBUILT</span>=aarch64-linux-android<br># include下全名<br><span class="hljs-attribute">INCLUDE_PLATFORMS</span>=aarch64-linux-android<br># CFLAGS是否有配置项<br><span class="hljs-attribute">CFLAGS_MARCH</span>=<span class="hljs-string">&quot;-march=armv8-a&quot;</span><br><span class="hljs-attribute">BUILD_NAME</span>=arm64-v8a<br>;;<br>x86)<br># 配置参数<br># toolchain下的文件夹名称前缀<br><span class="hljs-attribute">TOOLCHAIN_LEFT</span>=x86<br># android版本下的编译器<br><span class="hljs-attribute">ANDROID_PLATFORMS</span>=x86<br># CMAKE_SYSTEM_PROCESSOR下的配置名称<br><span class="hljs-attribute">PROCESSOR_PLATFORMS</span>=x86<br># prebuilt文件夹下的编译器名称<br><span class="hljs-attribute">TOOLCHAIN_PLEBUILT</span>=i686-linux-android<br># include下全名<br><span class="hljs-attribute">INCLUDE_PLATFORMS</span>=i686-linux-android<br># CFLAGS是否有配置项<br><span class="hljs-attribute">CFLAGS_MARCH</span>=<span class="hljs-string">&quot;-march=i686 -mtune=intel -mssse3 -mfpmath=sse -m32&quot;</span><br><span class="hljs-attribute">BUILD_NAME</span>=x86<br>;;<br>x86_64|x64)<br># 配置参数<br># toolchain下的文件夹名称前缀<br><span class="hljs-attribute">TOOLCHAIN_LEFT</span>=x86_64<br># android版本下的编译器<br><span class="hljs-attribute">ANDROID_PLATFORMS</span>=x86_64<br># CMAKE_SYSTEM_PROCESSOR下的配置名称<br><span class="hljs-attribute">PROCESSOR_PLATFORMS</span>=x86_64<br># prebuilt文件夹下的编译器名称<br><span class="hljs-attribute">TOOLCHAIN_PLEBUILT</span>=x86_64-linux-android<br># include下全名<br><span class="hljs-attribute">INCLUDE_PLATFORMS</span>=x86_64-linux-android<br># CFLAGS是否有配置项<br><span class="hljs-attribute">CFLAGS_MARCH</span>=<span class="hljs-string">&quot;-march=x86-64 -msse4.2 -mpopcnt -m64 -mtune=intel&quot;</span><br><span class="hljs-attribute">BUILD_NAME</span>=x86_64<br>;;<br>esac<br><span class="hljs-comment"># 基本配置</span><br><span class="hljs-comment">#ndk所在目录</span><br><span class="hljs-attribute">NDK_PATH</span>=...Android/sdk/ndk/android-ndk-r13b<br><span class="hljs-comment">#编译环境这里是 macOS</span><br><span class="hljs-attribute">BUILD_PLATFORM</span>=darwin-x86_64<br><span class="hljs-comment">#编译工具链版本</span><br><span class="hljs-attribute">TOOLCHAIN_VERSION</span>=4.9<br><span class="hljs-comment">#最低兼容</span><br><span class="hljs-attribute">ANDROID_VERSION</span>=21<br><br><span class="hljs-attribute">SYSROOT</span>=<span class="hljs-variable">$&#123;NDK_PATH&#125;</span>/platforms/android-$&#123;ANDROID_VERSION&#125;/arch-<span class="hljs-variable">$&#123;ANDROID_PLATFORMS&#125;</span><br><span class="hljs-attribute">TOOLCHAIN</span>=<span class="hljs-variable">$&#123;NDK_PATH&#125;</span>/toolchains/$&#123;TOOLCHAIN_LEFT&#125;-<span class="hljs-variable">$&#123;TOOLCHAIN_VERSION&#125;</span>/prebuilt/<span class="hljs-variable">$&#123;BUILD_PLATFORM&#125;</span>/bin/<span class="hljs-variable">$&#123;TOOLCHAIN_PLEBUILT&#125;</span>-<br><span class="hljs-comment"># 编译后文件路径</span><br><span class="hljs-attribute">PREFIX</span>=.../ffmpeg-android/obj/$&#123;BUILD_NAME&#125;<br><br>mkdir -p <span class="hljs-variable">$&#123;PREFIX&#125;</span><br><br>./configure \<br><span class="hljs-attribute">--prefix</span>=<span class="hljs-variable">$PREFIX</span> \<br><span class="hljs-attribute">--target-os</span>=android \<br><span class="hljs-attribute">--arch</span>=<span class="hljs-variable">$PROCESSOR_PLATFORMS</span> \<br><span class="hljs-attribute">--sysroot</span>=<span class="hljs-variable">$SYSROOT</span> \<br>--enable-cross-compile \<br><span class="hljs-attribute">--cross-prefix</span>=<span class="hljs-variable">$TOOLCHAIN</span> \<br><span class="hljs-attribute">--extra-cflags</span>=<span class="hljs-string">&quot;<span class="hljs-variable">$CFLAGS_MARCH</span>  -Os -fPIC -DANDROID -Wfatal-errors -Wno-deprecated&quot;</span> \<br><span class="hljs-attribute">--extra-cxxflags</span>=<span class="hljs-string">&quot;-D__thumb__ -fexceptions -frtti&quot;</span> \<br><span class="hljs-attribute">--extra-ldflags</span>=<span class="hljs-string">&quot;-L<span class="hljs-variable">$&#123;SYSROOT&#125;</span>/usr/lib&quot;</span> \<br>--enable-shared \<br>--enable-runtime-cpudetect \<br>--enable-gpl \<br>--enable-small \<br>--enable-cross-compile \<br>--disable-<span class="hljs-builtin-name">debug</span> \<br>--disable-static \<br>--disable-doc \<br>--disable-ffmpeg \<br>--disable-ffplay \<br>--disable-ffprobe \<br>--disable-postproc \<br>--disable-avdevice \<br>--disable-symver \<br>--disable-stripping<br><br>make clean<br>make<br>make install<br></code></pre></td></tr></table></figure><br><strong><em>注意： 这里需要着重注意，由于调用 make install，在编译完成后会将这个ffmpeg安装到本地将原ffmpeg进行覆盖(准确将不是覆盖软件，而是path环境覆盖，原软件并没有被删除或是升级)。需要注意，在编译完成后可以通过在安装软件的路径下卸载即可 make uninstall。这里删除后，覆盖的ffmpeg就会暴露出来，无需再次安装。</em></strong></p><h1 id="3-配置全架构脚本"><a href="#3-配置全架构脚本" class="headerlink" title="3.配置全架构脚本"></a>3.配置全架构脚本</h1><p>build_all.sh<br><figure class="highlight apache"><table><tr><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">for</span> arch in armeabi armeabi-v<span class="hljs-number">7</span>a arm<span class="hljs-number">64</span>-v<span class="hljs-number">8</span>a x<span class="hljs-number">86</span> x<span class="hljs-number">86</span>_<span class="hljs-number">64</span><br><span class="hljs-attribute">do</span><br><span class="hljs-attribute">bash</span> build.sh $arch<br><span class="hljs-attribute">done</span><br></code></pre></td></tr></table></figure></p><h2 id="三、编译FFmpeg-libX264-libfaac"><a href="#三、编译FFmpeg-libX264-libfaac" class="headerlink" title="三、编译FFmpeg + libX264 + libfaac"></a>三、编译FFmpeg + libX264 + libfaac</h2>]]></content>
    
    
    <summary type="html">编译Android端用ffmpeg</summary>
    
    
    
    <category term="编译的库" scheme="http://example.com/categories/%E7%BC%96%E8%AF%91%E7%9A%84%E5%BA%93/"/>
    
    
    <category term="ffmpeg" scheme="http://example.com/tags/ffmpeg/"/>
    
  </entry>
  
  <entry>
    <title>libfaac编译</title>
    <link href="http://example.com/2020/06/11/libfaac%E7%BC%96%E8%AF%91/"/>
    <id>http://example.com/2020/06/11/libfaac%E7%BC%96%E8%AF%91/</id>
    <published>2020-06-11T06:00:57.000Z</published>
    <updated>2021-04-17T07:10:38.655Z</updated>
    
    <content type="html"><![CDATA[<h2 id="一、简介"><a href="#一、简介" class="headerlink" title="一、简介"></a>一、简介</h2><p><a href="https://www.videolan.org/developers/x264.html">源码下载地址</a></p><p>我的环境：<br>mac+ndkr10e</p><h2 id="二、编译"><a href="#二、编译" class="headerlink" title="二、编译"></a>二、编译</h2><h3 id="1-clone"><a href="#1-clone" class="headerlink" title="1.clone"></a>1.clone</h3><p>这个就不说了<br>网站在上面</p><h3 id="2-编译"><a href="#2-编译" class="headerlink" title="2.编译"></a>2.编译</h3><p>脚本如下<br><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c">#!/bin/bash<br><br># 获取指定架构名称<br><span class="hljs-keyword">if</span> [ <span class="hljs-string">&quot;$#&quot;</span> -lt <span class="hljs-number">1</span> ]; then<br>THE_ARCH=armv7<br><span class="hljs-keyword">else</span><br>THE_ARCH=$(tr [A-Z] [a-z] &lt;&lt;&lt; <span class="hljs-string">&quot;$1&quot;</span>)<br>fi<br><br># 查找指定架构参数信息<br><span class="hljs-keyword">case</span> <span class="hljs-string">&quot;$THE_ARCH&quot;</span> in<br>arm|armv5|armv6|armv7|armeabi)<br># 配置参数<br><span class="hljs-meta"># toolchain下的文件夹名称前缀</span><br>TOOLCHAIN_LEFT=arm-linux-androideabi<br><span class="hljs-meta"># android版本下的编译器</span><br>ANDROID_PLATFORMS=arm<br># CMAKE_SYSTEM_PROCESSOR下的配置名称<br>PROCESSOR_PLATFORMS=arm<br><span class="hljs-meta"># prebuilt文件夹下的编译器名称</span><br>TOOLCHAIN_PLEBUILT=arm-linux-androideabi<br><span class="hljs-meta"># <span class="hljs-meta-keyword">include</span>下全名</span><br>INCLUDE_PLATFORMS=arm-linux-androideabi<br># CFLAGS是否有配置项<br>CFLAGS_MARCH=<span class="hljs-string">&quot;-march=armv7-a -mfloat-abi=softfp -mfpu=vfpv3-d16&quot;</span><br># 文件夹名称<br>BUILD_NAME=armeabi<br>;;<br>armv7a|armeabi-v7a)<br># 配置参数<br><span class="hljs-meta"># toolchain下的文件夹名称前缀</span><br>TOOLCHAIN_LEFT=arm-linux-androideabi<br><span class="hljs-meta"># android版本下的编译器</span><br>ANDROID_PLATFORMS=arm<br># CMAKE_SYSTEM_PROCESSOR下的配置名称<br>PROCESSOR_PLATFORMS=arm<br><span class="hljs-meta"># prebuilt文件夹下的编译器名称</span><br>TOOLCHAIN_PLEBUILT=arm-linux-androideabi<br><span class="hljs-meta"># <span class="hljs-meta-keyword">include</span>下全名</span><br>INCLUDE_PLATFORMS=arm-linux-androideabi<br># CFLAGS是否有配置项<br>CFLAGS_MARCH=<span class="hljs-string">&quot;-march=armv7-a -mfloat-abi=softfp -mfpu=vfpv3-d16&quot;</span><br>BUILD_NAME=armeabi-v7a<br>;;<br>armv8|armv8a|aarch64|arm64|arm64-v8a)<br># 配置参数<br><span class="hljs-meta"># toolchain下的文件夹名称前缀</span><br>TOOLCHAIN_LEFT=aarch64-linux-android<br><span class="hljs-meta"># android版本下的编译器</span><br>ANDROID_PLATFORMS=arm64<br># CMAKE_SYSTEM_PROCESSOR下的配置名称<br># PROCESSOR_PLATFORMS=aarch64<br>PROCESSOR_PLATFORMS=arm<br><span class="hljs-meta"># prebuilt文件夹下的编译器名称</span><br>TOOLCHAIN_PLEBUILT=aarch64-linux-android<br><span class="hljs-meta"># <span class="hljs-meta-keyword">include</span>下全名</span><br>INCLUDE_PLATFORMS=aarch64-linux-android<br># CFLAGS是否有配置项<br>CFLAGS_MARCH=<span class="hljs-string">&quot;&quot;</span><br>BUILD_NAME=arm64-v8a<br>;;<br>x86)<br># 配置参数<br><span class="hljs-meta"># toolchain下的文件夹名称前缀</span><br>TOOLCHAIN_LEFT=x86<br><span class="hljs-meta"># android版本下的编译器</span><br>ANDROID_PLATFORMS=x86<br># CMAKE_SYSTEM_PROCESSOR下的配置名称<br>PROCESSOR_PLATFORMS=x86<br><span class="hljs-meta"># prebuilt文件夹下的编译器名称</span><br>TOOLCHAIN_PLEBUILT=i686-linux-android<br><span class="hljs-meta"># <span class="hljs-meta-keyword">include</span>下全名</span><br>INCLUDE_PLATFORMS=i686-linux-android<br># CFLAGS是否有配置项<br>CFLAGS_MARCH=<span class="hljs-string">&quot;&quot;</span><br>BUILD_NAME=x86<br>;;<br>x86_64|x64)<br># 配置参数<br><span class="hljs-meta"># toolchain下的文件夹名称前缀</span><br>TOOLCHAIN_LEFT=x86_64<br><span class="hljs-meta"># android版本下的编译器</span><br>ANDROID_PLATFORMS=x86_64<br># CMAKE_SYSTEM_PROCESSOR下的配置名称<br>PROCESSOR_PLATFORMS=x86_64<br><span class="hljs-meta"># prebuilt文件夹下的编译器名称</span><br>TOOLCHAIN_PLEBUILT=x86_64-linux-android<br><span class="hljs-meta"># <span class="hljs-meta-keyword">include</span>下全名</span><br>INCLUDE_PLATFORMS=x86_64-linux-android<br># CFLAGS是否有配置项<br>CFLAGS_MARCH=<span class="hljs-string">&quot;&quot;</span><br>BUILD_NAME=x86_64<br>;;<br>esac<br># 基本配置<br><span class="hljs-meta">#ndk所在目录</span><br>NDK_PATH=/Users/yangshiqu/Library/Android/sdk/ndk/android-ndk-r10e<br>#编译环境这里是 macOS<br>BUILD_PLATFORM=darwin-x86_64<br>#编译工具链版本<br>TOOLCHAIN_VERSION=<span class="hljs-number">4.9</span><br>#最低兼容<br>ANDROID_VERSION=<span class="hljs-number">21</span><br><br>SYSROOT=$&#123;NDK_PATH&#125;/platforms/android-$&#123;ANDROID_VERSION&#125;/arch-$&#123;ANDROID_PLATFORMS&#125;<br><br># 编译配置<br>TOOLCHAIN=$&#123;NDK_PATH&#125;/toolchains/$&#123;TOOLCHAIN_LEFT&#125;-$&#123;TOOLCHAIN_VERSION&#125;/prebuilt/$&#123;BUILD_PLATFORM&#125;/bin/$&#123;TOOLCHAIN_PLEBUILT&#125;-<br>PREFIX=/Users/yangshiqu/Documents/Cache/AndroidCodeAndApp/NDKLib/libfaac/obj/$&#123;BUILD_NAME&#125;<br>mkdir -p $&#123;PREFIX&#125;<br><br><span class="hljs-keyword">export</span> CC=<span class="hljs-string">&quot;$&#123;TOOLCHAIN&#125;gcc --sysroot=$&#123;SYSROOT&#125;&quot;</span><br><span class="hljs-keyword">export</span> CXX=<span class="hljs-string">&quot;$&#123;TOOLCHAIN&#125;g++ --sysroot=$&#123;SYSROOT&#125;&quot;</span><br><span class="hljs-keyword">export</span> NM=<span class="hljs-string">&quot;$&#123;TOOLCHAIN&#125;nm&quot;</span><br><span class="hljs-keyword">export</span> STRIP=<span class="hljs-string">&quot;$&#123;TOOLCHAIN&#125;strip&quot;</span><br><span class="hljs-keyword">export</span> RANLIB=<span class="hljs-string">&quot;$&#123;TOOLCHAIN&#125;ranlib&quot;</span><br><span class="hljs-keyword">export</span> AR=<span class="hljs-string">&quot;$&#123;TOOLCHAIN&#125;ar&quot;</span><br><br>./configure \<br>--prefix=$PREFIX \<br>--host=$PROCESSOR_PLATFORMS-linux \<br>--without-mp4v2 \<br>--enable-shared=no \<br><br>make clean<br>make<br>make install<br></code></pre></td></tr></table></figure><br>需要注意这里编译的都是静态库。(build.sh)</p><p>这里的脚本在后面添加想要的cpu架构名称就会开始编译对应的静态库。</p><h3 id="一次性打包"><a href="#一次性打包" class="headerlink" title="一次性打包?"></a>一次性打包?</h3><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">for</span> arch in armeabi armeabi-v7a arm64-v8a x86 x86_64<br><span class="hljs-keyword">do</span><br>bash build.sh $arch<br>done<br></code></pre></td></tr></table></figure><p>添加上面的脚本信息在新脚本中即可(build_all.sh)。</p>]]></content>
    
    
    <summary type="html">libfaac编译</summary>
    
    
    
    <category term="编译的库" scheme="http://example.com/categories/%E7%BC%96%E8%AF%91%E7%9A%84%E5%BA%93/"/>
    
    
    <category term="libfaac" scheme="http://example.com/tags/libfaac/"/>
    
  </entry>
  
  <entry>
    <title>libx264编译</title>
    <link href="http://example.com/2020/06/11/libx264%E7%BC%96%E8%AF%91/"/>
    <id>http://example.com/2020/06/11/libx264%E7%BC%96%E8%AF%91/</id>
    <published>2020-06-11T05:51:36.000Z</published>
    <updated>2021-04-17T07:10:38.655Z</updated>
    
    <content type="html"><![CDATA[<h2 id="一、简介"><a href="#一、简介" class="headerlink" title="一、简介"></a>一、简介</h2><p><a href="https://www.audiocoding.com/index.html">源码下载地址</a><br>但是这里只能下载到最新版本，这里在编译时使用的是1.28版本的包。</p><p>我的环境：<br>mac+ndkr13b</p><h2 id="二、编译"><a href="#二、编译" class="headerlink" title="二、编译"></a>二、编译</h2><h3 id="1-下载"><a href="#1-下载" class="headerlink" title="1.下载"></a>1.下载</h3><p>这个就不说了<br>网站在上面</p><h3 id="2-编译"><a href="#2-编译" class="headerlink" title="2.编译"></a>2.编译</h3><p>脚本如下<br><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c">#!/bin/bash<br><br># 获取指定架构名称<br><span class="hljs-keyword">if</span> [ <span class="hljs-string">&quot;$#&quot;</span> -lt <span class="hljs-number">1</span> ]; then<br>THE_ARCH=armv7<br><span class="hljs-keyword">else</span><br>THE_ARCH=$(tr [A-Z] [a-z] &lt;&lt;&lt; <span class="hljs-string">&quot;$1&quot;</span>)<br>fi<br><br># 查找指定架构参数信息<br><span class="hljs-keyword">case</span> <span class="hljs-string">&quot;$THE_ARCH&quot;</span> in<br>arm|armv5|armv6|armv7|armeabi)<br># 配置参数<br><span class="hljs-meta"># toolchain下的文件夹名称前缀</span><br>TOOLCHAIN_LEFT=arm-linux-androideabi<br><span class="hljs-meta"># android版本下的编译器</span><br>ANDROID_PLATFORMS=arm<br># CMAKE_SYSTEM_PROCESSOR下的配置名称<br>PROCESSOR_PLATFORMS=arm<br><span class="hljs-meta"># prebuilt文件夹下的编译器名称</span><br>TOOLCHAIN_PLEBUILT=arm-linux-androideabi<br><span class="hljs-meta"># <span class="hljs-meta-keyword">include</span>下全名</span><br>INCLUDE_PLATFORMS=arm-linux-androideabi<br># CFLAGS是否有配置项<br>CFLAGS_MARCH=<span class="hljs-string">&quot;-march=armv7-a -mfloat-abi=softfp -fprefetch-loop-arrays&quot;</span><br># 文件夹名称<br>BUILD_NAME=armeabi<br>;;<br>armv7a|armeabi-v7a)<br># 配置参数<br><span class="hljs-meta"># toolchain下的文件夹名称前缀</span><br>TOOLCHAIN_LEFT=arm-linux-androideabi<br><span class="hljs-meta"># android版本下的编译器</span><br>ANDROID_PLATFORMS=arm<br># CMAKE_SYSTEM_PROCESSOR下的配置名称<br>PROCESSOR_PLATFORMS=arm<br><span class="hljs-meta"># prebuilt文件夹下的编译器名称</span><br>TOOLCHAIN_PLEBUILT=arm-linux-androideabi<br><span class="hljs-meta"># <span class="hljs-meta-keyword">include</span>下全名</span><br>INCLUDE_PLATFORMS=arm-linux-androideabi<br># CFLAGS是否有配置项<br>CFLAGS_MARCH=<span class="hljs-string">&quot;-march=armv7-a -mfloat-abi=softfp -fprefetch-loop-arrays&quot;</span><br>BUILD_NAME=armeabi-v7a<br>;;<br>armv8|armv8a|aarch64|arm64|arm64-v8a)<br># 配置参数<br><span class="hljs-meta"># toolchain下的文件夹名称前缀</span><br>TOOLCHAIN_LEFT=aarch64-linux-android<br><span class="hljs-meta"># android版本下的编译器</span><br>ANDROID_PLATFORMS=arm64<br># CMAKE_SYSTEM_PROCESSOR下的配置名称<br>PROCESSOR_PLATFORMS=aarch64<br><span class="hljs-meta"># prebuilt文件夹下的编译器名称</span><br>TOOLCHAIN_PLEBUILT=aarch64-linux-android<br><span class="hljs-meta"># <span class="hljs-meta-keyword">include</span>下全名</span><br>INCLUDE_PLATFORMS=aarch64-linux-android<br># CFLAGS是否有配置项<br>CFLAGS_MARCH=<span class="hljs-string">&quot;&quot;</span><br>BUILD_NAME=arm64-v8a<br>;;<br>x86)<br># 配置参数<br><span class="hljs-meta"># toolchain下的文件夹名称前缀</span><br>TOOLCHAIN_LEFT=x86<br><span class="hljs-meta"># android版本下的编译器</span><br>ANDROID_PLATFORMS=x86<br># CMAKE_SYSTEM_PROCESSOR下的配置名称<br>PROCESSOR_PLATFORMS=x86<br><span class="hljs-meta"># prebuilt文件夹下的编译器名称</span><br>TOOLCHAIN_PLEBUILT=i686-linux-android<br><span class="hljs-meta"># <span class="hljs-meta-keyword">include</span>下全名</span><br>INCLUDE_PLATFORMS=i686-linux-android<br># CFLAGS是否有配置项<br>CFLAGS_MARCH=<span class="hljs-string">&quot;&quot;</span><br>BUILD_NAME=x86<br>;;<br>x86_64|x64)<br># 配置参数<br><span class="hljs-meta"># toolchain下的文件夹名称前缀</span><br>TOOLCHAIN_LEFT=x86_64<br><span class="hljs-meta"># android版本下的编译器</span><br>ANDROID_PLATFORMS=x86_64<br># CMAKE_SYSTEM_PROCESSOR下的配置名称<br>PROCESSOR_PLATFORMS=x86_64<br><span class="hljs-meta"># prebuilt文件夹下的编译器名称</span><br>TOOLCHAIN_PLEBUILT=x86_64-linux-android<br><span class="hljs-meta"># <span class="hljs-meta-keyword">include</span>下全名</span><br>INCLUDE_PLATFORMS=x86_64-linux-android<br># CFLAGS是否有配置项<br>CFLAGS_MARCH=<span class="hljs-string">&quot;&quot;</span><br>BUILD_NAME=x86_64<br>;;<br>esac<br>NDK_PATH=/Users/yangshiqu/Library/Android/sdk/ndk/android-ndk-r13b<br>BUILD_PLATFORM=darwin-x86_64<br>TOOLCHAIN_VERSION=<span class="hljs-number">4.9</span><br>ANDROID_VERSION=<span class="hljs-number">21</span><br><br>SYSROOT=$&#123;NDK_PATH&#125;/platforms/android-$&#123;ANDROID_VERSION&#125;/arch-$&#123;ANDROID_PLATFORMS&#125;<br><br>TOOLCHAIN=$&#123;NDK_PATH&#125;/toolchains/$&#123;TOOLCHAIN_LEFT&#125;-$&#123;TOOLCHAIN_VERSION&#125;/prebuilt/$&#123;BUILD_PLATFORM&#125;/bin/$&#123;TOOLCHAIN_PLEBUILT&#125;-<br>PREFIX=/Users/yangshiqu/Documents/Cache/AndroidCodeAndApp/NDKLib/libx264obj/$&#123;BUILD_NAME&#125;<br>mkdir -p $&#123;PREFIX&#125;<br><br>./configure \<br>--prefix=$PREFIX \<br>--cross-prefix=$TOOLCHAIN \<br>--sysroot=$SYSROOT \<br>--enable-<span class="hljs-keyword">static</span> \<br>--enable-pic \<br>--disable-<span class="hljs-keyword">asm</span> \<br>--host=$PROCESSOR_PLATFORMS-linux \<br><br>make clean<br>make<br>make install<br></code></pre></td></tr></table></figure><br>需要注意这里编译的都是静态库。(build.sh)</p><p>这里的脚本在后面添加想要的cpu架构名称就会开始编译对应的静态库。</p><h3 id="一次性打包"><a href="#一次性打包" class="headerlink" title="一次性打包?"></a>一次性打包?</h3><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">for</span> arch in armeabi armeabi-v7a arm64-v8a x86 x86_64<br><span class="hljs-keyword">do</span><br>bash build.sh $arch<br>done<br></code></pre></td></tr></table></figure><p>添加上面的脚本信息在新脚本中即可(build_all.sh)。</p><h2 id="三、使用"><a href="#三、使用" class="headerlink" title="三、使用"></a>三、使用</h2><p><a href="https://blog.csdn.net/leixiaohua1020/article/details/45536607">强烈推荐雷神的博客</a></p>]]></content>
    
    
    <summary type="html">libx264编译</summary>
    
    
    
    <category term="编译的库" scheme="http://example.com/categories/%E7%BC%96%E8%AF%91%E7%9A%84%E5%BA%93/"/>
    
    
    <category term="libx264" scheme="http://example.com/tags/libx264/"/>
    
  </entry>
  
  <entry>
    <title>RTMP规范学习</title>
    <link href="http://example.com/2020/06/10/RTMP%E8%A7%84%E8%8C%83%E5%AD%A6%E4%B9%A0/"/>
    <id>http://example.com/2020/06/10/RTMP%E8%A7%84%E8%8C%83%E5%AD%A6%E4%B9%A0/</id>
    <published>2020-06-10T03:35:52.000Z</published>
    <updated>2021-04-17T07:10:38.652Z</updated>
    
    <content type="html"><![CDATA[<h2 id="一、简述"><a href="#一、简述" class="headerlink" title="一、简述"></a>一、简述</h2><p>RTMP（Real Time Messaging Protocol）即实时消息传输协议。该协议基于TCP，是一个协议族，包括RTMP基本协议及RTMPT/RTMPS/RTMPE等多种变种。是Adobe Systems公司为Flash播放器和服务器之间音频、视频和数据传输开发的开放协议。</p><blockquote><p>1.RTMP工作在TCP之上，默认使用端口1935；<br>2.RTMPE在RTMP的基础上增加了加密功能；<br>3.RTMPT封装在HTTP请求之上，可穿透防火墙；<br>4.RTMPS类似RTMPT，增加了TLS/SSL的安全功能；</p></blockquote><p>播放一个RTMP协议的流媒体需要经过以下几个步骤：握手，建立连接，建立流，播放。<br>需要了解专用词：<br>消息(Message)：基本的数据单元称为消息<br>消息块(Chunk)：传输数据的时候，消息会被拆分成更小的单元，称为消息块</p><h1 id="握手"><a href="#握手" class="headerlink" title="握手"></a>握手</h1><p>客户端要向服务器发送C0,C1,C2（按序）三个chunk，服务器向客户端发送S0,S1,S2（按序）三个chunk，然后才能进行有效的信息传输。</p><p><strong>需要注意：</strong></p><blockquote><p>客户端要等收到S1之后才能发送C2<br>客户端要等收到S2之后才能发送其他信息（控制信息和真实音视频等数据）<br>服务端要等到收到C0之后发送S1<br>服务端必须等到收到C1之后才能发送S2<br>服务端必须等到收到C2之后才能发送其他信息（控制信息和真实音视频等数据）</p></blockquote><p><img src="/img/RTMPAgreement/0.jpg" alt="github"></p><p>但是为了让六次通信过程效率提高，就可能出现下面的方式<br><img src="/img/RTMPAgreement/1.jpg" alt="github"></p><h1 id="建立网络连接"><a href="#建立网络连接" class="headerlink" title="建立网络连接"></a>建立网络连接</h1><blockquote><p>客户端发送连接命令到服务器，请求与服务应用实例建立连接<br>服务器接收到连接命令后，发送消息给客户端用于确认窗口大小，同时连接到发送连接命令的应用程序<br>服务器发送设置带宽消息到客户端<br>客户端处理设置带宽消息后，发送确认窗口大小消息到服务器端<br>服务器发送用户控制消息中的Stream Begin消息到客户端<br>服务器发送命令消息中的result，通知客户端连接的状态</p></blockquote><p><img src="/img/RTMPAgreement/2.jpg" alt="github"></p><h1 id="建立网络流"><a href="#建立网络流" class="headerlink" title="建立网络流"></a>建立网络流</h1><blockquote><p>客户端发送命令消息createStream命令到服务器端<br>服务器接收到消息后，发送命令消息中的_result，通知客户端流的状态</p></blockquote><p><img src="/img/RTMPAgreement/3.jpg" alt="github"></p><h1 id="播放"><a href="#播放" class="headerlink" title="播放"></a>播放</h1><hr><blockquote><p>客户端发送命令消息中play命令到服务器端<br>接收到play命令后，服务器端发送设置块大小(ChunkSize)的协议消息<br>服务器发送用户控制消息streambegin，告知客户端流ID<br>play命令成功后，服务器发送命令消息中NetStream.Play.Start &amp; NetStream.Play.reset，告知客户端play命令执行成功<br>此后，服务器发送客户端要播放的音频数据和视频数据</p></blockquote><p><img src="/img/RTMPAgreement/4.jpg" alt="github"></p><h2 id="二、基本概念"><a href="#二、基本概念" class="headerlink" title="二、基本概念"></a>二、基本概念</h2><h1 id="消息-Message"><a href="#消息-Message" class="headerlink" title="消息 Message"></a>消息 Message</h1><p>基本的数据单元称为消息<br><img src="/img/RTMPAgreement/5.jpg" alt="github"></p><ul><li>MessageTyoe：消息类型(Message Type ID在1-7的消息用于协议控制；Message Type ID为8，9的消息分别用于传输音频和视频数据；15-20的消息用于发送AMF编码的命令，负责用户与服务器之间的交互)</li><li>PayloadLength：消息体长度</li><li>Time Stamp：标识时间戳</li><li>Stream ID：标识消息所属媒体流</li></ul><h1 id="消息块-Chunk"><a href="#消息块-Chunk" class="headerlink" title="消息块 Chunk"></a>消息块 Chunk</h1><p>在网络传输数据时需要将消息拆分成较小的数据块，才适合在相应网络环境下传输。拆分为消息块(Chunk)，可以避免优先级低的消息持续发送阻塞优先级高的数据，并且可以提高数据压缩。<br><img src="/img/RTMPAgreement/6.jpg" alt="github"></p><ul><li>Chunk Basic Header：标识本块</li><li>Chunk Message Header：标识本快负载所属消息</li><li>Extended TimeStamp：当时间戳溢出时需要，扩展用</li></ul><p>Chunk的默认大小是128字节，在传输过程中，通过一个叫做Set Chunk Size的控制信息可以设置Chunk数据量的最大值，在发送端和接受端会各自维护一个Chunk Size，可以分别设置这个值来改变自己这一方发送的Chunk的最大大小。大一点可以减少UPC占用率，会占用更多时间在发送还是那个，低带宽下会占用大量传输时间，小一点虽然可以减少阻塞，但是这样会带来额外的信息，并且传输多次也会无法利用高带宽优势。</p><h3 id="Chunk-Basic-Header-基本的头信息"><a href="#Chunk-Basic-Header-基本的头信息" class="headerlink" title="Chunk Basic Header(基本的头信息)"></a>Chunk Basic Header(基本的头信息)</h3><p>包含了 Chunk Stream ID(流通道ID-CSID)和 Chunk Type(Chunk类型-fmt)。前者用来标识一个特定流通道，后者则是类型。该信息长度可能为1、2、3个字节，ChunkType长度固定占2bit。<br>因此决定头信息长度就是CSID，RTMP协议支持用户自定义。0、1、2由协议保留特殊信息(0表示总共要占用2个字节)</p><ul><li><p>1Byte</p><figure class="highlight asciidoc"><table><tr><td class="code"><pre><code class="hljs asciidoc"><span class="hljs-code"> 0 1 2 3 4 5 6 7</span><br><span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-+<br>|fmt|   cs id   |<br><span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-+<br></code></pre></td></tr></table></figure><p>可以看到fmt占用2位，之后6位都是CSID内容，取值范围在[0, 63]。其中用户自定义范围在[3, 63]。</p></li><li><p>2Byte</p><pre><code>  <figure class="highlight asciidoc"><table><tr><td class="code"><pre><code class="hljs asciidoc"><span class="hljs-code"> 0 1 2 3 4 5 6 7 0 1 2 3 4 5 6 7</span><br><span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-+<br>|fmt|    0      |  cs id - 64   |<br><span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-+<br></code></pre></td></tr></table></figure></code></pre><p>两字节时，CSID占14位，该协议将使fmt所在的字节中的其他位都置位0。剩下一个字节用来表示CSID，取值范围在[0, 255]。其中由于有6位无法读取，因此需要整体加上64，即[64, 319]。</p></li><li><p>3Byte</p><figure class="highlight asciidoc"><table><tr><td class="code"><pre><code class="hljs asciidoc"><span class="hljs-code"> 0 1 2 3 4 5 6 7 0 1 2 3 4 5 6 7 0 1 2 3 4 5 6 7</span><br><span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-+<br>|fmt|    1      |          cs id - 64           |<br><span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-+<br></code></pre></td></tr></table></figure><p>三字节时，CSID占用22位，类似的，该协议将使fmt所在的字节中的其他位都置为1，剩下的2个字节来表示CSID，取值范围在[0, 65535]。理由同上，需要整体加上64，即[64, 65599]。</p></li></ul><h3 id="Chunk-Message-Header-消息的头信息"><a href="#Chunk-Message-Header-消息的头信息" class="headerlink" title="Chunk Message Header(消息的头信息)"></a>Chunk Message Header(消息的头信息)</h3><p>包含了要发送实际信息的描述信息。该信息的格式和长度取决于 Basic Header 中的 Chunk Type，共有4中格式。其中第一种格式可以表示其他三种表示的所有数据，但由于其他三种格式是基于对之前的Chunk的差量化的表示，因此可以数据量更小。</p><ul><li><p>Chunk Type = 0</p><figure class="highlight asciidoc"><table><tr><td class="code"><pre><code class="hljs asciidoc"><span class="hljs-code"> 0               1               2               3</span><br><span class="hljs-code"> 0 1 2 3 4 5 6 7 0 1 2 3 4 5 6 7 0 1 2 3 4 5 6 7 0 1 2 3 4 5 6 7</span><br><span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-+<br>|                    timestamp                  |message length |<br><span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-+<br>|    message length (coutinue)  |message type id| msg stream id |<br><span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-+<br>|                  msg stream id                |<br><span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-+<br></code></pre></td></tr></table></figure><p>占用11个字节，其他三种能表示的数据都可以表示。但是在ChunkStream开始的第一个Chunk和头信息中的时间戳后退(回退播放)的适合必须采用这种格式。</p><blockquote><p>timestamp：时间戳，占用3个字节，当他超过最大值时，三个字节都置位1。这样实际的timestamp会转存到Extended Timestamp字段中，接受端会根据这个情况到Extended TimeStamp中解析实际的时间戳。<br>msg length：消息数据的长度，占用3个字节，表示实际发送消息的数据(音频、视频)长度。这里是指Chunk的总长度，而不是Chunk Data的长度。<br>msg type id：消息类型，占用1个字节，表示实际发送消息的类型，8表示音频，9表示视频。<br>msg stream id：流媒体ID，占用4个字节，表示该Chunk所在的流的ID，与Basic Hander中的CSID一样。</p></blockquote></li><li><p>Chunk Type = 1</p><figure class="highlight asciidoc"><table><tr><td class="code"><pre><code class="hljs asciidoc"><span class="hljs-code"> 0               1               2               3</span><br><span class="hljs-code"> 0 1 2 3 4 5 6 7 0 1 2 3 4 5 6 7 0 1 2 3 4 5 6 7 0 1 2 3 4 5 6 7</span><br><span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-+<br>|               timestamp delta                 |message length |<br><span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-+<br>|    message length (coutinue)  |message type id|<br><span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-+<br></code></pre></td></tr></table></figure><p>占用7个字节，省略的Message Stream Id的4个字节。表示该Chunk与上一次发的Chunk所在的流相同。</p><blockquote><p>timestamp delta：占用3个字节，和type=0时不同，存储的是和上一次Chunk的时间差。相同的在满存储后依旧会转存到Extended Timestamp字段汇总。<br>其他：参照上面的描述</p></blockquote></li><li><p>Chunk Type = 2</p><figure class="highlight asciidoc"><table><tr><td class="code"><pre><code class="hljs asciidoc"><span class="hljs-code"> 0               1               2               </span><br><span class="hljs-code"> 0 1 2 3 4 5 6 7 0 1 2 3 4 5 6 7 0 1 2 3 4 5 6 7 </span><br><span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-+<br>|               timestamp delta                 |<br><span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-<span class="hljs-code">+-+</span>-+<br></code></pre></td></tr></table></figure><p>占用3个字节，相对与type=1时，省去了消息长度和消息类型，表示与上一次发送的Chunk都相同。</p></li><li><p>Chunk Type = 3<br>为0字节，表示这个Chunk的MessageHeader与上一次发送的完全相同。时间上面则是当type=0时表示同一个时间戳，type=1或者2时表示同一个时间戳的差，但是比较的时候是正常比较。</p></li></ul><h3 id="Extended-Timestamp-扩展时间戳"><a href="#Extended-Timestamp-扩展时间戳" class="headerlink" title="Extended Timestamp(扩展时间戳)"></a>Extended Timestamp(扩展时间戳)</h3><p>这里应该就清除了为什么是在溢出时用到的时间戳信息。当Message Header中的时间戳信息全部置位1时才会在这里启用，表示时间戳信息已溢出，请到扩展时间戳内获取完整时间戳。</p><h3 id="Chunk-Data"><a href="#Chunk-Data" class="headerlink" title="Chunk Data"></a>Chunk Data</h3><p>数据块</p><h1 id="消息分块"><a href="#消息分块" class="headerlink" title="消息分块"></a>消息分块</h1><p>在消息被分割成多个消息块的过程中，默认每个固定块大小是128字节(最后一个数据库可小于该固定长度)。<br><img src="/img/RTMPAgreement/7.jpg" alt="github"><br>在传输过程中，通过一个叫做Set Chunk Size的制信息可以设置Chunk数据量的最大值，在发送端和接受端会各自维护一个Chunk Size，可以分别设置这个值来改变自己这一方发送的Chunk的最大大小。大一点可以减少UPC占用率，会占用更多时间在发送还是那个，低带宽下会占用大量传输时间，小一点虽然可以减少阻塞，但是这样会带来额外的信息，并且传输多次也会无法利用高带宽优势。<br>传输过程中，发送端首先将媒体数据封装成消息，然后将消息分割成消息块，最后将分割的消息通过TCP协议发送出去。接收端通过TCP协议收到数据后，首先将消息块组合起成消息，然后对消息进行解封装就可以恢复媒体数据。</p>]]></content>
    
    
    <summary type="html">RTMP协议学习</summary>
    
    
    
    <category term="音视频" scheme="http://example.com/categories/%E9%9F%B3%E8%A7%86%E9%A2%91/"/>
    
    
    <category term="RTMP" scheme="http://example.com/tags/RTMP/"/>
    
    <category term="直播" scheme="http://example.com/tags/%E7%9B%B4%E6%92%AD/"/>
    
  </entry>
  
  <entry>
    <title>React-Native热更新</title>
    <link href="http://example.com/2020/06/01/React-Native%E7%83%AD%E6%9B%B4%E6%96%B0/"/>
    <id>http://example.com/2020/06/01/React-Native%E7%83%AD%E6%9B%B4%E6%96%B0/</id>
    <published>2020-06-01T08:03:28.000Z</published>
    <updated>2021-04-17T07:10:38.653Z</updated>
    
    <content type="html"><![CDATA[<h2 id="一、分类"><a href="#一、分类" class="headerlink" title="一、分类"></a>一、分类</h2><p>有两种选择方式：</p><ul><li>Pushy：官方热更新方案</li><li>Code-Push：微软提供的热更新方案</li></ul><h2 id="二、CodePush"><a href="#二、CodePush" class="headerlink" title="二、CodePush"></a>二、CodePush</h2><p><a href="https://www.npmjs.com/package/code-push-cli#debugging-codepush-integration">官方网址</a></p><p>所有的操作最好都现在官方上过一遍，毕竟电脑配置，环境都会影响。</p><h1 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h1><h3 id="安装Code-Push-Cli"><a href="#安装Code-Push-Cli" class="headerlink" title="安装Code Push Cli"></a>安装<code>Code Push Cli</code></h3><p><code>npm install -g code-push-cli</code></p><h1 id="账户管理"><a href="#账户管理" class="headerlink" title="账户管理"></a>账户管理</h1><h3 id="认证方式"><a href="#认证方式" class="headerlink" title="认证方式"></a>认证方式</h3><h5 id="安装完成后，注册通过"><a href="#安装完成后，注册通过" class="headerlink" title="安装完成后，注册通过"></a>安装完成后，注册通过</h5><p><code>code-push register</code></p><blockquote><p>他会打开浏览器进行注册/登陆功能，可以使用 github 、Microsoft进行第三方账户登陆。登陆成功后会生成一个访问密钥，将其复制到命令行后即可登陆验证操作。</p></blockquote><h5 id="如果已经有一个帐户，通过将帐户链接到另一个身份提供者"><a href="#如果已经有一个帐户，通过将帐户链接到另一个身份提供者" class="headerlink" title="如果已经有一个帐户，通过将帐户链接到另一个身份提供者"></a>如果已经有一个帐户，通过将帐户链接到另一个身份提供者</h5><p><code>code-push link</code></p><h5 id="如果需要正常登陆则通过"><a href="#如果需要正常登陆则通过" class="headerlink" title="如果需要正常登陆则通过"></a>如果需要正常登陆则通过</h5><p><code>code-push login</code></p><h5 id="如果您想确定自己是否已经登录"><a href="#如果您想确定自己是否已经登录" class="headerlink" title="如果您想确定自己是否已经登录"></a>如果您想确定自己是否已经登录</h5><p><code>code-push whoami</code></p><blockquote><p>他会返回当前账户注册的邮件</p></blockquote><h5 id="退出登陆"><a href="#退出登陆" class="headerlink" title="退出登陆"></a>退出登陆</h5><p><code>code-push logout</code></p><h5 id="如果忘记从计算机注销，则不想保留正在运行的会话"><a href="#如果忘记从计算机注销，则不想保留正在运行的会话" class="headerlink" title="如果忘记从计算机注销，则不想保留正在运行的会话"></a>如果忘记从计算机注销，则不想保留正在运行的会话</h5><ul><li>通过<a href="https://appcenter.ms/settings/apitokens">移动中心网站</a></li><li>通过Mobile Center CLI命令：<figure class="highlight maxima"><table><tr><td class="code"><pre><code class="hljs maxima">mobile-<span class="hljs-built_in">center</span> <span class="hljs-built_in">tokens</span> list<br>mobile-<span class="hljs-built_in">center</span> <span class="hljs-built_in">tokens</span> <span class="hljs-built_in">delete</span> &lt;machineName&gt;<br></code></pre></td></tr></table></figure><h3 id="密钥登陆"><a href="#密钥登陆" class="headerlink" title="密钥登陆"></a>密钥登陆</h3>如果不想打开服务器进行验证登陆，则需要运行以下命令来创建“访问密钥”<br><code>code-push access-key add &quot;VSTS Integration&quot;</code></li></ul><p>创建新密钥后，可以使用命令的<code>--accessKey</code>标志指定其值，该标志login允许执行身份验证，而不是启动浏览器。<br><code>code-push login --accessKey &lt;accessKey&gt;</code></p><blockquote><p>通过这种方法登录时，访问密钥不会在注销时自动失效，并且可以在以后的会话中使用，直到将其从CodePush服务器中明确删除或到期为止。</p></blockquote><h3 id="代理支持"><a href="#代理支持" class="headerlink" title="代理支持"></a>代理支持</h3><h5 id="默认情况下，该login命令将自动查找通过HTTPS-PROXY或HTTP-PROXY环境变量指定的系统范围的代理，并使用该代理连接到CodePush服务器。如果您想禁用此行为，并让CLI建立与CodePush的直接连接，通过-noProxy完成："><a href="#默认情况下，该login命令将自动查找通过HTTPS-PROXY或HTTP-PROXY环境变量指定的系统范围的代理，并使用该代理连接到CodePush服务器。如果您想禁用此行为，并让CLI建立与CodePush的直接连接，通过-noProxy完成：" class="headerlink" title="默认情况下，该login命令将自动查找通过HTTPS_PROXY或HTTP_PROXY环境变量指定的系统范围的代理，并使用该代理连接到CodePush服务器。如果您想禁用此行为，并让CLI建立与CodePush的直接连接，通过--noProxy完成："></a>默认情况下，该login命令将自动查找通过HTTPS_PROXY或HTTP_PROXY环境变量指定的系统范围的代理，并使用该代理连接到CodePush服务器。如果您想禁用此行为，并让CLI建立与CodePush的直接连接，通过<code>--noProxy</code>完成：</h5><p><code>code-push login --noProxy</code></p><h5 id="而如果不想通过系统代理，而指定代理方式，可以通过-proxy完成："><a href="#而如果不想通过系统代理，而指定代理方式，可以通过-proxy完成：" class="headerlink" title="而如果不想通过系统代理，而指定代理方式，可以通过--proxy完成："></a>而如果不想通过系统代理，而指定代理方式，可以通过<code>--proxy</code>完成：</h5><p><code>code-push login --proxy https://foo.com:3454</code></p><blockquote><p>所有通过代理参数完成的登陆，都会在登陆成功后保存的本地，可以无需再次键入代理，如果需要取消则需要重新启动并设置代理参数即可。通过 code-push whoami 可以查看当前的代理地址</p></blockquote><h1 id="应用管理"><a href="#应用管理" class="headerlink" title="应用管理"></a>应用管理</h1><h3 id="基本操作"><a href="#基本操作" class="headerlink" title="基本操作"></a>基本操作</h3><p>需要使用以下命令在CodePush服务中注册应用程序：<br><code>code-push app add &lt;appName&gt; &lt;os&gt; &lt;platform&gt;</code></p><blockquote><p>appName：用户软件在codepush下的名字<br>os：当前软件的系统环境<br>platform：js集成环境(<a href="https://github.com/Microsoft/cordova-plugin-code-push">cordova</a>和<a href="http://github.com/Microsoft/react-native-code-push">react-native</a>)</p></blockquote><p>举个栗子：<br>使用<code>-Android</code>和<code>-iOS</code>为命名约定，无需担心创建后的相关内容，因为后面可以查看细节部分。<br><figure class="highlight armasm"><table><tr><td class="code"><pre><code class="hljs armasm"><span class="hljs-symbol">code</span>-<span class="hljs-keyword">push</span> app <span class="hljs-keyword">add</span> MyApp-Android android cordova<br><span class="hljs-symbol">code</span>-<span class="hljs-keyword">push</span> app <span class="hljs-keyword">add</span> MyApp-iOS ios react-native<br></code></pre></td></tr></table></figure></p><blockquote><p>注意：对于iOS和Android使用相同的应用程序可能会导致安装异常，因为为iOS生成的CodePush更新程序包将具有与为Android生成的更新内容不同的内容。</p></blockquote><p>所有新应用程序都会自动带有两个部署环境（Staging和Production）可以开始使用它们通过各自的SDK(<a href="https://github.com/Microsoft/cordova-plugin-code-push">cordova</a>和<a href="http://github.com/Microsoft/react-native-code-push">react-native</a>)进行配置。</p><p>如果决定不喜欢为应用程序提供的名称，则可以使用以下命令随时对其进行重命名：<br><code>code-push app rename &lt;appName&gt; &lt;newAppName&gt;</code></p><p>如果有时不再需要某个应用程序，则可以使用以下命令将其从服务器中删除：<br><code>code-push app rm &lt;appName&gt;</code></p><p>如果要列出已在CodePush服务器上注册的所有应用程序，则可以运行以下命令：<br><code>code-push app ls</code></p><h3 id="应用多人管理"><a href="#应用多人管理" class="headerlink" title="应用多人管理"></a>应用多人管理</h3><p>如果要在同一CodePush应用程序上与其他开发人员一起工作，则可以使用以下命令将他们添加为协作者：<br><code>code-push collaborator add &lt;appName&gt; &lt;collaboratorEmail&gt;</code></p><blockquote><p>appName：协作者可以查看的app名称<br>collaboratorEmail：协作者邮箱</p></blockquote><p>添加后，所有协作者将立即对新共享的应用程序具有以下权限：</p><blockquote><p>1.查看该应用程序，其协作者，部署和发布历史记录<br>2.发布对任何应用程序部署的更新<br>3.促进任何应用程序部署之间的更新<br>4.回滚任何应用程序的部署<br>5.修补任何应用程序部署中的所有版本</p></blockquote><p>相反，这意味着应用程序协作者不能执行以下任何操作：</p><blockquote><p>1.重命名或删除应用<br>2.在应用程序中创建，重命名或删除新的部署<br>3.清除部署的发布历史记录<br>4.在应用中添加或删除协作者</p></blockquote><p>如果某人不再与您一起处理应用程序，则可以使用以下命令将其作为协作者删除：<br><code>code-push collaborator rm &lt;appName&gt; &lt;collaboratorEmail&gt;</code></p><p>如果您想随时列出已添加到应用程序中的所有协作者，则只需运行以下命令：<br><code>code-push collaborator ls &lt;appName&gt;</code></p><h3 id="所有权转移"><a href="#所有权转移" class="headerlink" title="所有权转移"></a>所有权转移</h3><p><strong><em>在版本2.0.0.0的更新中，删除了该app transfer命令。</em></strong>这要求访问<a href="https://mobile.azure.com/">Mobile Center</a>并执行一些步骤。</p><blockquote><p>1.转到<a href="https://mobile.azure.com并创建一个新组织。">https://mobile.azure.com并创建一个新组织。</a><br>2.邀请希望将应用程序转移到组织的人。他们接受邀请后，将其访问权限更改为“管理员”。<br>3.导航到自己的应用程序，然后单击“管理应用程序”按钮（在该应用程序的“入门”页面上，在右上角）。点击那里的转移按钮，将应用转移到组织。请注意，尽管将来会更改此操作，但当前无法撤消该操作。<br>4.受邀者接受后，选择创建的组织并从中删除自己。</p></blockquote><h3 id="部署管理"><a href="#部署管理" class="headerlink" title="部署管理"></a>部署管理</h3><p>虽然每个App有对应的平台，但是对于 CodePush 而言，只是一个分组而已。他会帮助开发人员完成从测试、QA、过度环境，完成最后的生产环境。</p><blockquote><p>注意：将看到下面的release，promote并rollback命令是需要应用程序的名称，并以工作部署名称，因为它是唯一标识分布点两者的组合（例如我要发布的更新将我的iOS应用发送给Beta测试人员）。</p></blockquote><p>每当使用CLI注册应用程序时，默认情况下，CodePush服务都会包括两个部署：Staging(过度环境)和Production(生产环境)。如果这两个环境无法满足测试过程，可以使用：<br><code>code-push deployment add &lt;appName&gt; &lt;deploymentName&gt;</code></p><blockquote><p>appName：应用名称<br>deploymentName：部署环境名称</p></blockquote><p>与应用程序一样，您也可以分别使用以下命令来删除和重命名部署：<br><figure class="highlight vim"><table><tr><td class="code"><pre><code class="hljs vim">code-push deployment rm <span class="hljs-symbol">&lt;appName&gt;</span> <span class="hljs-symbol">&lt;deploymentName&gt;</span><br>code-push deployment <span class="hljs-built_in">rename</span> <span class="hljs-symbol">&lt;appName&gt;</span> <span class="hljs-symbol">&lt;deploymentName&gt;</span> <span class="hljs-symbol">&lt;newDeploymentName&gt;</span><br></code></pre></td></tr></table></figure></p><p>查看指定App下的部署列表详情：<br><code>code-push deployment ls &lt;appName&gt; [--displayKeys|-k]</code><br>查看详情包括：</p><blockquote><p>1.部署环境名称列表，软件名称等基本信息<br>2.部署环境相应的密钥(需要加上 -k 参数)<br>3.更新内容表述，版本号，是否强制下发，下发时间<br>4.active：当前正在运行此版本的成功安装次数。该指标显示了活跃用户的总数以及代表的总受众人数的百分比。<br>5.total：此更新获得的成功安装的总数。<br>6.pending：已下载但尚未安装此版本的次数<br>7.rollbacks：此版本已在客户端上自动回滚的次数。如果在安装过程中发布了包含崩溃的更新，则CodePush插件会将最终用户回滚到以前的版本，并将该问题报告给服务器。<br>8.rollout：表示有资格接收此更新的用户百分比。<br>9.disabled：指示该版本是否已标记为已禁用，因此最终用户可以下载该版本。仅对于实际禁用的版本显示此属性。</p></blockquote><h1 id="发布更新"><a href="#发布更新" class="headerlink" title="发布更新"></a>发布更新</h1><p>在完成上述配置后并下发软件下载包，即可开始发布更新。CodePush CLI包括三个用于发布更新的不同命令：</p><ul><li>常规：将由外部工具或构建脚本生成的更新发布到CodePush服务器。在适应现有工作流程方面提供了最大的灵活性。</li><li>react-native：执行与常规release命令相同的功能，但还处理生成更新的应用程序内容（JS包和资产）的任务。无需多个指令。</li><li>cordova：同react-native类似效果。</li></ul><p><strong><em>注意：客户端只能发现和下载部署中的50个最新版本。</em></strong></p><h3 id="发布更新-常规"><a href="#发布更新-常规" class="headerlink" title="发布更新(常规)"></a>发布更新(常规)</h3><figure class="highlight xml"><table><tr><td class="code"><pre><code class="hljs xml">code-push release <span class="hljs-tag">&lt;<span class="hljs-name">appName</span>&gt;</span> <span class="hljs-tag">&lt;<span class="hljs-name">updateContents</span>&gt;</span> <span class="hljs-tag">&lt;<span class="hljs-name">targetBinaryVersion</span>&gt;</span><br>[--deploymentName <span class="hljs-tag">&lt;<span class="hljs-name">deploymentName</span>&gt;</span>]<br>[--description <span class="hljs-tag">&lt;<span class="hljs-name">description</span>&gt;</span>]<br>[--disabled <span class="hljs-tag">&lt;<span class="hljs-name">disabled</span>&gt;</span>]<br>[--mandatory]<br>[--noDuplicateReleaseError]<br>[--rollout <span class="hljs-tag">&lt;<span class="hljs-name">rolloutPercentage</span>&gt;</span>]<br>[--privateKeyPath <span class="hljs-tag">&lt;<span class="hljs-name">pathToPrivateKey</span>&gt;</span>]<br></code></pre></td></tr></table></figure><ul><li>appName<br>  这指定了要为其发布此更新的CodePush应用的名称。</li><li><p>updateContents<br>  这指定了要发布的更新的应用程序代码和资源的位置。可以指定js文件或者目录，但是不要需要对他们压缩，因为codepush会自己完成。<br>  重要的是，您指定的路径是指特定于平台的应用程序的预准备/捆绑版本。<br>  平台|准备命令|包路径（相对于项目根）<br>  -|-|-<br>  Cordova（Android）|cordova prepare android|对于cordova-android版本7和更高版本：./platforms/android/app/src/main/assets/www目录；对于cordova-android版本6和更早版本：./platforms/android/assets/www目录；<br>  Cordova（iOS）|cordova prepare ios|./platforms/ios/www 目录<br>  React Native wo / assets（Android）|react-native bundle --platform android --entry-file &lt;entryFile&gt; --bundle-output &lt;bundleOutput&gt; --dev false|--bundle-output期权的价值<br>  React Native w / assets（Android）|react-native bundle --platform android --entry-file &lt;entryFile&gt; --bundle-output &lt;releaseFolder&gt;/&lt;bundleOutput&gt; --assets-dest &lt;releaseFolder&gt; --dev false|--assets-dest选项的值，应代表包含您的资产和JS捆绑包的新创建的目录<br>  React Native wo / assets（iOS）|react-native bundle --platform ios --entry-file &lt;entryFile&gt; --bundle-output &lt;bundleOutput&gt; --dev false|--bundle-output期权的价值<br>  React Native w / assets（iOS）|react-native bundle --platform ios --entry-file &lt;entryFile&gt; --bundle-output &lt;releaseFolder&gt;/&lt;bundleOutput&gt; --assets-dest &lt;releaseFolder&gt; --dev false|--assets-dest选项的值，应代表包含您的资产和JS捆绑包的新创建的目录</p></li><li><p>targetBinaryVersion<br>  这指定了要为其发布更新的应用程序的存储//二进制版本，以便只有运行该版本的用户才能收到更新，而运行较旧和/或较新版本的应用程序二进制文件的用户则不会。出于以下原因，这很有用：</p><blockquote><p>如果用户运行的是较旧的二进制版本，则CodePush更新中的重大更改可能与他们所运行的版本不兼容。<br>如果用户正在运行较新的二进制版本，则假定他们正在运行的版本与CodePush更新较新（并且可能不兼容）。<br>  如果希望通过更新来定位应用商店二进制文件的多个版本，我们还允许您将参数指定为<a href="https://github.com/npm/node-semver#advanced-range-syntax">semver range表达式</a>。这样，任何运行满足范围表达式（即semver.satisfies(version, range)return true）的二进制版本的客户端设备都将获得更新。有效的semver范围表达式的示例如下：<br>  范围表达|谁得到更新<br>  -|-<br>  1.2.3|仅运行特定二进制应用程序商店版本1.2.3的设备<br>  *|任何配置为使用CodePush应用程序更新的设备<br>  1.2.x|运行主要版本1，次要版本2和应用程序的任何修补程序版本的设备<br>  1.2.3 - 1.2.7|在1.2.3（包括）和1.2.7（包括）之间运行任何二进制版本的设备<br>  &gt;=1.2.3 &lt;1.2.7|运行介于1.2.3（包含）和1.2.7（包含）之间的任何二进制版本的设备<br>  1.2|相当于 &gt;=1.2.0 &lt;1.3.0<br>  ~1.2.3|相当于 &gt;=1.2.3 &lt;1.3.0<br>  ^1.2.3|相当于 &gt;=1.2.3 &lt;2.0.0<br>注意：如果您的semver表达了特殊的shell字符或运营商，如启动&gt;，^或** ，该命令可能不正确，如果你不用引号括作为外壳将不提供正确的价值观对我们的CLI值执行处理。因此，最好targetBinaryVersion在调用release命令时将参数用双引号引起来，例如code-push release MyApp-iOS updateContents “&gt;1.2.3”。<br>  下表概述了CodePush希望更新的版本范围可以满足每种应用类型的版本值：<br>  平台|应用商店版本的来源<br>  -|-<br>  科尔多瓦|文件中的&lt;widget version&gt;属性config.xml<br>  React Native（Android）|文件中的android.defaultConfig.versionName属性build.gradle<br>  React Native（iOS）|文件中的CFBundleShortVersionString密钥Info.plist<br>  React Native（Windows）|文件中的&lt;Identity Version&gt;密钥Package.appxmanifest<br>  如果用户正在运行较新的二进制版本，则假定他们正在运行的版本与CodePush更新较新（并且可能不兼容）。</p></blockquote></li><li><p>deploymentName<br>  这指定要将更新发布到哪个部署。默认值为Staging，但是准备部署到Production或自定义部署之一时，只需显式设置此参数即可。<br>  <strong><em>注意：可以使用“ —deploymentName”或“ -d”来设置参数。</em></strong></p></li><li><p>description<br>  这为部署提供了一个可选的“更改日志”。该值只是往返于客户端，以便在检测到更新时，应用可以选择将其显示给最终用户（例如，通过“新功能”对话框）。该字符串接受诸如\n和的控制字符，\t因此可以在描述中包括空格格式，以提高可读性。<br>  <strong><em>注意：可以使用“ —description”或“ —des”设置此参数</em></strong></p></li><li><p>disabled<br>  这指定更新是否应由最终用户下载。如果未指定，则不会禁用该更新。如果您要发布一个无法立即获得的更新，则此参数非常有用，直到您希望最终用户能够下载该更新。<br>  <strong><em>注意：可以使用“ —disabled”或“ -x”设置此参数</em></strong></p></li><li><p>mandatory<br>  这指定是否应将更新视为强制性更新（例如，该更新包含重要的安全修复程序）。<br>  例如，假设您对应用发布了以下三个更新：<br>  Release|Mandatory\?<br>  v1|No<br>  v2|Yes<br>  v3|No</p><blockquote><p>如果最终用户当前正在运行v1，并且他们查询服务器以获取更新，它将以v3（因为这是最新的）响应，但是由于在两者之间发布了强制更新，因此它将动态地将发布转换为强制更新。<br>如果最终用户当前正在运行v2，并且他们查询服务器以获取更新，它将以响应v3，但将发行版保留为可选。<br>  <strong><em>注意：可以使用—mandatory或设置此参数。-m</em></strong></p></blockquote></li><li><p>noDuplicateReleaseError<br>  这指定如果更新与部署中的最新版本相同，则CLI应该生成警告而不是错误。</p></li><li><p>rolloutPercentage<br>  该参数指定应有资格接收此更新的用户百分比（为1和之间的整数100）。如果您想让一部分受众（例如25％）“发布”新版本，并获得反馈和/或注意异常/崩溃，这可能会有所帮助，然后再将其广泛提供给所有人。如果未设置此参数，则将其设置为100%。<br>  利用推出功能时，需要牢记一些其他注意事项：</p><blockquote><p>1.不能将最新更新发布到其最新发布为“Active”发布（即，其发布属性为非null）的部署中。必须先完成部署（即，将该rollout属性设置为100），然后才能发布对部署的进一步更新。即必须<br>2.如果回滚最新版本为“active”的部署，则将清除回滚值，从而有效地“暂停”回滚行为。<br>3.与mandatory和description字段不同，当需要将发布从一个部署升级到另一个部署时，它不会传播该rollout属性，因此，如果希望新发布（在目标部署中）具有部署值，则需要显式设置它当您调用promote命令时。<br>  <strong><em>注意：为了使此参数真正生效，您的最终用户需要运行CodePush插件的版本1.6.0-beta+（对于Cordova）或1.9.0-beta+（对于React Native）。如果发布的更新指定了rollout属性，则运行较旧版本的Cordova或React Native插件的最终用户将无资格获得此更新。因此，在您采用必要版本的特定于平台的CodePush插件之前（如前所述），我们建议您不要在发行版中设置推广值，因为没有人最终会收到它。</em></strong><br>  <strong><em>注意：可以使用—rollout或设置此参数。-r</em></strong></p></blockquote></li><li><p>pathToPrevatekey<br>  此参数指定用于生成更新签名的私钥文件的路径。如果省略了私钥路径参数，则将忽略代码推送插件中的签名验证。<br>  <strong><em>注意：仅Android和iOS平台上的React Native应用程序支持此选项。</em></strong></p></li></ul><h3 id="发布更新-React-Native"><a href="#发布更新-React-Native" class="headerlink" title="发布更新(React-Native)"></a>发布更新(React-Native)</h3><figure class="highlight css"><table><tr><td class="code"><pre><code class="hljs css"><span class="hljs-selector-tag">code</span>-push release-react &lt;appName&gt; &lt;platform&gt;<br><span class="hljs-selector-attr">[--bundleName &lt;bundleName&gt;]</span><br><span class="hljs-selector-attr">[--deploymentName &lt;deploymentName&gt;]</span><br><span class="hljs-selector-attr">[--description &lt;description&gt;]</span><br><span class="hljs-selector-attr">[--development &lt;development&gt;]</span><br><span class="hljs-selector-attr">[--disabled &lt;disabled&gt;]</span><br><span class="hljs-selector-attr">[--entryFile &lt;entryFile&gt;]</span><br><span class="hljs-selector-attr">[--gradleFile &lt;gradleFile&gt;]</span><br><span class="hljs-selector-attr">[--mandatory]</span><br><span class="hljs-selector-attr">[--noDuplicateReleaseError]</span><br><span class="hljs-selector-attr">[--outputDir &lt;outputDir&gt;]</span><br><span class="hljs-selector-attr">[--plistFile &lt;plistFile&gt;]</span><br><span class="hljs-selector-attr">[--plistFilePrefix &lt;plistFilePrefix&gt;]</span><br><span class="hljs-selector-attr">[--sourcemapOutput &lt;sourcemapOutput&gt;]</span><br><span class="hljs-selector-attr">[--targetBinaryVersion &lt;targetBinaryVersion&gt;]</span><br><span class="hljs-selector-attr">[--rollout &lt;rolloutPercentage&gt;]</span><br><span class="hljs-selector-attr">[--privateKeyPath &lt;pathToPrivateKey&gt;]</span><br><span class="hljs-selector-attr">[--config &lt;config&gt;]</span><br></code></pre></td></tr></table></figure><p>简化了释放通过执行以下附加行为更新的过程：</p><blockquote><p>1.运行react-native bundle命令以生成将被发布到CodePush服务器的更新内容（JS包和资源）。它尽可能使用明智的默认值（例如，假设iOS入口文件命名为index.ios.js，创建非开发版本），而且还公开相关react-native bundle参数以实现灵活性（例如—sourcemapOutput）。<br>2.targetBinaryVersion通过使用项目Info.plist（对于iOS）和build.gradle（对于Android）文件中指定的版本名称来推断此发行版。</p></blockquote><p>举个例子：<br>一般发布需要打包和发布两个操作：<br><figure class="highlight jboss-cli"><table><tr><td class="code"><pre><code class="hljs jboss-cli"><span class="hljs-string">//</span> 打包<br>react-native bundle <span class="hljs-params">--platform</span> ios \<br><span class="hljs-params">--entry-file</span> index.ios.js \<br><span class="hljs-params">--bundle-output</span> <span class="hljs-string">./CodePush/main.jsbundle</span> \<br><span class="hljs-params">--assets-dest</span> <span class="hljs-string">./CodePush</span> \<br><span class="hljs-params">--dev</span> <span class="hljs-literal">false</span><br><span class="hljs-string">//</span> 发布<br>code-push release MyApp-iOS <span class="hljs-string">./CodePush</span> 1.0.0<br></code></pre></td></tr></table></figure><br>使用此命令可以一步完成：<br><figure class="highlight gauss"><table><tr><td class="code"><pre><code class="hljs gauss"><span class="hljs-built_in">code</span>-<span class="hljs-keyword">push</span> release-react MyApp-iOS ios<br></code></pre></td></tr></table></figure></p><ul><li><p>appName<br>  同上</p></li><li><p>platform<br>  这指定了当前更新所定位的平台，并且可以是android，ios或windows（不区分大小写）。该值仅用于确定如何正确捆绑更新内容，而实际上并未发送到服务器。</p></li><li><p>bundleName<br>  这指定了应用于生成的JS包的文件名。如果未指定，则标准捆绑包名称将用于指定的平台：main.jsbundle（iOS），index.android.bundle（Android）和index.windows.bundle（Windows）。<br>  <strong><em>注意：可以使用—bundleName或-b设置此参数</em></strong></p></li><li><p>updateContents<br>  同上</p></li><li><p>targetBinaryVersion<br>  同上</p></li><li><p>deploymentName<br>  同上</p></li><li><p>description<br>  同上</p></li><li><p>development<br>  这指定是否生成未缩小的开发JS包。如果未指定，则默认为false禁用警告并缩小包的位置。<br>  <strong><em>注意：可以使用—development或—dev设置此参数</em></strong></p></li><li><p>disabled<br>  同上</p></li><li><p>entryFile<br>  这指定了应用程序的根/条目JavaScript文件的相对路径。如果未指定，则默认为index.ios.js（对于iOS）， index.android.js（对于Android）或index.windows.bundle（对于Windows）（如果该文件存在）<br>  <strong><em>注意：可以使用—entryFile或-e设置此参数</em></strong></p></li><li><p>gradleFile(仅适用于Android)<br>  这指定了build.gradle在尝试自动检测发行版的目标二进制版本时CLI应该使用的文件的相对路径。此参数仅适用于高级方案，因为CLI可以自动build.grade在“标准” React Native项目中找到您的文件。但是，如果gradle文件位于CLI找不到的任意位置，则使用此参数可以使您继续释放CodePush更新，而无需显式设置<code>--targetBinaryVersion</code>参数。由于build.gradle是必需的文件名，因此指定包含文件夹的路径或文件本身的完整路径都将达到相同的效果。</p></li><li><p>plistFile(仅适用于IOS)<br>  这指定了Info.plist在尝试自动检测发行版的目标二进制版本时CLI应该使用的文件的相对路径。该参数仅适用于高级方案，因为CLI可以自动Info.plist在“标准” React Native项目中找到您的文件，并且您可以使用该<code>--plistFilePrefix</code>参数来支持每个环境的plist文件（例如STAGING-Info.plist）。但是，如果您的plist位于CLI找不到的任意位置，则使用此参数可以使您继续释放CodePush更新，而无需显式设置<code>--targetBinaryVersion</code>参数。<br>  <strong><em>注意：可以使用—plistFile或-p设置此参数</em></strong></p></li><li><p>plistFilePrefix(仅适用于IOS)<br>  这指定了Info.plistCLI在尝试自动检测发行版的目标二进制版本时应使用的文件的文件名前缀。如果你已经创建了每个环境的plist文件（例如，这可能是有用的DEV-Info.plist，STAGING-Info.plist），并且希望能够释放CodePush更新，而无需显式设置的—targetBinaryVersion参数。通过指定 —plistFilePrefx，CLI将在以下位置查找一个名为的文件&lt;prefix&gt;-Info.plist，而不是简单的文件名Info.plist（这是默认行为）：./ios和./ios/&lt;appName&gt;。如果您的plist文件不在这两个目录中（例如，您的应用程序是具有嵌入式RN视图的本机iOS应用程序），或者使用了完全不同的文件命名约定，请考虑使用—plistFile参数。<br>  <strong><em>注意：可以使用—plistFilePrefix或—pre设置此参数</em></strong></p></li><li><p>sourcemapOutput<br>  这指定了将生成的JS包的源映射文件写入的相对路径。如果未指定，将不会生成源映射。<br>  <strong><em>注意：可以使用—sourcemapOutput或-s设置此参数</em></strong></p></li><li><p>outputDir<br>  这指定资源，JS捆绑包和源地图文件应写入的相对路径。如果未指定，则资源，JS捆绑包和源地图将被复制到该/tmp/CodePush文件夹中。<br>  <strong><em>注意：复制之前，指定文件夹中的所有内容将被删除</em></strong><br>  <strong><em>注意：可以使用—outputDir或-o设置此参数</em></strong></p></li><li><p>mandatory<br>  同上</p></li><li><p>noDuplicateReleaseError<br>  同上</p></li><li><p>rolloutPercentage<br>  同上</p></li><li><p>pathToPrevatekey<br>  同上</p></li></ul><h3 id="发布更新-Cordova"><a href="#发布更新-Cordova" class="headerlink" title="发布更新(Cordova)"></a>发布更新(Cordova)</h3><p>略…</p><h1 id="调试CodePush集成"><a href="#调试CodePush集成" class="headerlink" title="调试CodePush集成"></a>调试CodePush集成</h1><p>为了调试CodePush更新发现体验，可以运行以下命令，以便在应用程序中轻松查看CodePush插件生成的诊断日志：<br><code>code-push debug &lt;platform&gt;</code><br><strong><em>注意：该命令无法调试ios真机</em></strong></p><h1 id="修补更新参数"><a href="#修补更新参数" class="headerlink" title="修补更新参数"></a>修补更新参数</h1><p>发布更新后，在某些情况下，需要修改与之关联的一个或多个数据属性（例如，忘记将关键的错误修复标记为强制性的，希望增加更新的发布百分比）。可以通过运行以下命令轻松地执行此操作：<br><figure class="highlight xml"><table><tr><td class="code"><pre><code class="hljs xml">code-push patch <span class="hljs-tag">&lt;<span class="hljs-name">appName</span>&gt;</span> <span class="hljs-tag">&lt;<span class="hljs-name">deploymentName</span>&gt;</span><br>[--label <span class="hljs-tag">&lt;<span class="hljs-name">releaseLabel</span>&gt;</span>]<br>[--mandatory <span class="hljs-tag">&lt;<span class="hljs-name">isMandatory</span>&gt;</span>]<br>[--description <span class="hljs-tag">&lt;<span class="hljs-name">description</span>&gt;</span>]<br>[--rollout <span class="hljs-tag">&lt;<span class="hljs-name">rolloutPercentage</span>&gt;</span>]<br>[--disabled <span class="hljs-tag">&lt;<span class="hljs-name">isDisabled</span>&gt;</span>]<br>[--targetBinaryVersion <span class="hljs-tag">&lt;<span class="hljs-name">targetBinaryVersion</span>&gt;</span>]<br></code></pre></td></tr></table></figure><br><strong><em>注意：此命令不允许修改发行版的实际更新内容，除了appName和deploymentName，其他都是可选参数</em></strong></p><ul><li><p>releaseLabel<br>  用于指定在哪个版本上进行更新。如果省略，则请求的更改将应用于指定部署中的最新版本。<br>  为了查找要更新的发行版的标签，您可以运行下面命令查看当前版本列表<br>  <code>code-push deployment history</code><br>  <strong><em>注意：可以使用—label或设置此参数。-l</em></strong></p></li><li><p>isMandatory<br>  仅允许更新是否应将发行版视为强制发行。<br>  <strong><em>注意：请注意，—mandatory和—mandatory true是等效的；而省略则是忽略，而不是—mandatory false。</em></strong></p></li><li><p>description</p><pre><code>  仅允许更新与发行版关联的描述（例如，在发行时输入了错字，或者根本没有添加描述）。如果省略此参数，则不会更改目标版本的description属性的值。</code></pre></li><li>rolloutPercentage<br>  仅允许您增加目标发行版的推出百分比。只能将此参数设置为一个值，该值大于当前推出的值。</li><li><p>isDisabled<br>  仅允许您更新是否应禁用发行版。<br>  <strong><em>注意：请注意，—disabled和—disabled true是等效的；而省略则是忽略，而不是—disabled false。</em></strong></p></li><li><p>targetBinaryVersion<br>  仅允许更新semver范围，该范围指示发行版与哪个二进制版本兼容。如果在最初发布更新时犯了一个错误（例如，指定1.0.0但表示1.1.0），或者想增加或减少该版本支持的版本范围（例如，发现某个发布根本无法使用1.1.2），这将非常有用。。如果忽略此参数，则不会更改目标发行版的version属性的值。</p></li></ul><h1 id="发布更新-1"><a href="#发布更新-1" class="headerlink" title="发布更新"></a>发布更新</h1><p>一旦针对特定的部署（例如Staging）测试了更新，并希望将其“下游”升级（例如dev-&gt; staging，staging-&gt; production），则只需使用以下命令从一个版本复制发布部署到另一个：<br><figure class="highlight css"><table><tr><td class="code"><pre><code class="hljs css"><span class="hljs-selector-tag">code</span>-push promote &lt;appName&gt; &lt;sourceDeploymentName&gt; &lt;destDeploymentName&gt;<br><span class="hljs-selector-attr">[--description &lt;description&gt;]</span><br><span class="hljs-selector-attr">[--label &lt;label&gt;]</span><br><span class="hljs-selector-attr">[--disabled &lt;disabled&gt;]</span><br><span class="hljs-selector-attr">[--mandatory]</span><br><span class="hljs-selector-attr">[--noDuplicateReleaseError]</span><br><span class="hljs-selector-attr">[--rollout &lt;rolloutPercentage&gt;]</span><br><span class="hljs-selector-attr">[--targetBinaryVersion &lt;targetBinaryVersion]</span><br></code></pre></td></tr></table></figure><br>该promote命令将为目标部署创建一个新版本，其中包括源部署的最新版本中的确切代码和元数据（描述，强制性版本和目标二进制版本）。尽管可以使用release命令“手动”将更新从一个环境迁移到另一个环境，但该promote命令具有以下优点：</p><blockquote><p>1.速度更快，因为不需要重组要发布的发行资产，也不需要记住与源部署的发行版关联的描述/应用商店版本。<br>2.由于提升操作可确保已在源部署（例如Staging）中测试的确切内容在目标部署（例如Production）中变为活动状态，因此它不那么容易出错。</p></blockquote><ul><li><p>descriptin<br>  同上，仅允许您覆盖将用于升级版本的描述。</p></li><li><p>label<br>  使用此可选参数，可以从源部署中选择指定的标签，并将其升级到目标部署。如果未指定，则将升级源部署上的最新版本。</p></li><li><p>disabled<br>  同上，仅允许您覆盖将用于升级版本的Disabled标志的值。如果未指定，则新版本将继承要升级的版本的disabled属性。</p></li><li><p>mandatory<br>  同上，仅允许您覆盖将用于升级版本的强制性标志。如果未指定，则新版本将继承要升级的版本的必需属性。</p></li><li><p>noDuplicateReleaseError<br>  同上</p></li><li><p>rolloutPercentage<br>  同上，并允许指定是否仅将新创建的发行版提供给部分用户。与其他发布元数据参数（例如description）不同，rollout发布的不会作为升级的一部分进行继承/继承，因此，如果不希望新创建的发布可用于所有人，则需要显式设置此设置。</p></li><li><p>targetBinaryVersion<br>  同上，仅允许覆盖将用于升级版本的目标二进制版本。如果未指定，则新版本将继承要升级的版本的目标二进制版本属性。</p></li></ul><h1 id="回滚更新"><a href="#回滚更新" class="headerlink" title="回滚更新"></a>回滚更新</h1><p>部署的发布历史记录是不可变的，因此一旦发布了单个更新，便不能删除或删除它们，而又不能删除所有部署的发布历史记录。但是，如果发布的更新已损坏或包含意外功能，则可以使用以下rollback命令轻松将其回滚：<br><figure class="highlight gauss"><table><tr><td class="code"><pre><code class="hljs gauss"><span class="hljs-built_in">code</span>-<span class="hljs-keyword">push</span> rollback &lt;appName&gt; &lt;deploymentName&gt;<br><span class="hljs-built_in">code</span>-<span class="hljs-keyword">push</span> rollback MyApp-iOS Production<br></code></pre></td></tr></table></figure><br>这具有为部署创建一个新版本的作用，该版本包括与最新版本完全相同的代码和元数据。例如，假设向应用发布了以下更新：<br>Release|描述|Mandatory<br>-|-|-<br>v1|初版本|Yes<br>v2|新增功能|No<br>v3|修复bug|Yes</p><p>如果rollback在该部署上运行该命令，v4则将创建一个包含v2发行版内容的新发行版<br>Release|描述|Mandatory<br>-|-|-<br>v1|初版本|Yes<br>v2|新增功能|No<br>v3|修复bug|Yes<br>v4（从v3还原到v2）|新增功能|没有</p><blockquote><p>v3现在，已经获取的最终用户将被“移回”到v2该应用执行更新检查时。此外，所有仍在运行的用户v2（因此从未获得过）不会v3收到更新，因为他们已经在运行最新版本（这就是为什么我们的更新检查除了使用发布标签之外，还使用程序包哈希值的原因）。</p></blockquote><p>如果要将部署回滚到除先前版本以外的版本（例如v3-&gt; v2），则可以指定可选<code>--targetRelease</code>参数：<br><code>code-push rollback MyApp-iOS Production --targetRelease v34</code></p><p><strong><em>注意：回滚产生的发行版将在deployment history命令的输出中进行注释，以帮助更轻松地识别它们。</em></strong></p><h1 id="查看发行记录"><a href="#查看发行记录" class="headerlink" title="查看发行记录"></a>查看发行记录</h1><p>可以使用以下命令查看特定应用程序部署的50个最新版本的历史记录：<br><code>code-push deployment history &lt;appName&gt; &lt;deploymentName&gt;</code><br><strong><em>注意：也可以使用“ h”别名来运行history命令</em></strong></p><h1 id="清除发布历史记录"><a href="#清除发布历史记录" class="headerlink" title="清除发布历史记录"></a>清除发布历史记录</h1><p>虽然无法删除单个发行版，但是可以使用以下命令清除与部署关联的整个发行版历史记录：<br><code>code-push deployment clear &lt;appName&gt; &lt;deploymentName&gt;</code><br><strong><em>运行此命令后，配置为使用其关联的部署密钥接收更新的客户端设备将不再接收已清除的更新。此命令是不可逆的，因此不应在生产部署中使用。</em></strong></p><h1 id="代码签名"><a href="#代码签名" class="headerlink" title="代码签名"></a>代码签名</h1><h5 id="他是什么"><a href="#他是什么" class="headerlink" title="他是什么"></a>他是什么</h5><p>代码签名是为捆绑创建数字签名的一种方式，以后可以在安装之前在客户端进行验证。</p><h5 id="为什么需要"><a href="#为什么需要" class="headerlink" title="为什么需要"></a>为什么需要</h5><p>开发人员想知道他们交付的代码就是他们编写的代码。代码签名是提供这种保证的主要机制，可以帮助减轻或消除整个中间级攻击。</p><h5 id="如何工作"><a href="#如何工作" class="headerlink" title="如何工作"></a>如何工作</h5><p>首先，开发人员生成一个非对称密钥对：私钥将用于对捆绑包进行签名；捆绑签名验证的公共密钥。该CodePush CLI然后使用私钥签名过程中捆绑release，release-react并release-cordova命令。公共密钥随移动应用程序一起提供。开发人员可以控制密钥的生成和管理。</p><h2 id="三、react-native-code-push"><a href="#三、react-native-code-push" class="headerlink" title="三、react-native-code-push"></a>三、react-native-code-push</h2><p>Android与Ios只是说了配置及基础更新方式。具体js层代码描述、API，都在Js中具体说明。</p><h1 id="Android"><a href="#Android" class="headerlink" title="Android"></a>Android</h1><h3 id="npm-install-react-native-code-push-—save-OR-yarn-add-react-native-code-push"><a href="#npm-install-react-native-code-push-—save-OR-yarn-add-react-native-code-push" class="headerlink" title="npm install react-native-code-push —save OR yarn add react-native-code-push"></a>npm install react-native-code-push —save OR yarn add react-native-code-push</h3><p>安装库<br><figure class="highlight gauss"><table><tr><td class="code"><pre><code class="hljs gauss">npm install react-native-<span class="hljs-built_in">code</span>-<span class="hljs-keyword">push</span> --<span class="hljs-keyword">save</span><br><span class="hljs-meta"># 或者</span><br>yarn add react-native-<span class="hljs-built_in">code</span>-<span class="hljs-keyword">push</span><br></code></pre></td></tr></table></figure></p><h3 id="基本配置"><a href="#基本配置" class="headerlink" title="基本配置"></a>基本配置</h3><p>[官方网址效果最佳]<a href="https://github.com/microsoft/react-native-code-push/blob/master/docs/setup-android.md">https://github.com/microsoft/react-native-code-push/blob/master/docs/setup-android.md</a></p><p><strong>配置时需要注意自己项目的react-native版本号对应上，否则会出现很多问题。</strong></p><p>以下react-native版本：0.63的配置实例：</p><blockquote><p>1.<code>android/settings.gradle</code>中添加以下配置<br><figure class="highlight reasonml"><table><tr><td class="code"><pre><code class="hljs reasonml"><span class="hljs-keyword">include</span> &#x27;:app&#x27;, &#x27;:react-native-code-push&#x27;<br>project(&#x27;:react-native-code-push&#x27;).projectDir = <span class="hljs-keyword">new</span> <span class="hljs-constructor">File(<span class="hljs-params">rootProject</span>.<span class="hljs-params">projectDir</span>, &#x27;..<span class="hljs-operator">/</span><span class="hljs-params">node_modules</span><span class="hljs-operator">/</span><span class="hljs-params">react</span>-<span class="hljs-params">native</span>-<span class="hljs-params">code</span>-<span class="hljs-params">push</span><span class="hljs-operator">/</span><span class="hljs-params">android</span><span class="hljs-operator">/</span><span class="hljs-params">app</span>&#x27;)</span><br></code></pre></td></tr></table></figure><br>2.<code>android/app/build.gradle</code>中添加以下配置<br><figure class="highlight sqf"><table><tr><td class="code"><pre><code class="hljs sqf"><span class="hljs-built_in">apply</span> <span class="hljs-keyword">from</span>: <span class="hljs-string">&quot;../../node_modules/react-native/react.gradle&quot;</span><br><span class="hljs-built_in">apply</span> <span class="hljs-keyword">from</span>: <span class="hljs-string">&quot;../../node_modules/react-native-code-push/android/codepush.gradle&quot;</span><br></code></pre></td></tr></table></figure><br>3.<code>MainApplication.java</code>中添加以下配置<br><figure class="highlight scala"><table><tr><td class="code"><pre><code class="hljs scala"><span class="hljs-comment">// 1. Import the plugin class.</span><br><span class="hljs-keyword">import</span> com.microsoft.codepush.react.<span class="hljs-type">CodePush</span>;<br><br>public <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">MainApplication</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">Application</span> <span class="hljs-title">implements</span> <span class="hljs-title">ReactApplication</span> </span>&#123;<br><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-type">ReactNativeHost</span> mReactNativeHost = <span class="hljs-keyword">new</span> <span class="hljs-type">ReactNativeHost</span>(<span class="hljs-keyword">this</span>) &#123;<br>        ...<br><br>        <span class="hljs-comment">// 2. Override the getJSBundleFile method in order to let</span><br>        <span class="hljs-comment">// the CodePush runtime determine where to get the JS</span><br>        <span class="hljs-comment">// bundle location from on each app start</span><br>        <span class="hljs-meta">@Override</span><br>        <span class="hljs-keyword">protected</span> <span class="hljs-type">String</span> getJSBundleFile() &#123;<br>            <span class="hljs-keyword">return</span> <span class="hljs-type">CodePush</span>.getJSBundleFile();<br>        &#125;<br>    &#125;;<br>&#125;<br></code></pre></td></tr></table></figure><br>4.配置gradle版本号<code>app/build.gradle</code>从两位改为三位<br><figure class="highlight nginx"><table><tr><td class="code"><pre><code class="hljs nginx"><span class="hljs-section">android</span> &#123;<br><br>    <span class="hljs-section">defaultConfig</span> &#123;<br>        <span class="hljs-attribute">versionName</span> <span class="hljs-string">&quot;1.0.0&quot;</span><br>    &#125;<br></code></pre></td></tr></table></figure><br>5.添加Deployment Key，将需要更新的值复制到<code>res/values/strings.xml</code>中<br><a href="/img/ReactNativeHotUpdate/0.png">!github</a><br><figure class="highlight applescript"><table><tr><td class="code"><pre><code class="hljs applescript">&lt;resources&gt;<br>&lt;<span class="hljs-built_in">string</span> <span class="hljs-built_in">name</span>=<span class="hljs-string">&quot;app_name&quot;</span>&gt;AppName&lt;/<span class="hljs-built_in">string</span>&gt;<br>&lt;<span class="hljs-built_in">string</span> moduleConfig=<span class="hljs-string">&quot;true&quot;</span> <span class="hljs-built_in">name</span>=<span class="hljs-string">&quot;CodePushDeploymentKey&quot;</span>&gt;DeploymentKey&lt;/<span class="hljs-built_in">string</span>&gt;<br>&lt;/resources&gt;<br></code></pre></td></tr></table></figure><br>6.编译！<br>7.JS中引入<br><figure class="highlight clean"><table><tr><td class="code"><pre><code class="hljs clean"><span class="hljs-keyword">import</span> CodePush <span class="hljs-keyword">from</span> <span class="hljs-string">&#x27;react-native-code-push&#x27;</span>;<br></code></pre></td></tr></table></figure></p></blockquote><h3 id="打包（Android基础包）"><a href="#打包（Android基础包）" class="headerlink" title="打包（Android基础包）"></a>打包（Android基础包）</h3><p>指令：<br><figure class="highlight jboss-cli"><table><tr><td class="code"><pre><code class="hljs jboss-cli"><span class="hljs-string">./gradlew</span> assembleRelease<br></code></pre></td></tr></table></figure><br>GUI：<br>双击-Gradle-AppName-app-Tasks-other-assembleRelease<br><a href="/img/ReactNativeHotUpdate/1.png">!github</a></p><p>完成的包就在<strong>app/build/output/</strong>文件夹下。</p><h3 id="开启第一次更新-简单实例"><a href="#开启第一次更新-简单实例" class="headerlink" title="开启第一次更新(简单实例)"></a>开启第一次更新(简单实例)</h3><blockquote><p>1.修改需要改动的js代码(原生代码无法更新)<br>2.打包<br>使用上面说明的指令或者GUI方式两种都可以。这里推荐GUI方式，因为这里可以看到很多日志。<br><a href="/img/ReactNativeHotUpdate/2.png">!github</a><br>3.上传更新包，这里分别使用<code>release-react</code>和<code>release</code>的方式来记录过程。</p><ul><li>3.1 code-push release-react<br>此方式不需要多想，直接敲命令行：<figure class="highlight apache"><table><tr><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">code</span>-push release-react CodePushDemoAndroid android -t <span class="hljs-number">1</span>.<span class="hljs-number">0</span>.<span class="hljs-number">0</span> -m --des <span class="hljs-string">&quot;1.0.1版本更新测试&quot;</span><br></code></pre></td></tr></table></figure>-t：需要更新的基础版本号<br>-m：是否强制更新<br>—des：当前更新描述</li><li>3.2 code-push release<br>此方式需要两个步骤，需要进行jsbundle打包，然后推送到服务器上。<figure class="highlight gradle"><table><tr><td class="code"><pre><code class="hljs gradle"># jsbundle打包 assets文件夹需要手动创建，也可以自定义位置<br>react-<span class="hljs-keyword">native</span> bundle --entry-<span class="hljs-keyword">file</span> index.js --platform android --dev <span class="hljs-keyword">false</span> --bundle-output .<span class="hljs-regexp">/android/</span>app<span class="hljs-regexp">/src/m</span>ain<span class="hljs-regexp">/assets/i</span>ndex.android.bundle --assets-dest .<span class="hljs-regexp">/android/</span>app<span class="hljs-regexp">/src/m</span>ain<span class="hljs-regexp">/assets/</span><br># 推送到服务器上<br>code-<span class="hljs-keyword">push</span> release CodePushDemoAndroid .<span class="hljs-regexp">/android/</span>app<span class="hljs-regexp">/src/m</span>ain/assets <span class="hljs-number">1.0</span>.<span class="hljs-number">0</span> -m --des <span class="hljs-string">&quot;1.0.1版本更新测试&quot;</span><br></code></pre></td></tr></table></figure>第一步，将js与资源打包成bundle文件及资源文件。<br>第二部，将指定的文件夹内资源推送至服务器<br>1.0.0：需要更新的基础版本号<br>-m：是否强制更新<br>—des：当前更新描述<br><strong><em>注意，出了任何问题可以清空更新库(code-push deployment clear CodePushDemoAndroid Staging)然后再做上传文件进行更新。</em></strong></li></ul></blockquote><h3 id="code-push-release-react-还是-code-push-release"><a href="#code-push-release-react-还是-code-push-release" class="headerlink" title="code-push release-react 还是 code-push release ?"></a>code-push release-react 还是 code-push release ?</h3><p>上面实例可以看出，前者提供了方便的套餐方式，过程不需要介入；而后者更将步骤显现的更清晰。</p><h3 id="Android-API"><a href="#Android-API" class="headerlink" title="Android-API"></a>Android-API</h3><p>[官方网址]<a href="https://github.com/microsoft/react-native-code-push/blob/master/docs/api-android.md">https://github.com/microsoft/react-native-code-push/blob/master/docs/api-android.md</a></p><blockquote><p>Public Key:用于代码签名功能中的捆绑包验证。添加到<code>app/src/main/res/values/strings/xml</code><br><figure class="highlight vbnet"><table><tr><td class="code"><pre><code class="hljs vbnet">&lt;<span class="hljs-type">string</span> moduleConfig=<span class="hljs-string">&quot;true&quot;</span> name=<span class="hljs-string">&quot;CodePushPublicKey&quot;</span>&gt;your-<span class="hljs-keyword">public</span>-<span class="hljs-keyword">key</span>&lt;/<span class="hljs-type">string</span>&gt;<br></code></pre></td></tr></table></figure><br>Server Url:用于指定CodePush服务器网址。<br><figure class="highlight vim"><table><tr><td class="code"><pre><code class="hljs vim">&lt;<span class="hljs-built_in">string</span> moduleConfig=<span class="hljs-string">&quot;true&quot;</span> name=<span class="hljs-string">&quot;CodePushServerUrl&quot;</span>&gt;http<span class="hljs-variable">s:</span>//yourcodepush.server.<span class="hljs-keyword">com</span>&lt;/<span class="hljs-built_in">string</span>&gt;<br></code></pre></td></tr></table></figure><br>CodePush创建<br><figure class="highlight vhdl"><table><tr><td class="code"><pre><code class="hljs vhdl">CodePush（<span class="hljs-built_in">String</span> deploymentKey，Activity mainActivity）<br>CodePush（<span class="hljs-built_in">String</span> deploymentKey，Activity mainActivity，bool isDebugMode）<br>CodePush（<span class="hljs-built_in">String</span> deploymentKey，<span class="hljs-keyword">Context</span> <span class="hljs-keyword">context</span>，<span class="hljs-built_in">boolean</span> isDebugMode，<span class="hljs-built_in">Integer</span> publicKeyResourceDescriptor）<br>CodePush（<span class="hljs-built_in">String</span> deploymentKey，<span class="hljs-keyword">Context</span> <span class="hljs-keyword">context</span>，<span class="hljs-built_in">boolean</span> isDebugMode，<span class="hljs-built_in">String</span> serverUrl）<br></code></pre></td></tr></table></figure></p><blockquote><p>isDebugMode: 是否置于调试模式<br>publicKeyResourceDescriptor: 允许指定读取公共密钥内容所需的公共密钥资源描述符。<br>serverUrl: 允许指定CodePush Server Url。默认值：”<a href="https://codepush.appcenter.ms/&quot;被中指定的值覆盖serverUrl。">https://codepush.appcenter.ms/&quot;被中指定的值覆盖serverUrl。</a></p></blockquote></blockquote><p>其中还可以使用构建者模式<code>CodePushBuilder</code>。</p><blockquote><p>static 函数</p><blockquote><p>getBundleUrl():返回index.android.bundle路径。<br>getBundleUrl(String bundleName)<br>CodePush.getJSBundleFile()<br>CodePush.getJSBundleFile(String assetsBundleFileName):<br>getPackageFolder():返回当前更新文件夹的路径。<br>overrideAppVersion(String appVersionOverride):设置应用程序二进制接口的版本，否则默认指定为Play商店的版本versionName中build.gradle。</p></blockquote></blockquote><h1 id="IOS"><a href="#IOS" class="headerlink" title="IOS"></a>IOS</h1><h1 id="JS代码"><a href="#JS代码" class="headerlink" title="JS代码"></a>JS代码</h1><h2 id="四、code-push-server"><a href="#四、code-push-server" class="headerlink" title="四、code-push-server"></a>四、code-push-server</h2><p>上面所使用的推送库都是微软所在的外网下。可以通过<code>code-push-server</code>来快速部署一个局域网下的热更新服务。<br>以下过程是在对codepush环境存在的情况下完成的。</p><h1 id="下载库"><a href="#下载库" class="headerlink" title="下载库"></a>下载库</h1><figure class="highlight gauss"><table><tr><td class="code"><pre><code class="hljs gauss">npm install <span class="hljs-built_in">code</span>-<span class="hljs-keyword">push</span>-server -g<br></code></pre></td></tr></table></figure><p>还需要<code>code-push-cli</code>。</p><h1 id="MySql"><a href="#MySql" class="headerlink" title="MySql"></a>MySql</h1><p>功能需要用到mysql服务。根据环境来配置MySql(建议使用5.6)。此处忽略</p><h1 id="修改数据存储路径"><a href="#修改数据存储路径" class="headerlink" title="修改数据存储路径"></a>修改数据存储路径</h1><p>在<strong>code-push-server文件夹下的config/config.js文件</strong>。<br><figure class="highlight 1c"><table><tr><td class="code"><pre><code class="hljs 1c">local: &#123;<br>  <span class="hljs-comment">// Binary files storage dir, Do not use tmpdir and it&#x27;s public download dir.</span><br><span class="hljs-comment">// 修改数据存储路径</span><br>  storageDir: process.env.STORAGE_DIR <span class="hljs-string">|| &quot;</span>/Users/.../stroage<span class="hljs-string">&quot;,</span><br>  <span class="hljs-comment">// Binary files download host address which Code Push Server listen to. the files storage in storageDir.</span><br><span class="hljs-comment">// 修改为外网地址</span><br>  downloadUrl: process.env.LOCAL_DOWNLOAD_URL <span class="hljs-string">|| &quot;</span>http:<span class="hljs-comment">//172.30.109.40:3000/download&quot;,</span><br>  <span class="hljs-comment">// public static download spacename.</span><br>  public: &#x27;/download&#x27;<br>&#125;,<br></code></pre></td></tr></table></figure></p><h1 id="数据库初始化"><a href="#数据库初始化" class="headerlink" title="数据库初始化"></a>数据库初始化</h1><p>打开数据库服务，然后输入下面命令：<br><figure class="highlight dsconfig"><table><tr><td class="code"><pre><code class="hljs dsconfig"><span class="hljs-string">code-push-server-db</span> <span class="hljs-string">init</span> <span class="hljs-built_in">--dbhost</span> <span class="hljs-string">localhost</span> <span class="hljs-built_in">--dbuser</span> <span class="hljs-string">root</span> <span class="hljs-built_in">--dbpassword</span> <span class="hljs-string">&#x27;数据库root账户密码&#x27;</span><br></code></pre></td></tr></table></figure><br>此处配置的数据库信息，都对应在<strong>code-push-server文件夹下的config/config.js文件</strong>。</p><p>另外里面还有其他配置项:</p><blockquote><p>common里面的storageType,dataDir<br>对应的storagetype中的配置<br>如果使用的是local，就需要修改storageDir和downloadUrl，不修改的话他就会自己创建相应文件夹，可能遇到无法创建的问题。</p></blockquote><h1 id="开启服务"><a href="#开启服务" class="headerlink" title="开启服务"></a>开启服务</h1><p>运行命令开启服务(注意数据库服务是否开启):<br><figure class="highlight gauss"><table><tr><td class="code"><pre><code class="hljs gauss"><span class="hljs-built_in">code</span>-<span class="hljs-keyword">push</span>-server<br></code></pre></td></tr></table></figure><br>[web地址]<a href="http://127.0.0.1:3000">http://127.0.0.1:3000</a><br>初始化用户名：admin<br>初始化密码：123456</p><p>登陆后获取token，用于code-push登陆</p><h1 id="code-push配置"><a href="#code-push配置" class="headerlink" title="code-push配置"></a>code-push配置</h1><h3 id="登陆"><a href="#登陆" class="headerlink" title="登陆"></a>登陆</h3><figure class="highlight gauss"><table><tr><td class="code"><pre><code class="hljs gauss"><span class="hljs-built_in">code</span>-<span class="hljs-keyword">push</span> login <span class="hljs-number">127.0</span><span class="hljs-number">.0</span><span class="hljs-number">.1</span>:<span class="hljs-number">3000</span><br></code></pre></td></tr></table></figure><p>数据完成后会提示输入token值，就是登陆web页面后获取的token值，复制即可。</p><h3 id="其他所有工作都使用code-push完成"><a href="#其他所有工作都使用code-push完成" class="headerlink" title="其他所有工作都使用code-push完成"></a>其他所有工作都使用code-push完成</h3><h1 id="react-native配置"><a href="#react-native配置" class="headerlink" title="react-native配置"></a>react-native配置</h1><p>修改CodePush的更新地址即可：YourCodePushServerUrl: <a href="http://外网地址:3000">http://外网地址:3000</a></p>]]></content>
    
    
    <summary type="html">React-Native热更新方式</summary>
    
    
    
    <category term="ReactNative" scheme="http://example.com/categories/ReactNative/"/>
    
    
    <category term="ReactNative" scheme="http://example.com/tags/ReactNative/"/>
    
  </entry>
  
  <entry>
    <title>JavaWeb</title>
    <link href="http://example.com/2020/06/01/JavaWeb/"/>
    <id>http://example.com/2020/06/01/JavaWeb/</id>
    <published>2020-06-01T03:09:17.000Z</published>
    <updated>2021-04-17T07:10:38.651Z</updated>
    
    <content type="html"><![CDATA[<h2 id="一、相关概念"><a href="#一、相关概念" class="headerlink" title="一、相关概念"></a>一、相关概念</h2><h1 id="软件架构"><a href="#软件架构" class="headerlink" title="软件架构"></a>软件架构</h1><ul><li>C/S：客户端/服务端</li><li>B/S：浏览器/服务端</li></ul><h1 id="资源分类"><a href="#资源分类" class="headerlink" title="资源分类"></a>资源分类</h1><ul><li>静态资源：所有用户访问后，得到的结果都是一样的，可以直接被浏览器解析</li><li>动态资源：每个用户访问相同资源后得到的结果不一样，被访问后需要先转换为静态资源，后返回给浏览器</li></ul><h1 id="网络通信三要素"><a href="#网络通信三要素" class="headerlink" title="网络通信三要素"></a>网络通信三要素</h1><ul><li>IP：电子设备在网络中的唯一标识</li><li>端口：应用程序在计算机中的唯一标识。0-65535</li><li>传输协议：规定了数据传输的规则<blockquote><p>tcp：安全协议，三次握手<br>udp：不安全协议</p></blockquote></li></ul><h2 id="二、Web服务器软件：Tomcat"><a href="#二、Web服务器软件：Tomcat" class="headerlink" title="二、Web服务器软件：Tomcat"></a>二、Web服务器软件：Tomcat</h2><h1 id="几款软件"><a href="#几款软件" class="headerlink" title="几款软件"></a>几款软件</h1><ul><li>webLogic：oracle公司，大型JavaEE服务器</li><li>webSphere：IBM公司，大型JavaEE服务器</li><li>JBOSS：JBOSS公司，大型JavaEE服务器</li><li>Tomcat：Apache基金组织，中小型JavaEE服务器</li></ul><h1 id="java"><a href="#java" class="headerlink" title="java"></a>java</h1><ul><li>JavaEE：Java语言在企业级开发中使用的技术规范总和，共规定了13项大的规范。</li></ul><h1 id="Tomcat"><a href="#Tomcat" class="headerlink" title="Tomcat"></a>Tomcat</h1><ul><li>下载</li><li>安装</li><li>卸载</li><li>启动<br><code>sudo ./startup.sh</code><br><code>http://localhost:8080</code></li><li>关闭<br><code>sudo ./shutdown.sh</code></li><li>配置<br>部署项目的方式:<blockquote><p>将项目放到webapps目录下，war包会自动解压缩<br>配置conf/server.xml文件，在<Host>表情中配置：<Context docBase="项目存放的路径" path="/虚拟路径"/><br>在conf/Catalina/localhost文件夹下创建xml文件，编写:<Context docBase="项目存放的路径"/>，文件名称就是虚拟路径</p></blockquote></li></ul><h1 id="静态项目和动态项目"><a href="#静态项目和动态项目" class="headerlink" title="静态项目和动态项目"></a>静态项目和动态项目</h1><p>目录结构</p><h3 id="Java动态项目的目录结构"><a href="#Java动态项目的目录结构" class="headerlink" title="Java动态项目的目录结构"></a>Java动态项目的目录结构</h3><p>项目目录<br>- WEB-INF目录<br>   |- web.xml：web项目的核心配置文件<br>   |- classes目录：放置字节码文件的目录<br>   - lib目录：放置依赖的jar包</p><h2 id="三、Servlet"><a href="#三、Servlet" class="headerlink" title="三、Servlet"></a>三、Servlet</h2><h1 id="核心知识"><a href="#核心知识" class="headerlink" title="核心知识"></a>核心知识</h1><p>概念： server applet，运行在服务器端的小程序。servlet就是一个接口，定义了Java类被浏览器访问识别。</p><h1 id="代码实现"><a href="#代码实现" class="headerlink" title="代码实现"></a>代码实现</h1><h3 id="创建JavaEE项目"><a href="#创建JavaEE项目" class="headerlink" title="创建JavaEE项目"></a>创建JavaEE项目</h3><h3 id="实现类，实现Servlet接口"><a href="#实现类，实现Servlet接口" class="headerlink" title="实现类，实现Servlet接口"></a>实现类，实现Servlet接口</h3><h3 id="实现所有接口"><a href="#实现所有接口" class="headerlink" title="实现所有接口"></a>实现所有接口</h3><ul><li>init</li><li>ServletConfig</li><li>service</li><li>getServletInfo</li><li>destroy</li></ul><h3 id="配置Servler，需要在web-xml文件中"><a href="#配置Servler，需要在web-xml文件中" class="headerlink" title="配置Servler，需要在web.xml文件中"></a>配置Servler，需要在web.xml文件中</h3><p>在web.xml文件中添加<code>&lt;servlet&gt;</code>和<code>&lt;servlet-mapping&gt;</code>标签：即servlet的类路径(反射)和url的资源路径来进行匹配。<br><figure class="highlight xml"><table><tr><td class="code"><pre><code class="hljs xml"><span class="hljs-tag">&lt;<span class="hljs-name">servlet</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">servlet-name</span>&gt;</span>demo1<span class="hljs-tag">&lt;/<span class="hljs-name">servlet-name</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">servlet-class</span>&gt;</span>cn.yang.web.servlet.Demo1<span class="hljs-tag">&lt;/<span class="hljs-name">servlet-class</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">servlet</span>&gt;</span><br><br><span class="hljs-tag">&lt;<span class="hljs-name">servlet-mapping</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">servlet-name</span>&gt;</span>demo1<span class="hljs-tag">&lt;/<span class="hljs-name">servlet-name</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">url-pattern</span>&gt;</span>/demo1<span class="hljs-tag">&lt;/<span class="hljs-name">url-pattern</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">servlet-mapping</span>&gt;</span><br></code></pre></td></tr></table></figure></p><h3 id="执行原理"><a href="#执行原理" class="headerlink" title="执行原理"></a>执行原理</h3><ul><li>1.当服务器接收到客户端浏览器的请求后，会解析请求URL路径，来获取访问的Servlet的资源路径</li><li>2.查找web.xml文件，是否有对于的<code>&lt;url-pattern&gt;</code>标签内容</li><li>3.如果有，则会找到对应的<code>&lt;servlet-class&gt;</code>全类名</li><li>4.tomcat会将字节码文件加载进内存，并创建其对象</li><li>5.调用方法</li></ul><h3 id="Servlet的生命周期"><a href="#Servlet的生命周期" class="headerlink" title="Servlet的生命周期"></a>Servlet的生命周期</h3><ul><li>init：初始化，只会在调用服务时才会检测是否已经初始化，懒加载。如果需要创建服务自动初始化需要在<code>&lt;servlet&gt;</code>标签中添加<code>&lt;load-on-startup&gt;</code>设置大于等于0即可。</li><li>ServletConfig：设置config，config内容可以在<code>init</code>函数中获取到，获取的内容需要在<code>web.xml</code>文件中的<code>&lt;Servlet&gt;</code>标签中添加<code>&lt;init-param&gt;</code>标签以Key-Value添加即可</li><li>service：每次调用服务就会运行</li><li>getServletInfo：获取基本信息</li><li>destroy：销毁。只有服务器正常关闭才会运行。</li></ul><h5 id="Servlet3-0"><a href="#Servlet3-0" class="headerlink" title="Servlet3.0"></a>Servlet3.0</h5><p>支持注解配置，不需要web.xml文件。<br>直接使用<code>@WebServlet(&quot;路径&quot;)</code>即可</p><h3 id="Servlet的体系"><a href="#Servlet的体系" class="headerlink" title="Servlet的体系"></a>Servlet的体系</h3><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">Servlet-接口<br>GenericServlet-抽象类<br>HttpServlet-抽象类<br></code></pre></td></tr></table></figure><ul><li>GenericServlet：将Servlet接口中其他方法做了默认空实现，只将servlet()方法作为抽象。</li><li>HttpServlet：对http协议的一种封装，简化操作。</li></ul><h3 id="Servlet相关配置"><a href="#Servlet相关配置" class="headerlink" title="Servlet相关配置"></a>Servlet相关配置</h3><h5 id="1-urlpartten-Servlet访问路径"><a href="#1-urlpartten-Servlet访问路径" class="headerlink" title="1.urlpartten:Servlet访问路径"></a>1.urlpartten:Servlet访问路径</h5><p>一个Servlet可以定义多个访问路径<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">&#123;<span class="hljs-string">&quot;/路径1&quot;</span>,<span class="hljs-string">&quot;/路径2&quot;</span>,<span class="hljs-string">&quot;路径3&quot;</span>&#125;<br>/路径<span class="hljs-number">1</span><br>/路径<span class="hljs-number">1</span>/路径<span class="hljs-number">1.1</span><br>*.后缀名<br></code></pre></td></tr></table></figure></p><h2 id="四、Http"><a href="#四、Http" class="headerlink" title="四、Http"></a>四、Http</h2><h1 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h1><p>Hyper Text Transfer Protocol 超文本传输协议</p><pre><code>* 传输协议：定义了客户端与服务端通信的数据格式* 特点    * 基于TCP/IP协议    * 默认端口号80    * 基于请求/相应模型，一次请求响应一次响应    * 无状态：每次请求之间相互独立* 历史版本    * 1.0：每次响应都会建立新的连接    * 1.1：复用响应</code></pre><h1 id="请求消息数据格式"><a href="#请求消息数据格式" class="headerlink" title="请求消息数据格式"></a>请求消息数据格式</h1><h3 id="请求行"><a href="#请求行" class="headerlink" title="请求行"></a>请求行</h3><pre><code>请求方式 请求url 请求协议/版本Get  /login.html  HTTP/1.1</code></pre><ul><li>请求方式(7中)<ul><li>GET<br>  请求参数在url中<br>  url长度是有限制的<br>  不太安全</li><li>POST<br>  请求参数在请求体中<br>  相对安全</li></ul></li></ul><h3 id="请求头"><a href="#请求头" class="headerlink" title="请求头"></a>请求头</h3><pre><code>请求头名称: 请求头值(多个则用,分割)</code></pre><h5 id="常见请求头"><a href="#常见请求头" class="headerlink" title="常见请求头"></a>常见请求头</h5><ul><li>User-Agent：浏览器告诉服务器，使用的浏览器版本信息，这样服务器会根据这些信息完成兼容性</li><li>Accept：能解析的格式</li><li>Accept-Language：语言</li><li>Accept-Encoding：压缩方式</li><li>Referer：告诉服务器，当前请求从哪里来，用于防盗链和统计工作</li><li>Connection：是否保持连接</li><li>Upgrade-Insecure-Requests：升级信息</li></ul><h3 id="请求空行"><a href="#请求空行" class="headerlink" title="请求空行"></a>请求空行</h3><pre><code>空行用于分割请求头与请求体</code></pre><h3 id="请求体-请求正文"><a href="#请求体-请求正文" class="headerlink" title="请求体(请求正文)"></a>请求体(请求正文)</h3><h2 id="五、Request"><a href="#五、Request" class="headerlink" title="五、Request"></a>五、Request</h2><h1 id="request对象与response对象的原理"><a href="#request对象与response对象的原理" class="headerlink" title="request对象与response对象的原理"></a>request对象与response对象的原理</h1><ul><li>1.tomcat服务器会根据请求url中的资源路径，创建对应的Servlet对象</li><li>2.tomcat服务器，会创建request和response对象，request对象中封装请求消息数据</li><li>3.tomcat将request和response对象传递给service方法，并调用该方法</li><li>4.我们通过request对象获取我们想要的内容，然后通过response对象响应出去</li><li>5.服务器在给浏览器作出响应之前，会从response对象中拿出设置的响应数据</li></ul><p>最重要为下面两点</p><blockquote><p>request和response对象是由服务器创建的<br>request对象是来获取请求消息，response对象是来设置响应消息</p></blockquote><h1 id="request功能"><a href="#request功能" class="headerlink" title="request功能"></a>request功能</h1><h3 id="获取请求数据"><a href="#获取请求数据" class="headerlink" title="获取请求数据"></a>获取请求数据</h3><ul><li><p>获取请求行数据<br>  GET /day/demo?name=zhangsan HTTP/1.1</p><ul><li>获取请求方式：GET<br>  <code>String getMethod()</code></li><li>获取虚拟目录：/day<br>  <code>String getContextPath()</code></li><li>获取Servlet路径：/demo<br>  <code>String getServletPath()</code></li><li>获取get方式请求参数：name=zhangsan<br>  <code>String getQueryString()</code></li><li>获取请求uri：/day/demo<br>  <code>String getRequestURI()：/day/demo</code><br>  <code>String getRequestURL()：http://localhost/day/demo</code></li><li>获取协议及版本：HTTP/1.1<br>  <code>String getProtocol()</code></li><li>获取客户机IP地址<br>  <code>String getRemoteAddr()</code></li></ul></li><li><p>获取请求头数据</p><ul><li>通过请求头名称获取请求头值<br><code>String getHeader(String name)</code></li><li>获取所有的请求头名称<br>  <code>Enumeration&lt;String&gt; getHeaderNames()</code></li></ul></li><li><p>获取请求体数据</p><ul><li>1.获取流对象<br>  <code>BufferedReader getReader()：获取字符输入流，只能操作字符数据</code><br>  <code>ServletInputStream getInputStream()：获取字节输入流，可以操作所有类型数据</code></li><li>2.再从流对象中拿数据</li></ul></li></ul><h3 id="其他功能"><a href="#其他功能" class="headerlink" title="其他功能"></a>其他功能</h3><ul><li><p>1.获取请求参数通用方式</p><ul><li>根据参数名称获取参数值<br>  <code>String getParameter(String name)</code></li><li>根据请求参数名称获取参数的数组<br>  <code>String[] getParameterValues(String name)</code></li><li>获取所有请求参数的参数名称<br>  <code>Enumeration&lt;String&gt; getParameterNames()</code></li><li>获取所有参数的map集合<br>  <code>Map&lt;String, String[]&gt; getParameterMap()</code></li><li>中文乱码问题<br>  tomcat8以上get请求完成中文乱码，而post请求依旧乱码<br>  <code>request.setCharacterEncoding(&quot;utf-8&quot;);</code></li></ul></li><li><p>2.请求转发：一种在服务器内部的资源跳转方式</p><ul><li>通过request对象获取请求转发器对象<br>  <code>RequestDispatcher getRequestDispatcher(String path)</code></li><li>使用RequestDispatcher对象来进行转发<br>  <code>forward(ServletRequest request, ServletResponse reponse)</code></li><li>特点<br>  1.浏览器地址路径不发生编号<br>  2.只能转发到当前服务器内部资源<br>  3.转发是一次请求</li></ul></li><li><p>3.共享数据</p><ul><li>域对象：一个有作用范围的对象，可以在范围内共享数据</li><li>request域：代表一次请求的范围，一般用于请求转发的多个资源中共享数据</li><li>方法<br>  <code>setAttribute(String name, Object obj) 存储数据</code><br>  <code>Object getAttribute(String name) 获取值</code><br>  <code>removeAttribute(String name) 移除值</code></li></ul></li><li><p>4.获取ServletContext<br><code>ServletContext getServletContext()</code></p></li></ul><h2 id="六、Response"><a href="#六、Response" class="headerlink" title="六、Response"></a>六、Response</h2><h1 id="HTTP协议的响应消息"><a href="#HTTP协议的响应消息" class="headerlink" title="HTTP协议的响应消息"></a>HTTP协议的响应消息</h1><h3 id="响应消息"><a href="#响应消息" class="headerlink" title="响应消息"></a>响应消息</h3><ul><li><p>数据格式</p><ul><li>响应行<br>  HTTP/1.1 200 OK<br>  1.组成：协议/版本 响应状态码 状态码描述<br>  2.响应状态码：服务器告诉客户端浏览器本次请求和响应的一个状态。分为五类：服务器接收客户端消息，但没有接收完成，等待一段时间后发送1XX、成功2XX、重定向3XX(30重定向，304访问资源)、客户端错误4XX(404请求路径没有对应资源，405请求方式没有对应的功能函数doXXX)、服务器端错误5XX。</li><li>响应头<br>  头名称: 头值<br>  Content-Type：服务器告诉客户端本次响应体数据格式以及编码格式<br>  Content-disposition：服务器告诉客户端以什么格式打开响应数据<pre><code>  &gt; in-line：默认值，在当前页面内打开  &gt; attachment;filenamt=xxx：以附件形式打开响应体。文件下载</code></pre></li><li>响应空行</li><li>响应体：传输的数据</li></ul></li><li><p>响应字符串格式</p></li></ul><h1 id="Response对象"><a href="#Response对象" class="headerlink" title="Response对象"></a>Response对象</h1><p>功能：</p><ul><li>1.设置响应行<ul><li><ol><li>格式 HTTP/1.1 200 ok</li></ol></li><li>2.设置状态码 setStatus(int sc)</li></ul></li><li>2.设置响应头:setHeader(String name, String value)</li><li>3.设置响应体<ul><li>1.获取输出流<br>  字符输出流：PrintWriter getWriter()<br>  字节输出流：ServletOutputStream()</li><li>2.使用输出流，将数据输出到客户端浏览器上</li></ul></li></ul><h3 id="重定向"><a href="#重定向" class="headerlink" title="重定向"></a>重定向</h3><ul><li><p>重定向与转发的区别</p><blockquote><p>1.重定向地址栏发生变化<br>2.重定向可以访问其他站点资源<br>3.重定向是两次请求</p></blockquote></li><li><p>路径写法</p><blockquote><p>1.相对路径</p><blockquote><p>找到当前资源与目标资源关系，./文件夹路径<br>2.绝对路径<br>以/开头，去除URL三个基本后剩下的部分，需要确定是客户端发出还是服务端发出：客户端需要加虚拟目录，服务器端不需要加虚拟目录。</p></blockquote></blockquote></li></ul><h3 id="服务器输出字符数据"><a href="#服务器输出字符数据" class="headerlink" title="服务器输出字符数据"></a>服务器输出字符数据</h3><p>首先需要注意乱码问题：<br><code>response.setContentType(&quot;text/html;charset=utf-8&quot;);</code><br>通过<code>response.getWriter()</code>获取流。</p><h3 id="服务器输出字节数据"><a href="#服务器输出字节数据" class="headerlink" title="服务器输出字节数据"></a>服务器输出字节数据</h3><p>通过<code>response.getOutputStream()</code>获取流。</p><h3 id="验证码"><a href="#验证码" class="headerlink" title="验证码"></a>验证码</h3><h1 id="ServletContext对象"><a href="#ServletContext对象" class="headerlink" title="ServletContext对象"></a>ServletContext对象</h1><p>概念：代表整个Web应用，可以和程序的容易来通信。<br>功能：</p><ul><li>1.获取MIME类型<ul><li>MIME类型：在互联网通信过程中的一种文件类型标准。大类型/小类型。</li><li>String getMIMEType(String type)</li></ul></li></ul><ul><li>2.域对象：共享数据</li></ul><ul><li>3.获取文件的真实路径</li></ul>]]></content>
    
    
    <summary type="html">JavaWeb学习</summary>
    
    
    
    <category term="JavaWeb" scheme="http://example.com/categories/JavaWeb/"/>
    
    
    <category term="java" scheme="http://example.com/tags/java/"/>
    
  </entry>
  
  <entry>
    <title>JDBC</title>
    <link href="http://example.com/2020/05/26/JDBC/"/>
    <id>http://example.com/2020/05/26/JDBC/</id>
    <published>2020-05-26T08:23:01.000Z</published>
    <updated>2021-04-17T07:10:38.651Z</updated>
    
    <content type="html"><![CDATA[<h2 id="一、JDBC"><a href="#一、JDBC" class="headerlink" title="一、JDBC"></a>一、JDBC</h2><h1 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h1><p>概念：Java DataBase Connectivity。Java语言操作数据库。<br>本质就是官方定义的一套操作所有关系型数据库的规则(接口)。各个数据库厂商去实现这套接口，提供数据库驱动jar包。就可以使用这套接口进行编码。即不需要修改代码，只需要修改jar包，来实现更换数据库的功能。</p><h1 id="步骤"><a href="#步骤" class="headerlink" title="步骤"></a>步骤</h1><ul><li>导入驱动jar包</li><li>注册驱动</li><li>获取数据库连接对象 Connection</li><li>定义sql</li><li>获取执行sql语句对象 Statement</li><li>执行sql，接受返回结果</li><li>处理结果</li><li>释放资源</li></ul><h1 id="步骤分解"><a href="#步骤分解" class="headerlink" title="步骤分解"></a>步骤分解</h1><h3 id="DriverManager：驱动管理对象"><a href="#DriverManager：驱动管理对象" class="headerlink" title="DriverManager：驱动管理对象"></a>DriverManager：驱动管理对象</h3><ul><li>功能<ul><li>注册驱动：告诉程序该使用哪一个数据库驱动jar包<pre><code>  * static void registerDriver(Driver driver)：注册与给定的驱动程序 DriverManager  * 写代码用：Class.forName(&quot;com.mysql.jdbc.Driver&quot;);  * 在com.mysql.jdbc.Driver中通过静态代码块中调用注册功能  * mysql5之后的驱动jar包可以省略注册驱动的步骤</code></pre></li><li>获取数据库连接<ul><li>static Connection getConnection(String url, String user, String password);</li><li>参数：<ul><li>url：jdbc:mysql://IP地址(域名):端口号/数据库名称</li><li>user：用户名</li><li>password：密码</li></ul></li></ul></li></ul></li></ul><h3 id="Connection：数据库连接对象"><a href="#Connection：数据库连接对象" class="headerlink" title="Connection：数据库连接对象"></a>Connection：数据库连接对象</h3><ul><li>功能<ul><li>获取执行sql的对象<ul><li>Statement createStatement()</li><li>PreparedStatement prepareStatement(String sql)</li></ul></li><li>管理事务<ul><li>开启事务<ul><li>setAotuCommit(boolean autoCommit) false时即开启事务</li></ul></li><li>提交事务<ul><li>commit()</li></ul></li><li>回滚事务<ul><li>rollback()</li></ul></li></ul></li></ul></li></ul><h3 id="Statement：执行sql的对象"><a href="#Statement：执行sql的对象" class="headerlink" title="Statement：执行sql的对象"></a>Statement：执行sql的对象</h3><ul><li>功能<ul><li>用于执行静态sql语句并返回生成的结果的对象</li><li>boolean execute(String sql):执行任何sql</li><li>int executeUpdate(String sql):执行DML语句、DDL语句，返回值为影响的行数</li><li>ResultSet executeQuery(String sql):执行DQL语句</li></ul></li></ul><h3 id="ResultSet：结果集对象"><a href="#ResultSet：结果集对象" class="headerlink" title="ResultSet：结果集对象"></a>ResultSet：结果集对象</h3><ul><li>功能 封装查询对象<ul><li>boolean next()：游标向下移动一行，如果返回false表示没有数据列，true则还有数据</li><li>getXxx(参数)：获取数据，Xxx为数据类型，参数为Int表示列的编号(从1开始)，参数为String为列的名称</li></ul></li></ul><h3 id="PreparedStatement：执行sql的对象-更强"><a href="#PreparedStatement：执行sql的对象-更强" class="headerlink" title="PreparedStatement：执行sql的对象(更强)"></a>PreparedStatement：执行sql的对象(更强)</h3><ul><li>sql注入问题：在拼接sql时，有一些sql的特殊关键字参与字符串的拼接，造成安全问题。</li><li>因此使用PreparedStatement来解决</li><li>预编译的SQL：参数使用?来作为占位符</li><li>步骤<ul><li>导入驱动jar包</li><li>注册驱动</li><li>获取数据库连接对象 Connection</li><li>定义sql<ul><li>注意参数的使用，使用?作为占位符select * from user where username=? and password=?;</li></ul></li><li>获取执行sql语句对象 PreparedStatement：Connection.prepareStatement(String sql)</li><li>赋值<ul><li>方法：setXxx(参数1, 参数2)参数1为?的位置，2的?值</li></ul></li><li>执行，接受返回结果</li><li>处理结果</li><li>释放资源</li></ul></li></ul><h2 id="二、连接池"><a href="#二、连接池" class="headerlink" title="二、连接池"></a>二、连接池</h2><h1 id="数据库连接池"><a href="#数据库连接池" class="headerlink" title="数据库连接池"></a>数据库连接池</h1><h3 id="概念-1"><a href="#概念-1" class="headerlink" title="概念"></a>概念</h3><p>其实是一个容器(集合)，存放数据库连接的容器。当系统初始化后，容器就被创建，容器中会申请一些连接对象，当用户来访问数据库时，从容器中获取连接对象，用户访问完后，会将连接对象归还给容器。</p><h3 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h3><ul><li>1.标准接口：DataSourcew javax.sql包下<ul><li>获取连接：getConnection()</li><li>归还连接：Connection.close()方法不会关闭连接而是归还连接</li></ul></li><li>2.真实使用<ul><li>1.C3P0：数据库连接池技术</li><li>2.Druid：数据库连接池技术，由阿里巴巴提供的</li></ul></li></ul><h3 id="C3P0"><a href="#C3P0" class="headerlink" title="C3P0"></a>C3P0</h3><ul><li>步骤<ul><li>1.导入包</li><li>2.定义配置文件<ul><li>名称：c3p0.properties或者c3p0-config.xml</li><li>路径：直接将文件放在src路径</li></ul></li><li>创建核心对象 数据库连接池对象 ComboPooledDataSource</li><li>获取连接：getConnection</li></ul></li></ul><h3 id="Druid"><a href="#Druid" class="headerlink" title="Druid"></a>Druid</h3><ul><li>步骤<ul><li>1.导入包</li><li>2.定义配置文件<ul><li>名称：druid.properties</li><li>路径：直接将文件放在src路径</li></ul></li><li>加载配置信息</li><li>创建核心对象 通过工厂类来获取 DruidDataSourceFactory</li><li>获取连接：getConnection</li></ul></li></ul><h1 id="spring-JDBC"><a href="#spring-JDBC" class="headerlink" title="spring JDBC"></a>spring JDBC</h1><p>由spring框架提供的JDBC的简单封装</p><h3 id="步骤-1"><a href="#步骤-1" class="headerlink" title="步骤"></a>步骤</h3><ul><li>1.导入jar</li><li>2.创建对象 JDBCTemplate对象。依赖DataSource<ul><li>JDBCTemplate j = new JDBCTemplate(dataSource)</li></ul></li><li>通过JDBCTemplate完成crud操作<ul><li>update()：DML语句</li><li>quertForMap()：将结果封装到map</li><li>quertForList()：将结果封装到List</li><li>quert()：将结果封装到JavaBean对象</li><li>quertForObject()：查询结果，将结果封装为对象</li></ul></li></ul>]]></content>
    
    
    <summary type="html">JDBC学习</summary>
    
    
    
    <category term="数据库" scheme="http://example.com/categories/%E6%95%B0%E6%8D%AE%E5%BA%93/"/>
    
    
    <category term="数据库" scheme="http://example.com/tags/%E6%95%B0%E6%8D%AE%E5%BA%93/"/>
    
    <category term="java" scheme="http://example.com/tags/java/"/>
    
  </entry>
  
  <entry>
    <title>了解跨域</title>
    <link href="http://example.com/2020/03/29/%E4%BA%86%E8%A7%A3%E8%B7%A8%E5%9F%9F/"/>
    <id>http://example.com/2020/03/29/%E4%BA%86%E8%A7%A3%E8%B7%A8%E5%9F%9F/</id>
    <published>2020-03-29T13:31:15.000Z</published>
    <updated>2021-04-17T07:10:38.655Z</updated>
    
    <content type="html"><![CDATA[<h2 id="什么是跨域"><a href="#什么是跨域" class="headerlink" title="什么是跨域"></a>什么是跨域</h2><p>跨域，是指浏览器不能执行其他网站的脚本。它是由浏览器的同源策略造成的，凡是发送请求url的协议、域名、端口三者之间任意一个与当前页面地址不同即为跨域。<br>无法跨域是浏览器对于用户安全的考虑。</p><p>同源策略限制了一下行为：</p><blockquote><p>Cookie、LocalStorage 和 IndexDB 无法读取<br>DOM 和 JS 对象无法获取<br>Ajax请求发送不出去</p></blockquote><h2 id="跨域请求资源的方法"><a href="#跨域请求资源的方法" class="headerlink" title="跨域请求资源的方法"></a>跨域请求资源的方法</h2><ul><li>JSONP跨域<br>JavaScript设计模式中的一种代理模式。利用script标签可以跨域的原理实现。</li><li>nginx反向代理<br>利用nginx反向代理把跨域为不跨域，支持各种请求方式</li><li>跨域资源共享CORS<br>目前主流的解决方案。W3C标准，全称是”跨域资源共享”（Cross-origin resource sharing）。允许浏览器向跨源服务器，发出XMLHttpRequest请求。</li></ul><h2 id="CORS"><a href="#CORS" class="headerlink" title="CORS"></a>CORS</h2><p>CORS需要浏览器和服务器同时支持。目前，所有浏览器都支持该功能，IE浏览器不能低于IE10。</p><p>一种是简单请求，另一种是非简单请求。浏览器对这两种请求方式的处理方式是不同的。</p><h1 id="简单请求"><a href="#简单请求" class="headerlink" title="简单请求"></a>简单请求</h1><p>要求如下：</p><ul><li><p>1.请求方式为<strong>HEAD</strong>、<strong>POST</strong> 或者 <strong>GET</strong></p></li><li><p>2.HTTP的头信息不超出以下几种字段<br><strong> Accept</strong> Accept-Language<br><strong> Content-Language</strong> Last-Event-ID<br><strong> Content-Type：只限于三个值</strong>application/x-www-form-urlencoded<strong>、</strong>multipart/form-data<strong>、</strong>text/plain**</p></li></ul><p>浏览器发送CORS请求时，在头信息之中，增加一个Origin字段。<br>Origin字段用来说明，本次请求来自哪个源（协议 + 域名 + 端口）。服务器根据这个值，决定是否同意这次请求。<br>如果服务器允许跨域，需要在返回的响应头中携带下面信息：<br><figure class="highlight yaml"><table><tr><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">Access-Control-Allow-Origin:</span> <span class="hljs-string">http://xxx.xxx.xxx</span><br><span class="hljs-attr">Access-Control-Allow-Credentials:</span> <span class="hljs-literal">true</span><br><span class="hljs-attr">Access-Control-Expose-Headers:</span> <span class="hljs-string">...</span><br><span class="hljs-attr">withCredentials:</span> <br></code></pre></td></tr></table></figure></p><ul><li><strong>Access-Control-Allow-Origin</strong>（必须）：可接受的域，是一个具体域名或者*，代表任意</li><li>Access-Control-Allow-Credentials：是否允许携带cookie，默认情况下，cors不会携带cookie，除非这个值是true</li><li>Access-Control-Expose-Headers：CORS请求时，XMLHttpRequest对象的getResponseHeader()方法只能拿到6个基本字段：Cache-Control、Content-Language、Content-Type、Expires、Last-Modified、Pragma。如果想拿到其他字段，就必须在Access-Control-Expose-Headers里面指定。<br>。</li><li>withCredentials：如果要把Cookie发到服务器，不仅要设置<strong>Access-Control-Allow-Credentials</strong>，还需要设置该属性。</li></ul><p>如果跨域请求要想操作cookie，需要满足3个条件：</p><blockquote><p>1.服务的响应头中需要携带Access-Control-Allow-Credentials并且为true。<br>2.浏览器发起ajax需要指定withCredentials 为true<br>3.响应头中的Access-Control-Allow-Origin一定不能为*，必须是指定的域名</p></blockquote><h1 id="非简单请求"><a href="#非简单请求" class="headerlink" title="非简单请求"></a>非简单请求</h1><p>非简单请求是那种对服务器有特殊要求的请求，比如请求方法是PUT或DELETE，或者Content-Type字段的类型是application/json。</p><p>非简单请求的CORS请求，会在正式通信之前，增加一次HTTP查询请求，称为”预检”请求（preflight）——浏览器先询问服务器，当前网页所在的域名是否在服务器的许可名单之中，以及可以使用哪些HTTP动词和头信息字段。只有得到肯定答复，浏览器才会发出正式的XMLHttpRequest请求，否则就报错。</p><h3 id="预检请求的HTTP头信息"><a href="#预检请求的HTTP头信息" class="headerlink" title="预检请求的HTTP头信息"></a>预检请求的HTTP头信息</h3><ul><li>OPTIONS /cors HTTP/1.1</li><li>Origin: <a href="http://api.bob.com">http://api.bob.com</a></li><li>Access-Control-Request-Method: PUT</li><li>Access-Control-Request-Headers: X-Custom-Header</li><li>Host: api.alice.com</li><li>Accept-Language: en-US</li><li>Connection: keep-alive</li><li>User-Agent: Mozilla/5.0…<br>与简单模式相比多了<strong>Origin</strong>、<strong>Access-Control-Request-Method</strong>和<strong>Access-Control-Request-Headers</strong>。<blockquote><p>Access-Control-Request-Method：接下来会用到的请求方式，比如PUT<br>Access-Control-Request-Headers：会额外用到的头信息，用逗号隔开</p></blockquote></li></ul><h3 id="确认允许跨源请求，就可以做出回应。"><a href="#确认允许跨源请求，就可以做出回应。" class="headerlink" title="确认允许跨源请求，就可以做出回应。"></a>确认允许跨源请求，就可以做出回应。</h3><ul><li>HTTP/1.1 200 OK</li><li>Date: Mon, 01 Dec 2008 01:15:39 GMT</li><li>Server: Apache/2.0.61 (Unix)</li><li>Access-Control-Allow-Origin: <a href="http://manage.leyou.com">http://manage.leyou.com</a></li><li>Access-Control-Allow-Credentials: true</li><li>Access-Control-Allow-Methods: GET, POST, PUT</li><li>Access-Control-Allow-Headers: X-Custom-Header</li><li>Access-Control-Max-Age: 1728000</li><li>Content-Type: text/html; charset=utf-8</li><li>Content-Encoding: gzip</li><li>Content-Length: 0</li><li>Keep-Alive: timeout=2, max=100</li><li>Connection: Keep-Alive</li><li>Content-Type: text/plain<br>这里又多了3个头<blockquote><p>Access-Control-Allow-Methods：允许访问的方式<br>Access-Control-Allow-Headers：允许携带的头<br>Access-Control-Max-Age：本次许可的有效时长，单位是秒，过期之前的ajax请求就无需再次进行预检了</p></blockquote></li></ul><p>如果浏览器得到上述响应，则认定为可以跨域。</p>]]></content>
    
    
    <summary type="html">了解跨域</summary>
    
    
    
    <category term="跨域" scheme="http://example.com/categories/%E8%B7%A8%E5%9F%9F/"/>
    
    
    <category term="后台" scheme="http://example.com/tags/%E5%90%8E%E5%8F%B0/"/>
    
  </entry>
  
  <entry>
    <title>JavaScript语法（一）</title>
    <link href="http://example.com/2020/03/08/JavaScript%E8%AF%AD%E6%B3%95%EF%BC%88%E4%B8%80%EF%BC%89/"/>
    <id>http://example.com/2020/03/08/JavaScript%E8%AF%AD%E6%B3%95%EF%BC%88%E4%B8%80%EF%BC%89/</id>
    <published>2020-03-08T02:29:30.000Z</published>
    <updated>2021-04-17T07:10:38.651Z</updated>
    
    <content type="html"><![CDATA[<h2 id="一、简介"><a href="#一、简介" class="headerlink" title="一、简介"></a>一、简介</h2><h2 id="JavaScript实现"><a href="#JavaScript实现" class="headerlink" title="JavaScript实现"></a>JavaScript实现</h2><p>由三个部分组成：</p><blockquote><p>核心（ECMAScript）<br>文档对象模型（DOM）<br>浏览器对象模型（BOM）</p></blockquote><h2 id="二、基本概念"><a href="#二、基本概念" class="headerlink" title="二、基本概念"></a>二、基本概念</h2><h2 id="变量：ECMAScript的变量是松散类型的，所谓松散类型就是可以用来保存任何类型的数据。"><a href="#变量：ECMAScript的变量是松散类型的，所谓松散类型就是可以用来保存任何类型的数据。" class="headerlink" title="变量：ECMAScript的变量是松散类型的，所谓松散类型就是可以用来保存任何类型的数据。"></a>变量：ECMAScript的变量是松散类型的，所谓松散类型就是可以用来保存任何类型的数据。</h2><p>在函数中不实用var来定义变量时，该变量会自定提升为全局变量<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-function">function <span class="hljs-title">test</span><span class="hljs-params">()</span> </span>&#123;<br>a = <span class="hljs-number">1</span>;<br>&#125;<br>test();<br>console.log(a); <span class="hljs-comment">// 1</span><br></code></pre></td></tr></table></figure><br>但是并不推荐该写法。</p><h2 id="数据类型："><a href="#数据类型：" class="headerlink" title="数据类型："></a>数据类型：</h2><blockquote><p>Undefined<br>Null<br>Boolean<br>Number<br>String<br>Object</p></blockquote><h2 id="typeof操作符"><a href="#typeof操作符" class="headerlink" title="typeof操作符"></a>typeof操作符</h2><p>用于检测给定变量的数据类型。</p><blockquote><p>“undefined”：如果这个值未定义<br>“boolean”：如果这个值是布尔值<br>“string”：如果这个值是字符串<br>“number”：如果这个值是数值<br>“object”：如果这个值是对象或null&gt; “function”：如果这个值是函数</p></blockquote><p>从技术角度讲，函数在ECMAScript中是对象。但是由于函数的特殊性，因此在不同浏览器中对函数的返回也不一样。</p><h2 id="由于undefined值是派生自null值，因此规定对他们的相等判断返回true。"><a href="#由于undefined值是派生自null值，因此规定对他们的相等判断返回true。" class="headerlink" title="由于undefined值是派生自null值，因此规定对他们的相等判断返回true。"></a>由于undefined值是派生自null值，因此规定对他们的相等判断返回true。</h2><h2 id="正负无穷大"><a href="#正负无穷大" class="headerlink" title="正负无穷大"></a>正负无穷大</h2><blockquote><p>+Infinity<br>-Infinity<br>isFinite() 用于检测</p></blockquote><h2 id="NaN"><a href="#NaN" class="headerlink" title="NaN"></a>NaN</h2><p>这是一个特殊的数值，用于返回无法返回的特殊情况的替代数据。（如任何数除以非数值会返回NaN，而不会报错）</p><blockquote><p>设计NaN的计算都会返回NaN。<br>NaN与任何值都不相等，包括自己。<br>isNaN()函数完成测试工作。该函数的参数如果是对象，则返回的数值则是对象的valueOf、toString。</p></blockquote><h2 id="字符串"><a href="#字符串" class="headerlink" title="字符串"></a>字符串</h2><p>String类型是由0个或多个16位Unicode字符组成。</p><h2 id="Object类型"><a href="#Object类型" class="headerlink" title="Object类型"></a>Object类型</h2><p>对象中存在的基础属性和方法</p><blockquote><p>constructor：构造函数<br>hasOwnProperty(propertyName)：用于检查给定的属性在当前对象实例中（而不是实例的原型中）是否存在。<br>isPrototypeOf(object)：用于检查传入的对象是否是当前对象的原型。<br>propertyIsEnumerable(propertyName)：用于检查给定的属性是否能够使用for-in语法来进行枚举。<br>toLocaleString：返回对象的字符串表示。与执行环境的地区对应。<br>toString：返回对象的字符串表示。<br>valueOf：返回对象的字符串、数值或布尔值表示。通常与toString返回相同。</p></blockquote><h2 id="二进制补码"><a href="#二进制补码" class="headerlink" title="二进制补码"></a>二进制补码</h2><p>用于表示二进制的负数形式。<br>步骤：</p><blockquote><p>1.求这个数值绝对值的二进制码<br>2.求二进制反码。<br>3.得到的二进制反码+1</p></blockquote><h2 id="逻辑与-逻辑或"><a href="#逻辑与-逻辑或" class="headerlink" title="逻辑与 逻辑或"></a>逻辑与 逻辑或</h2><p>在进行非布尔值计算时，返回的值不一定是布尔值。<br>作为短路逻辑与和短路逻辑或，在比较值不是布尔值时：</p><blockquote><p>逻辑与会确定第一个值是true的前提下，返回第二个操作数<br>逻辑或会确定第一个值是对象则返回第一个值，如果是false，则返回第二个值</p></blockquote><h2 id="with"><a href="#with" class="headerlink" title="with"></a>with</h2><p>将代码的作用域设置到一个特定的对象中。目的是为了简化多次编写同一个对象的工作。<br>下面代码描述了with的大致效果：<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">var</span> qs = location.search.substring(<span class="hljs-number">1</span>); <br><span class="hljs-keyword">var</span> hostName = location.hostname;<br><span class="hljs-keyword">var</span> url = location.href;<br><br>with(location)&#123;<br><span class="hljs-keyword">var</span> qs = search.substring(<span class="hljs-number">1</span>); <br><span class="hljs-keyword">var</span> hostName = hostname;<br><span class="hljs-keyword">var</span> url = href;<br>&#125;<br></code></pre></td></tr></table></figure><br><strong><em>严格模式下不允许使用with语句</em></strong></p><h2 id="函数参数"><a href="#函数参数" class="headerlink" title="函数参数"></a>函数参数</h2><p>ECMAScript 中的参数在内部是用一个数组来表示的。函数接收到的始终都是这个数组，而不关心数组中包含哪些参数(如果有参数的话)。，在函数体内可以通过 arguments 对象来 访问这个参数数组，从而获取传递给函数的每一个参数。<br><strong><em>这个arguments并不是数组，而是与数组类似(因为他可以使用方括号语法访问它的每一个元素，使用 length 属性来确定传递进来多少个参数。)</em></strong><br>ECMAScript 中的所有参数传递的都是值，不可能通过引用传递参数。</p><p>由于JS的函数使用类似数组来接受参数，而不会单独给参数指定参数签名，因此无法使用重载技术来设立相同名称不同参数类型和个数的函数。</p><h2 id="三、变量、作用域和内存"><a href="#三、变量、作用域和内存" class="headerlink" title="三、变量、作用域和内存"></a>三、变量、作用域和内存</h2><h2 id="instanceof操作符"><a href="#instanceof操作符" class="headerlink" title="instanceof操作符"></a>instanceof操作符</h2><p>typeof在比对基本数据类型时非常有用，但是在比对引用类型时需要instanceof操作符。</p><h2 id="执行环境"><a href="#执行环境" class="headerlink" title="执行环境"></a>执行环境</h2><blockquote><p>在 Web 浏览器中，全局执行环境被认为是 window 对象<br>在移动端中，全局执行环境被认为是 global 对象</p></blockquote><p>使用try-catch和with语句可以完成延长作用域链功能。</p><h2 id="JavaScript没有块级作用域"><a href="#JavaScript没有块级作用域" class="headerlink" title="JavaScript没有块级作用域"></a>JavaScript没有块级作用域</h2><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (a == <span class="hljs-number">1</span>) &#123;<br><span class="hljs-keyword">var</span> color = <span class="hljs-string">&#x27;0x0f0&#x27;</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>注意，这里并不会像C一样创建一个作用域。在 JavaScript 中，if 语句中的变量声明会将变量添加到当前的执行环境(在这里是 全局环境)中。这里尤其要注意for语句中。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">for</span> (<span class="hljs-keyword">var</span> i=<span class="hljs-number">0</span>; i &lt; <span class="hljs-number">10</span>; i++)&#123; doSomething(i);<br>&#125;<br>alert(i);      <span class="hljs-comment">//10</span><br></code></pre></td></tr></table></figure></p><h2 id="垃圾收集"><a href="#垃圾收集" class="headerlink" title="垃圾收集"></a>垃圾收集</h2><p>JavaScript 具有自动垃圾收集机制，执行环境会负责管理代码执行过程中使用的内存。</p><blockquote><p>标记清除：当变量进入环境(例如，在函数中声明一个变量)时，就将这个变量标记为“进入环境”。而当变量离开环境时，则将其 标记为“离开环境”。而在此之后再被加上标记 的变量将被视为准备删除的变量，原因是环境中的变量已经无法访问到这些变量了。<br>引用计数：引用计数的含义是跟踪记录每 个值被引用的次数。当这个值的引用次数变成 0 时，则说明没有办法再访问这 个值了，因而就可以将其占用的内存空间回收回来。但是由于循环引用导致的漏洞，后面不实用该方式清空垃圾。</p></blockquote><h2 id="四、引用类型"><a href="#四、引用类型" class="headerlink" title="四、引用类型"></a>四、引用类型</h2><p>在JavaScript 也可以使用方括号表示法来访问对象的属性。在使用方括号语法时，应该将要访问的属性 以字符串的形式放在方括号中，从功能上看，这两种访问对象属性的方法没有任何区别。但方括号语法的主要优点是可以通过变量 来访问属性。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">alert(person[<span class="hljs-string">&quot;name&quot;</span>]); <span class="hljs-comment">//&quot;Nicholas&quot;</span><br>alert(person.name); <span class="hljs-comment">//&quot;Nicholas&quot;</span><br><br><span class="hljs-keyword">var</span> propertyName = <span class="hljs-string">&quot;name&quot;</span>;<br>alert(person[propertyName]); <span class="hljs-comment">//&quot;Nicholas&quot;</span><br></code></pre></td></tr></table></figure></p><h2 id="数组Array"><a href="#数组Array" class="headerlink" title="数组Array"></a>数组Array</h2><p>依旧建议使用<code>Array</code>来创建数组，而不是用字面量方式创建：<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">var</span> arrays = Array();<br><span class="hljs-keyword">var</span> arrays = <span class="hljs-keyword">new</span> Array();<br><br><span class="hljs-keyword">var</span> arrays = [];<br></code></pre></td></tr></table></figure><br>由于部分浏览器不同原因，可能会导致空跳过的字面量数组数据无法控制。</p><h2 id="数组中length为可写参数，因此可以通过修改length来改变数组长度。"><a href="#数组中length为可写参数，因此可以通过修改length来改变数组长度。" class="headerlink" title="数组中length为可写参数，因此可以通过修改length来改变数组长度。"></a>数组中<code>length</code>为可写参数，因此可以通过修改<code>length</code>来改变数组长度。</h2><h2 id="数组最多可以包含-4-294-967-295-个项，这几乎已经能够满足任何编程需求了。"><a href="#数组最多可以包含-4-294-967-295-个项，这几乎已经能够满足任何编程需求了。" class="headerlink" title="数组最多可以包含 4 294 967 295 个项，这几乎已经能够满足任何编程需求了。"></a>数组最多可以包含 4 294 967 295 个项，这几乎已经能够满足任何编程需求了。</h2><h2 id="在判断数据是否是数组格式时，不使用instanceof操作符，因为这会牵扯多个不同的全局执行环境导致不同版本Array。为了解决这个问题Array-isArray-方法来解决。"><a href="#在判断数据是否是数组格式时，不使用instanceof操作符，因为这会牵扯多个不同的全局执行环境导致不同版本Array。为了解决这个问题Array-isArray-方法来解决。" class="headerlink" title="在判断数据是否是数组格式时，不使用instanceof操作符，因为这会牵扯多个不同的全局执行环境导致不同版本Array。为了解决这个问题Array.isArray()方法来解决。"></a>在判断数据是否是数组格式时，不使用<code>instanceof</code>操作符，因为这会牵扯多个不同的全局执行环境导致不同版本Array。为了解决这个问题<code>Array.isArray()</code>方法来解决。</h2><p><strong>支持该函数的浏览器： IE9+、Firefox 4+、Safari 5+、Opera 10.5+和 Chrome</strong>，要在尚未实现这个方法中的浏览器中准确检测数组。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">Object.prototype.toString.call(value) == <span class="hljs-string">&quot;[object Array]&quot;</span>;<br></code></pre></td></tr></table></figure></p><h2 id="数组在调用toString-、valueOf-和toLocaleString-来输出时，会调用数组内每一项的对应toString-、valueOf-和toLocaleString-，并使用逗号分割。"><a href="#数组在调用toString-、valueOf-和toLocaleString-来输出时，会调用数组内每一项的对应toString-、valueOf-和toLocaleString-，并使用逗号分割。" class="headerlink" title="数组在调用toString()、valueOf()和toLocaleString()来输出时，会调用数组内每一项的对应toString()、valueOf()和toLocaleString()，并使用逗号分割。"></a>数组在调用<code>toString()</code>、<code>valueOf()</code>和<code>toLocaleString()</code>来输出时，会调用数组内每一项的对应<code>toString()</code>、<code>valueOf()</code>和<code>toLocaleString()</code>，并使用逗号分割。</h2><p>而<code>join()</code>函数可以修改分割方式。</p><h2 id="栈模式"><a href="#栈模式" class="headerlink" title="栈模式"></a>栈模式</h2><p>数组也提供了一种让数组的行为类似于其他数据结构的方法。数组可以表 现得就像栈一样，LIFO(Last-In-First-Out， 后进先出)的数据结构。</p><blockquote><p> push()：将添加项添加至数组的末尾<br> pop()：弹出数组最后一项数据<br><img src="/img/JavaScript/0.png" alt="github"></p></blockquote><h2 id="队列模式"><a href="#队列模式" class="headerlink" title="队列模式"></a>队列模式</h2><p>队列数据结构的访问规则是 FIFO(First-In-First-Out， 先进先出)。队列在列表的末端添加项，从列表的前端移除项。</p><blockquote><p>push()：将添加像添加至数组的末尾<br>shift()：从数组前段获取一项数据<br><img src="/img/JavaScript/1.png" alt="github"></p></blockquote><h2 id="重排序方法"><a href="#重排序方法" class="headerlink" title="重排序方法"></a>重排序方法</h2><blockquote><p>sort：升序排列数组项<br>reverse：降序排列数组项</p></blockquote><p>排序函数可添加一个函数为参数，该函数用来完成排序方式。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-function">function <span class="hljs-title">compare</span><span class="hljs-params">(value1, value2)</span> </span>&#123;<br>    <span class="hljs-keyword">if</span> (value1 &lt; value2) &#123;<br><span class="hljs-keyword">return</span> -<span class="hljs-number">1</span>;<br>&#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (value1 &gt; value2) &#123;<br><span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;<br>&#125; <span class="hljs-keyword">else</span> &#123;<br><span class="hljs-keyword">return</span> <span class="hljs-number">0</span>; <br>&#125;<br>&#125;<br>values.sort(compare);<br></code></pre></td></tr></table></figure></p><h2 id="concat-函数会基于当前数组创建新的数组。"><a href="#concat-函数会基于当前数组创建新的数组。" class="headerlink" title="concat() 函数会基于当前数组创建新的数组。"></a>concat() 函数会基于当前数组创建新的数组。</h2><p>这个方法首先创建当前数组的一个副本，然后将接受到的参数添加至这个副本的末尾，返回一个新构建的数组(在没有参数时只会复制当前数组并返回副本)。</p><h2 id="slice"><a href="#slice" class="headerlink" title="slice()"></a>slice()</h2><p>能够基于当前数组中的一或多个项创建一个新数组。接受一或两个参数，即要返回项的起始和结束位置(左闭又开)。<br><strong><em>如果 slice()方法的参数中有一个负数，则用数组长度加上该数来确定相应的位置。</em></strong></p><h2 id="splice-函数的功能异常强大，切有多种用法。"><a href="#splice-函数的功能异常强大，切有多种用法。" class="headerlink" title="splice() 函数的功能异常强大，切有多种用法。"></a>splice() 函数的功能异常强大，切有多种用法。</h2><p>主要用途是向数组的中部插入项。有如下3中方式：</p><blockquote><p>删除：可以删除任意数量的项，只需指定 2 个参数(要删除的第一项的位置和要删除的项，左闭又开)。<br>插入：可以向指定位置插入任意数量的项，只需提供 3 个参数(起始位置、要删除的项数和要插入的项插入的项可以配置多个)。<br>替换：可以向指定位置插入任意数量的项，且同时删除任意数量的项，只需指定 3 个参数(起始位置、要删除的项数和要插入的任意数量的项)</p></blockquote><p>splice()方法始终都会返回一个数组，该数组中包含从原始数组中删除的项或空数组。</p><h2 id="位置方法"><a href="#位置方法" class="headerlink" title="位置方法"></a>位置方法</h2><blockquote><p>indexOf()：从头部搜索<br>lastIndexOf()：从尾部搜索</p></blockquote><p>两个方法都接收 两个参数:要查找的项和(可选的)表示查找起点位置的索引。</p><h2 id="迭代方法"><a href="#迭代方法" class="headerlink" title="迭代方法"></a>迭代方法</h2><p>为数组定义了 5 个迭代方法。每个方法都接收两个参数</p><blockquote><p>要在每一项上运行的函数<br>运行该函数的作用域对象(可选)，函数传递的三个参数</p><blockquote><p>数组项的值<br>该项在数组中的位置<br>数组对象本身</p></blockquote></blockquote><p>函数如下：</p><ul><li>event()：对数组中的每一项运行给定函数，如果该函数对每一项都返回 true，则返回 true。</li><li>some()：对数组中的每一项运行给定函数，如果该函数对任一项返回 true，则返回 true。</li><li>filter()：对数组中的每一项运行给定函数，返回该函数会返回 true 的项组成的数组。</li><li>forEach()：对数组中的每一项运行给定函数。这个方法没有返回值。</li><li>map()：对数组中的每一项运行给定函数，返回每次函数调用的结果组成的数组。</li></ul><p><code>event</code>与<code>some</code>类似逻辑与和逻辑或，他们都用于检测数组项是否满足条件后的结果判定。<br><code>filter</code>类似数组过滤器，返回新的数组。<code>map</code>则是将所有数组项进行自定义处理，并生成新数组。<br>最后的<code>forEach</code>如名称，类似for循环数组，没有返回项。</p><h2 id="归并方法"><a href="#归并方法" class="headerlink" title="归并方法"></a>归并方法</h2><p>这两个方法都会迭 12 代数组的所有项，然后构建一个最终返回的值。</p><blockquote><p>reduce()：从数组的第一项开始，逐个遍历到最后<br>reduceRight()：从数组的最后一项开始，向前遍历到第一项。</p></blockquote><p>这两个方法都接收两个参数:一个在每一项上调用的函数和(可选的)作为归并基础的初始值。这个函数接收 4 个参数:前一个值、当前值、项的索引和数组对象。这个函数返回的任何值都会作为第一个参数自动传给下一项。</p><h2 id="Date类型"><a href="#Date类型" class="headerlink" title="Date类型"></a>Date类型</h2><p>Date 类型使用自 UTC(Coordinated Universal Time，国际协调时间)1970 年 1 月 1 日午夜(零时)开始经过 的毫秒数来保存日期。<br>ECMAScript 提供了两个方法:<code>Date.parse()</code> 和 <code>Date.UTC()</code>。</p><blockquote><p>Date.parse()：如果传入方法的字符串不能表示日期，那么它会返回 NaN。<br>Date.UTC()：返回表示日期的毫秒数，但它与 Date.parse()在构建值时使用不同的信息。参数分别是年份、基于 0 的月份(一月是 0，二月是 1，以此类推)、月中的哪一天 (1 到 31)、小时数(0 到 23)、分钟、秒以及毫秒数。</p></blockquote><p>Date的构造函数会分别模仿上面的参数构造进行模仿。</p><blockquote><p>Date.now()：返回表示调用这个方法时的日期和时间的毫秒数。</p></blockquote><h4 id="其他函数"><a href="#其他函数" class="headerlink" title="其他函数"></a>其他函数</h4><ul><li>toLocaleString()：按照与浏览器设置的地区相适应的格式返回日期和时间。</li><li>toString()：返回带有时区信息的日期和 时间，其中时间一般以军用时间(即小时的范围是 0 到 23)表示</li><li>valueOf()：不返回字符串，而是返回日期的毫秒表示。</li></ul><h4 id="日期格式化方法"><a href="#日期格式化方法" class="headerlink" title="日期格式化方法"></a>日期格式化方法</h4><blockquote><p>toDateString()——以特定于实现的格式显示星期几、月、日和年;<br>toTimeString()——以特定于实现的格式显示时、分、秒和时区;<br>toLocaleDateString()——以特定于地区的格式显示星期几、月、日和年;<br>toLocaleTimeString()——以特定于实现的格式显示时、分、秒;<br>toUTCString()——以特定于实现的格式完整的 UTC 日期。</p></blockquote><h4 id="日期-时间组件方法"><a href="#日期-时间组件方法" class="headerlink" title="日期/时间组件方法"></a>日期/时间组件方法</h4><p>以上都是对整个日期处理的方法，下面则是对时间中的特定值处理的方法。<br><strong>UTC 日期指的是在没有时区偏差的情况下(将日期转换为 GMT 时间) 的日期值。</strong></p><blockquote><p>getTime()：返回表示日期的毫秒数;与valueOf()方法返回的值相同<br>setTime(毫秒)：以毫秒数设置日期，会改变整个日期<br>getFullYear()：取得4位数的年份(如2007而非仅07)<br>getUTCFullYear()：返回UTC日期的4位数年份<br>setFullYear(年)：设置日期的年份。传入的年份值必须是4位数字(如2007而非仅07)<br>setUTCFullYear(年)：设置UTC日期的年份。传入的年份值必须是4位数字(如2007而非仅07)<br>getMonth()：返回日期中的月份，其中0表示一月，11表示十二月<br>getUTCMonth()：返回UTC日期中的月份，其中0表示一月，11表示十二月<br>setMonth(月)：设置日期的月份。传入的月份值必须大于0，超过11则增加年份<br>setUTCMonth(月)：设置UTC日期的月份。传入的月份值必须大于0，超过11则增加年份<br>getDate()：返回日期月份中的天数(1到31)<br>getUTCDate()：返回UTC日期月份中的天数(1到31)<br>setDate(日)：设置日期月份中的天数。如果传入的值超过了该月中应有的天数，则增加月份<br>setUTCDate(日)：设置UTC日期月份中的天数。如果传入的值超过了该月中应有的天数，则增加月份<br>getDay()返回日期中星期的星期几(其中0表示星期日，6表示星期六)<br>getUTCDay()：返回UTC日期中星期的星期几(其中0表示星期日，6表示星期六)<br>getHours()：返回日期中的小时数(0到23)<br>getUTCHours()：返回UTC日期中的小时数(0到23)<br>setHours(时)：设置日期中的小时数。传入的值超过了23则增加月份中的天数<br>setUTCHours(时)：设置UTC日期中的小时数。传入的值超过了23则增加月份中的天数<br>getMinutes()：返回日期中的分钟数(0到59)<br>getUTCMinutes()：返回UTC日期中的分钟数(0到59)<br>setMinutes(分)：设置日期中的分钟数。传入的值超过59则增加小时数<br>setUTCMinutes(分)：设置UTC日期中的分钟数。传入的值超过59则增加小时数<br>getSeconds()：返回日期中的秒数(0到59)<br>getUTCSeconds()：返回UTC日期中的秒数(0到59)<br>setSeconds(秒)：设置日期中的秒数。传入的值超过了59会增加分钟数<br>setUTCSeconds(秒)：设置UTC日期中的秒数。传入的值超过了59会增加分钟数<br>getMilliseconds()：返回日期中的毫秒数<br>getUTCMilliseconds()：返回UTC日期中的毫秒数<br>setMilliseconds(毫秒)：设置日期中的毫秒数<br>setUTCMilliseconds(毫秒)：设置UTC日期中的毫秒数<br>getTimezoneOffset()：返回本地时间与UTC时间相差的分钟数。例如，美国东部标准时间返回300。在某地进入夏令时的情况下，这个值会有所变化</p></blockquote><h2 id="RegExp类型"><a href="#RegExp类型" class="headerlink" title="RegExp类型"></a>RegExp类型</h2><p>通过 RegExp 类型来支持正则表达式。使用Perl语法，可以创建一个正则表达式。<br><code>var expression = / pattern / flags ;</code></p><ul><li>pattern：可以是任何简单或复杂的正则表达式，可以包含字符类、限定符、分组、 向前查找以及反向引用。<br>模式中使用的所有元字符都必须转义。正则表达式中的元字符包括: 11 ( [ { \ ^ $ | ) ? * + .] } ] )</li></ul><ul><li>flags：可带有一或多个标志，正则表达式的匹配模式支持下列 3 个标志。v&gt; g:表示全局(global)模式，即模式将被应用于所有字符串，而非在发现第一个匹配项时立即停止;<blockquote><p>i:表示不区分大小写(case-insensitive)模式，即在确定匹配项时忽略模式与字符串的大小写;<br>m:表示多行(multiline)模式，即在到达一行文本末尾时还会继续查找下一行中是否存在与模式匹配的项。</p></blockquote></li></ul><p>一个正则表达式就是一个模式 &amp; 3个标志的组合体。</p><p>有两种写法:<br>`<br>// 写法一<br>var pattern1 = /[bc]at/i;</p><p>// 写法2=二<br>var pattern2 = new RegExp(“[bc]at”, “i”);<br>`<br>由于 RegExp 构造 函数的模式参数是字符串，所以在某些情况下要对字符进行双重转义。所有元字符都必须双重转义，那 些已经转义过的字符也是如此，例如\n(字符\在字符串中通常被转义为\，而在正则表达式字符串中就 会变成\\)。</p><h4 id="ECMAScript3中的问题"><a href="#ECMAScript3中的问题" class="headerlink" title="ECMAScript3中的问题"></a>ECMAScript3中的问题</h4><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">var</span> re = <span class="hljs-keyword">null</span>, i;<br><span class="hljs-keyword">for</span> (i=<span class="hljs-number">0</span>; i &lt; <span class="hljs-number">10</span>; i++)&#123; <br>re = /cat/g;<br>re.test(<span class="hljs-string">&quot;catastrophe&quot;</span>); &#125;<br><span class="hljs-keyword">for</span> (i=<span class="hljs-number">0</span>; i &lt; <span class="hljs-number">10</span>; i++)&#123;<br>re = <span class="hljs-keyword">new</span> RegExp(<span class="hljs-string">&quot;cat&quot;</span>, <span class="hljs-string">&quot;g&quot;</span>); <br>re.test(<span class="hljs-string">&quot;catastrophe&quot;</span>);<br>&#125;<br></code></pre></td></tr></table></figure><p>第一个循环中，只会创建一个RegExp实例，并且实例属性不会重置，因此在循环中再次调用<code>test()</code>函数会失败。因为第二次调用是从索引为3的字符开始。<br>第二个循环中，使用RegExp构造函数每次调用都会创建正则表达式。因此每次迭代都是在一个新的RegExp实例。因此都会返回true。</p><p>这个问题在ECMAScript5中，规定正则表达式的字面量必须像直接调用RegExp构造函数用于，每次都创建新的RegExp实例。</p><h4 id="RegExp实例属性"><a href="#RegExp实例属性" class="headerlink" title="RegExp实例属性"></a>RegExp实例属性</h4><p>通过这些属性可以得到一个正则表达式的各方面信息。</p><blockquote><p>global：布尔值，表示是否设置了 g 标志。<br>ignoreCase：布尔值，表示是否设置了 i 标志。<br>lastIndex：整数，表示开始搜索下一个匹配项的字符位置，从 0 算起。<br>multiline：布尔值，表示是否设置了 m 标志。<br>source：正则表达式的字符串表示，按照字面量形式而非传入构造函数中的字符串模式返回。</p></blockquote><h4 id="RegExp实例方法"><a href="#RegExp实例方法" class="headerlink" title="RegExp实例方法"></a>RegExp实例方法</h4><ul><li>exec()：专门为捕获组而设计的。<br>接受一个参数，即 要应用模式的字符串，然后返回包含第一个匹配项信息的数组;或者在没有匹配项的情况下返回 null。<br>返回类型数组中包含两个属性：index 和 input。<blockquote><p>index：表示匹配项在字符串中的位置。<br>input：表示应用正则表达式的字符串。<br>在数组中：第一项是与整个模式匹配 的字符串，其他项是与模式中的捕获组匹配的字符串(如果模式中没有捕获组，则该数组只包含一项)。</p></blockquote></li></ul><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">var</span> text = <span class="hljs-string">&quot;mom and dad and baby&quot;</span>;<br><span class="hljs-keyword">var</span> pattern = /mom( <span class="hljs-function">and <span class="hljs-title">dad</span><span class="hljs-params">( and baby)</span>?)?/gi</span>;<br><span class="hljs-keyword">var</span> matches = pattern.exec(text);<br>alert(matches.index); <span class="hljs-comment">//0</span><br>alert(matches.input); <span class="hljs-comment">// &quot;mom and dad and baby&quot;</span><br>alert(matches[<span class="hljs-number">0</span>]);    <span class="hljs-comment">// &quot;mom and dad and baby&quot;</span><br>alert(matches[<span class="hljs-number">1</span>]);    <span class="hljs-comment">// &quot; and dad and baby&quot;</span><br>alert(matches[<span class="hljs-number">2</span>]);    <span class="hljs-comment">// &quot; and baby&quot;</span><br></code></pre></td></tr></table></figure><p>在没有使用全局标志时，每次调用<code>exec()</code>时都只会返回第一个匹配项；而使用了全局标志时，则每次调用都只会返回一个匹配项，但是调用后都会继续从字符串中查找新的匹配项。</p><ul><li>test()：知道目标字符串与某个模式是否匹配<br>它接受一个字符串参数。在模式与该参数匹配的情况下返回 true;否则，返回 false。<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">var</span> text = <span class="hljs-string">&quot;000-00-0000&quot;</span>;<br><span class="hljs-keyword">var</span> pattern = /\d&#123;<span class="hljs-number">3</span>&#125;-\d&#123;<span class="hljs-number">2</span>&#125;-\d&#123;<span class="hljs-number">4</span>&#125;/;<br><span class="hljs-keyword">if</span> (pattern.test(text))&#123;<br>alert(<span class="hljs-string">&quot;The pattern was matched.&quot;</span>);<br>&#125;<br></code></pre></td></tr></table></figure>egExp 实例继承的 toLocaleString()和 toString()方法都会返回正则表达式的字面量，与创 建正则表达式的方式无关。<br>正则表达式的 valueOf()方法返回正则表达式本身。</li></ul><h4 id="RegExp构造函数属性"><a href="#RegExp构造函数属性" class="headerlink" title="RegExp构造函数属性"></a>RegExp构造函数属性</h4><p>这些属性适用于作用域中的所有正则表达式，并且基于所执行的最近一次正则表达式操作而变化。这些属性分别有一个长属性名和一个短属性名 (Opera 是例外，它不支持短属性名)。</p><blockquote><p>input($_)：最近一次要匹配的字符串。Opera未实现此属性<br>lastMatch($&amp;)：最近一次的匹配项。Opera未实现此属性<br>lastParen($+)：最近一次匹配的捕获组。Opera未实现此属性<br>leftContext($`)：input字符串中lastMatch之前的文本<br>multiline($*)：布尔值，表示是否所有表达式都使用多行模式。IE和Opera未实现此属性<br>rightContext($’)：Input字符串中lastMatch之后的文本<br>9 个用于存储捕获组的构造函数属性：访问这些属性的语法是 RegExp.$1、RegExp.$2…RegExp.$9，分别用于存储第一、第二……第九个匹配的捕获组。</p></blockquote><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">var</span> text = <span class="hljs-string">&quot;this has been a short summer&quot;</span>;<br><span class="hljs-keyword">var</span> pattern = /(.)hort/g;<br><span class="hljs-comment">/*</span><br><span class="hljs-comment"> * 注意:Opera 不支持 input、lastMatch、lastParen 和 multiline 属性 * Internet Explorer 不支持 multiline 属性</span><br><span class="hljs-comment"> */</span><br> <span class="hljs-keyword">if</span> (pattern.test(text))&#123; <br>alert(RegExp.input);         <span class="hljs-comment">// this has been a short summer</span><br>alert(RegExp.leftContext);   <span class="hljs-comment">// this has been a</span><br>alert(RegExp.rightContext);  <span class="hljs-comment">// summer</span><br>alert(RegExp.lastMatch);     <span class="hljs-comment">// short</span><br>alert(RegExp.lastParen);     <span class="hljs-comment">// s</span><br>alert(RegExp.multiline);     <span class="hljs-comment">// false</span><br>&#125;<br></code></pre></td></tr></table></figure><h2 id="Function类型"><a href="#Function类型" class="headerlink" title="Function类型"></a>Function类型</h2><h4 id="构造函数"><a href="#构造函数" class="headerlink" title="构造函数"></a>构造函数</h4><p><code>var sum = new Function(&quot;num1&quot;, &quot;num2&quot;, &quot;return num1 + num2&quot;); // 不推荐</code></p><h4 id="函数声明与函数表达式"><a href="#函数声明与函数表达式" class="headerlink" title="函数声明与函数表达式"></a>函数声明与函数表达式</h4><p>解析器会率先读取函数声明，并使其在执行 9 任何代码之前可用(可以访问);至于函数表达式，则必须等到解析器执行到它所在的代码行，才会真 正被解释执行。<br><figure class="highlight ada"><table><tr><td class="code"><pre><code class="hljs ada">alert(sum(<span class="hljs-number">10</span>,<span class="hljs-number">10</span>));<br><span class="hljs-keyword">function</span> <span class="hljs-title">sum</span>(num1, num2)&#123;<br><span class="hljs-keyword">return</span> <span class="hljs-type">num1</span> + num2;<br>&#125;<br></code></pre></td></tr></table></figure><br>以上代码完全可以正常运行。因为在代码开始执行之前，解析器就已经通过一个名为函数声明提升 (function declaration hoisting)的过程，读取并将函数声明添加到执行环境中。</p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">alert(sum(<span class="hljs-number">10</span>,<span class="hljs-number">10</span>));<br><span class="hljs-keyword">var</span> sum = function(num1, num2)&#123;<br>    <span class="hljs-keyword">return</span> num1 + num2;<br>&#125;;<br></code></pre></td></tr></table></figure><p>上面的代码在运行期间产生错误。</p><blockquote><p>也可以同时使用函数声明和函数表达式，例如 var sum = function sum(){}。 不过，这种语法在 Safari 中会导致错误。</p></blockquote><h4 id="函数内部属性"><a href="#函数内部属性" class="headerlink" title="函数内部属性"></a>函数内部属性</h4><ul><li><p>this<br>this 引用的是函数据以执行的环境对象。</p></li><li><p>arguments<br>其中arguments内部有一个属性<code>callee</code>，该属性指向拥有<code>arguments</code>对象的函数。<br>因此递归的时候可以直接在函数内部使用<code>this.arguments.callee()</code>调用函数本身。</p></li></ul><blockquote><p>当函数在严格模式下运行时，访问 arguments.callee 会导致错误。</p></blockquote><ul><li>caller<br>这是ECMAScript 5规范化了另一个函数对象的属性。这个属性中保存着调用当前函数的函数的引用， 如果是在全局作用域中调用当前函数，它的值为 null。<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-function">function <span class="hljs-title">outer</span><span class="hljs-params">()</span></span>&#123;<br>inner();<br>&#125;<br><span class="hljs-function">function <span class="hljs-title">inner</span><span class="hljs-params">()</span></span>&#123;<br>alert(arguments.callee.caller);<br>&#125; <br>outer();<br></code></pre></td></tr></table></figure><blockquote><p>严格模式还有一个限制:不能为函数的 caller 属性赋值，否则会导致错误。</p></blockquote></li></ul><h4 id="函数属性和方法"><a href="#函数属性和方法" class="headerlink" title="函数属性和方法"></a>函数属性和方法</h4><ul><li><p>属性</p><blockquote><p>length：表示函数希望接收的命名参数的个数。<br>prototype：prototype 是保存它们所有实例方法的真正所在。toString()和 valueOf()等方法实际上都保存在 prototype 名下，只不过是通过各自对象的实例访问。prototype 属性是不可枚举的，因此使用 for-in 无法发现。</p></blockquote></li><li><p>方法</p><blockquote><p>apply/call/bind：都是可以在特定的作用域中调用函数，类似可以修改函数内的this的指向。前者需要两个参数，分别是运行函数作用域和运行函数的参数数组或者arguments对象；而 call 则在传递的函数参数是逐一列举而不是数组或类数组对象。最后该方法会创建一个函数的实例，将this值绑定到指定的函数内部。<br>valueOf：同样也返回函数的代码<br>toLocalString/toString：始终返回函数的代码，结果因浏览器而异。</p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">// 运行函数</span><br><span class="hljs-function">function <span class="hljs-title">sum</span><span class="hljs-params">(n1, n2)</span> </span>&#123;<br><span class="hljs-keyword">return</span> n1+n2;<br>&#125;<br><br><span class="hljs-comment">// apply</span><br><span class="hljs-function">function <span class="hljs-title">applyTest</span><span class="hljs-params">(n1, n2)</span> </span>&#123;<br><span class="hljs-keyword">return</span> sum.apply(<span class="hljs-keyword">this</span>, [n1, n2]);<br>&#125;<br><br><span class="hljs-comment">// call</span><br><span class="hljs-function">function <span class="hljs-title">callTest</span><span class="hljs-params">(n1, n2)</span> </span>&#123;<br><span class="hljs-keyword">return</span> sum.call(<span class="hljs-keyword">this</span>, n1, n2);<br>&#125;```<br><br>上面看似多次依据，但并非真正的使用方式。而是能够扩充函数赖以运行的作用域：<br>```java<br>window.color = <span class="hljs-string">&#x27;red&#x27;</span>;<br><span class="hljs-keyword">var</span> o = &#123; color: <span class="hljs-string">&#x27;blue&#x27;</span> &#125;;<br><br><span class="hljs-function">function <span class="hljs-title">getColor</span><span class="hljs-params">()</span> </span>&#123;<br><span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.color;<br>&#125;<br><br><span class="hljs-comment">// 运行</span><br><span class="hljs-keyword">var</span> color = getColor.call(<span class="hljs-keyword">this</span>);   <span class="hljs-comment">// red</span><br><span class="hljs-keyword">var</span> color = getColor.call(window); <span class="hljs-comment">// red</span><br><span class="hljs-keyword">var</span> color = getColor.call(o)       <span class="hljs-comment">// blue</span><br></code></pre></td></tr></table></figure><p>经过修改的函数会根据作用域的数据而影响，数据不会和函数存在耦合现象。</p></blockquote></li></ul><h2 id="String-类型"><a href="#String-类型" class="headerlink" title="String 类型"></a>String 类型</h2><h4 id="字符方法"><a href="#字符方法" class="headerlink" title="字符方法"></a>字符方法</h4><p>用于访问字符串特定字符函数：两个方法都接收一个基于0的字符位置</p><ul><li>charAt()<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">var</span> s = <span class="hljs-string">&#x27;abcdefg&#x27;</span>;<br>s.charAt(<span class="hljs-number">2</span>); <span class="hljs-comment">// c</span><br></code></pre></td></tr></table></figure></li><li>charCodeAt()<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">var</span> s = <span class="hljs-string">&#x27;abcdefg&#x27;</span>;<br>s.charCodeAt(<span class="hljs-number">4</span>); <span class="hljs-comment">// &#x27;101&#x27;</span><br></code></pre></td></tr></table></figure>这里返回的是字符编码。</li></ul><h4 id="字符串操作方法"><a href="#字符串操作方法" class="headerlink" title="字符串操作方法"></a>字符串操作方法</h4><ul><li><p>concat<br>用于将1个或多个字符串拼接起来</p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">var</span> a = <span class="hljs-string">&#x27;aa&#x27;</span>;<br><span class="hljs-keyword">var</span> b = <span class="hljs-string">&#x27;bb&#x27;</span>;<br><span class="hljs-keyword">var</span> c = <span class="hljs-string">&#x27;cc&#x27;</span>;<br><br><span class="hljs-keyword">var</span> str = a.concat(b, c); <span class="hljs-comment">// aabbcc</span><br></code></pre></td></tr></table></figure></li><li><p>slice</p></li><li>substr</li><li>substring<br>上面三个函数都具有分割字符串的效果，都接收一至两个参数。第一个参数是截取字符串的第一个字符位置，第二个是可选参数，slice 和 substring 是截取字符串的停止下标，记住是左闭右开；而 substr 则是表示截取字段的长度。</li></ul><h4 id="字符串位置方法"><a href="#字符串位置方法" class="headerlink" title="字符串位置方法"></a>字符串位置方法</h4><ul><li>indexOf</li><li>lastIndexOf<br>接收第一个参数为查询字符串，第二个可选参数为开始查找的起始位置。 在没有找到字符串位置返回 -1。</li></ul><h4 id="trim-方法"><a href="#trim-方法" class="headerlink" title="trim 方法"></a>trim 方法</h4><p>删除前缀和后缀的空格。</p><h4 id="字符串大小写转换方法"><a href="#字符串大小写转换方法" class="headerlink" title="字符串大小写转换方法"></a>字符串大小写转换方法</h4><ul><li>toUpperCase：转为大写。</li><li>toLowerCase：转为小写。</li><li>toLocaleUpperCase：转为大写，针对特殊地区。</li><li>toLocaleLowerCase：转为小写，针对特殊地区。</li></ul><h4 id="字符串模式匹配"><a href="#字符串模式匹配" class="headerlink" title="字符串模式匹配"></a>字符串模式匹配</h4><h6 id="match：接收正则表达式或者-RegExp-对象。返回一个匹配项目数组。"><a href="#match：接收正则表达式或者-RegExp-对象。返回一个匹配项目数组。" class="headerlink" title="match：接收正则表达式或者 RegExp 对象。返回一个匹配项目数组。"></a>match：接收正则表达式或者 RegExp 对象。返回一个匹配项目数组。</h6><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">var</span> text = <span class="hljs-string">&#x27;cat, bat, sat, fat&#x27;</span>;<br><span class="hljs-keyword">var</span> pattern = /.at/<br><br><span class="hljs-keyword">var</span> matches = text.match(pattern); <span class="hljs-comment">// pattern.exec(text);</span><br>matches.index; <span class="hljs-comment">// 0</span><br>matches[<span class="hljs-number">0</span>];    <span class="hljs-comment">// cat</span><br></code></pre></td></tr></table></figure><h6 id="search：同行，但是返回的是第一个匹配项的索引。否则返回-1。"><a href="#search：同行，但是返回的是第一个匹配项的索引。否则返回-1。" class="headerlink" title="search：同行，但是返回的是第一个匹配项的索引。否则返回-1。"></a>search：同行，但是返回的是第一个匹配项的索引。否则返回-1。</h6><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">var</span> text = <span class="hljs-string">&#x27;cat, bat, sat, fat&#x27;</span>;<br>text.search(/at/); <span class="hljs-comment">// 1</span><br></code></pre></td></tr></table></figure><h6 id="replace：接收两个参数，第一个同上，但是字符串模式并不会转换为正则；第二个可以是字符串或函数。第一个如果是字符串，只会替换第一个字符串，如果需要替换所有则需要正则对象并且指定全局-g；"><a href="#replace：接收两个参数，第一个同上，但是字符串模式并不会转换为正则；第二个可以是字符串或函数。第一个如果是字符串，只会替换第一个字符串，如果需要替换所有则需要正则对象并且指定全局-g；" class="headerlink" title="replace：接收两个参数，第一个同上，但是字符串模式并不会转换为正则；第二个可以是字符串或函数。第一个如果是字符串，只会替换第一个字符串，如果需要替换所有则需要正则对象并且指定全局 g；"></a>replace：接收两个参数，第一个同上，但是字符串模式并不会转换为正则；第二个可以是字符串或函数。第一个如果是字符串，只会替换第一个字符串，如果需要替换所有则需要正则对象并且指定全局 g；</h6><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">var</span> text = <span class="hljs-string">&#x27;cat, bat, sat, fat&#x27;</span>;<br>text.replace(<span class="hljs-string">&#x27;at&#x27;</span>, <span class="hljs-string">&#x27;k&#x27;</span>);   <span class="hljs-comment">// ck, bat, sat, fat</span><br>text.replace(<span class="hljs-string">&#x27;/at/g&#x27;</span>, <span class="hljs-string">&#x27;k&#x27;</span>) <span class="hljs-comment">// ck, bk, sk, fk</span><br></code></pre></td></tr></table></figure><ul><li>如果第二个字符是字符串还可以有如下使用：<br>| 字符序列 | 替换文本                                                           |<br>| :———: | :————————————————————————————————: |<br>| $$       | $                                                                  |<br>| $&amp;       | 匹配整个模式的字符串                                               |<br>| $\’      | 匹配子字符串之前的字符串                                           |<br>| $`      | 匹配子字符串之后的字符串                                           |<br>| $n       | 匹配第n个捕获组的子字符串，n为0～9。$1表示第一个捕获组的子字符串。 |<br>| $nn      | 匹配第nn个捕获组的子字符串。nn为01～99。用法同上。                 |</li></ul><p>例子：</p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">var</span> text = <span class="hljs-string">&#x27;cat, bat, sat, fat&#x27;</span>;<br>text.replace(/(.at)/g, <span class="hljs-string">&#x27;word($1)&#x27;</span>);<span class="hljs-comment">// word(cat), word(bat), word(sat), word(fat)</span><br></code></pre></td></tr></table></figure><ul><li>如果第二个字符是函数<br>这个函数共有三个参数：模式的匹配项、模式匹配项在字符串的位置和原始字符串。</li></ul><h6 id="split"><a href="#split" class="headerlink" title="split"></a>split</h6><p>可以根据指定的分隔符来分割字符串为子字符串数组，也可以是 RegExp 对象参数。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">var</span> text = <span class="hljs-string">&#x27;red, blue, green, yellow&#x27;</span>;<br>text.split(<span class="hljs-string">&#x27;,&#x27;</span>);      <span class="hljs-comment">// [&#x27;red&#x27;, &#x27;blue&#x27;, &#x27;green&#x27;, &#x27;yellow&#x27;]</span><br>text.split(<span class="hljs-string">&#x27;,&#x27;</span>, <span class="hljs-number">2</span>);   <span class="hljs-comment">// [&#x27;red&#x27;, &#x27;blue&#x27;]</span><br>text.split(/[^\,]+/); <span class="hljs-comment">// [&#x27;&#x27;, &#x27;,&#x27;, &#x27;,&#x27;, &#x27;,&#x27;, &#x27;&#x27;]</span><br></code></pre></td></tr></table></figure></p><h4 id="localCompare-方法"><a href="#localCompare-方法" class="headerlink" title="localCompare 方法"></a>localCompare 方法</h4><p>比较两个字符串并返回下面其中一个：</p><ul><li>如果字符串在字母表中应该排在字符串参数之前，则返回一个负数。</li><li>如果字符串等于字符串参数，则返回0。</li><li>如果字符串在字母表中应该排在字符串参数之后，则返回一个正数。</li></ul><h4 id="fromCharCode-方法"><a href="#fromCharCode-方法" class="headerlink" title="fromCharCode 方法"></a>fromCharCode 方法</h4><p><strong>静态方法</strong>，接收字符编码，拼接成字符串。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">String.fromCharCode(<span class="hljs-number">104</span>, <span class="hljs-number">101</span>, <span class="hljs-number">108</span>, <span class="hljs-number">108</span>, <span class="hljs-number">111</span>); <span class="hljs-comment">// hello</span><br></code></pre></td></tr></table></figure></p><h4 id="HTML-方法"><a href="#HTML-方法" class="headerlink" title="HTML 方法"></a>HTML 方法</h4><p>简单的 HTML 格式化任务的方法。但是尽量不要使用。<br>| 方法             |<br>|:————————:|<br>| anthor(name)     |<br>| big()            |<br>| bold()           |<br>| fixed()          |<br>| fontcolor(color) |<br>| fontsize(size)   |<br>| italics()        |<br>| link(url)        |<br>| small()          |<br>| strike()         |<br>| sub()            |<br>| sup()            |</p><h2 id="单体内置对象"><a href="#单体内置对象" class="headerlink" title="单体内置对象"></a>单体内置对象</h2><hr><p>这个对象并不依赖于宿主环境，而是 ECMAScript 。</p><h4 id="Global"><a href="#Global" class="headerlink" title="Global"></a>Global</h4><p>不属于任何对象的属性和方法。所有在全局作用域中定义的函数和对象，都是 Global 对象的属性。</p><ul><li>encodeURI() / encodeURI()</li><li><p>encodeURIComponent() / decodeURIComponent()<br>他们可以对URI进行编码，以便发送给浏览器。</p></li><li><p>eval()<br>他像是 ECMAScript 的解析器。只接受一个参数，即要执行的 ECMAScript 字符串。</p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">eval(<span class="hljs-string">&#x27;alert(&#x27;</span>hi<span class="hljs-string">&#x27;)&#x27;</span>);<br>alera(<span class="hljs-string">&#x27;hi&#x27;</span>);<br></code></pre></td></tr></table></figure><p>上面的代码就等价于下面的代码。他会将传入的参数当作实际的 ECMAScript 语句来解析，然后把执行结果插入到原位置。</p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">var</span> msg = <span class="hljs-string">&#x27;hello&#x27;</span>;<br>eval(<span class="hljs-string">&#x27;alert(msg)&#x27;</span>); <span class="hljs-comment">// hello</span><br></code></pre></td></tr></table></figure></li><li><p>包含的属性<br>| 属性           |<br>|:———————:|<br>| undefined      |<br>| NaN            |<br>| infinity       |<br>| Object         |<br>| Array          |<br>| Function       |<br>| Boolean        |<br>| String         |<br>| Number         |<br>| Date           |<br>| RegExp         |<br>| Error          |<br>| EvalError      |<br>| RangeError     |<br>| ReferenceError |<br>| SyntaxError    |<br>| TypeError      |<br>| URIError       |</p></li><li><p>window对象<br>一般情况下没有指出如何访问 Global，因此Web浏览器将这个全局对象作为 window 的一部分。</p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">var</span> color = <span class="hljs-string">&#x27;red&#x27;</span>;<br><span class="hljs-function">function <span class="hljs-title">sayColor</span><span class="hljs-params">()</span> </span>&#123;<br>alert(window.color);<br>&#125;<br>window.sayColor();  <span class="hljs-comment">//red</span><br></code></pre></td></tr></table></figure></li></ul><h4 id="Math"><a href="#Math" class="headerlink" title="Math"></a>Math</h4><ul><li><p>属性<br>| 属性         | 说明          |<br>|:——————:|:——————-:|<br>| Math.E       | e             |<br>| Math.LN10    | 10的自然对数  |<br>| Math.LN2     | 2的自然对数   |<br>| Math.LOG2E   | 2为底e的对数  |<br>| Math.LOG10E  | 10为底e的对数 |<br>| Math.PI      | pi的值        |<br>| Math.SQRT1_2 | 1/2的平方根   |<br>| Math.SQRT2   | 2的平方根     |</p></li><li><p>min 和 max<br>获取一组数据的最大值和最小值。 </p></li><li><p>舍入方法</p><blockquote><p>Math.ceil()：向上舍入<br>Math.floor()：向下舍入<br>Math.round()：标准舍入</p></blockquote></li><li><p>random<br>返回介于0和1之间一个随机数，记住是开区间，即不包括0和1.</p></li></ul><h2 id="五、对象"><a href="#五、对象" class="headerlink" title="五、对象"></a>五、对象</h2><p>创建包括 Object 或者字面量 {}。</p><h2 id="属性类型"><a href="#属性类型" class="headerlink" title="属性类型"></a>属性类型</h2><p>第五版定义了只有内部才能用的特性(attribute)，买哦书了属性的各种特性。一般情况下不能直接访问。表示如下：<br><code>[[Enumerable]]</code></p><h4 id="数据属性"><a href="#数据属性" class="headerlink" title="数据属性"></a>数据属性</h4><p>有以下4个</p><ul><li>[[Configurable]]：表示是否通过 delete 删除属性从而重新定义属性，能否修改属性的特性，或者能否把属性修改为访问器属性。默认为true。</li><li>[[Enumerable]]：表示能否通过 for-in 循环返回属性。默认为true。</li><li>[[Writable]]：表示能否修改属性的值。默认true。</li><li>[[Value]]：包含这个属性的数据值。读取属性值的时候，从这个位置读；写入属性值的时候，把新值保存在这个位置。默认undefined。</li></ul><p><strong><em>例子：</em></strong><br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">var</span> person = &#123;<br>name: <span class="hljs-string">&#x27;Yang&#x27;</span><br>&#125;;<br></code></pre></td></tr></table></figure><br>上面的对象有一个属性name，值为’Yang’。即[[Value]]特性设置为’Yang’。</p><p>修改属性，Object.defineProperty()方法。接收三个参数：</p><blockquote><p>属性的对象<br>属性名称<br>描述符对象</p><blockquote><p>configurable<br>enumerable<br>writable<br>value</p></blockquote></blockquote><p><strong><em>例子</em></strong><br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">var</span> person = &#123;&#125;;<br>Object.defineProperty(person, <span class="hljs-string">&quot;name&quot;</span>, &#123;<br>writable: <span class="hljs-keyword">false</span>,<br>value: <span class="hljs-string">&#x27;Yang&#x27;</span><br>&#125;);<br><br>alert(person.name); <span class="hljs-comment">// &#x27;Yang&#x27;</span><br>person.name = <span class="hljs-string">&#x27;Li&#x27;</span>;<br>alert(person.name); <span class="hljs-comment">// &#x27;Li&#x27;</span><br></code></pre></td></tr></table></figure><br>这里的数据是只读的，不可修改。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">var</span> person = &#123;&#125;;<br>Object.defineProperty(person, <span class="hljs-string">&quot;name&quot;</span>, &#123;<br>configurable: <span class="hljs-keyword">false</span>,<br>value: <span class="hljs-string">&#x27;Yang&#x27;</span><br>&#125;);<br><br>alert(person.name); <span class="hljs-comment">// &#x27;Yang&#x27;</span><br>delete person.name;<br>alert(person.name); <span class="hljs-comment">// &#x27;Li&#x27;</span><br></code></pre></td></tr></table></figure><br>这里表示不能从对象中删除属性。一旦把属性定义为不可配置，就不能再把它变回可配置了。就会报错。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">var</span> person = &#123;&#125;;<br>Object.defineProperty(person, <span class="hljs-string">&quot;name&quot;</span>, &#123;<br>configurable: <span class="hljs-keyword">false</span>,<br>value: <span class="hljs-string">&#x27;Yang&#x27;</span><br>&#125;);<br><br>Object.defineProperty(person, <span class="hljs-string">&quot;name&quot;</span>, &#123;<br>configurable: <span class="hljs-keyword">true</span>,<br>value: <span class="hljs-string">&#x27;Yang&#x27;</span><br>&#125;);<br></code></pre></td></tr></table></figure><br>上面的例子就是报错。</p><h4 id="访问器属性"><a href="#访问器属性" class="headerlink" title="访问器属性"></a>访问器属性</h4><p>通过 getter 和 setter 函数设置读取访问器属性。访问器属性有以下四个特性：</p><ul><li>[[Configurable]]：表示是否通过 delete 删除属性从而重新定义属性，能否修改属性的特性，或者能否把属性修改为访问器属性。默认为true。</li><li>[[Enumerable]]：表示能否通过 for-in 循环返回属性。默认为true。</li><li>[[Get]]：读取属性时调用的函数，默认undefined。</li><li>[[Set]]：写入属性时调用的函数，默认undefined。</li></ul><p><strong>访问器属性不能直接定义，必须使用Object.defineProperty()来定义。</strong></p><p>例子：<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">var</span> book = &#123;<br>_year: <span class="hljs-number">2004</span>,<br>edition: <span class="hljs-number">1</span><br>&#125;;<br>Object.defineProperty(book, <span class="hljs-string">&quot;year&quot;</span>, &#123;<br>get: function() &#123;<br><span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>._year;<br>&#125;,<br>set: function(newValue) &#123;<br><span class="hljs-keyword">if</span> (newValue &gt; <span class="hljs-number">2004</span>) &#123;<br><span class="hljs-keyword">this</span>._year = newValue;<br><span class="hljs-keyword">this</span>.edition += newValue - <span class="hljs-number">2004</span>;<br>&#125;<br>&#125;<br>&#125;);<br>book.year = <span class="hljs-number">2005</span>; <span class="hljs-comment">// 2</span><br></code></pre></td></tr></table></figure><br>_year的下划线用于表示只能通过对象方法访问的属性。这里添加的访问器属<strong>year</strong>就包含一个 getter 和一个 setter 函数。<br>要创建访问器属性，一般使用下面两个非标准函数:<strong>defineGetter</strong>() 和 ————defineSetter__()。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">var</span> book = &#123;<br>_year: <span class="hljs-number">2004</span>,<br>edition: <span class="hljs-number">1</span><br>&#125;;<br>book.__defineGetter__(<span class="hljs-string">&quot;year&quot;</span>, function() &#123;<br><span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>._year;<br>&#125;);<br>book.__defineSetter__(<span class="hljs-string">&quot;year&quot;</span>, function(newValue) &#123;<br><span class="hljs-keyword">if</span> (newValue &gt; <span class="hljs-number">2004</span>) &#123;<br><span class="hljs-keyword">this</span>._year = newValue;<br><span class="hljs-keyword">this</span>.edition += newValue - <span class="hljs-number">2004</span>;<br>&#125;<br>&#125;);<br></code></pre></td></tr></table></figure><br>上面代码例子与之前效果一样。</p><p><strong><em> 注意：在不支持Object.defineProperty()方法的浏览器中不能修改[[Configurable]]和[[Enumerable]]。</em></strong></p><p>Object.defineProperties()支持多个属性设置。<br>Object.getOwnPropertyDescriptor()获取给定属性的描述符。</p><h2 id="创建对象"><a href="#创建对象" class="headerlink" title="创建对象"></a>创建对象</h2><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-function">funcion <span class="hljs-title">Person</span><span class="hljs-params">(name, age, job)</span> </span>&#123;<br><span class="hljs-keyword">this</span>.name = name;<br><span class="hljs-keyword">this</span>.age = age;<br><span class="hljs-keyword">this</span>.job = job;<br><span class="hljs-keyword">this</span>.sayName = function() &#123;<br>...<br>&#125;;<br>&#125;<br><span class="hljs-keyword">var</span> person1 = <span class="hljs-keyword">new</span> Person(<span class="hljs-string">&#x27;Yang&#x27;</span>, <span class="hljs-number">19</span>, <span class="hljs-string">&#x27;Coder&#x27;</span>);<br></code></pre></td></tr></table></figure><p>创建对象需要以下四个步骤：</p><ul><li>1.创建一个新对象；</li><li>2.将构造函数的作用域赋值给新对象，即this；</li><li>3.执行构造函数代码；</li><li>4.返回新对象；</li></ul><p>并且，person1保存着Person的一个不同的实例，constructor。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">person1.constructor == Person <span class="hljs-comment">// true</span><br></code></pre></td></tr></table></figure><br>该属性最初用来标识对象类型，但是使用 instanceof 更优。并且这种方式创建的构造函数是定义在 Global 对象汇总。</p><h2 id="原型模式"><a href="#原型模式" class="headerlink" title="原型模式"></a>原型模式</h2><p>创建的每个函数都有一个 prototype 原型属性，是一个指针指向一个对象。这个对象的用途是包含可以由特定类型的所有实例共享的属性和方法。</p><h4 id="理解原型对象"><a href="#理解原型对象" class="headerlink" title="理解原型对象"></a>理解原型对象</h4><ul><li>无论什么时候，只要创建一个新函数，就会根据一组特定的规则为该函数创建一个prototype属性，这个属性指向函数的原型对象。</li><li>所有原型对象都会自动获得一个constructor构造函数属性，这个属性包含一个指向prototype属性所在函数的指针。</li><li>当调用构造函数创建一个新实例后，该实例的内部将包含一个指针（内部属性），指向原型对象，即[[Prototype]]，但是由于这里不是标准，因此其他实现中不一样。但是她们的链接存在于实例与构造函数的原型对象之间。</li></ul><p><img src="/img/JavaScript/2.png" alt="github"><br>上图包括了文字中描述的内容。<br>可以通过 isPrototypeOf() 方法确定对象之间是否存在原型关系。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">Person.prototype.isPrototypeOf(person1); <span class="hljs-comment">// true</span><br></code></pre></td></tr></table></figure><br>通过 getPrototypeOf() 方法来返回[[Prototype]]的值。<br> <figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">Object.getPrototypeOf(person1) == Person.prototype; <span class="hljs-comment">// true</span><br></code></pre></td></tr></table></figure><br>通过 hasOwnProperty() 方法可以检测一个属性是否存在与实例中(true)，还是在原型(false)。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">person1.hasOwnProperty(<span class="hljs-string">&#x27;name&#x27;</span>); <span class="hljs-comment">// true</span><br></code></pre></td></tr></table></figure></p><h4 id="原型与-in-操作符"><a href="#原型与-in-操作符" class="headerlink" title="原型与 in 操作符"></a>原型与 in 操作符</h4><p>in 有两种使用：单独使用和在for-in中使用。单独使用时，in操作符会在通过对象能够访问属性时返回true，无论是在原型还是实例中。<br>为了区别是否在原型中的属性，使用 hasOwnProperty() 方法判断。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-function">function <span class="hljs-title">hasPrototypeProperty</span><span class="hljs-params">(object, name)</span> </span>&#123;<br><span class="hljs-keyword">return</span> !object.hasOwnProperty(name) &amp;&amp; (name in object);<br>&#125;<br></code></pre></td></tr></table></figure><br>for-in 循环时，返回的都是可枚举循环（Emunerated）的属性值。<br>Object.keys(obj) 可以返回当前实例中所有的可枚举属性的 key 的字符串数组。注意这里的key是键值。这个函数的功能与 getOwnPropertyNames() 相反，他会获取对象中所有的属性。</p><h4 id="原型的问题"><a href="#原型的问题" class="headerlink" title="原型的问题"></a>原型的问题</h4><h6 id="重写原型对象后混乱"><a href="#重写原型对象后混乱" class="headerlink" title="重写原型对象后混乱"></a>重写原型对象后混乱</h6><p>下面的例子：<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-function">function <span class="hljs-title">Person</span><span class="hljs-params">()</span> </span>&#123;&#125;<br><span class="hljs-keyword">var</span> person1 = <span class="hljs-keyword">new</span> Person();<br>Person.prototype = &#123;<br>constructor: Person,<br>name: <span class="hljs-string">&#x27;Yang&#x27;</span>,<br>age: <span class="hljs-number">18</span><br>&#125;;<br>person1.age <br></code></pre></td></tr></table></figure><br><img src="/img/JavaScript/3.png" alt="github"><br>注意在重写原型对象后会切断现有原型与任何之前实例的关系。如上图。</p><h6 id="引用类型属性"><a href="#引用类型属性" class="headerlink" title="引用类型属性"></a>引用类型属性</h6><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-function">function <span class="hljs-title">Person</span><span class="hljs-params">()</span> </span>&#123;&#125;;<br>Person.prototype = &#123;<br>constructor: Person,<br>name: <span class="hljs-string">&#x27;Yang&#x27;</span>,<br>age: <span class="hljs-number">18</span>,<br>friends: [<span class="hljs-string">&#x27;Li&#x27;</span>, <span class="hljs-string">&#x27;Wu&#x27;</span>]<br>&#125;;<br>val person1 = <span class="hljs-keyword">new</span> Person();<br>val person2 = <span class="hljs-keyword">new</span> Person();<br><br>person1.friends.push(<span class="hljs-string">&#x27;Xia&#x27;</span>);<br>person1.friends === person2.friends; <span class="hljs-comment">// true</span><br></code></pre></td></tr></table></figure><p>由于共享原型导致引用类型完全共享。</p><h2 id="构造函数与原型模式"><a href="#构造函数与原型模式" class="headerlink" title="构造函数与原型模式"></a>构造函数与原型模式</h2><p>下面这种模式是ECMAScript中认可度最好的构造类型的方法。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-function">function <span class="hljs-title">Person</span><span class="hljs-params">(name, age)</span> </span>&#123;<br><span class="hljs-keyword">this</span>.name = name;<br><span class="hljs-keyword">this</span>.age = age;<br><span class="hljs-keyword">this</span>.friends = [<span class="hljs-string">&#x27;Li&#x27;</span>, <span class="hljs-string">&#x27;Xia&#x27;</span>];<br>&#125;;<br>Person.prototype = &#123;<br>constructor: Person,<br>sayName: function() &#123;<br><span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.name;<br>&#125;<br>&#125;;<br><br><span class="hljs-keyword">var</span> person1 = <span class="hljs-keyword">new</span> Person(<span class="hljs-string">&#x27;Yang&#x27;</span>, <span class="hljs-number">17</span>);<br><span class="hljs-keyword">var</span> person2 = <span class="hljs-keyword">new</span> Person(<span class="hljs-string">&#x27;Wu&#x27;</span>, <span class="hljs-number">20</span>);<br><br>person1.friends.push(<span class="hljs-string">&#x27;Mi&#x27;</span>);<br>person1.friends === person2.friends; <span class="hljs-comment">// false;</span><br></code></pre></td></tr></table></figure></p><h2 id="动态原型模式"><a href="#动态原型模式" class="headerlink" title="动态原型模式"></a>动态原型模式</h2><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-function">function <span class="hljs-title">Person</span><span class="hljs-params">(name, age)</span> </span>&#123;<br><span class="hljs-keyword">this</span>.name = name;<br><span class="hljs-keyword">this</span>.age = age;<br><span class="hljs-keyword">this</span>.friends = [<span class="hljs-string">&#x27;Li&#x27;</span>, <span class="hljs-string">&#x27;Xia&#x27;</span>];<br><span class="hljs-keyword">if</span> (typeof Person.prototype.sayName != <span class="hljs-string">&#x27;Function&#x27;</span>) &#123;<br>Person.prototype.sayName = function() &#123;<br><span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.name;<br>&#125;;<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>在不存在原型函数的情况下自动添加原型函数，这样就只需要对构造函数进行封装。</p><h2 id="寄生构造函数模式"><a href="#寄生构造函数模式" class="headerlink" title="寄生构造函数模式"></a>寄生构造函数模式</h2><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-function">function <span class="hljs-title">Person</span><span class="hljs-params">(name, age)</span> </span>&#123;<br><span class="hljs-keyword">var</span> o = <span class="hljs-keyword">new</span> Object();<br>o.name = name;<br>o.age = age;<br>o.sayName = function() &#123;<br><span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.name;<br>&#125;;<br><span class="hljs-keyword">return</span> o;<br>&#125;<br></code></pre></td></tr></table></figure><p>整个过程类似工厂模式。</p><h2 id="稳妥构造函数模式"><a href="#稳妥构造函数模式" class="headerlink" title="稳妥构造函数模式"></a>稳妥构造函数模式</h2><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-function">function <span class="hljs-title">Person</span><span class="hljs-params">(name, age)</span> </span>&#123;<br><span class="hljs-keyword">var</span> o = <span class="hljs-keyword">new</span> Object();<br>o.name = name;<br>o.age = age;<br>o.sayName = function() &#123;<br><span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.name;<br>&#125;;<br><span class="hljs-keyword">return</span> o;<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="继承问题"><a href="#继承问题" class="headerlink" title="继承问题"></a>继承问题</h2><p>ECMAScript通过原型链来作为实现继承的主要方法。<br>原型：每个构造函数都有一个原型对象，原型对象都包括一个指向构造函数的指针；实例则有一个指针指向原型对象。<br>原型链：原型对象包含一个指针指向另一个原型，层层递进形成了实例与原型的链条。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-function">function <span class="hljs-title">SuperType</span><span class="hljs-params">()</span> </span>&#123;<br><span class="hljs-keyword">this</span>.property = <span class="hljs-keyword">true</span>;<br>&#125;<br>SuperType.prototype.getSuperValue = function() &#123;<br><span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.property;<br>&#125;<br><span class="hljs-function">function <span class="hljs-title">SubType</span><span class="hljs-params">()</span> </span>&#123;<br><span class="hljs-keyword">this</span>.subproperty = <span class="hljs-keyword">false</span>;<br>&#125;<br><span class="hljs-comment">// 继承SubType</span><br>SubType.prototype = <span class="hljs-keyword">new</span> SuperType();<br><br>SubType.prototype.getSubValue = function() &#123;<br><span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.subproperty;<br>&#125;<br><br><span class="hljs-keyword">var</span> instance = <span class="hljs-keyword">new</span> SubType();<br>instance.getSuperValue():  <span class="hljs-comment">//true</span><br></code></pre></td></tr></table></figure><br>完成代码后看一下类图：<br><img src="/img/JavaScript/4.png" alt="github"><br>给 SubType 换了一个新原型；就是 SuperType 的实例。新原型不仅具有作为一个 SuperType 的实例所拥有的全部属性和方法，而且内部的指针指向了 SuperType的原型。<br>注意：这里的SubType的原型 constructor 属性不是被重写，而是他的原型指向了另一个对象，SuperType的原型，而他的原型的 constructor 属性指向的是 SuperType。</p><h4 id="原型链的问题"><a href="#原型链的问题" class="headerlink" title="原型链的问题"></a>原型链的问题</h4><p>虽然强大，但是在实现继承上依旧存在问题。最主要的就是来自包含引用类型值的原型。即所有对象都共享原型链上的所有对象数据；第二个是在创建子类型时，无法初始化父类的构造函数。</p><h2 id="借用构造函数"><a href="#借用构造函数" class="headerlink" title="借用构造函数"></a>借用构造函数</h2><p>也叫做伪造对象。即在子类型构造函数的内部调用超类型构造函数。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-function">function <span class="hljs-title">SuperType</span><span class="hljs-params">()</span> </span>&#123;<br><span class="hljs-keyword">this</span>.colors = [<span class="hljs-string">&#x27;red&#x27;</span>, <span class="hljs-string">&#x27;blue&#x27;</span>, <span class="hljs-string">&#x27;green&#x27;</span>];<br>&#125;<br><span class="hljs-function">function <span class="hljs-title">SubType</span><span class="hljs-params">()</span> </span>&#123;<br><span class="hljs-comment">// 继承了 SuperType</span><br>SuperType.call(<span class="hljs-keyword">this</span>);<br>&#125;<br><span class="hljs-keyword">var</span> instance1 = <span class="hljs-keyword">new</span> SubType();<br>instance1.colors.push(<span class="hljs-string">&#x27;black&#x27;</span>);<br>instance1.colors;  <span class="hljs-comment">// red blue green black</span><br><span class="hljs-keyword">var</span> instance2 = <span class="hljs-keyword">new</span> SubType();<br>instance2.colors;  <span class="hljs-comment">// red blue green</span><br></code></pre></td></tr></table></figure><br>通过调用call()方法，创建SubType的环境下调用了SuperType的构造函数。这样就初始化了父类构造函数。</p><h2 id="组合继承"><a href="#组合继承" class="headerlink" title="组合继承"></a>组合继承</h2><h2 id="原型式继承"><a href="#原型式继承" class="headerlink" title="原型式继承"></a>原型式继承</h2><h2 id="寄生试继承"><a href="#寄生试继承" class="headerlink" title="寄生试继承"></a>寄生试继承</h2><h2 id="寄生组合式继承"><a href="#寄生组合式继承" class="headerlink" title="寄生组合式继承"></a>寄生组合式继承</h2>]]></content>
    
    
    <summary type="html">学习JavaScript语法细节记录</summary>
    
    
    
    <category term="JavaScript" scheme="http://example.com/categories/JavaScript/"/>
    
    
    <category term="javaScript" scheme="http://example.com/tags/javaScript/"/>
    
  </entry>
  
  <entry>
    <title>React-native 高德地图</title>
    <link href="http://example.com/2020/03/06/React-native-%E9%AB%98%E5%BE%B7%E5%9C%B0%E5%9B%BE/"/>
    <id>http://example.com/2020/03/06/React-native-%E9%AB%98%E5%BE%B7%E5%9C%B0%E5%9B%BE/</id>
    <published>2020-03-06T02:07:45.000Z</published>
    <updated>2021-04-17T07:10:38.653Z</updated>
    
    <content type="html"><![CDATA[<h2 id="一、地图"><a href="#一、地图" class="headerlink" title="一、地图"></a>一、地图</h2><p>这里使用的是<code>react-native-amap3d</code><a href="https://github.com/qiuxiang/react-native-amap3d">https://github.com/qiuxiang/react-native-amap3d</a>，大佬的流程安装基本没问题，这里只记录自己安装的过程。<br>即高德地图的封装库。</p><h2 id="二、安装"><a href="#二、安装" class="headerlink" title="二、安装"></a>二、安装</h2><p>`<br>npm install react-native-amap3d —save</p><p>cd ios<br>pod install<br>`<br>由于使用最新版本RN，添加第三方库不需要<strong>link</strong>。</p><h3 id="Android"><a href="#Android" class="headerlink" title="Android"></a>Android</h3><h5 id="1-获取高德Keyhttps-lbs-amap-com-dev-key-app"><a href="#1-获取高德Keyhttps-lbs-amap-com-dev-key-app" class="headerlink" title="1.获取高德Keyhttps://lbs.amap.com/dev/key/app"></a>1.获取高德Key<a href="https://lbs.amap.com/dev/key/app">https://lbs.amap.com/dev/key/app</a></h5><p>这里就不用多说了，看图<br><img src="/img/ReactNativeMap/0.png" alt="github"><br>第一步问题不大，第二、三步需要注意最好不要使用同一个Android Key的SHA1码，在自己注册高德Key的时候高德也会有提醒。</p><ul><li>获取SHA1码<br><img src="/img/ReactNativeMap/1.png" alt="github"><br>双击这里可以直接看到自己所有Android Key的SHA1码。</li><li>PackageName 包名<br>直接着到<code>AndoridManifest.xml</code>中的<code>package</code>复制粘贴即可。</li></ul><p>点击提交后就可以看到自己注册的Android端Key。</p><h5 id="2-添加高德Key至Android"><a href="#2-添加高德Key至Android" class="headerlink" title="2.添加高德Key至Android"></a>2.添加高德Key至Android</h5><p>找到代码中的<code>AndroidManifest.xml</code>文件添加如下代码。<br><figure class="highlight"><table><tr><td class="code"><pre><code class="hljs java">&lt;manifest ...&gt;<br><br>    &lt;application<br>      &gt;<br>&lt;!-- 添加的代码 --&gt;<br>      &lt;meta-data<br>        android:name=<span class="hljs-string">&quot;com.amap.api.v2.apikey&quot;</span><br>        android:value=<span class="hljs-string">&quot;高德的Key&quot;</span> /&gt;<br>      &lt;activity<br>      ... <br>    &lt;/application&gt;<br>&lt;/manifest&gt;<br></code></pre></td></tr></table></figure><br>这里注意除了需要添加的高德key外，其他所有的<code>meta-data</code>标签内容全部不要变动。</p><h5 id="addPackage"><a href="#addPackage" class="headerlink" title="addPackage"></a>addPackage</h5><p>最后好了就编译项目。完成后进入<code>MainApplication.java</code>文件中查看一下是否需要<strong>addPackage</strong>添加map包。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">List&lt;ReactPackage&gt; packages = <span class="hljs-keyword">new</span> PackageList(<span class="hljs-keyword">this</span>).getPackages();<br></code></pre></td></tr></table></figure><br>这里进入<code>PackageList</code>查看是否有添加<code>AMap3DPackage()</code>。如果没有，则我们自己手动添加，而添加方式则在<code>MainApplication.java</code>中添加该包。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">List&lt;ReactPackage&gt; packages = <span class="hljs-keyword">new</span> PackageList(<span class="hljs-keyword">this</span>).getPackages();<br>packages.add(<span class="hljs-keyword">new</span> AMap3DPackage());<br></code></pre></td></tr></table></figure></p><h5 id="测试"><a href="#测试" class="headerlink" title="测试"></a>测试</h5><p>完成上述工作后，测试一下代码。<br>在自己的RN页面中添加下面代码：<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">import</span> &#123; MapView &#125; from <span class="hljs-string">&quot;react-native-amap3d&quot;</span>;<br><br><span class="hljs-keyword">const</span> App = () =&gt; &#123;<br><span class="hljs-keyword">return</span> (<br>&lt;MapView<br>style=&#123;&#123;flex: <span class="hljs-number">1</span>&#125;&#125;/&gt;<br>);<br>&#125;;<br></code></pre></td></tr></table></figure><br>没有大问题如Gradle，则会一路顺风完成过程。</p><h3 id="Ios"><a href="#Ios" class="headerlink" title="Ios"></a>Ios</h3><h5 id="1-获取高德Keyhttps-lbs-amap-com-dev-key-app-1"><a href="#1-获取高德Keyhttps-lbs-amap-com-dev-key-app-1" class="headerlink" title="1.获取高德Keyhttps://lbs.amap.com/dev/key/app"></a>1.获取高德Key<a href="https://lbs.amap.com/dev/key/app">https://lbs.amap.com/dev/key/app</a></h5><p><img src="/img/ReactNativeMap/2.png" alt="github"></p><ul><li>Bundle ID<br>ID如图：<br><img src="/img/ReactNativeMap/3.png" alt="github"><br>这就是<code>Bundle ID</code>。<br>添加后就可以创建高德Key了。</li></ul><h5 id="2-CocoaPods"><a href="#2-CocoaPods" class="headerlink" title="2.CocoaPods"></a>2.CocoaPods</h5><p>由于这里用的是新的ReactNative库，所以IOS中支持CocoaPods。这里只是描述在Pod中的问题。<br>调用<br><figure class="highlight cmake"><table><tr><td class="code"><pre><code class="hljs cmake">pod <span class="hljs-keyword">install</span><br></code></pre></td></tr></table></figure><br>添加第三方库，完成后进入<strong>node_modules</strong>库中找到<strong>react-Native-amap3d</strong>。找到ios文件中的<strong>readMe</strong>，这里需要好好看看文件该如何修改Pod。</p><p>大部分问题的出先都是由于需要手动添加<code>react-native-amap3d</code>至<code>Podfile</code>文件中。添加后继续调用<code>pod install</code>即可。</p><p>上面过程就是将第三方库添加至CocoaPods，然后将这个库完成链接。<br><img src="/img/ReactNativeMap/4.png" alt="github"></p><h5 id="3-添加高德Key"><a href="#3-添加高德Key" class="headerlink" title="3.添加高德Key"></a>3.添加高德Key</h5><p>添加高德Key，找到<strong>AppDelegate.m</strong>文件，添加如下代码：<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">#import &lt;AMapFoundationKit/AMapFoundationKit.h&gt;<br><br>  [AMapServices sharedServices].apiKey = @<span class="hljs-string">&quot;自己注册的高德Key&quot;</span>;<br></code></pre></td></tr></table></figure><br><strong><em>如果这里的库加载报错，表示过程2中完成有误，即库找不到或添加库异常，那么这一步可以完成检测第二步是否有误的辅助工作。</em></strong></p><h5 id="测试-1"><a href="#测试-1" class="headerlink" title="测试"></a>测试</h5><p>完成上述工作后，测试一下代码。<br>在自己的RN页面中添加下面代码：<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">import</span> &#123; MapView &#125; from <span class="hljs-string">&quot;react-native-amap3d&quot;</span>;<br><br><span class="hljs-keyword">const</span> App = () =&gt; &#123;<br><span class="hljs-keyword">return</span> (<br>&lt;MapView<br>style=&#123;&#123;flex: <span class="hljs-number">1</span>&#125;&#125;/&gt;<br>);<br>&#125;;<br></code></pre></td></tr></table></figure><br>没有大问题，则会一路顺风完成过程。</p><h2 id="三、API"><a href="#三、API" class="headerlink" title="三、API"></a>三、API</h2><p>使用API最好的方法还是进入代码中查看是否由相关功能。以下的api也仅供参考。</p><h3 id="地图类型"><a href="#地图类型" class="headerlink" title="地图类型"></a>地图类型</h3><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">&lt;MapView style=&#123;&#123;flex: <span class="hljs-number">1</span>&#125;&#125; mapType=&#123;mapType&#125;/&gt;;<br></code></pre></td></tr></table></figure><ul><li>standard: 标准地图</li><li>satellite: 卫星地图</li><li>navigation: 导航地图</li><li>night: 夜间地图</li><li>bus: 公交地图</li></ul><h3 id="地图状态"><a href="#地图状态" class="headerlink" title="地图状态"></a>地图状态</h3><blockquote><p>coordinate：中心坐标<br>zoomLevel：缩放级别<br>minZoomLevel：最小缩放级别<br>tilt：倾斜角度<br>roration：旋转角度<br>region：显示区域<br>limitRegion：限制地图只能显示某个矩形区域</p></blockquote><ul><li>coordinate：使用纬度<code>latitude</code>与经度<code>longitude</code>设置为中心坐标。</li><li>region：相对于上面还添加了两个属性<code>longitudeDelta</code>和<code>latitudeDelta</code>。用于计算显示区域矩形参数。<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">fun ReadableMap.toLatLngBounds(): LatLngBounds &#123;<br>    val latitude = <span class="hljs-keyword">this</span>.getDouble(<span class="hljs-string">&quot;latitude&quot;</span>)<br>    val longitude = <span class="hljs-keyword">this</span>.getDouble(<span class="hljs-string">&quot;longitude&quot;</span>)<br>    val latitudeDelta = <span class="hljs-keyword">this</span>.getDouble(<span class="hljs-string">&quot;latitudeDelta&quot;</span>)<br>    val longitudeDelta = <span class="hljs-keyword">this</span>.getDouble(<span class="hljs-string">&quot;longitudeDelta&quot;</span>)<br>    <span class="hljs-keyword">return</span> LatLngBounds(<br>            LatLng(latitude - latitudeDelta / <span class="hljs-number">2</span>, longitude - longitudeDelta / <span class="hljs-number">2</span>),<br>            LatLng(latitude + latitudeDelta / <span class="hljs-number">2</span>, longitude + longitudeDelta / <span class="hljs-number">2</span>)<br>    )<br>&#125;<br></code></pre></td></tr></table></figure>这里是计算方式。</li></ul><h3 id="地图图层"><a href="#地图图层" class="headerlink" title="地图图层"></a>地图图层</h3><ul><li>showsIndoorMap: 是否显示室内地图</li><li>showsIndoorSwitch: 是否显示室内地图楼层切换控件（似乎不能正常现实）</li><li>showsBuildings: 是否显示3D建筑</li><li>showsLabels: 是否显示文本标签</li><li>showsCompass: 是否显示指南针</li><li>showsZoomControls: 是否显示放大缩小按钮</li><li>showsScale: 是否显示比例尺</li><li>showsLocationButton: 是否显示定位按钮</li><li>showsTraffic: 是否显示路况</li></ul><p>上面这几个功能都是字面意思。但是注意默写功能ios中并不支持：定位按钮、缩放按钮。</p><h3 id="定位"><a href="#定位" class="headerlink" title="定位"></a>定位</h3><blockquote><p>locationEnable：用于定位功能<br>coordinate（<code>latitude</code>纬度，<code>longitude</code>经度）<br>locationInterval：定位间隔时间(默认是2000ms)仅Android可以使用<br>distanceFilter：定位的最小更新距离，仅Ios可以使用</p></blockquote><p>这里需要注意Android6.0以上需要打开定位权限。可以使用<code>PermissionsAndroid</code>。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">PermissionsAndroid.request(PermissionsAndroid.PERMISSIONS.ACCESS_COARSE_LOCATION);<br></code></pre></td></tr></table></figure></p><p>准备好后，由于IOS没有定位按钮，因此我们需要手动开始定位，通过<code>onLocation</code>回调函数来获取定位经纬度，然后在进行设置<code>animateTo</code>当前位置。</p><h3 id="手势交互"><a href="#手势交互" class="headerlink" title="手势交互"></a>手势交互</h3><p>以下这些手势用来控制地图的操作：</p><ul><li>zoomEnabled：定点缩放</li><li>scrollEnabled：水平滑动</li><li>rotateEnabled：旋转</li><li>tiltEnabled：倾斜角度</li></ul><h3 id="动画跳转"><a href="#动画跳转" class="headerlink" title="动画跳转"></a>动画跳转</h3><p>使用<code>animateTo</code>函数完成跳转定位动画效果<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">this</span>.mapView.animateTo(<br>&#123;<br>tilt: <span class="hljs-number">0</span>,<br>rotation: <span class="hljs-number">0</span>,<br>zoomLevel: <span class="hljs-number">16</span>,<br>coordinate: &#123;<br>latitude: <span class="hljs-number">39.90864</span>,<br>longitude: <span class="hljs-number">116.39745</span><br>&#125;<br>&#125;,<br><span class="hljs-number">1000</span><br>);<br><br>...<br>&lt;MapView ref=&#123;ref=&gt;(<span class="hljs-keyword">this</span>.mapView = ref)&#125;<br>...<br></code></pre></td></tr></table></figure></p><h3 id="点击事件"><a href="#点击事件" class="headerlink" title="点击事件"></a>点击事件</h3><blockquote><p>onPress：点击事件<br>onLongPress：长按事件<br>onLocation：定位事件<br>onStatusChange：地图状态变化事件，变化时会一直调用<br>onStatusChangeComplete：地图状态变化结束事件<br>onAnimateFinish：动画完成事件<br>onAnimateCancel：动画取消事件</p></blockquote><h3 id="标记"><a href="#标记" class="headerlink" title="标记"></a>标记</h3><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">&lt;MapView&gt;<br>&lt;MapView.Marker&gt;<br>...<br></code></pre></td></tr></table></figure><blockquote><p>coordinate：坐标<br>title： 标题<br>description：描述<br>color：图标颜色，ios可选颜色较少<br>icon：自定义图标，通过自定义控件的方式修改图片<br>image：自定义图片，对应原生图片名称即系统各定义图片存放位置<br>opacity：透明度<br>draggable：是否可拖拽<br>flat：是否平贴地图<br>zIndex：层级<br>anchor：覆盖物锚点比例<br>centerOffset：覆盖物偏移位置<br>active：是否选中该marker<br>clickDisabled：是否禁用点击，默认不禁用<br>infoWindowDisabled：是否禁用弹出窗口，默认不禁用<br>onPress：点击事件<br>onDragStart：拖放开始事件<br>onDrag：拖放进行事件<br>onDragEnd：拖放结束事件<br>onInfoWindowPress：信息窗体点击事件，对于自定义信息窗体无效</p></blockquote><p>自定义弹出狂框则需要在<code>Marker</code>标签内部进行自定义。</p><h3 id="折线"><a href="#折线" class="headerlink" title="折线"></a>折线</h3><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">&lt;MapView&gt;<br>&lt;MapView.Polyline&gt;<br>...<br></code></pre></td></tr></table></figure><blockquote><p>coordinates：节点坐标<br>width：线段宽度<br>color：线段颜色<br>zIndex：层级<br>colors：多段颜色<br>gradient：是否使用渐变色<br>geodesic：是否使用大地线<br>dashed：是否绘制虚线<br>onPress：点击事件</p></blockquote><h3 id="多边形"><a href="#多边形" class="headerlink" title="多边形"></a>多边形</h3><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">&lt;MapView&gt;<br>&lt;MapView.Polyline&gt;<br>...<br></code></pre></td></tr></table></figure><blockquote><p>coordinates：节点数组<br>strokeWidth：边线宽度<br>strokeColor：边线颜色<br>fillColor：填充颜色<br>zIndex：层级</p></blockquote><h3 id="热力图"><a href="#热力图" class="headerlink" title="热力图"></a>热力图</h3><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">&lt;MapView&gt;<br>&lt;MapView.HeatMap&gt;<br>...<br></code></pre></td></tr></table></figure><blockquote><p>coordinates：节点坐标数组<br>radius：半径<br>opacity：透明度</p></blockquote><h3 id="海量图"><a href="#海量图" class="headerlink" title="海量图"></a>海量图</h3><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">&lt;MapView&gt;<br>&lt;MapView.MultiPoint&gt;<br>...<br></code></pre></td></tr></table></figure><blockquote><p>points：节点数组<br>image：海量图标，只接受原生图片<br>onItemPress：点击事件</p></blockquote>]]></content>
    
    
    <summary type="html">ReactNative配置高德地图</summary>
    
    
    
    <category term="ReactNative" scheme="http://example.com/categories/ReactNative/"/>
    
    
    <category term="ReactNative" scheme="http://example.com/tags/ReactNative/"/>
    
  </entry>
  
  <entry>
    <title>React 16.8 Hooks</title>
    <link href="http://example.com/2020/03/03/React-16-8-Hooks/"/>
    <id>http://example.com/2020/03/03/React-16-8-Hooks/</id>
    <published>2020-03-03T02:07:02.000Z</published>
    <updated>2021-04-17T07:10:38.652Z</updated>
    
    <content type="html"><![CDATA[<h2 id="一、介绍"><a href="#一、介绍" class="headerlink" title="一、介绍"></a>一、介绍</h2><p>使用过一段时间，特别是开发过App肯定清楚在组件使用时，会给不同的组件完成不同的state配置用来描述不同的工作任务。虽然简单控件容易编写页面，但是对于其状态逻辑的数据管理和跟踪却显得难以控制和复杂。<br>如：我们需要对一个简单的数据<code>count</code>参数在<code>componentDidMount</code>、<code>componentWillUnmount</code>来进行不可预计的处理，更不需要说<code>shouldComponentUpdate</code>来进行数据更新检测、<code>componentDidUpdate</code>检测部分更新完成数据的二次封装任务。更有情况我们需要对一个指定的数据修改功能添加2个，3个维护数据进行管理。<br>这样由于生命周期的管理必要，和多组数据的共同管理，使得在一个极小的组件里维护显得庞大。<br>为了解决这个问题，<strong>Hooks</strong>允许根据相关的部分（例如设置订阅或获取数据）将一个组件拆分为较小的功能，而不是基于生命周期方法强制拆分。</p><p>当然，<strong>Hooks</strong>是向后兼容，因此没有必须学习或者将项目全部修改。</p><p>二、State Hook</p><h2 id="const-state-setState-useState-initialState"><a href="#const-state-setState-useState-initialState" class="headerlink" title="const [state, setState] = useState(initialState);"></a><code>const [state, setState] = useState(initialState);</code></h2><h3 id="简单使用"><a href="#简单使用" class="headerlink" title="简单使用"></a>简单使用</h3><p>这是一个累加器。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">import</span> React, &#123; useState &#125; from <span class="hljs-string">&#x27;react&#x27;</span>;<br><span class="hljs-keyword">import</span> &#123;<br>  View,<br>  Text,<br>TouchableOpacity<br>&#125; from <span class="hljs-string">&#x27;react-native&#x27;</span>;<br><br><span class="hljs-keyword">const</span> Ex = function () &#123;<br><span class="hljs-keyword">const</span> [count, setCount] = useState(<span class="hljs-number">0</span>);<br><br><span class="hljs-keyword">return</span> (<br>&lt;View style=&#123;&#123;justifyContent: <span class="hljs-string">&#x27;flex-start&#x27;</span>, flexDirection: <span class="hljs-string">&#x27;column&#x27;</span>, alignItems: <span class="hljs-string">&#x27;center&#x27;</span>&#125;&#125;&gt;<br>&lt;Text&gt;&#123;count&#125;&lt;/Text&gt;<br>&lt;TouchableOpacity onPress=&#123;() =&gt; setCount(count+1)&#125;&gt;&lt;Text&gt;下一个&lt;/Text&gt;&lt;/TouchableOpacity&gt;<br>&lt;/View&gt;<br>);<br>&#125;<br>export <span class="hljs-keyword">default</span> Ex;<br></code></pre></td></tr></table></figure><br>看到这里返回了一个组件，而其中最注意的就是<code>useState()</code>。这是一个<strong>Hook</strong>，添加一个状态。React将会在重新渲染时保留这个状态，<code>userState()</code>函数返回一个当前的状态值(参数为初始化数值)和一个用于更新他的函数，他和<code>this.setState()</code>很像。<br>该语法为数组解构。创建的两个新变量为<code>count</code>和<code>setCount</code>。类似如下<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">var</span> countStateValue = useState(<span class="hljs-number">0</span>);<br><span class="hljs-keyword">var</span> first = countStateValue[<span class="hljs-number">0</span>];<br><span class="hljs-keyword">var</span> setFirst = countStateValue[<span class="hljs-number">1</span>];<br></code></pre></td></tr></table></figure></p><h3 id="初始化回调函数"><a href="#初始化回调函数" class="headerlink" title="初始化回调函数"></a>初始化回调函数</h3><p>也可以将初始化的工作放到回调函数中完成，类似class中的<code>constructor</code>构建，他只会在初始化的时候完成调用该回调函数。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">const</span> [count, setCount] = useState(() =&gt; &#123;<br>console.log(<span class="hljs-string">&#x27;-----&gt;初始化&#x27;</span>);<br><span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>&#125;)<br></code></pre></td></tr></table></figure></p><p>这里就类似最简单的，没有与生命周期相关的Hook。</p><blockquote><p>与class组件中的setState方法不同，useState不会自动合并更新对象。</p></blockquote><h2 id="三、Effect-Hook"><a href="#三、Effect-Hook" class="headerlink" title="三、Effect Hook"></a>三、Effect Hook</h2><p><code>useEffect(didUpdate);</code></p><h3 id="基本使用"><a href="#基本使用" class="headerlink" title="基本使用"></a>基本使用</h3><p><code>useEffect()</code>这里开始就与生命周期。他会影响到<code>componentDidMount</code>、<code>componentDidUpdate</code>和<code>componentWillUnmount</code>。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">const</span> ExEffect = function () &#123;<br><span class="hljs-keyword">const</span> [count, setCount] = useState(<span class="hljs-number">0</span>);<br><br>useEffect(() =&gt; &#123;<br>console.log(<span class="hljs-string">&#x27;-----&gt;&#x27;</span>);<br><span class="hljs-keyword">return</span> () =&gt; &#123;<br>console.log(<span class="hljs-string">&#x27;-----&gt;over&#x27;</span>);<br>&#125;<br>&#125;);<br><br><span class="hljs-keyword">return</span> (<br>&lt;View style=&#123;&#123;justifyContent: <span class="hljs-string">&#x27;flex-start&#x27;</span>, flexDirection: <span class="hljs-string">&#x27;column&#x27;</span>, alignItems: <span class="hljs-string">&#x27;center&#x27;</span>&#125;&#125;&gt;<br>&lt;Text&gt;&#123;count&#125;&lt;/Text&gt;<br>&lt;TouchableOpacity onPress=&#123;() =&gt; setCount(count+1)&#125;&gt;&lt;Text&gt;+1&lt;/Text&gt;&lt;/TouchableOpacity&gt;<br>&lt;/View&gt;<br>);<br>&#125;<br></code></pre></td></tr></table></figure></p><h3 id="与生命周期对比"><a href="#与生命周期对比" class="headerlink" title="与生命周期对比"></a>与生命周期对比</h3><p>看到使用<code>useEffect</code>函数第一个参数是一个函数，其内部的正常调用就是调用<code>componentDidMount</code>和<code>componentDidUpdate</code>时会调用除return外的所有部分；调用<code>componentWillUnmount</code>函数时会调用return函数内部内容。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">...<br>componentDidMount() &#123;<br>console.log(<span class="hljs-string">&#x27;-----&gt;&#x27;</span>);<br>&#125;<br>componentWillUnmount() &#123;<br>console.log(<span class="hljs-string">&#x27;-----&gt;over&#x27;</span>);<br>console.log(<span class="hljs-string">&#x27;-----&gt;&#x27;</span>);<br>&#125;<br>componentDidUpdate() &#123;<br>console.log(<span class="hljs-string">&#x27;-----&gt;over&#x27;</span>);<br>console.log(<span class="hljs-string">&#x27;-----&gt;&#x27;</span>);<br>&#125;<br></code></pre></td></tr></table></figure></p><h3 id="其他参数"><a href="#其他参数" class="headerlink" title="其他参数"></a>其他参数</h3><p>在开发优化时，根据判断来减少调用次数，这在开发中很常见。为了解决这个问题，<code>useEffect</code>提供了第二个参数来控制调用条件。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">const</span> ExEffect = function () &#123;<br><span class="hljs-keyword">const</span> [count, setCount] = useState(<span class="hljs-number">0</span>);<br><br>useEffect(() =&gt; &#123;<br>console.log(<span class="hljs-string">&#x27;-----&gt;&#x27;</span>);<br><span class="hljs-keyword">return</span> () =&gt; &#123;<br>console.log(<span class="hljs-string">&#x27;-----&gt;over&#x27;</span>);<br>&#125;<br>&#125;, []);<br>...<br></code></pre></td></tr></table></figure><br>第二个参数必须是数组格式，在传递空数组时，由于我们没有给控制条件(空数组)，所以在调用<code>setCount</code>更新state过程中<code>componentDidUpdate</code>函数会过滤掉，即只有<code>componentDidMount</code>和<code>componentWillUnmount</code>有意义。</p><p>如果添加数组内容：<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">const</span> ExEffect = function () &#123;<br><span class="hljs-keyword">const</span> [count, setCount] = useState(<span class="hljs-number">0</span>);<br><br>useEffect(() =&gt; &#123;<br>console.log(<span class="hljs-string">&#x27;-----&gt;&#x27;</span>);<br><span class="hljs-keyword">return</span> () =&gt; &#123;<br>console.log(<span class="hljs-string">&#x27;-----&gt;over&#x27;</span>);<br>&#125;<br>&#125;, [count]);<br>...<br></code></pre></td></tr></table></figure><br>只有在数组内的数值发生变化时，才会使该<code>useEffect</code>Hook在修改state时有意义，如果没有变化，则会继续过滤掉<code>componentDidUpdate</code>函数</p><h3 id="自定义Effect-Hooks"><a href="#自定义Effect-Hooks" class="headerlink" title="自定义Effect Hooks"></a>自定义Effect Hooks</h3><p>官方实例：<br>写一个判断朋友是否在线的功能，将这段状态抽出来。</p><p>这里判断某个id是否上线：<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">const</span> useFriendStatus = (friendId) =&gt; &#123;<br><span class="hljs-keyword">const</span> [isOnline, setIsOnline] = useState(<span class="hljs-keyword">null</span>);<br><br><span class="hljs-function">function <span class="hljs-title">handleStatusChange</span><span class="hljs-params">(status)</span> </span>&#123;<br>setIsOnline(status.isOnline);<br>&#125;<br><br>useEffect(() =&gt; &#123;<br>ChatAPI.subscribeToFriendStatus(friendId, handleStatusChange);<br><span class="hljs-keyword">return</span> () =&gt; &#123;<br>ChatAPI.unsubscribeFromFriendStatus(friendId, handleStatusChange);<br>&#125;;<br>&#125;);<br><br><span class="hljs-keyword">return</span> isOnline;<br>&#125;<br></code></pre></td></tr></table></figure></p><p>完成state状态封装后，我们就可以在两个组件中使用他：<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">const</span> FriendStatus = (props) =&gt; &#123;<br><span class="hljs-keyword">const</span> isOnline = useFriendStatus(props.friend.id);<br><br><span class="hljs-keyword">if</span> (isOnline === <span class="hljs-keyword">null</span>) &#123;<br><span class="hljs-keyword">return</span> <span class="hljs-string">&#x27;Loading...&#x27;</span>;<br>&#125;<br><span class="hljs-keyword">return</span> isOnline ? <span class="hljs-string">&#x27;Online&#x27;</span>:<span class="hljs-string">&#x27;Offline&#x27;</span>;<br>&#125;<br></code></pre></td></tr></table></figure></p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">const</span> <span class="hljs-title">FriendListItem</span><span class="hljs-params">(props)</span> </span>&#123;<br><span class="hljs-keyword">const</span> isOnline = useFriendStatus(props.friend.id);<br><br><span class="hljs-keyword">return</span> (<br>&lt;li style=&#123;&#123;color: isOnline? <span class="hljs-string">&#x27;green&#x27;</span>:<span class="hljs-string">&#x27;black&#x27;</span>&#125;&#125;&gt;<br>&#123;props.friend.name&#125;<br>&lt;/li&gt;<br>);<br>&#125;<br></code></pre></td></tr></table></figure><p>这些组件的状态是完全独立的。Hooks是重用有状态逻辑而不是状态本身的一种方法。因此每个对Hooks的调用都具有完全隔离的状态。<br>这里使用的函数名称<code>use</code>开头表示了自定义Hook的意思，并且内部也调用了其他Hook。类似一种约定命名。</p><h2 id="四、其他Hooks"><a href="#四、其他Hooks" class="headerlink" title="四、其他Hooks"></a>四、其他Hooks</h2><p>有一些不太常用的内置Hooks可能会有用。</p><h3 id="useContext"><a href="#useContext" class="headerlink" title="useContext"></a>useContext</h3><p><code>const value = useContext(MyContext);</code><br>该Hook接受一个<code>React.createContext()</code>返回的值。<strong>Context</strong>的作用就是对他所包含的组件树提供全局共享数据的一种技术规范。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">const</span> ThemeContext = React.createContext();<br><br><span class="hljs-keyword">const</span> ExContext = function () &#123;<br><span class="hljs-keyword">const</span> theme = useContext(ThemeContext);<br><br><span class="hljs-keyword">return</span> (<br>&lt;View style=&#123;&#123;backgroundColor: theme&#125;&#125;&gt;<br>&lt;TouchableOpacity &gt;&lt;Text&gt;Context&lt;/Text&gt;&lt;/TouchableOpacity&gt;<br>&lt;/View&gt;<br>);<br>&#125;<br><br><span class="hljs-comment">//根节点</span><br>render() &#123;<br><span class="hljs-keyword">return</span> (<br>&lt;ThemeContext.Provider value=<span class="hljs-string">&#x27;#00f&#x27;</span>&gt;<br>&lt;View&gt;<br>&lt;ExContext/&gt;<br>&lt;/View&gt;<br>&lt;/ThemeContext.Provider&gt;<br>);<br>&#125;<br></code></pre></td></tr></table></figure><br>必须将<code>&lt;ThemeContext.Provider&gt;</code>放置父组件位置才能传递内部存放数据。当这个数据更新时，将触发Hook来重新渲染。当这个渲染组件开销很大，可能需要看<a href="https://github.com/facebook/react/issues/15156#issuecomment-474590693">https://github.com/facebook/react/issues/15156#issuecomment-474590693</a>来优化。<br>使用<code>useContext</code>后就会在这里得到父节点传递下来的数据内容。</p><h3 id="useReducer"><a href="#useReducer" class="headerlink" title="useReducer"></a>useReducer</h3><p><code>const [state, dispatch] = useReducer(reducer, initialArg, init);</code><br><code>useState</code>的高级Hook。看名字就知道他是一个Reducer函数，<strong>(state, action) => newState</strong>。与Redux具有相同的工作原理。<br>先来看看使用<code>useReducer</code>的实例：<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">const</span> ExReducer = function () &#123;<br><span class="hljs-keyword">const</span> initialState = &#123;count: <span class="hljs-number">0</span>&#125;;<br><span class="hljs-keyword">const</span> [state, dispatch] = useReducer(reducer, initialState);<br><br><span class="hljs-keyword">return</span> (<br>&lt;View style=&#123;&#123;justifyContent: <span class="hljs-string">&#x27;flex-start&#x27;</span>, flexDirection: <span class="hljs-string">&#x27;column&#x27;</span>, alignItems: <span class="hljs-string">&#x27;center&#x27;</span>&#125;&#125;&gt;<br>&lt;Text&gt;&#123;state.count&#125;&lt;/Text&gt;<br>&lt;TouchableOpacity onPress=&#123;() =&gt; dispatch(&#123;type: &#x27;t1&#x27;&#125;)&#125;&gt;&lt;Text&gt;+1&lt;/Text&gt;&lt;/TouchableOpacity&gt;<br>&lt;/View&gt;<br>);<br>&#125;<br><span class="hljs-keyword">const</span> reducer = (state, action) =&gt; &#123;<br><span class="hljs-keyword">switch</span>(action.type) &#123;<br><span class="hljs-keyword">case</span> <span class="hljs-string">&#x27;t1&#x27;</span>:<br><span class="hljs-keyword">return</span> &#123;count: state.count+<span class="hljs-number">1</span>&#125;;<br><span class="hljs-keyword">break</span>;<br><span class="hljs-keyword">case</span> <span class="hljs-string">&#x27;t2&#x27;</span>:<br><span class="hljs-keyword">return</span> &#123;count: state.count-<span class="hljs-number">1</span>&#125;;<br><span class="hljs-keyword">break</span>;<br>&#125;<br>&#125;<br></code></pre></td></tr></table></figure><br>这样就完全模拟出<code>useState</code>时完成的功能状态。</p><h5 id="延迟初始化"><a href="#延迟初始化" class="headerlink" title="延迟初始化"></a>延迟初始化</h5><p>添加第三个传递参数完成延迟初始状态。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">const</span> ExReducer = function () &#123;<br><span class="hljs-keyword">const</span> initialState = &#123;count: <span class="hljs-number">0</span>&#125;;<br><span class="hljs-keyword">const</span> [state, dispatch] = useReducer(reducer, initialState, init);<br><br><span class="hljs-keyword">return</span> (<br>&lt;View style=&#123;&#123;justifyContent: <span class="hljs-string">&#x27;flex-start&#x27;</span>, flexDirection: <span class="hljs-string">&#x27;column&#x27;</span>, alignItems: <span class="hljs-string">&#x27;center&#x27;</span>&#125;&#125;&gt;<br>&lt;Text&gt;&#123;state.count&#125;&lt;/Text&gt;<br>&lt;TouchableOpacity onPress=&#123;() =&gt; dispatch(&#123;type: &#x27;t1&#x27;&#125;)&#125;&gt;&lt;Text&gt;+1&lt;/Text&gt;&lt;/TouchableOpacity&gt;<br>&lt;TouchableOpacity onPress=&#123;() =&gt; dispatch(&#123;type: &#x27;reset&#x27;, payload: initialState&#125;)&#125;&gt;&lt;Text&gt;reset&lt;/Text&gt;&lt;/TouchableOpacity&gt;<br>&lt;/View&gt;<br>);<br>&#125;<br><span class="hljs-keyword">const</span> init = (initialState) =&gt; &#123;<br><span class="hljs-keyword">return</span> &#123;count: initialState.count&#125;;<br>&#125;<br><span class="hljs-keyword">const</span> reducer = (state, action) =&gt; &#123;<br><span class="hljs-keyword">switch</span>(action.type) &#123;<br><span class="hljs-keyword">case</span> <span class="hljs-string">&#x27;t1&#x27;</span>:<br><span class="hljs-keyword">return</span> &#123;count: state.count+<span class="hljs-number">1</span>&#125;;<br><span class="hljs-keyword">break</span>;<br><span class="hljs-keyword">case</span> <span class="hljs-string">&#x27;t2&#x27;</span>:<br><span class="hljs-keyword">return</span> &#123;count: state.count-<span class="hljs-number">1</span>&#125;;<br><span class="hljs-keyword">break</span>;<br><span class="hljs-keyword">case</span> <span class="hljs-string">&#x27;reset&#x27;</span>:<br><span class="hljs-keyword">return</span> init(action.payload);<br><span class="hljs-keyword">break</span>;<br>&#125;<br>&#125;<br></code></pre></td></tr></table></figure><br>这里添加了一个<code>init</code>初始化函数参数，可以在这里添加需要初始化的功能(这个初始化也会在复位的时候重复调用)。这也很方便某个操作的重置状态。<br>React使用<code>Object.is</code>比较算法，判断Hook中返回与当前状态相同的值，则React将不会进行数据返回。</p><h3 id="useMemo"><a href="#useMemo" class="headerlink" title="useMemo"></a>useMemo</h3><p><code>const memoizedValue = useMemo(() =&gt; computeExpensiveValue(a, b), [a, b]);</code><br>返回一个<strong>memoized</strong>数值。</p><h5 id="memo大致"><a href="#memo大致" class="headerlink" title="memo大致"></a>memo大致</h5><p><strong>memoize</strong>是一种<strong>JavaScript</strong>技术，通过缓存数据的参数特征与其对应结果，来提升数据重复调用的速度。核心思想就是通过在每个指定功能函数中添加一个数组，来存储刚刚说的入参特征与其对应结果，来达到提升效率的方法。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">memoize = (fn) =&gt; &#123;<br><span class="hljs-keyword">return</span> function() &#123;<br><span class="hljs-keyword">var</span> args = Array.prototype.slice.call(arguments);<br>fn.cache = fn.cache || &#123;&#125;;<br><span class="hljs-keyword">return</span> fn.cache[args]? fn.cache[args] : (fn.cache[args] = fn.apply(<span class="hljs-keyword">this</span>, args));<br>&#125;<br>&#125;<br></code></pre></td></tr></table></figure><br>当我们将存储层级提升到<code>Function</code>原型中，就可以在每个函数内部找到<code>memoize</code>函数，并完成调用。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">Function.prototype.memoize = function() &#123;<br>    <span class="hljs-keyword">var</span> self = <span class="hljs-keyword">this</span><br>    <span class="hljs-keyword">return</span> function () &#123;<br>        <span class="hljs-keyword">var</span> args = Array.prototype.slice.call(arguments)<br>        self.cache = self.cache || &#123;&#125;;<br>        <span class="hljs-keyword">return</span> self.cache[args] ? self.cache[args] : (self.cache[args] = self(args))<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure></p><h5 id="回到Hook"><a href="#回到Hook" class="headerlink" title="回到Hook"></a>回到Hook</h5><p>相似的，<code>useMemo</code>思路与<code>memo</code>相似。他也会保存指定数据的最近数值根据对比判断是否需要对页面进行重新渲染的过程。<br>先看一个反例：<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">const</span> ExMemo = function() &#123;<br><span class="hljs-keyword">const</span> [count, setCount] = useState(<span class="hljs-number">0</span>);<br><span class="hljs-keyword">const</span> [value, setValue] = useState(<span class="hljs-number">0</span>);<br><br><span class="hljs-function">function <span class="hljs-title">sum</span><span class="hljs-params">()</span> </span>&#123;<br>console.log(<span class="hljs-string">&#x27;-----&gt;至查看count值&#x27;</span>);<br><span class="hljs-keyword">return</span> count + <span class="hljs-number">1</span>;<br>&#125;<br><br><span class="hljs-keyword">return</span> (<br>&lt;View style=&#123;&#123;justifyContent: <span class="hljs-string">&#x27;flex-start&#x27;</span>, flexDirection: <span class="hljs-string">&#x27;column&#x27;</span>, alignItems: <span class="hljs-string">&#x27;center&#x27;</span>&#125;&#125;&gt;<br>&lt;Text&gt;&#123;sum()&#125;&lt;/Text&gt;<br>&lt;TouchableOpacity onPress=&#123;() =&gt; &#123;<br>setCount(count+<span class="hljs-number">1</span>);<br>&#125;&#125;&gt;&lt;Text&gt;Btn1&lt;/Text&gt;&lt;/TouchableOpacity&gt;<br>&lt;TouchableOpacity onPress=&#123;() =&gt; &#123;<br>setValue(value+<span class="hljs-number">1</span>);<br>&#125;&#125;&gt;&lt;Text&gt;Btn2&lt;/Text&gt;&lt;/TouchableOpacity&gt;<br>&lt;/View&gt;<br>)<br>&#125;<br></code></pre></td></tr></table></figure><br>这里有两个状态<strong>count</strong>和<strong>value</strong>，并添加两个对应的按钮来分别给这两个数据做累加处理，而<strong>sum</strong>函数只会对<strong>count</strong>进行一次查询处理。这个过程，无论是修改<strong>count</strong>还是<strong>value</strong>都会引起函数<strong>sum</strong>的调用，但是这个函数的关心点只有<strong>count</strong>。这就引出了多余的计算问题。</p><p>而<code>useMemo</code>则是解决这个问题的方式。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">const</span> ExMemo = function() &#123;<br><span class="hljs-keyword">const</span> [count, setCount] = useState(<span class="hljs-number">0</span>);<br><span class="hljs-keyword">const</span> [value, setValue] = useState(<span class="hljs-number">0</span>);<br><br><span class="hljs-keyword">const</span> sumValue = useMemo(() =&gt; &#123;<br>console.log(<span class="hljs-string">&#x27;-----&gt;至查看count值&#x27;</span>);<br><span class="hljs-keyword">return</span> count + <span class="hljs-number">1</span>;<br>&#125;, [count]);<br><br><span class="hljs-keyword">return</span> (<br>&lt;View style=&#123;&#123;justifyContent: <span class="hljs-string">&#x27;flex-start&#x27;</span>, flexDirection: <span class="hljs-string">&#x27;column&#x27;</span>, alignItems: <span class="hljs-string">&#x27;center&#x27;</span>&#125;&#125;&gt;<br>&lt;Text&gt;&#123;sumValue&#125;&lt;/Text&gt;<br>&lt;TouchableOpacity onPress=&#123;() =&gt; &#123;<br>setCount(count+<span class="hljs-number">1</span>);<br>&#125;&#125;&gt;&lt;Text&gt;Btn1&lt;/Text&gt;&lt;/TouchableOpacity&gt;<br>&lt;TouchableOpacity onPress=&#123;() =&gt; &#123;<br>setValue(value+<span class="hljs-number">1</span>);<br>&#125;&#125;&gt;&lt;Text&gt;Btn2&lt;/Text&gt;&lt;/TouchableOpacity&gt;<br>&lt;/View&gt;<br>)<br>&#125;<br></code></pre></td></tr></table></figure><br>这里就可以看到，当修改<strong>value</strong>参数时，这个调用函数不会被执行，而是通过缓存提升速度。</p><h3 id="useCallback"><a href="#useCallback" class="headerlink" title="useCallback"></a>useCallback</h3><p><code>const memoizedCallback = useCallback(  () =&gt; &#123;    doSomething(a, b);  &#125;,  [a, b],);</code><br>这里和<code>useMemo</code>的思想类似。但这里缓存的是函数。</p><h3 id="useRef"><a href="#useRef" class="headerlink" title="useRef"></a>useRef</h3><p><code>const refContainer = useRef(initialValue);</code><br>返回一个可变的ref对象。通过<code>.current</code>对象来保存。<br>ref本是用来获取组件实例对象或Dom对象。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">const</span> ExRef = function() &#123;<br><span class="hljs-keyword">const</span> [count, setCount] = useState(<span class="hljs-number">0</span>);<br><span class="hljs-keyword">const</span> thisRef = useRef();<br><span class="hljs-keyword">const</span> doubleCount = useMemo(() =&gt; &#123;<br><span class="hljs-keyword">return</span> <span class="hljs-number">2</span>*count;<br>&#125;, [count]);<br>useEffect(() =&gt; &#123;<br>thisRef.current = setInterval(() =&gt; &#123;<br>setCount(count =&gt; count+<span class="hljs-number">1</span>);<br>&#125;, <span class="hljs-number">1000</span>);<br>&#125;, []);<br>useEffect(() =&gt; &#123;<br><span class="hljs-keyword">if</span> (count&gt;<span class="hljs-number">10</span>)<br>clearInterval(thisRef.current);<br>&#125;)<br><br><span class="hljs-keyword">return</span> (<br>&lt;View ref=&#123;thisRef&#125; style=&#123;&#123;justifyContent: <span class="hljs-string">&#x27;flex-start&#x27;</span>, flexDirection: <span class="hljs-string">&#x27;column&#x27;</span>, alignItems: <span class="hljs-string">&#x27;center&#x27;</span>&#125;&#125;&gt;<br>&lt;Text&gt;&#123;count&#125;-&#123;doubleCount&#125;&lt;/Text&gt;<br>&lt;/View&gt;<br>)<br>&#125;<br></code></pre></td></tr></table></figure></p><h3 id="useImperativeHandle"><a href="#useImperativeHandle" class="headerlink" title="useImperativeHandle"></a>useImperativeHandle</h3><p><code>useImperativeHandle(ref, createHandle, [deps])</code><br>用于自定义暴露给父组件的ref属性。需要配个<code>forwardRef</code>一起使用。</p><p>先看看子组件：<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">var</span> ExImperativeHandle = function(props, ref) &#123;<br><span class="hljs-keyword">const</span> textRef = useRef();<br>useImperativeHandle(ref, () =&gt; (&#123;<br>value: textRef.current<br>&#125;));<br><span class="hljs-keyword">return</span> (<br>&lt;View&gt;<br>&lt;Text ref=&#123;textRef&#125;&gt;DDD&lt;/Text&gt;<br>&lt;/View&gt;<br>);<br>&#125;<br>ExImperativeHandle = forwardRef(ExImperativeHandle);<br>export &#123;<br>ExImperativeHandle<br>&#125;;<br></code></pre></td></tr></table></figure><br>看到这里首先准备了Ref对象，将这个对象里面的数据暴露给父组件使用。这个过程就使用了<code>useImperativeHandle</code>，完成后还需要前面提到的<code>forwardRef</code>。</p><h3 id="useLayoutEffect"><a href="#useLayoutEffect" class="headerlink" title="useLayoutEffect"></a>useLayoutEffect</h3><p>和前面的<code>useEffect</code>相同。但它会在所有的 DOM 变更之后同步调用 effect。可以使用它来读取 DOM 布局并同步触发重渲染。在浏览器执行绘制之前，useLayoutEffect 内部的更新计划将被同步刷新。尽可能使用标准的 useEffect 以避免阻塞视觉更新。</p><h3 id="useDebugValue"><a href="#useDebugValue" class="headerlink" title="useDebugValue"></a>useDebugValue</h3><p>用于在React DevTools中显示自定义Hooks的标签。会在自定义Hoos标签中调试</p>]]></content>
    
    
    <summary type="html">React 16.8新功能Hooks</summary>
    
    
    
    <category term="ReactNative" scheme="http://example.com/categories/ReactNative/"/>
    
    
    <category term="React" scheme="http://example.com/tags/React/"/>
    
  </entry>
  
  <entry>
    <title>React-Native原生通信</title>
    <link href="http://example.com/2020/01/06/React-Native%E5%8E%9F%E7%94%9F%E9%80%9A%E4%BF%A1/"/>
    <id>http://example.com/2020/01/06/React-Native%E5%8E%9F%E7%94%9F%E9%80%9A%E4%BF%A1/</id>
    <published>2020-01-06T08:34:00.000Z</published>
    <updated>2021-04-17T07:10:38.652Z</updated>
    
    <content type="html"><![CDATA[<h2 id="一、Android-通信"><a href="#一、Android-通信" class="headerlink" title="一、Android 通信"></a>一、Android 通信</h2><p>在使用通信时分为自定义控件和功能组件两个部分。但是两者都离不开以下几个类：</p><ul><li>ReactPackage: 总管理包</li><li>ReactContextBaseJavaModule: 管理模块</li><li>SimpleViewManager: 管理控件</li></ul><h2 id="1-1-ReactPackage"><a href="#1-1-ReactPackage" class="headerlink" title="1.1 ReactPackage"></a>1.1 ReactPackage</h2><h4 id="1-1-1-初始化Package"><a href="#1-1-1-初始化Package" class="headerlink" title="1.1.1 初始化Package"></a>1.1.1 初始化Package</h4><p>为了将Android功能接入ReactNative，首先需要定义<em>ReactPackage</em>。实现功能，实现的功能分为<em>ReactModule</em>和<em>ViewManager</em>。<br>如下面代码：<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">import</span> com.facebook.react.ReactPackage;<br><span class="hljs-keyword">import</span> com.facebook.react.bridge.NativeModule;<br><span class="hljs-keyword">import</span> com.facebook.react.bridge.ReactApplicationContext;<br><span class="hljs-keyword">import</span> com.facebook.react.uimanager.ViewManager;<br><br><span class="hljs-keyword">import</span> java.util.Arrays;<br><br><span class="hljs-keyword">import</span> androidx.annotation.NonNull;<br><br><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">GAMapPackage</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">ReactPackage</span> </span>&#123;<br><br>    <span class="hljs-meta">@NonNull</span><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-function"><span class="hljs-keyword">public</span> List&lt;NativeModule&gt; <span class="hljs-title">createNativeModules</span><span class="hljs-params">(<span class="hljs-meta">@NonNull</span> ReactApplicationContext reactContext)</span> </span>&#123;<br>        <span class="hljs-keyword">return</span> Arrays.asList();<br>    &#125;<br><br>    <span class="hljs-meta">@NonNull</span><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-function"><span class="hljs-keyword">public</span> List&lt;ViewManager&gt; <span class="hljs-title">createViewManagers</span><span class="hljs-params">(<span class="hljs-meta">@NonNull</span> ReactApplicationContext reactContext)</span> </span>&#123;<br>        <span class="hljs-keyword">return</span> Arrays.asList();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><br>完成后就做好了接入RN层的功能入口，如果需要手动加入则需要找到<em>Application</em>中添加该功能库。<strong>需要注意，上的两个接口函数一定要有返回值，如果返回为null，则无法运行。</strong><br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">MainApplication</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">Application</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">ReactApplication</span> </span>&#123;<br>...<br>    List&lt;ReactPackage&gt; packages = <span class="hljs-keyword">new</span> PackageList(<span class="hljs-keyword">this</span>).getPackages();<br>...<br></code></pre></td></tr></table></figure><br>上面代码会进行对所有添加的<em>package</em>进行初始化操作，而我们要做的就是进入<em>getPackages</em>中，手动添加。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">package</span> com.facebook.react;<br><br><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">PackageList</span> </span>&#123;<br>...<br>  <span class="hljs-function"><span class="hljs-keyword">public</span> ArrayList&lt;ReactPackage&gt; <span class="hljs-title">getPackages</span><span class="hljs-params">()</span> </span>&#123;<br>    <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> ArrayList&lt;&gt;(Arrays.&lt;ReactPackage&gt;asList(<br>      <span class="hljs-keyword">new</span> MainReactPackage(mConfig),<br>      <span class="hljs-keyword">new</span> RNCMaskedViewPackage(),<br>      <span class="hljs-keyword">new</span> RNGestureHandlerPackage(),<br><span class="hljs-comment">// 这里添加该功能库即可，如果是使用Library的方式添加，就不需要手动添加</span><br>    ));<br>  &#125;<br>&#125;<br></code></pre></td></tr></table></figure><br>如上，但是要注意，如果是在原有Android项目下添加<em>package</em>就需要手动添加，但是如果使用RN的Library方式添加的话，就不需要进行手动处理操作。另外需要注意最好在自定义添加package的时候，在Application中添加，因为package的创建会在每次编译后重置掉。</p><h4 id="1-1-2-package的组成"><a href="#1-1-2-package的组成" class="headerlink" title="1.1.2 package的组成"></a>1.1.2 package的组成</h4><p>实现后可以看到两个接口函数：</p><blockquote><p>public List<NativeModule> createNativeModules(@NonNull ReactApplicationContext reactContext)<br>public List<ViewManager> createViewManagers(@NonNull ReactApplicationContext reactContext)</p></blockquote><p>根据函数所需要的泛型不难看出重要的就是<em>NativeModule</em>和<em>ViewManager</em>两个类型。前者用来实现功能方法，后者可以用来实现UI控件。<br>唯一参数<em>ReactApplicationContext</em>则是该库所在的上下文对象。其内部封装了很多实用的函数，如：getJSModule()、getCurrentActivity()，用来完成js层通信和与主要的activity的沟通功能。</p><h4 id="1-1-3-Js-对应-Java-的所有类型映射"><a href="#1-1-3-Js-对应-Java-的所有类型映射" class="headerlink" title="1.1.3 Js 对应 Java 的所有类型映射"></a>1.1.3 Js 对应 Java 的所有类型映射</h4><div class="table-container"><table><thead><tr><th>JavaScript</th><th>Java</th></tr></thead><tbody><tr><td>Bool</td><td>Boolean</td></tr><tr><td>Number</td><td>Integer</td></tr><tr><td>Number</td><td>Float</td></tr><tr><td>Number</td><td>Double</td></tr><tr><td>String</td><td>String</td></tr><tr><td>ReadableMap、WritableMap</td><td>Object</td></tr><tr><td>ReadableArray、WritableArray</td><td>Array</td></tr></tbody></table></div><h2 id="1-2-ReactModule"><a href="#1-2-ReactModule" class="headerlink" title="1.2 ReactModule"></a>1.2 ReactModule</h2><h4 id="1-2-1-初始化ReactModule"><a href="#1-2-1-初始化ReactModule" class="headerlink" title="1.2.1 初始化ReactModule"></a>1.2.1 初始化ReactModule</h4><p>这里继承<em>ReactContextBaseJavaModule</em>，该库内部实现<em>NativeModule</em>。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">GAOfflineMapModule</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">ReactContextBaseJavaModule</span> </span>&#123;<br><br><span class="hljs-keyword">private</span> ReactApplicationContext context;<br><br><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">GAOfflineMapModule</span><span class="hljs-params">(<span class="hljs-meta">@NonNull</span> ReactApplicationContext reactContext)</span> </span>&#123;<br><span class="hljs-comment">// 注意这里需要调用super，将Context传递过去</span><br><span class="hljs-keyword">super</span>(reactContext);<br><span class="hljs-keyword">this</span>.context = reactContext;<br>&#125;<br><br><span class="hljs-meta">@NonNull</span><br><span class="hljs-meta">@Override</span><br><span class="hljs-function"><span class="hljs-keyword">public</span> String <span class="hljs-title">getName</span><span class="hljs-params">()</span> </span>&#123;<br><span class="hljs-comment">// 该Module在js层定义的名称</span><br><span class="hljs-keyword">return</span> <span class="hljs-string">&quot;GaoModule&quot;</span>;<br>&#125;<br><br>...<br>&#125;<br></code></pre></td></tr></table></figure></p><h4 id="1-2-2-在JS层找到该库"><a href="#1-2-2-在JS层找到该库" class="headerlink" title="1.2.2 在JS层找到该库"></a>1.2.2 在JS层找到该库</h4><p>完成初始化java层库后，就需要在js层找到该库，这样我们就可以完成中间穿透，进行函数调用。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">import</span> &#123; NativeModules, NativeEventEmitter &#125; <span class="hljs-keyword">from</span> <span class="hljs-string">&#x27;react-native&#x27;</span>;<br><br><span class="hljs-keyword">const</span> &#123; GaoModule &#125; = NativeModules;<br><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">default</span> GaoModule;<span class="hljs-comment">// 使用该类直接调用java层函数</span><br></code></pre></td></tr></table></figure><br>这样就完成了最简单的双向初始化操作，后面只需要添加指定的函数，就可以进行完整的函数调用、函数回调操作。</p><h4 id="1-2-3-从-JS-到-Java"><a href="#1-2-3-从-JS-到-Java" class="headerlink" title="1.2.3 从 JS 到 Java"></a>1.2.3 从 JS 到 Java</h4><p>如果需要将函数暴露给JS层，则需要使用注解<em>@ReactMethod</em><br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">...<br><span class="hljs-meta">@ReactMethod</span><br><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">test</span><span class="hljs-params">(<span class="hljs-keyword">int</span> a)</span> </span>&#123;<br>...<br>&#125;<br><br><span class="hljs-meta">@ReactMethod</span><br><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">test</span><span class="hljs-params">(<span class="hljs-keyword">float</span> a)</span> </span>&#123;<br>...<br>&#125;<br><br><span class="hljs-meta">@ReactMethod</span><br><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">test</span><span class="hljs-params">(ReadableArray a, ReadableMap b)</span> </span>&#123;<br>...<br>a.getString(<span class="hljs-number">0</span>);<br>a.getInt(<span class="hljs-number">1</span>);<br><br>b.getString(<span class="hljs-string">&#x27;key&#x27;</span>);<br>&#125;<br></code></pre></td></tr></table></figure><br>完成函数定义和注解后，就可以在JS层找到该函数了<br><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">GaoModule.test(<span class="hljs-number">1</span>);<br>GaoModule.test(<span class="hljs-number">1.1</span>);<br>GaoModule.test([<span class="hljs-string">&#x27;1&#x27;</span>, <span class="hljs-number">2</span>], &#123; <span class="hljs-string">&#x27;key&#x27;</span>: <span class="hljs-string">&#x27;value&#x27;</span> &#125;);<br></code></pre></td></tr></table></figure><br><strong><em>注意：整个过程在代码编写后需要编译操作，否则会出现意想不到的问题。多 clean 多 rebuild 。</em></strong></p><h4 id="1-2-4-从-JS-到-Java-并回调数据给-JS"><a href="#1-2-4-从-JS-到-Java-并回调数据给-JS" class="headerlink" title="1.2.4 从 JS 到 Java 并回调数据给 JS"></a>1.2.4 从 JS 到 Java 并回调数据给 JS</h4><p><strong><em>数据回调是异步的过程。</em></strong>回调也提供了多种手段。</p><h6 id="Callback"><a href="#Callback" class="headerlink" title="Callback"></a>Callback</h6><p>重新定义一个函数，并在这个函数最后使用Callback对象进行处理回调方式<em>invoke</em>。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">import</span> com.facebook.react.bridge.Callback;<br>...<br><br><span class="hljs-meta">@ReactMethod</span><br><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">test</span><span class="hljs-params">(<span class="hljs-keyword">int</span> a, <span class="hljs-keyword">int</span> b, Callback callback)</span> </span>&#123;<br>   callback.invoke((a + b));<br>&#125;<br></code></pre></td></tr></table></figure><br>上面的invoke函数接收的是多个参数，因此可以在后面继续添加。<br>然后在Js层可以这么做:<br><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">GaoModule.test(<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-function">(<span class="hljs-params">result</span>) =&gt;</span> &#123;<br><span class="hljs-built_in">this</span>.setState(&#123; <span class="hljs-attr">data</span>: result &#125;);<br>&#125;);<br></code></pre></td></tr></table></figure><br>但是由于多数情况在异步操作随时可能出现异常情况，在异常处理后返回的数据无法在js层正常获取，因此在多数情况下会使用两个<em>Callback</em>来管理回调预测的完整性。<br><figure class="highlight typescript"><table><tr><td class="code"><pre><code class="hljs typescript"><span class="hljs-comment">// ---------JAVA</span><br><span class="hljs-keyword">import</span> com.facebook.react.bridge.Callback;<br>...<br><br><span class="hljs-meta">@ReactMethod</span><br><span class="hljs-keyword">public</span> <span class="hljs-built_in">void</span> <span class="hljs-function"><span class="hljs-title">test</span>(<span class="hljs-params">int a, int b, Callback successCallback, Callback failCallback</span>)</span> &#123;<br><span class="hljs-keyword">try</span> &#123;<br>successCallback.invoke((a + b));<br>&#125; <span class="hljs-keyword">catch</span>(Exception e) &#123;<br>failCallback.invoke(<span class="hljs-string">&#x27;err&#x27;</span>);<br>&#125;<br>&#125;<br><br><span class="hljs-comment">// ----------JS</span><br>GaoModule.test(<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-function">(<span class="hljs-params">result</span>) =&gt;</span> &#123;<br><span class="hljs-built_in">this</span>.setState(&#123; <span class="hljs-attr">data</span>: result &#125;);<br>&#125;, <span class="hljs-function">(<span class="hljs-params">err</span>) =&gt;</span> &#123;<br><span class="hljs-built_in">console</span>.log(err)<br>&#125;);<br></code></pre></td></tr></table></figure></p><h6 id="Promise"><a href="#Promise" class="headerlink" title="Promise"></a>Promise</h6><p>es6提供了<em>promise</em>来完成异步操作，同样也可以适用于原生调用：<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">import</span> com.facebook.react.bridge.Promise;<br>...<br><br><span class="hljs-meta">@ReactMethod</span><br><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">test</span><span class="hljs-params">(<span class="hljs-keyword">int</span> a, <span class="hljs-keyword">int</span> b, Promise promise)</span> </span>&#123;<br>promise.resolve((a + b));<br><span class="hljs-comment">// promise.reject(&quot;404&quot;, &quot;error&quot;);</span><br>&#125;<br></code></pre></td></tr></table></figure><br>熟悉promise可以知道具有两个回调方式，但是只能使用一种方式来完成回调。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">GaoModule.test(<span class="hljs-number">1</span>, <span class="hljs-number">2</span>)<br>.then(<span class="hljs-function"><span class="hljs-params">data</span> =&gt;</span> &#123;<br><span class="hljs-built_in">this</span>.setState(&#123; <span class="hljs-attr">data</span>: result &#125;);<br>&#125;)<br>.catch(<span class="hljs-function"><span class="hljs-params">err</span> =&gt;</span> &#123;<br><span class="hljs-built_in">console</span>.log(err)<br>&#125;);<br></code></pre></td></tr></table></figure><br>相对于Callback，promise更具有完整的异步模式。</p><h4 id="1-2-5-从-Java-到-JS"><a href="#1-2-5-从-Java-到-JS" class="headerlink" title="1.2.5 从 Java 到 JS"></a>1.2.5 从 Java 到 JS</h4><p>流程如下：</p><blockquote><p>1.注册监听回调<br>2.监听回调下完成java层内容到js层的所有数据<br>3.取消注册监听</p></blockquote><h6 id="代码实例"><a href="#代码实例" class="headerlink" title="代码实例"></a>代码实例</h6><p>首先在java层需要做一些准备，需要准备<em>emit</em>函数来完成到js层的函数入口。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">GAOfflineMapModule</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">ReactContextBaseJavaModule</span> </span>&#123;<br><br><span class="hljs-keyword">private</span> ReactApplicationContext context;<br><br><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">GAOfflineMapModule</span><span class="hljs-params">(<span class="hljs-meta">@NonNull</span> ReactApplicationContext reactContext)</span> </span>&#123;<br><span class="hljs-comment">// 注意这里需要调用super，将Context传递过去</span><br><span class="hljs-keyword">super</span>(reactContext);<br><span class="hljs-keyword">this</span>.context = reactContext;<br>&#125;<br><br><span class="hljs-meta">@NonNull</span><br><span class="hljs-meta">@Override</span><br><span class="hljs-function"><span class="hljs-keyword">public</span> String <span class="hljs-title">getName</span><span class="hljs-params">()</span> </span>&#123;<br><span class="hljs-comment">// 该Module在js层定义的名称</span><br><span class="hljs-keyword">return</span> <span class="hljs-string">&quot;GaoModule&quot;</span>;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">emit</span><span class="hljs-params">(String key, Object value)</span> </span>&#123;<br><span class="hljs-keyword">try</span> &#123;<br>context.getJSModule(DeviceEventManagerModule.RCTDeviceEventEmitter.class).emit(key, value);<br>&#125; <span class="hljs-keyword">catch</span> (InterruptedException e) &#123;<br>e.printStackTrace();<br>&#125;<br>&#125;<br>&#125;<br></code></pre></td></tr></table></figure><br>可以看到<em>emit</em>函数所需要的两个参数解释：</p><blockquote><p>key: 监听对象<br>value: 数据回调，注意格式必须是之前表格的js对应格式。</p></blockquote><p>首先需要找到该监听器对象：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">import</span> &#123; NativeModules, NativeEventEmitter &#125; <span class="hljs-keyword">from</span> <span class="hljs-string">&#x27;react-native&#x27;</span>;<br><br><span class="hljs-keyword">const</span> &#123; GaoModule &#125; = NativeModules;<br><span class="hljs-keyword">const</span> listenerGaoModule = <span class="hljs-keyword">new</span> NativeEventEmitter(ImSdkModule);<br>GaoModule.listenerGaoModule = listenerGaoModule;<br><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">default</span> GaoModule;<br></code></pre></td></tr></table></figure><br>进行注册：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">import</span> GaoModule <span class="hljs-keyword">from</span> <span class="hljs-string">&#x27;...&#x27;</span>;<br><br>...<br><span class="hljs-function"><span class="hljs-title">componentDidMount</span>(<span class="hljs-params"></span>)</span> &#123;<br><span class="hljs-comment">// 注册</span><br><span class="hljs-built_in">this</span>.listener = GaoModule.listener.addListener(key, <span class="hljs-function">(<span class="hljs-params">value</span>) =&gt;</span> &#123;<br><span class="hljs-comment">// 逻辑处理</span><br>&#125;);<br>&#125;<br><br><span class="hljs-function"><span class="hljs-title">componentWillUnmount</span>(<span class="hljs-params"></span>)</span> &#123;<br><span class="hljs-comment">// 取消注册</span><br>GaoModule.listener.removeListener(<br><span class="hljs-built_in">this</span>.listener<br>);<br>&#125;<br></code></pre></td></tr></table></figure></p><h2 id="1-3-ViewManager"><a href="#1-3-ViewManager" class="headerlink" title="1.3 ViewManager"></a>1.3 ViewManager</h2><p>用于完成UI组件部分功能。<br>说到组件Android就会想到<em>ViewGroup</em>和<em>View</em>，而这里也可以有迹可循。</p><h4 id="SimpleViewManager"><a href="#SimpleViewManager" class="headerlink" title="SimpleViewManager"></a>SimpleViewManager</h4><h6 id="1-创建java层viewManager"><a href="#1-创建java层viewManager" class="headerlink" title="1.创建java层viewManager"></a>1.创建java层viewManager</h6><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">DemoViewManager</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">SimpleViewManager</span>&lt;<span class="hljs-title">TextView</span>&gt; </span>&#123;<br>    <span class="hljs-meta">@NonNull</span><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-function"><span class="hljs-keyword">public</span> String <span class="hljs-title">getName</span><span class="hljs-params">()</span> </span>&#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;DemoView&quot;</span>;<br>    &#125;<br><br>    <span class="hljs-meta">@NonNull</span><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-function"><span class="hljs-keyword">protected</span> TextView <span class="hljs-title">createViewInstance</span><span class="hljs-params">(<span class="hljs-meta">@NonNull</span> ThemedReactContext reactContext)</span> </span>&#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> TextView(reactContext);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>看到这里需要如下几个：</p><blockquote><p>泛型UI：表明该viewmanager管理那个控件。<br>getName：返回该控件在JS端上的命名。<br>createViewInstance：返回该控件的一个函数，这里返回的类型就是指定的泛型。</p></blockquote><h6 id="2-注册"><a href="#2-注册" class="headerlink" title="2.注册"></a>2.注册</h6><p>最后别忘了将这个ViewManager添加到package中:<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">DemoPackage</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">ReactPackage</span> </span>&#123;<br>...<br>    <span class="hljs-meta">@NonNull</span><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-function"><span class="hljs-keyword">public</span> List&lt;ViewManager&gt; <span class="hljs-title">createViewManagers</span><span class="hljs-params">(<span class="hljs-meta">@NonNull</span> ReactApplicationContext reactContext)</span> </span>&#123;<br>        <span class="hljs-keyword">return</span> Arrays.asList(<span class="hljs-keyword">new</span> DemoViewManager());<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure></p><h6 id="3-JS层找到该控件"><a href="#3-JS层找到该控件" class="headerlink" title="3.JS层找到该控件"></a>3.JS层找到该控件</h6><p>完成java层后，看看js层如何操作：<br><figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">import</span> &#123; requireNativeComponent &#125; <span class="hljs-keyword">from</span> <span class="hljs-string">&#x27;react-native&#x27;</span>;<br><span class="hljs-built_in">module</span>.exports = requireNativeComponent(<span class="hljs-string">&#x27;DemoView&#x27;</span>);<br></code></pre></td></tr></table></figure></p><h6 id="4-设置属性"><a href="#4-设置属性" class="headerlink" title="4.设置属性"></a>4.设置属性</h6><p>通过<em>@ReactProp</em>或<em>@ReactPropGroup</em>注解函数来进行属性设置，定义好名称和类型后，就可以直接使用了。<br><figure class="highlight java"><figcaption><span>& js</span></figcaption><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">DemoViewManager</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">SimpleViewManager</span>&lt;<span class="hljs-title">TextView</span>&gt; </span>&#123;<br>...<br>    <span class="hljs-meta">@ReactProp(name = &quot;text&quot;)</span><br>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">setText</span><span class="hljs-params">(TextView view, String text)</span> </span>&#123;<br>        <span class="hljs-keyword">if</span> (text != <span class="hljs-keyword">null</span>)<br>            view.setText(text);<br>    &#125;<br><br>&lt;DemoView<br>style=&#123;&#123; width: <span class="hljs-number">100</span>, height: <span class="hljs-number">100</span>, backgroundColor: <span class="hljs-string">&#x27;#0f0&#x27;</span> &#125;&#125;<br>text=&#123;<span class="hljs-string">&#x27;好的&#x27;</span>&#125;<br>/&gt;<br></code></pre></td></tr></table></figure><br>当我们在初始化、修改属性时都会调用到java层面上。</p><h6 id="5-点击事件"><a href="#5-点击事件" class="headerlink" title="5.点击事件"></a>5.点击事件</h6><p>通过一系列的注册来完成该功能：<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">DemoViewManager</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">SimpleViewManager</span>&lt;<span class="hljs-title">TextView</span>&gt; </span>&#123;<br>...<br><span class="hljs-comment">//    @Nullable</span><br><span class="hljs-comment">//    @Override</span><br><span class="hljs-comment">//    public Map&lt;String, Object&gt; getExportedCustomDirectEventTypeConstants() &#123;</span><br><span class="hljs-comment">//        return MapBuilder.&lt;String, Object&gt;builder()</span><br><span class="hljs-comment">//                .put(&quot;callback&quot;, MapBuilder.of(&quot;registrationName&quot;, &quot;onCallback&quot;))</span><br><span class="hljs-comment">//                .build();</span><br><span class="hljs-comment">//    &#125;</span><br><br>    <span class="hljs-meta">@Nullable</span><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-function"><span class="hljs-keyword">public</span> Map&lt;String, Object&gt; <span class="hljs-title">getExportedCustomBubblingEventTypeConstants</span><span class="hljs-params">()</span> </span>&#123;<br>        <span class="hljs-keyword">return</span> MapBuilder.&lt;String, Object&gt;builder()<br>                .put(<span class="hljs-string">&quot;callback&quot;</span>, MapBuilder.of(<span class="hljs-string">&quot;phasedRegistrationNames&quot;</span>, MapBuilder.of(<span class="hljs-string">&quot;bubbled&quot;</span>, <span class="hljs-string">&quot;onCallback&quot;</span>)))<br>                .build();<br>    &#125;<br></code></pre></td></tr></table></figure><br>首先需要注册java层和js层的函数映射及名称，上面的注册前者是java层名称、后者是js层函数名称。上面两个可以使用任意一个，都可以完成注册任务，但是依旧推荐未被注释的方式。<br>注册好函数后，就可以开始调用并传递参数：<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@Override</span><br><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">onClick</span><span class="hljs-params">(View v)</span> </span>&#123;<br>    Log.e(<span class="hljs-string">&quot;-----&gt;&quot;</span>, <span class="hljs-string">&quot;click&quot;</span>);<br>    WritableMap map = Arguments.createMap();<br>    map.putString(<span class="hljs-string">&quot;name&quot;</span>, <span class="hljs-string">&quot;click&quot;</span>);<br>    emit(<span class="hljs-string">&quot;callback&quot;</span>, map);<br>&#125;<br><br><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">emit</span><span class="hljs-params">(String key, WritableMap map)</span> </span>&#123;<br>    reactContext.getJSModule(RCTEventEmitter.class).receiveEvent(textView.getId(), key, map);<br>&#125;<br></code></pre></td></tr></table></figure><br>注意传递的参数格式，以及最重要的<em>view.getId()</em>，表明这个函数可以在自定义控件中添加，也可以在viewManager中使用，但是一定是指定的view的id。<br>最后就可以在js层面上使用了：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">&lt;DemoView<br>  style=&#123;&#123; <span class="hljs-attr">width</span>: <span class="hljs-number">100</span>, <span class="hljs-attr">height</span>: <span class="hljs-number">100</span>, <span class="hljs-attr">backgroundColor</span>: <span class="hljs-string">&#x27;#0f0&#x27;</span> &#125;&#125;<br>  text=&#123;text&#125;<br>  onCallback=&#123;<span class="hljs-function">(<span class="hljs-params">data</span>) =&gt;</span> &#123;<br>    <span class="hljs-built_in">console</span>.log(<span class="hljs-string">&#x27;-----&gt; callback:&#x27;</span>, data);<br>  &#125;&#125;<br>/&gt;<br></code></pre></td></tr></table></figure></p><h6 id="6-refs函数调用"><a href="#6-refs函数调用" class="headerlink" title="6.refs函数调用"></a>6.refs函数调用</h6><p>需要注意新老函数的调用区别。</p><ul><li>新函数<blockquote><p>public void receiveCommand(@NonNull TextView root, String commandId, @Nullable ReadableArray args)<br>只需要在这里处理好 commandId 的功能即可，无需要注册。</p><figure class="highlight java"><figcaption><span>& js</span></figcaption><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">DemoViewManager</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">SimpleViewManager</span>&lt;<span class="hljs-title">TextView</span>&gt; <span class="hljs-keyword">implements</span> <span class="hljs-title">View</span>.<span class="hljs-title">OnClickListener</span> </span>&#123;<br>...<br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">receiveCommand</span><span class="hljs-params">(<span class="hljs-meta">@NonNull</span> TextView root, String commandId, <span class="hljs-meta">@Nullable</span> ReadableArray args)</span> </span>&#123;<br>        <span class="hljs-keyword">super</span>.receiveCommand(root, commandId, args);<br>    &#125;<br><br><br><span class="hljs-keyword">import</span> &#123; UIManager, findNodeHandle &#125; from <span class="hljs-string">&#x27;react-native&#x27;</span>;<br>...<br>  sendCommand = (viewName, funcName, data = []) =&gt; &#123;<br>    UIManager.dispatchViewManagerCommand(<br>      findNodeHandle(<span class="hljs-keyword">this</span>),<br>      funcName,<br>      data,<br>    );<br>  &#125;;<br></code></pre></td></tr></table></figure></blockquote></li></ul><ul><li>老函数<blockquote><p>public Map&lt;String, Integer&gt; getCommandsMap()<br>public void receiveCommand(@NonNull TextView root, int commandId, @Nullable ReadableArray args)<br>首先需要通过 getCommandsMap 函数进行注册函数名称和 commandId 进行绑定，然后在 receiveCommand 函数中处理 commandId 的功能即可。<br>在JS层找到该控件名称、函数名称、需要的参数数组即可。</p><figure class="highlight java"><figcaption><span>& js</span></figcaption><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">DemoViewManager</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">SimpleViewManager</span>&lt;<span class="hljs-title">TextView</span>&gt; <span class="hljs-keyword">implements</span> <span class="hljs-title">View</span>.<span class="hljs-title">OnClickListener</span> </span>&#123;<br>...<br>    <span class="hljs-meta">@Nullable</span><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-function"><span class="hljs-keyword">public</span> Map&lt;String, Integer&gt; <span class="hljs-title">getCommandsMap</span><span class="hljs-params">()</span> </span>&#123;<br>        <span class="hljs-keyword">return</span> MapBuilder.&lt;String, Integer&gt;builder()<br>                .put(<span class="hljs-string">&quot;funcName&quot;</span>, <span class="hljs-number">1</span>)<br>                .build();<br>    &#125;<br><br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">receiveCommand</span><span class="hljs-params">(<span class="hljs-meta">@NonNull</span> TextView root, <span class="hljs-keyword">int</span> commandId, <span class="hljs-meta">@Nullable</span> ReadableArray args)</span> </span>&#123;<br>        <span class="hljs-keyword">super</span>.receiveCommand(root, commandId, args);<br>    &#125;<br><br><br><span class="hljs-keyword">import</span> &#123; UIManager, findNodeHandle &#125; from <span class="hljs-string">&#x27;react-native&#x27;</span>;<br>...<br>sendCommand = (viewName, funcName, data = []) =&gt; &#123;<br>UIManager.dispatchViewManagerCommand(<br>findNodeHandle(<span class="hljs-keyword">this</span>),<br>UIManager.getViewManagerConfig(viewName).Commands[funcName],<br>data<br>)<br>&#125;;<br></code></pre></td></tr></table></figure><p>这里传入的this就是组件的refs，这里在使用<em>sendCommand</em>函数时对自定义控件进行封装后的结果。</p></blockquote></li></ul><h4 id="ViewGroupManager"><a href="#ViewGroupManager" class="headerlink" title="ViewGroupManager"></a>ViewGroupManager</h4><p>与ViewManager的区别就在于 ViewGroupManager 可以对内部组件进行管理，例如地图内部的标记效果。</p><h2 id="二、IOS"><a href="#二、IOS" class="headerlink" title="二、IOS"></a>二、IOS</h2><h2 id="2-1-Module"><a href="#2-1-Module" class="headerlink" title="2.1 Module"></a>2.1 Module</h2><h4 id="2-1-1-RCT-EXPORT-MODULE-：创建Module初始化"><a href="#2-1-1-RCT-EXPORT-MODULE-：创建Module初始化" class="headerlink" title="2.1.1 RCT_EXPORT_MODULE()：创建Module初始化"></a>2.1.1 RCT_EXPORT_MODULE()：创建Module初始化</h4><p>使用<em>RCT_EXPORT_MODULE()</em>来创建需要的Module，与Android对应。<br>准备好.h文件，这里的文件名称、类名称都无需在意。<br><figure class="highlight plain"><table><tr><td class="code"><pre><code class="hljs module.h">#ifndef DemoModule_h<br>#define DemoModule_h<br><br>#import &lt;Foundation&#x2F;Foundation.h&gt;<br>#import &lt;React&#x2F;RCTBridgeModule.h&gt;<br><br>@interface DemoModule : NSObject &lt;RCTBridgeModule&gt;<br><br>@end<br><br>#endif &#x2F;* DemoModule_h *&#x2F;<br></code></pre></td></tr></table></figure></p><p>在<em>RCT_EXPORT_MODULE()</em>中添加的参数既是定义在JS层中的类名称，<strong>注意这里不要加引号，否在在JS层的类名称也会有引号。</strong><br><figure class="highlight plain"><table><tr><td class="code"><pre><code class="hljs module.m">#import &quot;DemoModule.h&quot;<br><br>@implementation DemoModule<br>RCT_EXPORT_MODULE(DemoModule)<br>@end<br></code></pre></td></tr></table></figure></p><h4 id="2-1-2-JS中找到该库"><a href="#2-1-2-JS中找到该库" class="headerlink" title="2.1.2 JS中找到该库"></a>2.1.2 JS中找到该库</h4><p>需要注意的是使用过程中如果出现问题，请先重新build后在找问题。<br><figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">import</span> &#123; NativeModules &#125; <span class="hljs-keyword">from</span> <span class="hljs-string">&#x27;react-native&#x27;</span>;<br><span class="hljs-keyword">const</span> &#123; DemoModule &#125; = NativeModules;<br></code></pre></td></tr></table></figure></p><h4 id="2-1-3-从-JS-到-ios"><a href="#2-1-3-从-JS-到-ios" class="headerlink" title="2.1.3 从 JS 到 ios"></a>2.1.3 从 JS 到 ios</h4><p>使用<em>RCT_EXPORT_METHOD()</em>来创建我们需要的功能函数:<br><figure class="highlight plain"><table><tr><td class="code"><pre><code class="hljs oc">@implementation DemoModule<br><br>RCT_EXPORT_METHOD(test1) &#123;<br>  NSLog(@&quot;-----&gt; test1()&quot;);<br>&#125;<br><br>RCT_EXPORT_METHOD(test2:(int)a) &#123;<br>  NSLog(@&quot;-----&gt; test2(%d)&quot;, a);<br>&#125;<br></code></pre></td></tr></table></figure><br>整个过程只需要将函数的定义放到该函数中即可。</p><h4 id="2-1-4-从-JS-到-ios-并回调数据给-JS"><a href="#2-1-4-从-JS-到-ios-并回调数据给-JS" class="headerlink" title="2.1.4 从 JS 到 ios 并回调数据给 JS"></a>2.1.4 从 JS 到 ios 并回调数据给 JS</h4><h6 id="Callback-1"><a href="#Callback-1" class="headerlink" title="Callback"></a>Callback</h6><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-comment">// ----------JS</span><br>DemoModule.test(<span class="hljs-number">2</span>, <span class="hljs-function">(<span class="hljs-params">result</span>) =&gt;</span> &#123;<br><span class="hljs-built_in">this</span>.setState(&#123; <span class="hljs-attr">data</span>: result &#125;);<br>&#125;);<br></code></pre></td></tr></table></figure><p>此处代码的js层与android代码一模一样，不同只在于原生<br><figure class="highlight plain"><table><tr><td class="code"><pre><code class="hljs oc">@implementation DemoModule<br><br>RCT_EXPORT_METHOD(test3:(int)a:(RCTResponseSenderBlock)callback) &#123;<br><br>  NSLog(@&quot;-----&gt; test3(%d)&quot;, a);<br>  callback(@[[NSNumber numberWithInt:a]]);<br>&#125;<br></code></pre></td></tr></table></figure><br>注意返回的是一个NSObject数组，并且内部的每项数据都应该是NSObject类型，因此基本类型int需要进行封箱成对象。</p><h6 id="Promise-1"><a href="#Promise-1" class="headerlink" title="Promise"></a>Promise</h6><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">GaoModule.test(<span class="hljs-number">1</span>, <span class="hljs-number">2</span>)<br>.then(<span class="hljs-function"><span class="hljs-params">data</span> =&gt;</span> &#123;<br><span class="hljs-built_in">this</span>.setState(&#123; <span class="hljs-attr">data</span>: result &#125;);<br>&#125;)<br>.catch(<span class="hljs-function"><span class="hljs-params">err</span> =&gt;</span> &#123;<br><span class="hljs-built_in">console</span>.log(err)<br>&#125;);<br></code></pre></td></tr></table></figure><p>同上，来看看ios如果处理<br><figure class="highlight plain"><table><tr><td class="code"><pre><code class="hljs oc">@implementation DemoModule<br><br>RCT_EXPORT_METHOD(test4:(int)a:(RCTPromiseResolveBlock)resolve:(RCTPromiseRejectBlock)reject) &#123;<br>  NSLog(@&quot;-----&gt; test4(%d)&quot;, a);<br>  if (a &#x3D;&#x3D; 11) &#123;<br>    resolve(@[[NSNumber numberWithInt:a]]);<br>  &#125; else &#123;<br>    NSError *error &#x3D; [NSError errorWithDomain:@&quot;Test&quot; code:0 userInfo:nil];<br>    reject(@&quot;CODE&quot;, @&quot;MESSAGE&quot;, error);<br>  &#125;<br>&#125;<br></code></pre></td></tr></table></figure></p><h4 id="2-1-5-从-ios-到-JS"><a href="#2-1-5-从-ios-到-JS" class="headerlink" title="2.1.5 从 ios 到 JS"></a>2.1.5 从 ios 到 JS</h4><p>通android一样，ios也可以直接访问到Js层面上，方式也有迹可循，在js监听字段，然后ios通过该映射字段进行数据传递。<br>JS端:<br><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">import</span> &#123; NativeModules, NativeEventEmitter &#125; <span class="hljs-keyword">from</span> <span class="hljs-string">&#x27;react-native&#x27;</span>;<br>...<br><span class="hljs-keyword">const</span> emitter = <span class="hljs-keyword">new</span> NativeEventEmitter(NativeModules.TestEmitterModule);<br><span class="hljs-comment">// 注册</span><br>emitter.addListener(<span class="hljs-string">&#x27;EventName&#x27;</span>, <span class="hljs-function">(<span class="hljs-params">name</span>) =&gt;</span> &#123;<br><span class="hljs-comment">// 回调</span><br><span class="hljs-built_in">console</span>.log(<span class="hljs-string">&#x27;-----&gt;&#x27;</span>, <span class="hljs-string">&#x27;callback:&#x27;</span>+name.name);<br>&#125;);<br><span class="hljs-comment">// 模拟发送消息</span><br>NativeModules.TestEmitterModule.send(<span class="hljs-string">&#x27;test&#x27;</span>);<br><br><span class="hljs-comment">// 取消注册</span><br>emitter.remove();<br></code></pre></td></tr></table></figure><br>上面的代码大概回顾之前的功能。</p><blockquote><p>1.需要继承RCTEventEmitter。<br><figure class="highlight plain"><table><tr><td class="code"><pre><code class="hljs oc">#import &lt;Foundation&#x2F;Foundation.h&gt;<br>#import &lt;React&#x2F;RCTBridgeModule.h&gt;<br>#import &lt;React&#x2F;RCTEventEmitter.h&gt;<br><br>@interface DemoModule : RCTEventEmitter &lt;RCTBridgeModule&gt;<br><br>+ (instancetype)instance;<br><br>@end<br><br>#endif &#x2F;* DemoModule_h *&#x2F;<br></code></pre></td></tr></table></figure><br>2.初始化工作。<br><figure class="highlight plain"><table><tr><td class="code"><pre><code class="hljs oc">... <br>static DemoModule * _sharedInstance &#x3D; nil;<br>+ (instancetype)instance &#123;<br>    return _sharedInstance;<br>&#125;<br><br>- (NSArray&lt;NSString*&gt; *)supportedEvents<br>&#123;<br>&#x2F;&#x2F; 定义的监听字段<br>    return @[@&quot;callback&quot;];<br>&#125;<br><br>- (void)startObserving &#123;<br>    if (_sharedInstance &#x3D;&#x3D; nil) &#123;<br>        _sharedInstance &#x3D; self;<br>    &#125;<br>    [super startObserving];<br>&#125;<br><br>- (void)stopObserving &#123;<br>    _sharedInstance &#x3D; nil;<br>    [super stopObserving];<br>&#125;<br></code></pre></td></tr></table></figure><br>3.编写类似emit的代码作为传递入口。<br><figure class="highlight plain"><table><tr><td class="code"><pre><code class="hljs oc">&#x2F;&#x2F; 功能回调封装<br>- (void)emit:(NSString *) key :(NSObject *)value &#123;<br>  [self sendEventWithName:key body:value];<br>&#125;<br><br>RCT_EXPORT_METHOD(send) &#123;<br>  NSLog(@&quot;-----&gt; send()&quot;);<br>  [self emit:@&quot;callback&quot; :@&#123;<br>    @&quot;a&quot;: @&quot;a&quot;,<br>    @&quot;b&quot;: [NSNumber numberWithInt:2]<br>  &#125;];<br>&#125;<br></code></pre></td></tr></table></figure><br>4.js层面注册及解绑</p></blockquote><h2 id="2-2-View"><a href="#2-2-View" class="headerlink" title="2.2 View"></a>2.2 View</h2><h4 id="2-2-1-RCTViewManager"><a href="#2-2-1-RCTViewManager" class="headerlink" title="2.2.1 RCTViewManager"></a>2.2.1 RCTViewManager</h4><p>这里使用通过<strong>RCTViewManager</strong>来管理ios中的ui控件。</p><h4 id="2-2-2-创建自定义View"><a href="#2-2-2-创建自定义View" class="headerlink" title="2.2.2 创建自定义View"></a>2.2.2 创建自定义View</h4><p>自定义一个TextView<br><figure class="highlight plain"><table><tr><td class="code"><pre><code class="hljs DemoView.h">#ifndef DemoView_h<br>#define DemoView_h<br><br>#import &lt;Foundation&#x2F;Foundation.h&gt;<br>#import &lt;UIKit&#x2F;UIKit.h&gt;<br><br>@interface DemoView : UITextView<br>@end<br><br>#endif &#x2F;* DemoView_h *&#x2F;<br></code></pre></td></tr></table></figure><br>然后完成这个View，并且让他实现一个自己的本文设置功能函数:<br><figure class="highlight plain"><table><tr><td class="code"><pre><code class="hljs DemoView.m">#import &quot;DemoView.h&quot;<br><br>@implementation DemoView<br>&#x2F;&#x2F; 添加一个设置字符串功能函数<br>- (void)setTexts:(NSString *)text &#123;<br>  [self setText:text];<br>&#125;<br>@end<br></code></pre></td></tr></table></figure></p><h4 id="2-2-3-创建ViewManager"><a href="#2-2-3-创建ViewManager" class="headerlink" title="2.2.3 创建ViewManager"></a>2.2.3 创建ViewManager</h4><p>需要继承<strong>RCTViewManager</strong>:<br><figure class="highlight plain"><table><tr><td class="code"><pre><code class="hljs oc">#import &lt;React&#x2F;RCTViewManager.h&gt;<br>#import &quot;DemoView.h&quot;<br><br>@interface DemoViewManager : RCTViewManager<br>@end<br><br>@implementation DemoViewManager<br>RCT_EXPORT_MODULE(DemoView)<br><br>&#x2F;&#x2F; RCT_EXPORT_VIEW_PROPERTY(texts, NSString)<br><br>&#x2F;&#x2F; 找到我们定义的UI<br>- (UIView *)view &#123;<br>  return [[DemoView alloc] init];<br>&#125;<br>@end<br></code></pre></td></tr></table></figure><br>然后在view函数找返回我们定义的UI控件。<br>同module一样，通过<strong>RCT_EXPORT_MODULE</strong>来定义控件在JS层的命名，注意别加引号</p><h4 id="2-2-4-JS-中找到该View"><a href="#2-2-4-JS-中找到该View" class="headerlink" title="2.2.4 JS 中找到该View"></a>2.2.4 JS 中找到该View</h4><p>官方文档说明：渲染时，请不要忘记拉伸UI即给UI大小，否则将无法在屏幕上看到。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">import</span> &#123; requireNativeComponent &#125; <span class="hljs-keyword">from</span> <span class="hljs-string">&#x27;react-native&#x27;</span>;<br><span class="hljs-built_in">module</span>.exports = requireNativeComponent(<span class="hljs-string">&#x27;DemoView&#x27;</span>);<br><br>...<br><br>        &lt;Demo.DemoView<br>          ref=&#123;<span class="hljs-function">(<span class="hljs-params">refs</span>) =&gt;</span> (<span class="hljs-built_in">this</span>.demoView = refs)&#125;<br>          style=&#123;&#123; <span class="hljs-attr">width</span>: <span class="hljs-number">100</span>, <span class="hljs-attr">height</span>: <span class="hljs-number">100</span>, <span class="hljs-attr">backgroundColor</span>: <span class="hljs-string">&#x27;#0f0&#x27;</span> &#125;&#125;<br>        /&gt;<br></code></pre></td></tr></table></figure></p><h4 id="2-2-5-设置属性"><a href="#2-2-5-设置属性" class="headerlink" title="2.2.5 设置属性"></a>2.2.5 设置属性</h4><p>使用<strong>RCT_EXPORT_VIEW_PROPERTY()</strong>来标注属性命名和属性类型。<br><figure class="highlight plain"><table><tr><td class="code"><pre><code class="hljs oc">@implementation DemoViewManager<br>RCT_EXPORT_MODULE(DemoView)<br>&#x2F;&#x2F; 标注属性<br>RCT_EXPORT_VIEW_PROPERTY(texts, NSString)<br></code></pre></td></tr></table></figure></p><p>然后在控件中定义这个属性<br><figure class="highlight plain"><table><tr><td class="code"><pre><code class="hljs oc">#import &quot;DemoView.h&quot;<br><br>@implementation DemoView<br>&#x2F;&#x2F; 添加一个设置字符串功能函数<br>- (void)setTexts:(NSString *)text &#123;<br>  [self setText:text];<br>&#125;<br>@end<br></code></pre></td></tr></table></figure></p><h4 id="2-2-6-设置函数"><a href="#2-2-6-设置函数" class="headerlink" title="2.2.6 设置函数"></a>2.2.6 设置函数</h4><p>添加相应的<strong>Delegate</strong>协议，这里使用的是UITextView，使用的就是UITextViewDelegate。<br><figure class="highlight plain"><table><tr><td class="code"><pre><code class="hljs oc">&#96;&#96;&#96;<br><br><br><br>#### 2.2.7 refs函数调用<br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br><br>重要函数有如下几个<br>* &#96;RCT_EXPORT_MODULE&#96;: 描述当前类为通信类。<br>* &#96;RCT_EXPORT_METHOD&#96;: 描述该函数为单向通信函数。<br>* &#96;RCT_REMAP_METHOD&#96;: 解决函数重载问题。<br><br>#### 带参数的原生调用<br><br>这里在JS端使用类似上面Android端的方式放置代码，来完善ios端的代码。<br>&#96;&#96;&#96;java<br>NativeModules.TestModule.testTwo(&#39;a&#39;, &#39;b&#39;, &#39;c&#39;);<br></code></pre></td></tr></table></figure></p><p>与Android端一致，需要Module名称，函数名称，参数，及回调。</p><ul><li>模块名称：<br>TestModule.m<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">#import &lt;Foundation/Foundation.h&gt;<br>#import &lt;React/RCTBridgeModule.h&gt;<br><br><span class="hljs-meta">@interface</span> TestModule : NSObject &lt;RCTBridgeModule&gt;<br><br><span class="hljs-meta">@end</span><br></code></pre></td></tr></table></figure></li></ul><p>TestModule.m<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">mport <span class="hljs-string">&quot;TestModule.h&quot;</span><br><br><span class="hljs-meta">@implementation</span> TestModule<br><span class="hljs-comment">// 用于标记为指定RCTBridgeModule协议类</span><br>RCT_EXPORT_MODULE();<br><br><span class="hljs-comment">// 实现函数</span><br>RCT_EXPORT_METHOD(testTwo:(NSString*) text1:(NSString*) text2:(NSString*) text3) &#123;<br>  NSLog(@<span class="hljs-string">&quot;-----&gt; IOS%@ - %@ - %@&quot;</span>, text1, text2, text3);<br>&#125;<br><br><span class="hljs-meta">@end</span><br></code></pre></td></tr></table></figure></p><ul><li>模块名称就是实现类名称</li><li>这里在实现<code>TestModule</code>接口后，需要在类中添加<code>RCT_EXPORT_MODULE()</code>宏。</li><li>实现的函数必须使用<code>RCT_EXPORT_METHOD()</code>来定义。</li></ul><p>函数参数：<br>RCT_EXPORT_METHOD(函数名称:(参数类型)参数名称: (参数类型)参数名称)</p><p>这样就完成了单向函数调用，由于调用过程异步，所以回调时也和Android端类似。</p><h4 id="原生回调"><a href="#原生回调" class="headerlink" title="原生回调"></a>原生回调</h4><h6 id="Callback-2"><a href="#Callback-2" class="headerlink" title="Callback"></a>Callback</h6><p>使用<code>Callback</code>完成函数回调。<br>IOS端：<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">RCT_EXPORT_METHOD(testFour:(NSString*)text:(RCTPromiseResolveBlock)resolve:(RCTPromiseRejectBlock)reject) &#123;<br>resolve([@<span class="hljs-string">&quot;Callback:&quot;</span> stringByAppendingString:text]);<br>&#125;<br></code></pre></td></tr></table></figure><br>JS端：<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">this</span>.test();<br>...<br>test = () =&gt; &#123;<br>NativeModules.TestModule.testFour(<span class="hljs-string">&#x27;msg&#x27;</span>)<br>.then((msg) =&gt; &#123;<br>console.log(<span class="hljs-string">&#x27;-----&gt;&#x27;</span>, <span class="hljs-string">&#x27;over:&#x27;</span>+msg);<br>&#125;);<br>&#125;<br><span class="hljs-comment">// async函数解析与Promise类似</span><br>test = async () =&gt; &#123;<br><span class="hljs-keyword">var</span> a = await NativeModules.TestModule.testFour(<span class="hljs-string">&#x27;msg&#x27;</span>);<br>console.log(<span class="hljs-string">&#x27;-----&gt;&#x27;</span>, <span class="hljs-string">&#x27;over&#x27;</span>+a);<br>&#125;<br></code></pre></td></tr></table></figure><br>上面代码实例看到<code>Callback</code>的回调方式，但是这种方式必须在JS端触发，才能进行反响回调。</p><h6 id="Promise-2"><a href="#Promise-2" class="headerlink" title="Promise"></a>Promise</h6><p>与<code>Callback</code>类似，<code>Promise</code>也具有异步的效果。<br>Ios端：<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">RCT_EXPORT_METHOD(testFour:(NSString*)text:(RCTPromiseResolveBlock)resolve:(RCTPromiseRejectBlock)reject) &#123;<br>resolve([@<span class="hljs-string">&quot;Callback:&quot;</span> stringByAppendingString:text]);<br>&#125;<br></code></pre></td></tr></table></figure><br>JS端：<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"> <br> NativeModules.TestModule.testThree(test1, test2)<br>.then((...) =&gt; &#123;<br>...<br>&#125;).<span class="hljs-keyword">catch</span>(e =&gt; &#123;<br>...<br>&#125;);<br></code></pre></td></tr></table></figure></p><p>但是上面两种方式都无法需要在JS端触发。</p><h6 id="RCTEventEmitter"><a href="#RCTEventEmitter" class="headerlink" title="RCTEventEmitter"></a>RCTEventEmitter</h6><p>该功能类似广播监听。不同于上面两者函数回调，一次回调，此接口用于多次回调，长期监听。</p><p>IOS端:<br>TestEmitterModule.h<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@interface</span> TestEmitterModule : RCTEventEmitter &lt;RCTBridgeModule&gt;<br>- (NSArray&lt;NSString*&gt; *) supportedEvents;<br>- (<span class="hljs-keyword">void</span>)register: (NSString*)string;<br><span class="hljs-meta">@end</span><br></code></pre></td></tr></table></figure><br>TestEmitterModule.m<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@implementation</span> TestEmitterModule<br>RCT_EXPORT_MODULE();<br><br>- (NSArray&lt;NSString*&gt; *)supportedEvents &#123;<br>  <span class="hljs-keyword">return</span> @[@<span class="hljs-string">&quot;EventName&quot;</span>];<br><br>&#125;<br><span class="hljs-comment">// 注册通信</span><br>- (<span class="hljs-keyword">void</span>)register: (NSString*) string &#123;<br>  [self sendEventWithName:@<span class="hljs-string">&quot;EventName&quot;</span> body:@&#123;@<span class="hljs-string">&quot;name&quot;</span>: string&#125;];<br><br>&#125;<br><span class="hljs-comment">// 发送信息提示回调，这里仅用于测试用</span><br>RCT_EXPORT_METHOD(send:(NSString*)text) &#123;<br>  [self register:@<span class="hljs-string">&quot;callback&quot;</span>];<br><br>&#125;<br><span class="hljs-meta">@end</span><br></code></pre></td></tr></table></figure></p><p>JS端:<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">import</span> &#123; NativeModules, NativeEventEmitter &#125; from <span class="hljs-string">&#x27;react-native&#x27;</span>;<br>...<br><span class="hljs-keyword">const</span> emitter = <span class="hljs-keyword">new</span> NativeEventEmitter(NativeModules.TestEmitterModule);<br><span class="hljs-comment">// 注册</span><br>emitter.addListener(<span class="hljs-string">&#x27;EventName&#x27;</span>, (name) =&gt; &#123;<br><span class="hljs-comment">// 回调</span><br>console.log(<span class="hljs-string">&#x27;-----&gt;&#x27;</span>, <span class="hljs-string">&#x27;callback:&#x27;</span>+name.name);<br>&#125;);<br><span class="hljs-comment">// 模拟发送消息</span><br>NativeModules.TestEmitterModule.send(<span class="hljs-string">&#x27;test&#x27;</span>);<br><br><span class="hljs-comment">// 取消注册</span><br>emitter.remove();<br></code></pre></td></tr></table></figure></p><blockquote><p>注意，如果在未注册监听的情况下发送消息，则会发送一个警告<code>Sending XXX with no listeners registered</code>，告知未找到注册该字段的监听器。</p></blockquote><p>在需要判断该问题时，可以使用<code>startObserving</code>和<code>stopObserving</code>方法。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@implementation</span> TestEmitterModule<br>bool hasListener;<br>RCT_EXPORT_MODULE();<br>- (NSArray&lt;NSString*&gt; *)supportedEvents &#123;<br>  <span class="hljs-keyword">return</span> @[@<span class="hljs-string">&quot;EventName&quot;</span>];<br>&#125;<br>- (<span class="hljs-keyword">void</span>)startObserving &#123;<br>  hasListener = YES;<br>&#125;<br>- (<span class="hljs-keyword">void</span>)stopObserving &#123;<br>  hasListener = NO;<br>&#125;<br>- (<span class="hljs-keyword">void</span>)register: (NSString*) string &#123;<br><span class="hljs-keyword">if</span> (hasListener) &#123;<br>  [self sendEventWithName:@<span class="hljs-string">&quot;EventName&quot;</span> body:@&#123;@<span class="hljs-string">&quot;name&quot;</span>: string&#125;];<br>&#125;<br>&#125;<br></code></pre></td></tr></table></figure></p><h6 id="类型转换"><a href="#类型转换" class="headerlink" title="类型转换"></a>类型转换</h6><p>这里时<code>RCTConvert.h</code>文件。<br><a href="https://github.com/facebook/react-native/blob/master/React/Base/RCTConvert.h">https://github.com/facebook/react-native/blob/master/React/Base/RCTConvert.h</a><br>当在定义一组常量时，我们需要对<code>RCTConvert</code>类进行扩展。<br>如，需要使用如下：<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">#import &lt;React/RCTConvert.h&gt;<br><br><span class="hljs-meta">@implementation</span> RCTConvert (AMap3DView)<br><br><span class="hljs-comment">// 类型</span><br>RCT_ENUM_CONVERTER(MAMapType, (@&#123;<br>  @<span class="hljs-string">&quot;normal&quot;</span>: @(MAMapTypeStandard),<br>  @<span class="hljs-string">&quot;satellite&quot;</span>: @(MAMapTypeSatellite),<br>  @<span class="hljs-string">&quot;night&quot;</span>: @(MAMapTypeStandardNight),<br>  @<span class="hljs-string">&quot;navi&quot;</span>: @(MAMapTypeNavi),<br>  @<span class="hljs-string">&quot;bus&quot;</span>: @(MAMapTypeBus)<br>&#125;), MAMapTypeStandard, integerValue);<br><br><span class="hljs-meta">@end</span><br></code></pre></td></tr></table></figure><br>如上，我们将在JS中需要的字符串，在转换到ios时会自动根据这个内容进行转换到相应枚举类型中。</p><blockquote><p>第一个参数为ios中枚举类型<br>第二个参数为转换对应表<br>第三个参数为默认数值<br>第四个参数为转换器工具</p></blockquote><h4 id="自定义类型"><a href="#自定义类型" class="headerlink" title="自定义类型"></a>自定义类型</h4><p>当我们在js层建立一个对象，需要在ios层读取这个对象时，需要对自定义类型进行解析处理，在android中，使用的<code>ReadableMap</code>、<code>ReadableArray</code>来解决，在ios上的处理也很像。</p><p>当在定义一组常量时，我们需要对<code>RCTConvert</code>类进行扩展；这里的自定义类型也需要。<br><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@implementation</span> RCTConvert (AMap3DView)<br>...<br><span class="hljs-comment">// 配置自定义对象</span><br>+ (LocationStyle *)LocationStyle:(id)json &#123;<br>    LocationStyle * locationStyle = [LocationStyle <span class="hljs-keyword">new</span>];<br><br>    locationStyle.locationIcon = [UIImage imageNamed:[self NSString:json[@<span class="hljs-string">&quot;locationIcon&quot;</span>]]];<br>    locationStyle.strokeColor = [self NSString:json[@<span class="hljs-string">&quot;strokeColor&quot;</span>]];<br>    locationStyle.strokeWidth = [self CGFloat:json[@<span class="hljs-string">&quot;strokeWidth&quot;</span>]];<br>    locationStyle.showsAccuracyRing = [self BOOL:json[@<span class="hljs-string">&quot;showsAccuracyRing&quot;</span>]];<br><br>    <span class="hljs-keyword">return</span> locationStyle;<br>&#125;<br>...<br></code></pre></td></tr></table></figure><br>代码中可以看到，这里完成了一个对json格式数据到ios对象数据的转换过程，也是RN在对象转换的一个转换表，所有自定义的对象类型都会像这样进行转换对照。</p><h2 id="自定义控件"><a href="#自定义控件" class="headerlink" title="自定义控件"></a>自定义控件</h2><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><br>#import &lt;Foundation/Foundation.h&gt;<br>#import &lt;React/RCTViewManager.h&gt;<br><br><span class="hljs-meta">@interface</span> AMap3DViewManager : RCTViewManager<br><span class="hljs-meta">@end</span><br><br><span class="hljs-meta">@implementation</span> AMap3DViewManager<br>RCT_EXPORT_MODULE();<br>RCT_EXPORT_VIEW_PROPERTY(mapType, MAMapType);<br><br><span class="hljs-comment">// 创建view组件</span><br>- (UIView *)view<br>&#123;<br>    AMap3DView *mapView = [AMap3DView <span class="hljs-keyword">new</span>];<br>    <span class="hljs-keyword">return</span> mapView;<br>&#125;<br><span class="hljs-meta">@end</span><br></code></pre></td></tr></table></figure><p>这里创建了一个地图控件管理类，用与管理我们自定义的<code>AMap3DView</code>。</p><ul><li>继承<code>RCTViewManager</code></li><li>实现的函数必须使用<code>RCT_EXPORT_METHOD()</code>来定义。</li><li>需要的属性必须使用<code>RCT_EXPORT_VIEW_PROPERTY()</code>来定义，前者为属性名称，后面则是属性的类型，如果需要自定义类型，则必须要在<code>RCTConvert</code>的类中去添加这个自定义类。然后如果我们需要设置这个属性到View中的话，还必须要在自定义的View中添加指定函数设置这个属性。必须以<code>setXxx</code>格式设置属性函数。<figure class="highlight erlang"><table><tr><td class="code"><pre><code class="hljs erlang">- <span class="hljs-params">(void)</span>setLocationTypeIos:<span class="hljs-params">(MAUserTrackingMode)</span>mode<br>&#123;<br>...<br>&#125;<br></code></pre></td></tr></table></figure></li><li>最后通过<code>view()</code>函数添加这个控件。</li></ul>]]></content>
    
    
    <summary type="html">React-Native与原生通信方式</summary>
    
    
    
    <category term="ReactNative" scheme="http://example.com/categories/ReactNative/"/>
    
    
    <category term="ReactNative" scheme="http://example.com/tags/ReactNative/"/>
    
  </entry>
  
</feed>
